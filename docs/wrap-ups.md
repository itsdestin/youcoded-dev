# Wrap-up ledger

What each session's `/wrap-up` found, and where it landed. **Newest at the bottom** — read
it with `tail -n 80 docs/wrap-ups.md`, append your entry with `>>`.

It exists because a wrap-up with no memory cannot see recurrence, and recurrence is the
strongest signal in the whole process: a finding that shows up twice means the first fix
was prose that nobody honored. `/audit` has had a paper trail in `docs/audits/` since
2026-04; this is the same thing for the half of the job only a live session can do.

One entry per session, kept to a handful of lines:

```
## YYYY-MM-DD — what the session was doing (branch, or "no branch")
- <finding, one line> → applied: <file> | roadmap: <area file> | dropped: <reason>  [cost, if measured]
- deleted/merged: <what got smaller>, or "none found"
```

Do not edit past entries. A finding that recurs gets a NEW line saying so, on the day it
recurred — the repetition is the data.

---

## 2026-09-05 — reviewed the wrap-up skill itself (chore/wrap-up-retrospective-first)
- Wrap-up had no memory of itself: could not see whether past findings landed, or recurred → applied: `docs/wrap-ups.md` (this file), read at Step 1
- Replay questions only asked about Claude's friction, never about product intent Destin voiced mid-session → applied: fifth replay question in `SKILL.md`
- Findings were ranked by feel because nothing was ever counted → applied: "quantify or qualify where you honestly can" in `SKILL.md`
- "Prefer subtracting" was a preference with no mechanism; instruction files kept growing → applied: each wrap-up must name one deletion or say it found none
- Push/close-out ran first and ate the attention the retrospective needed → applied: retrospective is now Steps 1–5, push/close-out is Step 6
- Retrospective edits had no stated home, so they risked a separate branch → applied: Step 5 lands them on the session's own branch
- Cross-repo sweep pushed other sessions' branches to public repos without a secrets scan → applied: scan now explicitly covers swept branches
- Goals were implicit; the two that matter (fewer tokens next session, more automation + preferences captured once) were never stated → applied: "What better means here" section
- Roadmap entries filed by wrap-up are not tagged, so the next wrap-up cannot check whether they moved → dropped: Destin deferred; revisit once the ledger has run a few sessions
- deleted/merged: `SKILL.md` narrative trimmed hard enough to absorb a new section, a new step and a fifth question and still come out smaller — 1504 → 1492 words

## 2026-09-05 — guidance cleanup (session/guidance-serena-cleanup)
- Native path-rule support was initially uncertain from static guidance; source inspection confirmed it, but delivery follows the tool step → applied: accurate wording in CLAUDE/MAP; roadmap: first-write timing, hook compatibility and runtime capability facts in native-harness.
- Retired checkout-pinned search setup competed with the isolated-worktree rule → applied on branch: empty workspace MCP registry, removed app index config, branch-local search guidance and CI regression test; live/shared registration deliberately untouched.
- Always-loaded recipes and incident narratives crowded out core guidance → applied: CLAUDE.md 6,186 → 1,985 whitespace-delimited words; detailed recipes moved to docs/workspace-workflows.md. Existing approval/verification gates retained.
- Instruction provenance overlaps an existing context-and-knowledge idea → roadmap: expanded that item rather than creating a second project. Full workflow/skill routing consolidation deferred in dev-workspace.
- A helper stopped on provider credit limits; work continued directly. Runtime also refused simultaneous write-capable helpers → applied: qualify parallelism by available runtime support rather than assuming it.
- Roadmap validator downgraded stale workbench-sync and file-chip claims to needs-verify; no product fix claimed. Full anchor audit is blocked by absent sibling component repos; desktop verify and focused workspace tests passed.
- Close-out mistook the untouched branch tip's ancestry for completed work despite uncommitted edits → roadmap: dev-workspace. Its deletion suggestions were not followed; these worktrees hold the actual changes.
- deleted/merged: retired setup instructions and active handoff removed from current guidance; historical handoff archived with its unresolved ideas preserved in the roadmap. No global cache deletion, live-app changes or runtime implementation.

## 2026-09-05 — competitive review of the native prompt, then shipped it (feat/prompt-doctrine, feat/prompt-capability, feat/prompt-plain-language)
- Paid for an 8-cell before/after eval at one run per arm, reported the gap as a finding, retracted it when a re-run of the SAME builds swung 2-3 points on the same judged items → applied: `harness-eval.mjs` warns at the estimate when a ≥2-arm plan has `repeats: 1`, guard `harness-eval-comparison-noise.test.ts`  [$1.90 spent, one wrong conclusion Destin acted on]
- `builds` (the code-version axis) had no worked example and no key-file guidance; learned it from `matrix.ts` source and searched five places for a key that does not exist → applied: `prompt-doctrine.json` committed as the example, three bullets in `.claude/rules/harness-evaluator.md`  [~10 tool calls + one blocked turn]
- Built two review surfaces Destin rejected (a card matrix, then an 11k-word reading page — "a fuck ton of poorly formatted text") before the workbench landed. `feedback-review-page-format` already recorded that a prose page and a card board were rejected for UI reviews; nobody had generalised it beyond UI → applied: `~/system/me/README.md`  [2 build/screenshot/commit cycles, 2 rounds of his time]
- Rebuilt the question deck's loopback answer server because its spec has no diff card → roadmap: `docs/roadmap/dev-workspace.md` (rigs)
- Edited the roadmap in the shared stale checkout first, hit another session's already-fixed error, spent ~6 calls diagnosing it. RECURRENCE: `CLAUDE.md` warns about this explicitly → dropped: a hook blocking edits (not commits) in the shared checkout would break legitimate work by other sessions; the pre-commit hook already catches the commit
- His "a closing offer to act is good behaviour" contradicted the eval rubric that scored it as padding → applied: `prose-rubric.ts` carve-out, so the case stops arguing against a shipped intention
- Clickable URLs in chat: he asked for a handoff mid-session, a fresh session built and merged `markdown-linkify.ts` before this one ended → nothing to file; the handoff worked
- deleted/merged: `.claude/rules/harness-evaluator.md` went 778 → 547 words — three invariants about *changing* the evaluator moved into the depth doc it already points at, leaving the rule to cover *running* one
- Ran `audit-anchors.mjs` in the shared checkout and got two failures that were both already FIXED upstream (a doc reported as living in active AND archive; a word-budget violation) — spent calls on each before checking against `origin/master`. RECURRENCE of the stale-checkout trap, twice in one session → applied: the auditor now prints how many commits behind the checkout is, before its findings

## 2026-09-05 — the helpers chip counts Claude Code subagents (session/cc-subagent-chip)
- Shipped the whole feature — renderer edits, a new test, a review deck — with ZERO path-scoped rules loaded, because reading/editing via shell `cat`/`sed`/heredocs never triggers them → applied: the measured reason next to CLAUDE.md's existing "prefer dedicated file tools"; roadmap: `docs/roadmap/dev-workspace.md` (a hook could name the rule that did not load)  [6 rules matched my edits, 0 fired — counted in `~/.claude/instructions-loaded.log`]
- Copied `probe: false` into a new review plan from an existing one, which switched off the painted-pixel contrast probe; `contrast.md` came out EMPTY and I read that as "no data" rather than "you turned it off" → applied: `contrast-report.mjs` now states what it checked, `contrast-report.test.mjs` pins it (red before green), `probe` dropped from `plans/cc-subagents.json`  [4 extra six-theme sweeps + 2 hand-rolled pixel scripts to measure a failure the rig gives free; I also stated wrong ratios mid-turn and retracted them]
- `UI_REVIEW_PLANS=<name>` naming a plan absent from that checkout's `plans/` selected nothing and the sweep still printed "0 covered · 0 partial · 0 missed" and exited 0 — a clean bill for a run that screenshotted nothing → applied: `run-review.sh` exits 2 and lists the available plans  [1 wasted sweep]
- Destin: "why is working blue? should be green" — the app HAS one status-colour vocabulary (`StatusDot.tsx` STATUS_LABEL) and nothing pointed other surfaces at it, so the specialists popup shipped saying the opposite of the session pills → applied: `.claude/rules/react-renderer.md`, with the contrast half (a fixed status colour must never be the word) and its measured ratios
- Wrote a new review plan into the SHARED checkout, and the deck folder into `youcoded/desktop/docs/`, both from cwd drift with relative paths. RECURRENCE: the stale/shared-checkout trap is already in this ledger twice from 2026-09-05 → dropped: both were caught and moved within a call; the pre-commit hook is the real backstop and it held
- Reconstructed the deck spec format from `spec.py`/`crops.py`/`boxes.py` + an example deck → dropped: `.claude/rules/review-deck.md` already exists on the unmerged `feat/deck-consistency` branch, with an `AUTHORING.md`. Filing it again would duplicate in-flight work
- Windows CI was already red on master before this branch (`runtime-default.test.tsx` compared `join()` paths to forward-slash literals); a second Windows run then flaked on `chatgpt-auth.test.ts` and passed on re-run → applied: the separator fix, on the feature branch; roadmap: `docs/roadmap/dev-workspace.md` for the flake
- Claimed the ≤600-word rule budget was an unenforced convention and shipped `react-renderer.md` over it. It is enforced, and CI failed — I had run the auditor locally but filtered its output to two lines and never saw the budget line. RECURRENCE of `feedback-read-check-output-before-merging` → applied: the rule's status-colour detail moved to `youcoded/docs/renderer-chrome.md`, leaving a pointer
- deleted/merged: `react-renderer.md`'s whole "UI iteration tooling" section — CLAUDE.md carries it (twice) and points BACK at this rule, and `docs/workspace-workflows.md` carries the full version. The rule ends at 582 words, 14 BELOW the 596 it started at, having gained a new invariant
- Master went red twice under me from other sessions' pushes: a Windows path-separator bug in a source guard, and two roadmap entries using a `local-models` token that is not one of the 29 surfaces → applied: both fixed, each in its own commit saying it was not this branch's work
## 2026-09-05 — built and shipped voice prompting end to end (feat/voice-prompting, design/voice-prompting)
- A `cp file /tmp/x.bak` … `cp /tmp/x.bak file` restore silently reverted a finished fix and its comments: the save half never ran (unexpected cwd) and the restore succeeded anyway from a stale backup, printing nothing, exit 0. Caught ~6 calls later by grepping for a comment that should have been there. This workspace ACTIVELY INDUCES the pattern — test-suite-hygiene tells every session to mutate a guard and "put it back" → applied: `.claude/hooks/glob-guard.py` guard 6 refuses a `.bak`-shaped restore over a git-tracked file and names `git stash push` / `git checkout --` instead; 5 tests, mutation-checked (78 pass / 2 fail with the offender disabled)  [~6 calls, one nearly-shipped revert]
- Then did the same thing in reverse: `git checkout --` on a file whose new code was UNCOMMITTED threw it away, and it had to be retyped from scratch → applied: the same guard's message says COMMIT FIRST, because git-checkout-as-undo is the identical accident wearing a different hat  [~4 calls]
- `./gradlew test` printed BUILD SUCCESSFUL with `103 tasks up-to-date` — a green nobody earned. TWO agents hit it the same day: the review agent reported cached tests and flagged it, this session then hit `--tests` being unsupported on the umbrella `test` task → applied: `docs/workspace-workflows.md` → Local build & test now names `testDebugUnitTest --rerun-tasks` and how to read the count out of the XML
- `npx vitest` resolves a cached global copy with no jsdom and dies at `Cannot find package 'jsdom'`, which reads as a broken test rather than a wrong binary → applied: same section, `./node_modules/.bin/vitest run <file>` from `desktop/`
- Flappy's held-key guard trusts `KeyboardEvent.repeat`, which Electron on Linux does not reliably set — the same cause that made the message box's space bar a coin flip for Destin. Shipped code, his own machine → roadmap: `docs/roadmap/games.md`
- The local-model engine install still shells out to the system `tar`; voice now unpacks its own downloads in process → roadmap: `docs/roadmap/local-models.md`
- Destin's "setup should be seamless — no terminal commands, nothing downloaded externally" was a standard, not a request about voice → applied to the code (the whole install now runs no other program, and a source-scanning guard fails the build if it ever imports child_process again), and it is what the local-models item above is measured against
- The auditor's "this checkout is N commits behind" line — added by a previous wrap-up after the stale-checkout trap fired twice — did its job today: the one MAP failure was a file that exists upstream, and the note said so before any calls were spent on it. RECURRENCE PREVENTED, first observed win for that fix
- One review finding (F10) is that no contract row is mechanically checkable, so ~2,800 lines of new tests are attached to no promise → carried to the acceptance deck rather than fixed quietly; it is a gap in how the contract was written
- deleted/merged: `voice-assets.ts` lost the entire named-bzip2 apparatus — `systemTar`, `findBzip2`, `probeProgram`, `unpackTarBz2`, `pipeThroughTar`, `BZIP2_PROGRAMS` — plus the test file's bzip2 capability probe and four `skipIf` guards, replaced by an in-process unpacker. The install now runs no external program on any platform

## 2026-09-06 — local-model engine upgrades, 25 tasks in 6 waves (feat/local-engine-upgrades, merged 7ec2a929)
- Shipped "Switch to ROCm (faster on AMD)" with **no measurement behind it**; measured on request at the very end and it is 24–46% SLOWER at writing replies (~20% faster reading). The roadmap even defined future backends as needing "the same measured trial ROCm got" — which was none, so the bar was met by doing nothing → applied: measurement + method in `youcoded/docs/engine-dependencies.md`, label reworded, switch demoted under Advanced, roadmap rule rewritten to "time both halves on the target machine before offering"  [would have shipped a false performance claim to every AMD user]
- A dev instance rewrote Destin's LIVE app's engine settings: `~/.youcoded/config.json` is not scoped per profile, so `run-dev.sh`'s isolation does not cover it → roadmap: `docs/roadmap/dev-workspace.md` (rigs). Reverted his config by hand, with his say-so  [his next restart would have booted the slower backend]
- **A check that passes for the wrong reason, four times in one session**, in four different tools: a mutation harness reading "errored" as "passed"; a workbench boot check reporting 16 green routes against a server that never started; a shell chain printing "serving" for a dead process; and my own `curl -s .../health && break`, which treats llama-server's 503-while-loading as ready and cost four discarded benchmark runs → applied: guard 6 in `.claude/hooks/glob-guard.py` + 5 tests in `glob-guard.test.mjs`, verified red (2 fail) before green (85 pass)
- Two agents ran mutation batteries in ONE worktree and each destroyed the other's uncommitted work and corrupted the other's measurements — `git status` came back clean, which reads as "nothing to save" → applied: `docs/PITFALLS.md` → Worktrees, covering both halves (the loss, and that no measurement taken while another agent writes the same files is evidence)
- Every self-reported mutation score that was independently re-run came back lower (26/26 → 34/42, 41/41 → 52/60, 54/54 → 26/29 on a larger set). None was dishonest; the harnesses were measuring themselves → applied: nothing new — the fix already in place is that an independent reviewer re-runs the battery, and it worked every time
- Destin found three defects in ~10 minutes of real use that two adversarial reviewers, 32 signed contract rows and a full suite all passed: the size breakdown was unopenable by touch (he has no mouse), the memory warning was invisible on two of four BUILT-IN themes, and typing right after creating a session said "this session is no longer running" while a 29 GB model loaded → applied: all three fixed on the branch; the touch and theme cases are now pinned
- His product ideas voiced mid-session and filed nowhere → applied: tokens-per-second tag with red/yellow/green in `docs/roadmap/local-models.md`; cost/intelligence tags in `docs/roadmap/user-interface.md`, in his words
- A fresh install's model picker shows an EMPTY list — favourites are the default view and a new device has none, so downloaded local models are invisible until you guess to type → applied: added as a second cause on the existing favourites item in `docs/roadmap/sync.md` rather than filing a duplicate
- RECURRENCE (third session): the stale shared checkout. A reviewer produced a wrong finding by searching `youcoded-dev` while it was 34 commits behind, and the doc auditor had to be run against a scratch root symlinked at the worktree → dropped: `audit-anchors.mjs` already prints how many commits behind it is (added last session, and it did fire); what bit this time was a SUBAGENT searching the shared path, which that warning does not reach
- deleted/merged: `mock-only.ts`'s stand-in list reached EMPTY for the first time; a third stale copy of the speed defaults deleted from `EngineCard.tsx`; a duplicate `LocalModelsSection.tsx` row removed from `docs/MAP.md`; MAP stopped keeping its own copy of which probes a bump re-runs and points at the one authority instead

## 2026-09-07 — reviewed a scrollback investigation, then fixed the bug it half-diagnosed (session/chat-scrollback)
- Reviewed the investigation doc against ITSELF and endorsed its root cause, when the symptom quoted at the top of that same doc ("only the most recent messages") already contradicted it — its cause produces a BLANK conversation. I flagged the mismatch as a review finding and offered `/clear` as the alternative instead of tracing the code path the real symptom implies. Destin had to re-describe the symptom before the actual cause (every request after the first cannot locate the transcript) was found → dropped as a filing: the generalisable form is "when a document's symptom and its root cause predict different user experiences, trace the symptom before reviewing anything else", and a paragraph saying that is exactly the kind nobody reads. Recorded here instead  [one full review round of Destin's time]
- RECURRENCE, and the filed hypothesis is WRONG: zero path-scoped rules loaded all session, and this session made every source edit through Edit/Write, not Bash. `~/.claude/instructions-loaded.log`: 4 loads, all `session_start`; 6 files edited that 4+ rules cover. The log records 309 `path_glob_match` loads for other sessions, 273 of them on worktree files, so neither the tool nor the worktree is the discriminator — the remaining variable is cwd (theirs inside the worktree, mine at the workspace root) → applied: `docs/roadmap/dev-workspace.md` item rewritten with the counter-evidence and the one-run experiment that would settle it
- A new main-side module used `import * as os` and its test's own `vi.spyOn(os, 'homedir')` silently did not apply, so the fixture home was ignored and the first page resolved nowhere. Measured all four combinations: DEFAULT import sees the spy, NAMESPACE does not, either specifier. 9 modules under `src/main/` already read `os.homedir()` through a namespace import → applied: `tests/home-sandbox-import-style.test.ts` + two probe fixtures, mutation-checked (1 fail with the fixture flipped, 3 pass restored); the WHY comment in `transcript-page-source.ts` corrected — it claimed the module would read the developer's REAL home, and it would not, the suite-wide sandbox still holds  [~3 calls debugging a test that resolved to the wrong directory, and one wrong claim shipped in a comment]
- `npx vitest` type-checks nothing, so a new test file with the wrong call arity passed locally and failed only in `verify.sh`'s `tsc -p tsconfig.tests.json` → dropped: verify.sh is the documented gate, it caught it, and the full run costs ~2 minutes. Not worth a second instruction
- `SAFE_ID_RE` is defined three times under `src/main/` with two different character sets, and the permissive one (`[A-Za-z0-9._-]+`, which `..` matches) shapes a path → dropped: verified NOT a hazard — the values it validates come from a directory scan, so a segment named `..` cannot exist. Checked before filing, which is the only reason a wrong finding did not land
- deleted/merged: three hand-rolled first-page retry loops (App, the buddy floater) now share one tested policy in `first-page-retry.ts`, and the duplicated transcript-path construction left `ipc-handlers.ts` for `transcript-page-source.ts`. The investigation doc went 2000 → 1148 words when the dropped branch stopped being written up as an open item
- A close-out commit merged as a PURE FILE MOVE with a message describing four changes it did not contain: `git add` named a path `git mv` had already removed, git aborts the whole add on an unmatched pathspec, and `2>/dev/null` hid the error. `git status` showed the files still unstaged (" M") in the very next line of output and I read past it. RECURRENCE of `feedback-read-check-output-before-merging`, this time by suppressing the stream that would have said so → applied: caught and landed in a follow-up commit that names it; the durable rule already exists in CLAUDE.md ("stage explicit paths and INSPECT THE STAGED DIFF") and was simply not followed — no new instruction is warranted, but never `2>/dev/null` a `git add`  [one hollow commit merged to master, one extra PR to repair it]

## 2026-09-07 — cross-window session status, then the dropdown's dead drag grip (session/session-switcher-dropdown-styling)
- The handoff said green-across-windows "would need a session to actively broadcast I'm working — more plumbing than today's fix". It needed none: every window ALREADY reports its own sessions' finished dot colour to main, main merges and broadcasts it back, and the buddy's SessionPill has drawn peer dots from that feed since it shipped. The switcher was the only surface not subscribed → applied: `useAttentionSummary.ts` + a pull-on-mount (`attention:get-summary`), ~80 lines instead of a new state machine  [one abandoned follow-up, reinstated as a 20-minute change]
- Wiring peer colours into `sessionStatuses` silently armed two things nobody asked for: the attention CHIME fires from that same map, so every open window would have played the same alert at once; and App's attention reporter walked the whole map, so a bystander window's placeholder "idle" could overwrite the owner's "green" in the very feed the switcher now reads (main keys the aggregate by session id, last report wins) → applied: both scoped to sessions the window owns, with the WHY at each site
- Destin: "i cant seem to grab the drag handle for same-window sessions." The grip was inert from the day it was drawn — menu rows share the pill bar's pointer drag, which picks its slot from `clientX` against the BAR's rects and ignores Y "on purpose", so the one gesture a vertical list invites could never land, while a sideways drag reordered against pills the dropdown is nowhere near → applied: the grip is a native draggable with its own insertion-line reorder; `.claude/rules/session-strip-motion.md` gained the invariant  [pre-existing on master, found only because the peer rows beside it now visibly work]
- **RECURRENCE (fourth session running): a check that passes for the wrong reason.** `drag-fuzz.mjs` emulates a 460px window; the strip packs to ONE pill there and a scenario needs three, so it bailed on iteration 1 and printed an all-dash `worst:` line at exit 0. Six sweeps of that, indistinguishable from six clean sweeps, and I nearly reported them as the motion rule's sign-off → applied: `FUZZ_W`/`FUZZ_H`, exit 1 on zero scenarios, and the tell written into `scripts/ui-review/README.md` next to the "all-zero" bar it invalidates
- Two of my OWN new tests passed while proving nothing, in one sitting. jsdom gives a synthetic drag event NO coordinates at all (`fireEvent.dragOver(el,{clientY})` arrives `undefined`), so both halves of a midline test took the same branch; and a negative assertion raced an effect that lands behind an async drop resolution → applied: real `MouseEvent`s named `dragover`/`drop`, stubbed row boxes, and a wait on the mock settling — each re-mutated red before green  [caught only because the mutation pass is mandatory; the "guard you did not break" rule earned its keep twice]
- `roadmap-check.mjs --fix` downgraded two `confirmed` items to `needs-verify` in area files this session never touched. I first blamed the 57-commits-behind checkout and did not file it; it then did the identical thing on a freshly merged tree, so the checkout was never the cause → applied: reverted both twice and filed it in `docs/roadmap/dev-workspace.md`. The filing grammar tells EVERY session to run `--fix` before committing, so each one is currently one unread diff away from shipping another session's downgrade
- The continuity score the drag sweep reports (27.3-29.0, every seed) was ALREADY a parked roadmap item from 2026-09-03 → extended that item with the 2026-09-07 numbers and the control run proving it independent of this work, rather than filing a second one
- Close-out's leftover-directory scan named `worktrees/sessions` — the directory holding FOUR other sessions' live worktrees — as a leftover to remove, every time the last session inside it finished. It looks one level under `worktrees/` and this workspace's own session flow nests them two → applied: the scan skips a directory whose children are worktrees. Getting there took THREE junk measurements in a row (a `compgen` run under zsh where it is not the bash builtin; a patched script run from a temp worktree with no sub-repo, so it bailed before the scan; and a fixture that put `.git` a level deeper than a real session worktree has it) — each looked like a verdict on the fix
- deleted/merged: an `Array.isArray` guard in `useAttentionSummary` that no test could make fail — the case it claimed to catch is caught a line later — removed rather than left as code nothing justifies

## 2026-09-07 — Assistant settings: the build deck, the build, and a memory bug that was not ours (feat/assistant-settings, design/assistant-settings)
- The backend spec still described the fallback-to-Claude behaviour Destin's own corrections had replaced — in its tests section and in R3-5's self-healing label. A builder reading either would have written the opposite of the rule the rest of the document states → applied: both sections reconciled on the branch. The general shape: when a decision REVERSES accepted review findings, the revision has to sweep the whole document, not just the section that was argued about  [caught by reading; would have cost a wrong build + a re-review]
- Ended a summary with "Starting on the four visible pieces now" after he had approved the DECISIONS, not the build — "i didn't say to start yet. what are ya doing exactly?" → applied: `~/system/me/preferences.md`, "a plan is not a go-ahead"
- `verify.sh` caught a hand-rolled `FieldError` that `tsc` and `eslint` both passed. Destin had asked for no test runs during the build, which was the right call for iteration, but the guard only fires in the full suite → nothing to file; the guard worked and the report named which checks had run
- The session-opening handoff asserted a workbench "still serving on port 5233"; nothing was on 5233, and 5223 belonged to a different session's worktree → dropped: self-correcting, two calls to check, and the alternative (a convention about naming live ports in handoffs) would be prose nobody reads. Worth remembering only as: never kill a port you did not start
- His screenshot of a 2B model reading "will be tight" on a 128 GB laptop → roadmap: `local-models.md`, with a measured investigation. Reproduced the exact sentence from the app's own cached numbers; the estimate scores against a 4 GiB BIOS carve-out instead of the ~90 GB the graphics chip reaches. Not from this branch — those files are untouched by it  [4 curated models mislabelled today; his direction on unified memory captured in the report]
- deleted/merged: `OPEN-QUESTIONS.md` went from seven live questions and 3.3 KB of prose to a one-table record of where each answer lives — every question was answered on the build deck

## 2026-09-08 — fixed quick-space composer caret regression (session/composer-space-caret)
- A delayed animation-frame caret repair could overwrite the caret after newer typing → applied: native `setRangeText()` insertion with a WHY comment and eight pinning regressions in `InputBar.tsx` / `InputBar.test.tsx`
- Incorrect Grep `type` values were assistant misuse of an already-clear tool schema → dropped: expanding guidance would duplicate the tool contract
- Broad browser timing and undo behavior were not exercised → dropped: outside this focused regression; no claim made
- deleted/merged: removed the deferred cursor operation and manual string slicing

## 2026-09-08 — native step-guard setting (session/native-step-guard-setting)
- Two implementation specialists repeatedly exhausted broad task budgets without reports; splitting the remainder into runtime, UI, integration, and final-fix jobs produced four bounded reports and clean ownership → applied in-session: bounded specialist briefs; dropped as a durable rule because the existing guidance already says to delegate one focused piece of work  [2 interrupted workers before the split]
- The supplied document path pointed at the shared checkout, while workspace-start had moved it into the session worktree; locating it cost a failed read plus a glob → dropped: workspace-start already prints the absolute paths, and the mismatch was specific to this resumed session
- Final review caught a destructive validation asymmetry: malformed persisted values correctly read as None, but malformed write values also cleared the preference → applied: `step-guard-settings.test.ts` pins reject-without-mutation before the main-process fix
- deleted/merged: removed the whole model-name-based `model-step-budget.ts` subsystem and its test; ordinary root limits now come from one explicit persisted snapshot

## 2026-09-09 — buddy floater and friendly mascot palettes (session/buddy-floater-regression)
- Shared host zoom clipped 112px art inside an 85px CSS viewport; source guesses were insufficient → applied: preload per-frame zoom guard and tests; isolated Electron 41 probe preserved buddy 112px through main zoom changes and reload.
- Theme sync reached more windows than theme-install notifications; a stale buddy reset a valid new theme → applied: multiwindow watcher and deferred incoming-theme activation with cancellation tests.
- Default mascot art copied accessible button colors and produced frightening pale eyes; authoring references contradicted their own dark-eye guidance → applied: default-only palette/catchlight contract, small-size rim tests, and consolidated theme-builder/reference advice. UI text contrast remains unchanged.
- Specialist failures and repeated status steers delayed work; one capture contained a browser error → dropped as new policy: existing tool and capture rules already require observable readiness and image inspection. Parent-owned server and actual DOM waits corrected the capture before review. No failed image was presented as accepted work.
- deleted/merged: removed conflicting accent/on-accent mascot-palette prescriptions and stale paper-white Halftone advice; reused existing authoring references rather than another guide.
- Paid model evaluation, interactive motion rigs, packaged beta and multi-monitor validation were not run → dropped from this task; existing buddy release-validation roadmap items remain open.

## 2026-09-09 — buddy animation continuity (session/buddy-animation-races)
- Blink/pose renders replaced the SVG and attention keys remounted its whole host → applied: four failing-then-passing DOM-identity regressions; memoized SVG markup and stable attention wrapper preserve animation state and the PeekHands observer.
- deleted/merged: removed bounceKey state/effect and keyed remount; class changes already retrigger the existing bounce.
- Negative spring timestep and stale hover-swing theories were not demonstrated as the reported failure → dropped from this fix; CSS specificity disproved the proposed hover override. No speculative speed changes.
- Final desktop verify passed all six checks; fresh source review found no blockers. Docked motion was not visually rechecked; Destin explicitly authorized merge/close. App PR #455 merged.

## 2026-09-08 — safe Markdown disclosure blocks (session/markdown-details-fix)
- `workspace-start` created the app worktree without the documented auto-provisioned `desktop/node_modules`, causing one failed investigation command and a manual `cp -al` recovery → dropped: startup provisioning is already specified and this single miss did not recur; no new rule would improve on the existing mechanism [2 shell calls]
- A first-pass raw-node transform passed component tests but failed TypeScript because root HAST content is wider than element content; the required `verify.sh` caught it before commit → applied: the final regression tests plus existing verification gate are the durable guard [1 failed verification run]
- The first security review found unsupported HTML attributes could leak text into the URL linkifier and spaced `<details>`/`<summary>` was missed; both now have failing-then-passing tests → applied: `MarkdownContent.test.tsx` [2 caught edge cases]
- A repo-wide source sweep found one production Markdown pipeline and all callers inherit it; no duplicate renderer or separate Android parser needed repair → dropped: no additional change exists to make
- deleted/merged: no redundant code or guidance found; arbitrary raw-HTML enablement was deliberately avoided rather than adding `rehype-raw`

## 2026-09-08 — remote-access review and Mesh direction (session/remote-mesh-roadmap)
- Destin rejected an engineering-heavy plan and asked for plain-language options with pros/cons → applied in the handoff: lead with the decision and trade-offs, details on demand; dropped as another global rule because concise, accessible explanations are already required [one extra clarification turn].
- Older remote backlog reports described turn-ID collisions and repeated PTY-buffer copying already fixed in inspected app HEAD 37340a29 → applied in the handoff: reverify each issue against the new worktree, never implement the old list blindly; deferred backlog reconciliation to the authorized remote implementation rather than claiming source-only review was a shipped fix.
- Account-based access, automatic device selection for duties, and optional paid cloud fallback were new product direction → roadmap: remote-access and native-harness, parked around v1.4, not release commitments. Tailscale remains the near-term choice.
- Incorrect Grep type `tsx` recurred from the ledger → dropped: tool schema already explains built-in types; corrected to `ts` in-session rather than adding duplicate guidance.
- deleted/merged: folded the near-term Tailscale decision into the existing encryption item instead of creating an overlapping feature entry; no code changed.
- Verification: roadmap structure clean; code-linked roadmap checks partially skipped because this documentation-only worktree has no component checkouts. Anchor audit: 412/412 anchors and 511/511 MAP paths pass, but unrelated existing duplicate focus-plan/spec documents and a native-specialists rule budget violation fail; dropped from this documentation-only scope, not silently repaired.
- Close-out incorrectly called the work landed because the branch has no new commits; all four changed files are still uncommitted. Retain the branch/worktree, do not follow its deletion suggestions. Dropped as tooling work from this handoff-only scope; warn the next session explicitly. No app tests, live-app interaction, commits, pushes or merge authorized/performed in this session.

## 2026-09-10 — remote access batch 1: build, three reviews, and the first real phone (session/remote-mesh-roadmap)
- **RECURRENCE (fifth session running): a check that passes for the wrong reason** — five in one sitting. A guard asserting the end-of-setup check "exists" passed while the success path fabricated its result; `toContain('disabled={hostOnly}')` passed with that prop deleted, because two other controls carry it; a `not.toContain` matched the WHY note quoting the old line; a rate-limit guard asserted words naming a five-attempt budget that was unreachable; and `min-width: min(max-content,280px)` is invalid CSS, silently dropped, so a "fix" verified twice by reading changed nothing. The habit (mutate red before green) caught every one — but `tests/helpers/guard-scope.ts` already had `readStripped()` and `assertPatternMatches()`, used by ~20 guards, and nothing pointed there → applied: moved it UP the ladder from habit to shape in `.claude/rules/test-suite-hygiene.md`'s Guard line, and converted this session's guards to use it
- **A capability flag that was false everywhere it mattered.** `isRemoteMode()` was set only by `connectToHost` — the ANDROID pairing path — so a phone BROWSER never set it. Three fixes keyed on it were inert on the one surface they were written for, including two I had already reported to Destin as done: the attention classifier kept polling the host for terminal text (the channel id was still reaching his screen after the "fix"), and the theme kept a wallpaper whose file only exists on the host → applied: declared on auth:ok, gated on `isAndroidLocal()`, with a pinning test. Found only while chasing an unrelated report; nothing failed loudly, because false is what it had always been
- **I marked three review findings "accepted — fixed" and had written none of the code.** Caught by the fresh contract agent, which reads the branch to place `checkedBy` and refused to write rows promising behaviour it could not find → applied: the two real gaps built with inverted tests; the triage marks now distinguish `deferred (filed)` from `accepted`, because the contract agent's rule is a binary read of the first word and "accepted, filed not fixed" would have become a row promising unbuilt work. Dropped as new prose: `quote what the check returned` already exists in this skill; the flow caught it, which is the system working
- **Every defect that mattered was a failure wearing the costume of an answer.** A blank Remote Access panel on a phone (one channel missing from the WS host, swallowed by a `Promise.all`); Unpair removing a row while the device kept access; "No previous sessions found" for a user with 946 conversations; a password box for a computer with no password; `listen EADDRINUSE` shown verbatim. Same shape each time: the code caught a failure and rendered the empty/success case → applied per site, with `ErrorState` + Retry and `plainReason()` for OS codes. Worth naming as the batch's through-line, not re-filed
- Destin's phone found six defects across two sessions that 2,289 desktop tests, three reviewers and I did not. His words, filed: the toast storm, the wrong theme, the intermittent empty conversation browser (cause unidentified, symptom now visible), and browser encryption being a drawing → roadmap: `remote-access.md` ×6
- `run-dev.sh` can start a dev instance and cannot stop one; killing Electron by pid orphaned its Vite, so the relaunch died naming Vite rather than the leftover, mid-test while he waited → roadmap: `dev-workspace.md`
- deleted/merged: `remote:disconnect-client`'s explicit `{ok:false}` refusal in the WS host — kept so an un-upgraded client would be "told no", but no shim lists it in `REJECT_ON_NOT_OK`, so that refusal resolved as success. The `default:` arm's `{unsupported:true}`, which every shim version rejects, was always the honest answer. Also trimmed `.claude/rules/test-suite-hygiene.md` back under its 600-word budget to pay for the addition, and closed the clipped-No-button item in `dev-workspace.md` by fixing it (`selfie`: 2 pages changed, both the answer row)
- Not merged, not asked to be. Verify green (desktop) and Android 741/741; the acceptance deck is built and served but unanswered — 11 rows are Destin's, 4 of them phone questions
## 2026-09-08 — planned workspace startup reorientation (session/2026-09-08-workspace-sync)
- Shared startup guidance was older than the freshly provisioned workspace: the latter already provisioned dependency hardlinks, absent from the initially inspected startup script → applied to planning: preserve that behavior explicitly in `docs/archive/plans/2026-09-08-workspace-start-reorientation.md`; runtime repair deferred to its implementation tasks, not claimed complete. Serves fewer repeated searches and automatic freshness.
- Existing sync treats a file matching historical upstream bytes as disposable; historical equality does not establish whether a rollback was intentional → applied to approved design/plan: preserve such files and pin that boundary in disposable-repository regression tests during implementation. Serves safe automatic behavior.
- Destin requested the simplest build and declined harness evaluation → applied: one shared sync/report module specified, no new service/dependency/model call; declined evaluation recorded in both spec and plan. Implementation belongs to a different session.
- Additional global rules and duplicate roadmap filing → dropped: this authorized work already has an active specification and implementation plan; more always-loaded prose would add no enforcement.
- deleted/merged: consolidated proposed startup/maintenance policy into one planned module; no runtime code deleted or changed in this planning session. Removed the spec's obsolete invitation to offer the declined evaluation.

## 2026-09-09 — recovered and landed an interrupted session's reorientation build (session/2026-09-08-workspace-sync)
- **RECURRENCE of the 2026-09-01 "work keeps existing on one disk only" item, and the expensive kind.** A COMPLETE, review-clean implementation — 3,383 lines, 53 specialist dispatches, a final reviewer returning zero findings — sat uncommitted for 14 hours because its native session was interrupted mid-`verification-before-completion` and never got to ask. Its branch had ZERO commits and no upstream, so the wrap-up ref sweep (`for-each-ref … %(upstream:track)`) reported nothing to push: **ref sweeps are structurally blind to a working tree.** Eight other session worktrees were dirty with zero commits the same morning → applied, moved up the ladder from a wish to a check: `strandedWorktrees()` in `scripts/audit-anchors.mjs` + 5 tests, red-before-green, mutation-checked. WARN not FAIL — an active session is legitimately dirty, and a permanently red audit is one everybody learns to ignore. First real run named three worktrees idle 35–42h  [a full day of finished work with no backup; 66 uncommitted files across 8 worktrees]
- The mechanism that actually stranded it was a plan constraint with no exit: "No commits, pushes or merges during this implementation without further permission" was right during the build and never said when it stopped applying. The interrupted session obeyed it correctly, all the way into the ground → folded into the same roadmap item rather than filed twice; the wanted change is an exit condition in plan templates, not another sweep
- Deliberately did NOT build the other half that item asked for — "a one-shot sweep that commits what it finds". Committing another session's in-flight WIP under this session's authorship is the worse failure, and three of the eight dirty worktrees are plausibly live → recorded in the item as an explicit non-goal
- **RECURRENCE, my own, of "a check that passes for the wrong reason" — fifth session running.** My first mutation check reported all five stranded tests still green, which should have been impossible; the `sed` that was supposed to weaken the threshold had silently not landed and I never confirmed the bytes changed. Re-running with a confirmed edit turned the right test red immediately → the durable rule already exists and was simply not followed; the only addition worth making is that a mutation check must PRINT the mutated line, not assume the edit applied  [one near-miss claim that a vacuous test was meaningful]
- Locating the interrupted session cost a chatsearch `find` plus four `show` calls, because three of the day's native sessions were untitled and only the opening message distinguished them → dropped: session naming is already in flight today ("Reliable Session Naming With User Overrides"), and a second filing would be a duplicate
- `docs/active/{plans,specs}/2026-09-07-permission-prompt-composer-focus*.md` are byte-identical to their `docs/archive/` copies, so `audit-anchors` has a standing FAIL that belongs to nobody's current work and will train the next session to read past a red audit → reported to Destin, NOT deleted: they are another session's merged records, and a surprise deletion on a branch he is about to judge is exactly the change that costs trust
- `node scripts/audit-anchors.mjs` is RED on origin/master itself, for two reasons neither of which belongs to any current branch: the duplicate 2026-09-07 docs above, and `.claude/rules/native-specialists.md` at 624 words against a 600 limit (`44a2c4c2`, which is the THIRD time that one file has gone over — `52f91daf`'s message says the previous fix's sed never matched, the same not-confirming-the-edit failure recorded above) → reported, not fixed: trimming another session's just-written rule is not this branch's call. Worth knowing before anyone reads a red audit as their own
- deleted/merged: none in this session's own footprint — looked, and the only available subtraction is the duplicate pair above, which is not this branch's to take

## 2026-09-09 — session naming with user overrides, backend + UI (session/session-naming, both repos)
- **The workbench fake dispatched an event the product never did**, and every review looked at the workbench. `mock-shim.ts` sent `youcoded:session-renamed` after a rename; `ResumeBrowser` listened for it; nothing in `src/renderer` outside `dev/workbench/` ever sent it. So renaming a saved conversation repainted perfectly under review and did nothing in the app. Six visual rounds, a signed contract row (R8, R12) and a full suite all passed over it; a fresh code reviewer reading the code found it in minutes. `mock-only.ts` guards fake CHANNELS and the mock-contract test pins channels against preload — neither can see an EVENT → applied: `youcoded/desktop/tests/workbench-event-contract.test.ts`, mutation-checked (RED naming the event and the file with the product dispatch removed, GREEN restored), plus a vacuity check so it cannot pass by scanning nothing  [two contract rows shipped visibly broken; found by review, not by any gate]
- **RECURRENCE, third form: a check that passes for the wrong reason.** My first attempt to reproduce Destin's "Save flickers instead of closing" PASSED — my model had the parent updating inside the event listener, where React batches it after the close ran. The real ordering is that main sends `session:renamed` BEFORE returning from the rename call, so the remount lands during the await. A fix shipped on the passing model would have been a guess → applied: the test models main's ordering explicitly and fails on the old key; recorded here because the generalisable form ("a reproduction that passes is not a reproduction") is already the ledger's most-repeated lesson
- **`CLAUDE.md` said Android cannot be built on this machine. It can — 741 tests, 0 failures, twice.** The SDK is at `~/.android-sdk` with platform-tools/build-tools/cmdline-tools and java-21-openjdk exists; `ANDROID_HOME` is simply not exported. The claim was in THREE places (`CLAUDE.md`, `docs/workspace-workflows.md`, a `dev-workspace` roadmap item), each correct when written on 2026-09-04/05 and each stale by the time it was read: this session a subagent asserted "no SDK on this machine" inside a code review, and I nearly recorded Android as unverifiable → applied: all three corrected with the working command and the read-the-XML-not-the-console warning; roadmap item closed into `docs/roadmap/shipped.md`. CLAUDE.md's paragraph shrank 124 → 82 words  [a false cross-platform negative in a committed review; five days of every session being told a platform was untestable]
- **`run-workbench.sh` could not be pointed at a session worktree.** It knew only `worktrees/<name>/desktop`; session worktrees are `worktrees/sessions/<name>/youcoded`, and it also refused to run from inside one. Three failed launches before typing the absolute path → applied: `scripts/lib/resolve-checkout.sh`, sourced by `run-workbench.sh`, accepting everything `run-dev.sh` does including a branch name; verified it resolves `session-naming`, `session/session-naming` and refuses an unknown name with the worktree listing
- **A deck's `runs` are documented "relative to this file" and were resolved against the CURRENT DIRECTORY.** From anywhere but the spec's own folder every crop reported "not captured" and the deck built with no pictures — an empty deck, not an error. Three failed builds before switching to absolute paths → applied: `deck/spec.py` resolves run folders against the spec's directory; 227 deck tests green, and proven by building the session's own deck from `/tmp` with relative paths
- **I introduced a bug into Destin's hands.** The fix for the workbench-masked finding made saved renames reach the parent — which turned the rename dialog's `[id, name]` React key into a remount mid-save, swallowing its close. Live renames had had it all along; my fix widened it to saved ones. He hit it within minutes of the dev window opening → applied: keyed on the id alone, two tests, one of which also pins that a rename arriving from elsewhere no longer wipes a draft mid-edit
- **Destin's reaction to the shipped default, filed in his words:** "i thought it would still be a bit more semantically useful or derive the name somehow rather than just literally re-typing my original message." Basic quoted the opening request, which is indistinguishable from the transcript-derived fallback the app already had, so the default looked like it did nothing → applied: `basicNameFrom` is now subtraction (markdown marks, first sentence, trailing politeness, openers, clause boundary, seven words), measured against **400 real conversation openings on this machine** rather than invented fixtures — about a third become a name, the rest stay shortened quotes, which is the honest ceiling without a model
- A manually named conversation cannot be handed back to automatic naming — review 3 removed that action — and the reply counter may double-count after a `/clear` truncation → roadmap: both in `docs/roadmap/chat-data.md`, rather than built against an approved contract or guessed at
- `npx vitest` resolves a DIFFERENT vitest without jsdom, so single-file runs die on `Cannot find package 'jsdom'` → dropped: `verify.sh` is the documented gate and never uses npx; the working form is `./node_modules/.bin/vitest` from `youcoded/desktop`. Recorded here rather than added as an instruction nobody reads
- deleted/merged: `native-title-feeder.ts` and its test retired into `session-namer.ts` (one policy for both session lanes instead of one per lane); the entire return-to-automatic path removed across six files after review 3 took its button away — channel, preload, shim, handler, remote-server case, Android entry and service function — rather than shipping a remote-reachable write no UI could reach; two checkout resolvers merged into one shared lib; a duplicated eight-line capability comment at the second `auth:ok` site reduced to a pointer

## 2026-09-09 — the promo video's second cut (session/promo-v2, app branch session/promo-v2)
- Full drafts rendered unasked, twice, after a preference he had stated on 2026-09-04 → applied: the rule is now in `scripts/promo/README.md` (drafts are HIS call, iterate on stills) and the memory file; `scripts/promo/still-sheets.sh` makes the line check from one bundle plus stills (~2 s a still, no draft). Cost this session: three unasked drafts and two re-renders, ~35 min of render.
- Three scenes broke against app changes since the first film (a reworded tool line, a picker that lost its field, a title that became a Rename control); each was diagnosed with a hand-made probe scene because the recorder deleted its frames on exit → applied: `record.mjs` keeps the frame it died on as `<scene>.fail.png`. Cost: three failed film runs plus a probe each, ~25 min.
- The music plan (`song.py` PLAN) and the beat list (`timeline.ts` BEATS) were re-keyed by hand three times and the test only pinned the plan to itself → applied: `timeline.test.ts` pins every joint (drop 1 = the theme beat's third bar, drop 2 = the marketplace, break = the phone, groove2 = conversations, end = the close's last bar) against the grid the sequencer wrote.
- Two deck builds failed on run paths (they resolve from the workspace root, not the spec) and the choice slide's picture layout for non-workbench stills was unwritten → applied: both in `deck/AUTHORING.md`, with the "build again and reload" route for a picture on a deck he is looking at.
- The bubble tail "gap" survived three decks because it was a geometry fault (a corner arc reaching past the tail, then a straight line back through it), not a stroke → applied: `attach()` in `Bubble.tsx`, lesson in the promo README. A magnified crop found it in one look where whole-frame stills had not.
- The chess board lost its top ranks in a short pane (an app bug the film exposed) → applied: `ChessBoard.tsx` sizes to the game area's height (app branch, verify.sh green).
- `line-sheets.sh` resolved its outdir with a bare `require()` → applied: fixed.
- deleted/merged: nothing subtracted — looked; the first film's tooling was reused rather than duplicated, and the three music tracks share one plan and one mix path rather than three.
## 2026-09-09 — "Claude models unavailable, you're not signed in" on a signed-in machine (session/claude-auth-live-status, both repos)
- **The model menu asked the SETUP WIZARD whether Claude Code was signed in.** `main.ts` answers `firstRun.getState()` with a bare `{currentStep:'COMPLETE'}` on every launch after the first — no auth fields at all — and `availability.ts` read that blank as "signed out", greying all four Claude models with "Sign in to use" while the pre-filled default ran normally. Destin's own instinct named the fix ("this menu shouldn't rely on setup, but on current connection state") and he was more right than that: ChatGPT, OpenRouter and the local engine ALL already answer live from `providerRegistry.list()`; Claude Code, the built-in engine, was the only provider with no live check at all → applied: `claude-code:status` on all five surfaces over `claude auth status` (0.13s measured), cached 60s, with a REAL Android arm because the phone runs Claude Code too. `main.ts` deliberately untouched — its bare answer is correct FOR THE WIZARD; a guard in `ipc-channels.test.ts` pins that no sign-in reader points at `firstRun` again
- **Four states, not two, and the fourth is the whole point.** `unknown` (probe timed out, output unparsable, a surface that cannot answer) leaves every model enabled; only a definite `signed-out`/`not-installed` greys anything, and each gets its own words. Put to Destin as a two-option Question and answered `leave-on`, then written into the type's own comment naming the deck answer, so a future session that finds it "unsafe" owes him a deck rather than a patch
- **A review shot that omitted a `?switch=` cost Destin a bogus product request.** My deck's Cloud-providers picture had no Claude usage bars because the plan lacked `?planUsage=1`; he read the picture correctly and asked for usage limits "in the same style as chatgpt" — which had shipped four days earlier and works from `~/.claude/.usage-cache.json` (8%/21% on this machine). **An omitted switch does not fail: it captures the EMPTY state, indistinguishable from a feature that was never built.** Measured: 11 of the 18 switches `mock-shim.ts` reads appeared nowhere in `scripts/ui-review/README.md`, `?planUsage` and `?chatgpt` among them — the two shaping the exact surface under review → applied, and moved up the ladder from prose to a check: all 11 documented, plus `undocumentedWorkbenchSwitches()` in `audit-anchors.mjs` + 6 tests, mutation-checked (3 red with the filter stubbed, green restored) and proven against the real files by removing `?planUsage` from an in-memory README copy. Ships GREEN — WARN not FAIL, since a switch is legitimately undocumented for an hour mid-build
- **A crop's SHAPE picks the deck layout, and a tall one silently eats your Risk card.** Six preview rebuilds and four recrops before I understood it: a portrait crop wins the side-column arrangement, which is ~2 cards deep, so the third clips mid-sentence and reads as a rendering fault — and shortening the words does not fix it. Nothing in `AUTHORING.md` or `review-deck.md` mentioned aspect at all (`rg -i 'aspect|portrait|landscape'` → 0 hits) → applied: the rule and the measurement in `deck/AUTHORING.md`. Cropping the whole window is the opposite failure — the pane grows until the cards leave the screen
- **`window.claude` is a Proxy: assigning ONE method is silently dropped.** `window.claude.firstRun.getState = …` in a shot's `eval` did nothing and the "before" capture came back identical to the "after" — a passing-looking comparison of two identical pictures, which I nearly shipped as the deck's evidence. Replacing the whole namespace sticks → applied: `scripts/ui-review/README.md` fidelity gaps. Same family as the ledger's most-repeated lesson, in a new place: **a check that passes for the wrong reason**
- **RECURRENCE, my own, of that same lesson — and it nearly ended this wrap-up.** I verified the new audit check by running `node /home/destin/youcoded-dev/scripts/audit-anchors.mjs`, the SHARED checkout's copy, which does not contain my change; it printed a clean result that meant nothing. Caught only because the JSON key was missing where it had been present a run earlier → nothing new to file; the rule already exists. Recorded because the trap here is specific and cheap to state: **from a worktree, an absolute path to a workspace script silently runs master's version**
- **I asked a trade-off as an Approve slide and got back the single word "confused".** The kind table's Question row read "ask before anything is drawn", which actively steered me away from the right kind for a decision about behaviour already built; re-asked as a Question with `today`/`problem`/`proposal` and two options carrying pros and cons, it was answered in ten seconds → applied: the table row now says "unbuilt, or already built", plus the "if 'should it' fits your headline, it is a Question" test
- **RECURRENCE (third) of `roadmap-check.mjs --fix` rewriting untouched files.** Filing one dev-workspace item downgraded a native-harness item to `needs-verify` and rewrote the index to match. Reverting takes THREE steps, because re-running `--fix` to repair the index re-applies the downgrade → extended the existing 2026-09-07 item with the third occurrence and the revert recipe rather than filing a duplicate; two sessions have now worked it out from scratch
- `workbench-boot-check.mjs` boots only the `scenario=*`/`view=*` routes, so none of those 11 switches has ever been booted by any check — while the README's own "Extending" section has told every session to add one → roadmap: `dev-workspace.md`. Out of this branch's scope to build, and my new check catches the documentation half of the same instruction but cannot boot anything
- deleted/merged: trimmed `review-deck.md` back under its 600-word budget after my own addition pushed it to 645 — the `deck/` selfie invariant lost its field list, which the same line already points at `AUTHORING.md` for. Net +19 words for two new invariants

## 2026-09-10 — session-context panel: the backend, Claude Code chats, and a tool that lied (session/context-truncation)
- **`scripts/cdp-eval.mjs` was dead where it lives.** It imports the `ws` package; the workspace root has no `node_modules`, so running it from `scripts/` — the only place anyone finds it — died on `Cannot find package 'ws'` before doing anything. Its own header documented the workaround (cd into `youcoded/desktop`, or symlink), which is prose asking to be honored instead of a tool that works. Node has had a global WebSocket since v22 and this repo runs v26 → applied: dependency-free, verified with a real run against a dev instance from the workspace root. Serves goal 1 directly — this is the tool for asking a running Electron window a question, and the next session that reaches for it will not have to write its own  [1 failed run + a 12-line inline replacement written from scratch]
- **`ui-probe` reports painted state that is one change out of date, stably, and its screenshot agrees.** After clicking a tab, `getComputedStyle` named the PREVIOUS tab as carrying the active background while the DOM classes and `aria-selected` were both correct — across reloads and multi-second settles. It looked exactly like a real rendering bug, so it was filed as one; the identical control in a real Electron dev instance was correct, and the roadmap item was withdrawn the same day → applied at two rungs: a header warning, AND a runtime warning that fires when an `--eval` asks about colour or style, pinned by two tests (one that it fires, one that it does NOT fire on a structural query — a warning on everything is a warning on nothing). Red-before-green confirmed by forcing the condition false  [the single largest block of the session: a wrong bug filed, a dev instance launched, a roadmap entry written and withdrawn]
- **Destin's rule for reporting what the app knows, which was written nowhere:** *"reflect what we can reflect accurately. And then for the stuff that's less certain. We should reflect it as such."* → applied: `docs/error-message-standards.md` grew a section extending its own rule from errors to any screen reporting state, with the shape that gets missed — **never let "unknown" render as "none"**. An unknown tool set sent as `[]` reads as "this assistant has no tools", false in the direction that makes someone trust it less than they should. Absent and empty are different values; keep them different to the screen. Pinned by `desktop/tests/claude-code-context.test.ts`
- **A guard I wrote was tautological, and the existing rule caught it.** The first version compared `assembleSystemPrompt` against `assembleSystemPromptParts` joined — but the former is DEFINED as that join, so it proved nothing. Mutation-checking it (test-suite-hygiene: "a guard you did not break is a guard you did not test") is what exposed it → dropped as a new rule: the rule already exists, was followed, and worked. The reusable shape, recorded here rather than as more prose: **a function defined as `f(g(x))` cannot be tested against `g(x)`** — guard the definition at the source level instead, which is what shipped
- **This branch's `CLAUDE.md` still says Android cannot be built on this machine.** It can — 741 tests, 0 failures, run today. Master fixed that paragraph on 2026-09-09; this branch is based on an older master → dropped: merging resolves it, and hand-editing it here would only manufacture a conflict with text that is already correct upstream. Worth recording that the auto-memory `env-android-sdk-exists` is what stopped me believing the doc — the memory earned its keep against a stale always-loaded file
- `run-dev.sh --offset` collides silently with another session's dev instance and fails late with a bare Vite port error → roadmap: `dev-workspace.md`. With four-plus concurrent session worktrees this is the normal case; CLAUDE.md's "use distinct --offset" cannot help, because the rule was followed and the guess was just taken  [1 wasted launch]
- The stranded-worktree WARN names four OTHER sessions holding 60 uncommitted files (idle 26-57h) → reported to Destin, not touched: committing another session's in-flight work under this session's authorship is the worse failure, exactly as the 2026-09-09 entry concluded
- deleted/merged: the duplicate `SessionContext` type — it was declared in the renderer while main had to build it, so it moved to `shared/types.ts` and the renderer now re-exports one definition instead of maintaining a second. Also `MOCK_ONLY` is back to empty (its last row, `native.onSessionContext`, got its real backend), and the wrongly-filed tab-paint roadmap item was withdrawn the day it was written rather than left to age

## 2026-09-10 — app-themed hover tooltips (session/app-themed-tooltips, both repos)
- **The roadmap item and its investigation had been carrying a count that was a third wrong for six weeks, and it shaped the plan.** "354 `title=` sites" came from a raw grep, which also matches component PROPS of the same name — `<Dialog title=>`, `<SettingRow title=>`, `<Callout title=>` are headings, not hover hints. The real number is 236. It was re-verified twice (08-12, 09-01) without anyone re-deriving it → applied: the investigation is archived `status: superseded` with both errors stated rather than edited away, and the roadmap item narrowed to what is actually left. The generalisable half is now enforceable, not prose — see the next item  [six weeks of a plan priced against a number ~50% too big]
- **`title` is not always a hint, and an automated sweep cannot tell.** Three separate components take it as CONTENT — `<SessionPreviewPane title>` (threaded down), `<AgentSection title>` (a section heading), `<Dialog title>` (a dialog heading). A transform wrapped all three, which both hid the heading in a hover bubble and DELETED the real prop. Nothing failed: not tsc, not the suite. All three were caught by hand, one per review round → applied: `youcoded/desktop/tests/tooltip-adoption.test.ts`, mutation-checked (re-wrapping `<Dialog>` turns two cases red), refusing a wrapper around any of those and around any component not on a named forwards-to-DOM list  [3 silent prop deletions, 1 per round]
- **Cloning a child to add behaviour REPLACES the handlers it already had.** `React.cloneElement(child, {...handlers})` overwrote `onPointerDown/Move/Up` on QuickChips' rows and SessionDrawer's resize grip — so wrapping a chip in a hint deleted drag-to-reorder outright, with no failure anywhere. Found by an audit script, not by a test → applied: handlers compose (the control's first), pinned in `tooltip-primitive.test.tsx` and mutation-checked  [1 shipped-quality bug caught pre-merge]
- **A synthetic `dispatchEvent` cannot answer a hit-testing question, and it answers confidently.** I "proved" that disabled controls receive no pointer events by dispatching one, and built a wrapper special-case on it — a wrapper that would have collapsed StatusBar's `w-full` theme-cycle row. Real CDP mouse input over a disabled `<button>` produces `pointerover`/`pointerenter`/`pointermove`; only `click` is suppressed. The whole special case was deleted → folded into the existing `ui-probe.mjs` roadmap item rather than filed twice: the durable want is `--hover`/`--move-to` on ui-probe, because settling this needed a ~50-line one-off CDP script  [1 wrong special case, 1 throwaway rig]
- **RECURRENCE of 2026-09-09's "run-workbench could not be pointed at a session worktree", in both of its remaining halves.** (a) `resolve-checkout.sh` knows the session layout but is handed the directory above `scripts/`, which INSIDE a worktree is the worktree — so every workspace-relative shape it checks silently cannot match; and a session worktree's basename is always `youcoded`, so the by-basename fallback never distinguishes sessions. (b) `deck/serve.py:resolve_worktree` is a SECOND copy that never learned the layout at all; its docstring says "the same three shapes" while the shared lib has five → applied to both, each with a red-before-green test (`scripts/ui-review/tests/resolve-checkout.test.sh`, wired into `workspace-ci.yml`; a `test_live.py` case covering three spellings). A THIRD copy lives in `run-dev.sh`; filed rather than touched, because it is the launcher that paints a real window on his desktop  [2 failed launches + 1 failed deck serve this session, 3 failed launches on 2026-09-09]
- **A live deck could not show the status bar it was reviewing.** The pane allow-list named six app scenarios, none of which pushes a `status:data` fixture — and the Cost/In/Out/Cached/Reuse/Speed/lines chips bail when they have no value, so they render in none of them. Also `yes`/`no` were silently dropped for a live step, so a pane of already-built, already-running work read "Yes, build it" → applied: the five `statusbar-*` scenarios allowed, labels passed through (`deck/build.py`); 228 deck tests green
- **A hover hint cannot be reviewed from a screenshot** — it does not appear in one — so this ran on Live decks with real-app panes throughout. Worth stating because the instinct is an Approve deck with before/after crops, and the before and after are pixel-identical
- **Destin reversed one of his own answers after using it, which is the flow working.** The questions deck chose the circled-i for paragraph-length hints; four hours later, hovering the built version: "keep hover tooltips actually." Reverted verbatim from the pre-change commit. He also corrected his own timing answer ("just a smidge faster", plus a fade between neighbours) and caught two scope misses I had guessed at — the Chat/Terminal switch, then "tags/notes? probably other status bar chips" → applied: stopped guessing and walked the component tree out from the four bars, which found 28 more
- **My own blanket replaces hit files outside the change twice in one session** (`.toBeInTheDocument()` across a whole test file; `getAttribute('title')` across six files where one was in scope). Both caught by reading the diff, both reverted → dropped: no durable guard beyond reading the diff, which is already the rule. Recorded because it happened twice in three hours
- Four other sessions' worktrees hold 60 uncommitted files (idle 27–57h), and `audit-anchors` is red on master for two reasons belonging to nobody's current branch — the duplicate 2026-09-07 permission-prompt docs and `native-specialists.md` at 624/600 words, the FOURTH time that file has gone over → reported, not touched: same call as 2026-09-09, another session's records are not this branch's to delete
- **A commit whose message described nine files contained two, and said so nowhere.** `git add a b c…` fails for the WHOLE pathspec if any one path does not match, and one of mine had just been `git mv`d — with `2>/dev/null` on the line, so the failure was invisible and the `&& git commit` that followed captured only what a later `add` had staged. It surfaced eight commits later, when a merge refused to run on a dirty tree; without that it would have merged, and the retrospective would have existed only in a commit message → applied here rather than as a rule: the durable form is **never hide stderr on a staging command**, and `git status --porcelain` after `add` shows the staged column (`M ` vs ` M`) that makes it obvious. Same family as the ledger's most-repeated lesson — a step that looks like it worked  [9 files of retrospective, silently uncommitted]
- deleted/merged: the disabled-control wrapper and its whole code path, removed once measurement showed it bought nothing; `docs/active/investigations/2026-09-01-app-native-tooltips.md` archived rather than kept beside the design folder that supersedes it; the ui-probe finding folded into the existing pointer-emulation item instead of filed beside it

## 2026-09-10 — the Markdown open, and a probe that was timing itself (perf/markdown-open, both repos)
- **RECURRENCE, and the ledger's most-repeated lesson in its purest form: a check that reports for the wrong reason.** The rig's `viewerState()` read `innerText` for its error tail, and `waitForViewer` polled it every 50 ms *while timing an artifact open*. `innerText` forces a synchronous layout, so the probe reflowed the very document it was timing and billed the app — the more document, the bigger the bill, which reads exactly like "markdown is slow". That is the whole of the "a 3.5 KB Markdown file costs 570 ms against 117 ms for a 400 KB code file" claim: a layout-free poll measures **93 ms**. A previous session filed THREE fixes against the Markdown renderer on the strength of it; that renderer turns the same file into a page in ~30 ms, so two of the three would have changed nothing a user could feel → applied: tail is opt-in (off in the loop, on at timeout), two mutation-checked tests in `scenario-artifacts.test.mjs`, and the general form written where the next rig author will hit it — *anything a poll touches must not force style, layout or text serialisation; `innerText`, `offsetHeight`, `getBoundingClientRect` and `getComputedStyle` all do*  [~500 ms of a 570 ms number was the instrument; 3 misdirected fixes filed, 2 of them worthless]
- **The gap that let it stand for a day: `explain.mjs` names the STEP, nothing named the FUNCTIONS.** The roadmap called this "not obviously general… may be a documented recipe rather than a tool" and deferred it. It was general, and it was a tool: `profile-open.mjs` takes a real V8 CPU profile across one open and ranks self time, bucketed by subsystem — which located the cost in the probe in a single run after a day of source-reading had not → applied, with 8 tests of the fold itself (self-vs-cumulative, one row per function across call paths, negative deltas, unmatched frames still bucketed), mutation-checked. Roadmap item deleted. **A deferred tool that is deferred because it looks specific is worth re-costing the next time it would have answered the question**
- **A benchmark can measure the right library and the wrong program.** My first two Node benchmarks timed the unified pipeline and got 3 ms for a small file — true, and useless, because the app's pipeline carries four custom rehype plugins the benchmark did not have. Only rendering the REAL component (30 ms) made the 570 ms gap undeniable enough to go looking at the instrument → dropped as a new rule: this is the existing "a check that passes for the wrong reason" wearing a different hat, and the ledger already carries it three times. Recorded for the specific shape: **when reproducing a number, reproduce the caller, not the dependency**
- **The fix that shipped is not the fix that was filed.** All three filed candidates aimed at making the Markdown pipeline faster. What actually costs is what highlighting LEAVES BEHIND — 7,056 elements become 108,576 — so the win came from `content-visibility` (the browser skips layout for off-screen code blocks) and a provable early return in `detectLinkTokens`, neither of which touches the pipeline: 1,487 ms → 939 ms, 93 ms → 60 ms, nothing visible changed → applied. The roadmap entry is rewritten rather than closed, because the remaining lever (stop colouring very large documents) changes what the user sees and is Destin's call, and its withdrawn 570 ms claim is corrected *in place* where the next reader hits it
- **`close-out.sh` told me three just-merged branches were "never pushed — nobody else can review this branch yet".** Its merge-commit lookup required a `/` or `'` before the branch name, which only ever matches GitHub's "Merge pull request #1 from itsdestin/<branch>"; a plain `git merge --no-ff` writes "Merge <branch>" with a space. Master's own history is full of the second form, so this had been misreporting every hand-merged branch — and it is the same misleading-message failure the two fixes beside it in that file were already for → applied: leading side now accepts whitespace or start-of-subject, trailing boundary kept so a prefix name still cannot match, `close-out-direct-merge.test.sh` red-before-green and re-checked against real history  [3 branches reported lost, minutes after landing]
- **Two sessions in one day stopped to prove the same test failure environmental.** `fixture.test.mjs` reads the app repo's `engine-pin.ts` on purpose (the rig must not keep a second copy of the engine version), so in any workspace worktree created without `youcoded` — every bare merge worktree — it fails with a bare ENOENT that looks like a real break → applied: both tests now `skip` with the reason and the command that fixes it. Absent is not failing  [2 diagnosis rounds, same cause, same day]
- **I warned Destin that profiling would open a window on his desktop. It does not.** Every rig entry point launches on Xvfb; the README mentioned Xvfb only in a config table and a WebGL note, never as "this does not touch your desktop", so the standing warn-before-a-window rule looked like it applied → applied: stated at the top of the perf-lab README, both halves — it does not apply here, and a session that assumes it does will either warn for nothing or avoid the right instrument
- **A backtick inside a template literal silently ate a module.** My WHY comment for the probe fix used `` `withTail` `` — inside the big in-page-source template — which closed the string and turned the file into a syntax error at import. Loud and immediate (the test run failed on import), so cheap → applied as a WHY at the edit site ("No backticks in this comment — it lives inside a template literal"), not a rule: one occurrence, caught in seconds, and a lint rule for it would fire on every legitimate nested backtick
- `step-guard-row.test.tsx` flaked a THIRD time, a third of its eight tests, on a branch touching only markdown rendering and CSS; 3/3 isolated green and the next full run green at 10,280 → recorded on the existing entry with the evidence rather than filed again. Three tests, three unrelated branches, never outside a full run: the file's shared timer setup is the suspect, and it now costs a re-run on every branch that trips it
- **`roadmap-check.mjs --fix` did NOT rewrite untouched files this session** — checked after all four runs, only intended files changed. Recorded because the ledger carries three prior occurrences; whatever changed, it is behaving
- deleted/merged: the "which stage inside a slow step" roadmap item, deleted because the tool now exists rather than left open beside it; three throwaway benchmark scripts written during the investigation, none kept — `profile-open.mjs` replaces all of them and the two that measured the wrong program would have misled the next reader
## 2026-09-10 — Resume browser filter chips: three decks, contract, two reviews, acceptance (session/resume-filter-chips, both repos)
- **I built four decks and both review files in the SHARED `youcoded-dev` checkout, not the session worktree `workspace-start` had returned**, and moved ~50 files across before the first workspace commit; the rule is in `CLAUDE.md`, but the deck docs said only "docs/active/design/<feature>/" → applied: `deck/AUTHORING.md` "Where a deck lives" now names the session worktree.
- **Rounds 2 and 3 reused round 1's deck key; nothing complained until the contract agent, hours later, could not resolve six sources** and I re-keyed two answered decks by hand → roadmap: `dev-workspace.md` (a build-time warning). Cost: one agent round-trip and a hand edit to answer files.
- **Both UX testers lost their first shots to the workbench root page, which frames the app where the kit's tool cannot see** (tester 1 found `&child=1&view=app`, tester 2 had to be told) → applied: `ux-tester.md` briefing carries the working address and says why.
- **The grader caught a crash I shipped in the LAST fix of the day**: a layout effect that depended on a position object it rewrote every run ("Maximum update depth exceeded" on the phone panel). Neither `verify.sh` nor my phone-width screenshots caught it, because the panel had never been opened by a test → applied: `resume-browser-filter-row.test.tsx` mounts the browser at both widths; removing the bail-out turns it red. Lesson kept in the test's header.
- **The run-path note in `deck/AUTHORING.md` (2026-09-09: "from the workspace root") sent my first build into `<spec dir>/docs/active/design/…/runs`**; on today's code the loader resolves `runs` against the spec's folder → applied: the line now says so, with the date it was measured.
- **`close-out.sh` was the only thing that noticed the app branch was 51 commits behind master** (a one-file conflict in `ContentFindBar.tsx`); nothing during the day said the base had moved. The contract, grading and acceptance all ran on the stale base — harmless here, not in general. Dropped as a finding: `workspace-start`'s report already prints the behind-count; I did not read it back after day one.
- **Destin's calls worth knowing beyond this row** (all on the answers files): a segmented switch for a two-way sort is "weird"; filter groups on a phone should be dropdowns, not chip rows; a single flipping arrow reads wrong, the sort-bars glyph is right; the word stays "sessions" on this screen; the dialog may keep shrinking; the shared search pill was rejected for the desktop Resume box → applied to the design guide's `SearchFilterPill` row (do not re-propose); the rest are recorded by the decks.
- **Fresh-agent reviews earned their cost this time**: UX run 1 → 11 accepted (a real mis-click bug); code review → 6 accepted (a ref-nulling bug across the breakpoint, a guard that tested nothing); UX run 2 → 3 accepted (the menu-closing tap was also closing the browser); grader → 1 crash. Four real defects none of my own checks saw.
- deleted/merged: three inline copies of the dropdown chevron (`Select`, the find bar, the new chip) became one `ui/ChevronDown`; the old 11px `FilterPill` and its `callout-authority` exemption are gone; the 8 MB `runs/` capture folders were dropped rather than archived (the cut crops are the record).


## 2026-09-10 — addendum, same session: "why do we keep leaving work unpushed?" (we don't)
- **Destin asked whether something was programmatically breaking the local checkouts. Nothing was — the three things that ANSWER that question were all wrong, in the same way, at once.** `git push origin <branch>` without `-u` uploads everything and sets no upstream; all three readers treated a missing upstream as "never pushed". Measured across six repos: **77 branches flagged as local-only, every one already on the server byte-identical**, one of them 7 commits past master and therefore reported as 7 lost commits. Zero branches anywhere held a commit the server lacked → applied in all three: `strandedWorktrees()` and the session-start worktree list now ask `git rev-list HEAD --not --remotes` (can any remote reach this?), and the wrap-up sweep in this skill was rewritten from `%(upstream:track)` to the same test. Each mutation-checked red against the old logic  [77 false alarms standing for weeks; ~3 sessions' worth of "report the stranded branches" paragraphs written about work that was never at risk]
- **A cry-wolf check is worse than no check, and this is the proof.** These three exist precisely to catch stranded work. At 77 false positives a real one is invisible — and there IS a real one: minutes after the fix landed, the corrected sweep found `session/remote-mesh-roadmap` holding a commit no remote had, in both repos. It turned out to be another session committing 22 seconds earlier (live work, correctly left alone), which is exactly the signal-to-noise the old version could not have delivered
- **The exposure was never branches.** Same sweep: zero unpushed commits, **~100 UNCOMMITTED files across eight worktrees**. No push rule reaches those, and a full day of finished work was lost that way on 2026-09-01. `strandedWorktrees` already catches them but only runs inside `audit-anchors.mjs`, which nobody runs at session start → applied: the session-start list now marks uncommitted worktrees with ⚠ alongside genuinely-unpushed ones, so the two states where work can be lost stand out from a list of twenty. `ahead` is kept for what it is actually good for — spotting a worktree that is finished with
- **`push.autoSetupRemote=true` set globally**, on Destin's say-so, so future pushes self-link and no session has to remember `-u`. The durable half is the detectors above: config fixes new branches, the tests fix the reading of every branch
- **A wording fix inside an existing test was the tell.** It asserted the hook says "no upstream to compare against" — and "upstream" is the exact word that produced the whole confusion. A missing upstream says nothing about backup; a missing ORIGIN is what stops the comparison. Changed deliberately, with the reason in the test  [the phrase had been carrying the wrong mental model into every session that read the hook]
- deleted/merged: nothing new subtracted this addendum — the three fixes replaced wrong logic in place rather than adding a fourth checker beside them, which was the available mistake

## 2026-09-10 — Android parity audit, rebuild decisions and steps 2–3 (session/android-parity-audit, session/android-rebuild-1, android-docs-cleanup; both repos)
- **The bridge's catch-all reply resolved as data, and the guard that would have caught it existed only for the browser.** `remote-shim-unsupported.test.ts` pinned `{unsupported:true}` for the desktop's remote server months ago; Kotlin's `else ->` answered a bare `{error}` and nothing read it → applied: `MessageRouter.buildUnsupportedResponse` + `android-honest-build.test.ts` + `remote-shim-phone-refusals.test.ts`, each shown red on revert. Project View crashed on the phone because of it; unknown how long
- **Four "read the red check" investigations in one session, each the same eight calls, each ending "already red on master"** (workspace git-identity test ×3, desktop Windows harness checkpoint ×2, one real Electron-download hiccup) → applied: `scripts/ci-red-vs-master.sh` (compares failing test names on the PR with master's latest run of the same workflow; exit 0 = all pre-existing), one line in `docs/workspace-workflows.md`. ~2–3 min and ~8 calls per repeat
- **The desktop Windows CI leg is red on master and nobody had filed it** (only the workspace identity test was) → roadmap: `dev-workspace.md` → tests
- `step-guard-row.test.tsx` flaked a FOURTH time, on a branch touching only Kotlin and the shim; alone ×2 green, master alone green → recorded on the existing dev-workspace entry
- **Deleting a block of Kotlin handlers took an unrelated handler that sat in the middle of it** (`shell:open-external` between two `sync:restore:*` arms); `ipc-channels.test.ts` caught it before merge → dropped: the guard executed. The `when`-label diff that proved only restore labels were gone is in the fix commit message
- **"Five-surface parity" in ~25 commit subjects meant channel-name stubs, not features** — reading the git log overstated Android by a lot → applied in the audit report (§1) and its Status block; the honest ledger is `SessionService.kt`'s not-implemented arm
- **Destin's decisions, all filed:** deck answers committed; D-U-N-S arrival in the roadmap and `~/system/legal/playbooks.md`; iOS exploration as a roadmap item; Play prioritized on the dev-workspace item
- **The script's first version compared against ONE master run and called an intermittent runner failure "new"** (perf-lab LIVE tests: "Chrome never opened its debugging port", red on one master run in five). Now the last five completed runs → applied in the script; the flake itself → roadmap: `dev-workspace.md` → tests
- The Android CI job failed once on `npm ci` downloading Electron ("fetch failed") and passed on re-run → dropped: runner network, no pattern yet
- deleted/merged: 30 Android roadmap items folded into one report appendix and ONE rebuild item; two resolved investigations archived; ~1,300 lines of phone-only restore backend and the `layoutInsets` plumbing deleted; the tracked April web bundle untracked

## 2026-09-10 — set up r/youcoded by driving Reddit in a Chrome window Destin logged into; filed moderation automation (session/reddit-mod-automation)
- **RECURRENCE of the ledger's top lesson, a step that reports for the wrong reason, this time in throwaway scratch scripts.** The first driver called `process.exit()` in `finally`, which swallowed a ReferenceError: the run deleted r/youcoded's two starter rules, added nothing, and printed nothing. Later a UI step logged "ok" for five saved responses Reddit had refused (the name went into the Rule box). Both were caught only by reading live state back → dropped as a workspace change: one-off scripts on tmpfs, the lesson is already the ledger's most repeated, and the moderation route now filed is Reddit's own tools, not a browser rig. Cost: ~6 extra calls, and a few minutes with no rules on a one-member community.
- **`docs/MAP.md` named the landing page `itsdestin.github.io/youcoded`; the site is youcoded.ai** (`youcoded/docs/CNAME`; the github.io address 301s there). Cost three searches across three candidate domains → applied: the MAP row.
- **Destin's subreddit goal and one unanswered proposal, filed:** moderation automation → roadmap: `dev-workspace.md` → release (`decision`). Linking r/youcoded from the app and youcoded.ai (proposed, not answered; 0 Reddit matches on youcoded origin/master) → roadmap: `other-features.md` → misc (`decision`).
- Reddit facts a later session would otherwise search for (new API access closed Nov 2025; welcome messages moved to the Community Guide; removal reasons are now Saved Responses; old.reddit.com still works when logged in) → dropped as workspace knowledge: recorded in `~/Documents/youcoded-subreddit-setup.md`, which the roadmap item links.
- A `cd` into the shared `youcoded` checkout inside one Bash call moved the session's working directory out of its worktree → dropped: my slip, already in memory ("cd persists"); recovered with absolute paths.
- deleted/merged: none found. Looked; the MAP fix replaced a wrong address in place rather than adding a line.

## 2026-09-10 — app chrome can't be highlighted or copied (session/unselectable-chrome, both repos)
- **RECURRENCE (same day as resume-filter-chips): the workbench frames the app, and a probe that forgets it measures the toolbar.** My first two "Before" probes timed out waiting for `.status-bar` and selected only the workbench toolbar; the earlier fix went into `ux-tester.md`, which a session using `ui-probe.mjs` directly never reads → applied: the trap and the `iframe.contentDocument` reach-in are now at the top of `ui-probe.mjs`'s usage block, where every caller looks  [2 probes + 1 screenshot read]
- **`?seed=none` is documented as "empty chat" but only empties the `site` scenario** (`seed-chat.ts:175`); on the default scenario it silently seeds the full conversation, so a whole commit/swap/capture round produced two pictures of the wrong screen → applied: `.claude/rules/landing-page.md` and `scripts/ui-review/README.md` now say `&scenario=site`  [1 capture round, 2 deleted pictures]
- **RECURRENCE of the ui-probe "no real input" item: two more throwaway CDP scripts** — a mouse drag to prove a text box still selects, then a rerun because headless `:focus` never matches without `Emulation.setFocusEmulationEnabled` (the first run reported the focused style absent and nearly sent me chasing a non-bug) → roadmap: the evidence added to the existing `dev-workspace.md` ui-probe item, with `--drag` and default focus emulation named; not built here because it is its own tested change to a shared tool
- **The "obvious" iOS-safety rule caused the exact bug being fixed.** `textarea { user-select: text }` (the common fix for fields under an unselectable parent) made a page-wide select-all paint the EMPTY composer's placeholder; only the After screenshot showed it. A six-variant headless paint + drag test settled it: re-enable on `:focus` only → applied: WHY in `globals.css`, and `unselectable-chrome.test.ts` refuses an always-on field rule (mutation-checked)  [caught pre-merge by looking at the picture, not by any test]
- **Buttons are not always chrome: clickable file names in messages are `<button>`s.** A blanket `button { user-select: none }` would have silently dropped file names from every copied sentence → applied: `select-text` opt-in on both `FilepathToken` variants, pinned in both the source test and `build-menu.test.tsx`
- **`git add $F` in the Bash tool does not word-split** (it is zsh), so a space-joined path list became one bogus pathspec; loud, nothing staged or reverted → dropped: failed safely in one call; arrays in a bash script fixed it
- **My rule note pushed `react-renderer.md` to 633/600 words**; `audit-anchors.mjs` caught it → applied: cut to one line. `native-specialists.md` at 624/600 is still red on master (fifth sighting in this ledger) → reported, not mine
- deleted/merged: none found — the change added a rule line and a test rather than replacing anything; I looked for an existing selection guard to extend and there was none
