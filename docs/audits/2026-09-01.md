---
date: 2026-09-01
scope: diff-scoped
residue: 1
verified_shas:
  workspace: 591932a949ac5bb724d92c0a2d910f0b8d497c54
  youcoded: f2d229e4bff42bceb56b777f8694bbd9027a4108
  youcoded-core: 39fb9413aefa7029b3fd070384914ac1a382264e
  youcoded-admin: a4d9e8cb2b3c82186cf61c12053b92451da983d8
  wecoded-themes: 908b9380ce0223d71a0c552dcdc2a232aaadc46c
  wecoded-marketplace: a497eeb25ef47f183d5e35f97cd7a779c9c41c77
---

# Workspace audit — 2026-09-01

Run at the end of the roadmap migration (spec §6.3.7), on branch `feat/roadmap-migration`
(youcoded-dev PR #14) with the sub-repos linked in read-only. Diff-scoped from the
2026-08-31 report: 302 changed files intersected twelve rules. Every rule below was
semantically re-verified by a read-only agent against the code, and every drifted claim
was corrected in the same run — 62 corrections across three repos.

## Mechanical pass

`node scripts/audit-anchors.mjs --json` → ok. All `verify:` anchors, doc anchors, MAP paths
and rule globs resolve; every rule body ≤600 words (four had to be trimmed back after the
corrections below: native-runtime, react-renderer, registries, sync-spaces); eager load
7,623 tokens. `strayRuleDirs` still WARNs on `youcoded/.claude/rules/android-runtime.md` —
its deletion is the second commit of youcoded PR #378 (see Residue).

## Roadmap

```
## Roadmap check

### Structure — clean

### Claims — 103 checked, 0 broken
checked against: workspace=591932a9 youcoded=f2d229e4 youcoded-core=39fb9413 youcoded-admin=a4d9e8cb wecoded-themes=908b9380 wecoded-marketplace=a497eeb2

### For Destin — 19 decision(s), 0 item(s) unconfirmed for 60+ days
- decision android-only:25 Android keeps a native "layout insets" reading of the chat's header/bottom chrome that
- decision android-only:54 What should "Online" mean on a phone? Desktop counts you present only when awake and rece…
- decision chat-data:21 "Welcome back" on cold start — after a window close, crash or OS kill, list the chats tha…
- decision dev-workspace:63 The session-retrospective triage tool (`conversation-triage.mjs`, 526 lines, the accelera…
- decision dev-workspace:73 The old review-harness script still lets the model it runs read the OpenRouter key (its
- decision dev-workspace:123 The sync-spaces rule's line "sync dots are the ONE sanctioned status-color use" is over-b…
- decision local-models:5 Loading a second large local model took the whole machine down — Qwen3.5-122B and
- decision marketplace:14 Below five votes a marketplace card says "3 people found this helpful" / "1 of 2 people f…
- decision marketplace:41 Put the Worker on a custom domain. It is served from a `workers.dev` address, where
- decision native-harness:27 Resuming a native conversation that never got a title (predates the title feeder, or all
- decision native-harness:43 Design call: when an edit of a queued message refills the composer in the same instant the
- decision native-harness:125 After the shell has cd'd elsewhere, Read and Bash can silently open two different files f…
- decision remote-access:8 Over remote access whole features are simply missing: the files panel cannot open any
- decision sync:40 Decide: should stray `*.tmp` files be ignored by sync outright? A crash between write and…
- decision sync:76 The last open v1.3 gate: after signing in to GitHub from inside the app (Connect GitHub),…
- decision themes:24 A theme's icon overrides are accepted, and the Library shows a "custom icons" badge for
- decision user-interface:18 Decide the app-wide GB convention — the Local Models row says 74.2 GB for a download
- decision user-interface:40 Fold Defaults + Permissions + Model Providers into one "Assistant settings" panel —
- decision user-interface:78 Two chip questions left over from the Phase B review, unanswered: the theme chip's

### Index — matches the area files
```

Two tool quirks surfaced by the migration, both now documented in the PR body and worth a
fix in `roadmap-check.mjs`: `checkAnchor` compiles `contains` without the `m` flag, so a
leading `^` only ever matches line 1; `harvestDocAnchors` strips backtick spans before
parsing, so a backtick inside `contains` is deleted before the regex is built. Seven agent
anchors were rewritten around them.

## Semantic verification — twelve rules, what was fixed

| Rule | Drift found → fixed | Where |
|---|---|---|
| android-runtime | 9 (setup-node@v7; netrc sync runs in first-run `setup()`; PtyBridge line refs; `screenMode`/`viewModeRequest` gone; ~200 dispatcher branches; rclone fix is in `SyncService.kt`; execve artifact deleted `e94154b3`) | rule; `youcoded/docs/android-runtime.md` (PR #378) |
| artifacts | 13 (count helpers live in `projects-index.ts`, no shared cache; Android `check-existence` stub + more not-implemented channels; FilesTab has no externals/Exclude; four hero IPCs; sequential `useOpenFilepath`; unconditional `applyGitTreatment`; `h-44` is the file card; `paths:` key; switcher fallback count) | rule; `youcoded/docs/artifacts.md` |
| engine-local-models | 10 (pin b10665; card states idle/loading/error; `models:orphaned-partials` gone; rule now covers `providers/provider-registry.ts`; adaptive 400 ms/1.5 s poll; `--sleep-idle-seconds`; spawn lines 290-309; `HOSTED_MAX_CONCURRENT_SPECIALISTS`; `parallel_tool_calls` gated on `serialToolCalls`) | rule; `youcoded/docs/engine-dependencies.md` |
| harness-evaluator | 6 (`max_steps` allowed once; two exempt read roots; orchestrator+estimate tests pinned; no single input:output ratio; `send()` quirk is pinned; `providerCostUsd` per cell) | rule; `youcoded/docs/harness-evaluator-internals.md` |
| landing-page | 3 (theme button clicks the app's Appearance row — swatch bar gone in `8d077dcd`; never-claim list is the audit's; footer sentence, not a paragraph; anchor moved onto `index.html`) | rule |
| narrow-viewport | 4 (700 px collapse survives; one `.frame-edge` in TerminalRightSlot; three `TOGGLE_PANEL` callers; shim adopts server platform unless `preservePlatform`) | rule |
| native-runtime | 7 (`createSession` throw condition; `mutateJson` → `mutateFileUnderLock`; interrupt/retry fire-and-forget; askUser at ~:2496 via `childAskRouter`; `shared/subject-glob.ts`; MAX_TAGS/MAX_DEPTH ordering) | rule; `youcoded/docs/native-runtime.md` |
| react-renderer | 13 (contrast thresholds 8/5.5/4/3/2 on five surfaces; contrast CI blocking; resizable pane vars; `--canvas` drawer bg; attention diff in App; `useNarrowViewport` in HeaderBar; narrow toggle placement; `--panels-*` tokens; six L3 confirmations; ProjectHero L4 / ProjectView z-40; `escapeHost` unused; ~200 branches) | rule; `youcoded/docs/renderer-chrome.md`, `shared-ui-architecture.md` |
| registries | 7 (only `{{plugin_root}}` is expanded; `sourceMarketplace: "youcoded"`; theme size gate is the directory; `command_windows`; rebuild gated on a `plugin.json` dir; five footguns) | rule; `docs/registries.md`; `wecoded-marketplace/docs/mcp-authoring.md` (PR #81) |
| sync-spaces | 9 (11 contract tests at 120 s; nine Android stubs exist; description fields on master; only description is `{v,at}`-wrapped; health check generates three codes; retry lives in `reconnecting-ws.ts`; dead test name; `runDiscovery()` after the hub send) | rule; `youcoded/docs/sync-spaces.md` |
| test-suite-hygiene | 6 (ipc-handlers ~4,500 lines; watchdog margins 4 vs 250/400 ms; shell ids random; 98 waitFor calls / 14 files; 154 tsx suites; verify.sh's 296 → ~600) | `docs/testing-under-load.md`; `scripts/verify.sh` |
| worker-backend | 8 (`requireAdminAccount`; typecheck gate; `KNOWN_DEV_DEVICES` not CI-pushed; vitest-pool-workers ^0.22; D1/KV mirrors; rate-limit coverage; seven endpoints; ingest shipped 2026-08-30) | rule; `docs/build-and-release.md`; `wecoded-marketplace/docs/worker-backend.md` (PR #81) |

Unverifiable claims (runtime measurements, upstream facts, historical counts) are listed
in each agent's transcript and were left as written; none contradicts the code.

## Findings that became roadmap items

- marketplace → install: `${PACKAGE_DIR}` in two shipped MCP manifests is expanded by nobody;
  and those manifests declare `platforms: [...]` while the reconciler reads a singular
  `platform`, so the filter is a no-op.
- dev-workspace → release: `app/build.gradle.kts` labels the vendored terminal emulator GPLv3
  while `VENDORED.md` says Apache 2.0.

## Gardening

- Six docs in `docs/active/` already marked shipped/superseded moved to `docs/archive/`
  (twelve references repointed); `2026-05-20-floating-glass-theme-design.md` gained YAML
  frontmatter (`status: draft`). Generated rig outputs under `docs/active/design/2026-08-25-ui-audit/`
  and `docs/active/investigations/harness-eval-runs/` carry no frontmatter by design.
- `.claude/rules/engine-local-models.md`'s orphaned-partials bullet named a channel and a
  function that no longer exist; rewritten from the code.
- Outward-facing docs: no README, LICENSE, sub-repo CLAUDE.md, landing page or privacy copy
  changed since the 2026-08-31 report.
- Auto-memory: nothing planning-shaped; left as is.
- `docs/audits/2026-08-31-retrieval-repair.md`'s one residue item (the stray sub-repo rule) is
  closed by youcoded PR #378.

## Residue

**One item, cross-repo ordering.**

- `scripts/audit-anchors.mjs` should turn `strayRuleDirs` from WARN into a failure once
  `youcoded/.claude/rules/android-runtime.md` is gone. Recommendation: merge youcoded
  PR #378 first, then flip the check in a one-line follow-up on master. Flipping it on this
  branch would hold workspace CI red until #378 lands.
