---
plan: docs/active/plans/2026-07-15-workspace-knowledge-mgmt-phases-1-2.md
started: 2026-07-15
residue: 3
---

# Knowledge-management execution changelog

Running record of every disposition made while executing Phases 1+2. `residue:` above counts
open items awaiting Destin's decision (listed under ## Residue). Update the count whenever
the list changes — the session-start hook greps it.

## Dispositions

Task 3 — knowledge-debt triage → ROADMAP.md. Rows 1–36 are the 36 dated knowledge-debt
entries in file order; rows 37–40 are the extra deferred-work items swept in from PITFALLS.md
(Task Step 3) that had no knowledge-debt entry. `docs/knowledge-debt.md` is deleted in Task 4.

| # | Item | Disposition | Confidence | Notes |
|---|------|-------------|------------|-------|
| 1 | Restore-from-backup removed; redesign deferred (2026-07-14) | → ROADMAP (seeded) | High | Skeleton Someday "Restore-from-backup redesign" line captures it; the Android restore-backend demolition follow-up is folded into the v1.3.1 Android item. No duplicate added. |
| 2 | DiffusionGemma support deferred (2026-07-13) | → ROADMAP | High | Someday idea `#local-models`; upstream-gated on llama.cpp PR #24427 + llama-server diffusion support. |
| 3 | Misleading error messages — full audit + replacement (2026-07-14) | → ROADMAP (seeded) | High | Skeleton v1.3.1 line captures it; enriched detail with the shipped engine sub-fix (PR #123) + open scope (workspace audit + two-action fallback component). No duplicate. |
| 4 | StatusBar usage chips not fed for native sessions (2026-07-13) | → ROADMAP | High | Features `#native-runtime`; renderer→main IPC to feed status:data. Land with Phase 2 (youcoded PR #119). |
| 5 | Cross-device project auto-discovery (2026-07-12) ✅ RESOLVED | Drop (resolved) | High | Verified merged to youcoded master (1f397c87 + b5d29f34 + followups). Deferred residuals already tracked in handoff A01. |
| 6 | Onboarding.tsx screen deferred (2026-04-12) | → ROADMAP | High | Verified Onboarding.tsx absent in youcoded 2026-07-15; Features. |
| 7 | Icon override system is dead code (2026-04-12) | → ROADMAP | High | Verified no `theme.icons[slot]` consumers in components 2026-07-15; Features (wire-or-remove). |
| 8 | Sign + size-cap announcement payload (2026-04-21) | → ROADMAP | High | Features `#security` `#announcements`; defense-in-depth hardening. |
| 9 | CC-drift: Glob/Grep merge into Bash tool card verify (2026-04-21) | Drop (subsumed) | High | Pure CC-version regression verification — subsumed by the youcoded/docs/cc-dependencies.md CC-bump review flow (redesign's home for CC-version watch items). |
| 10 | CC-drift: CLAUDE_CODE_FORK_SUBAGENT toggle (2026-04-21) | → ROADMAP | High | Someday idea (dev-mode settings toggle); no coupling. |
| 11 | CC-drift: cleanupPeriodDays coverage audit (2026-04-21) RESOLVED 2026-07-15 | Drop (resolved) | High | Entry marked RESOLVED (Plan 2c; retention-default.ts seeds 365d correctly). |
| 12 | Legacy conversation-index READ-ONLY / full retirement (2026-07-15) | → ROADMAP | High | Features `#sync`; delete read path + on-disk file once residual legacy rows unneeded. Same item as PITFALLS-sweep "legacy conversation-index full retirement" — deduped to one ROADMAP line. |
| 13 | Document Go-binary exec trap in Android runtime docs (2026-04-23) | Fix now | High | Verified missing from docs/android-runtime.md; added a "Go binaries can't exec scripts in ~/.claude-mobile/" subsection under System Fundamentals (why/symptom/two safe paths, cites the rclone fix 6469e058). |
| 14 | Android Library doesn't show locally-built themes (2026-04-25) | → ROADMAP | High | Features `#android`; port synthesizer to Kotlin. Same as PITFALLS-sweep "Android local-theme synthesis parity gap" — deduped to one line. |
| 15 | Android integrations install/connect/uninstall (2026-04-28) | → ROADMAP | High | Features `#android` `youcoded#78`. |
| 16 | Analytics payload ↔ privacy copy must stay in sync (2026-04-24) | Drop (subsumed) | High | Subsumed by /audit's outward-facing-docs review (redesign) — the audit now checks copy/code sync; no standing ledger entry needed. |
| 17 | CC-drift: adopt PostToolUse updatedToolOutput (2026-04-29) | → ROADMAP | High | Someday idea `#hooks` (secret/PII redaction at tool-output boundary). |
| 18 | CC-drift: wrap claude ultrareview CLI in admin skill (2026-04-29) | → ROADMAP | High | Someday idea (headless review in release pipeline). |
| 19 | CC-verification: scrollback/orphan-spinner/MCP-spawn/TaskList (2026-04-29) | Drop (subsumed) | High | CC-version regression verification — subsumed by the cc-dependencies.md CC-bump review flow. (Its scrollback bullet's product angle lives in the ROADMAP "xterm scrollback duplicate-chrome mitigation" idea.) |
| 20 | CC-verification: install-prereq bash/curl + reg.exe (2026-04-29) | → ROADMAP | High | curl→wget fallback (v1.2.4) + dynamic reg.exe path (2026-05-22) already RESOLVED; only the optional `bash --version` probe carries → Someday idea `#install`. |
| 21 | CC-drift: surface CC /goal in the UI (2026-05-18) | → ROADMAP | High | Someday idea (status-bar widget/banner). |
| 22 | CC-drift: CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN for Android scrollback (2026-05-18) | → ROADMAP | High | Folded into the single "xterm scrollback duplicate-chrome mitigation" Someday idea (two approaches: bump scrollback 5000+, or the env var) — deduped with the PITFALLS-sweep scrollback item. |
| 23 | CC-drift: surface CC agent view / background sessions (2026-05-18) | → ROADMAP | High | Someday idea `#sessions`; large/speculative. |
| 24 | Local-mode tool views: list/patch render raw (2026-05-19) | Drop (subsumed) | High | OpenCode-adapter-specific tools with no native-harness equivalent; feat/opencode-mvp is archived. Nothing to carry forward. |
| 25 | Local-mode subagent (task) empty card (2026-05-19) | Drop (subsumed) | High | OpenCode AgentPart translation; native-harness subagent routing is already cataloged (PITFALLS Phase-0/1 / PR #115). Archived branch — no carry. |
| 26 | Local-mode: no Android parity (2026-05-19) | → ROADMAP | High | The desktop-only gap carries to the native harness → Features `#native-runtime` `#android`. |
| 27 | Local-mode: image input (FilePart) unhandled (2026-05-19) | → ROADMAP | High | Multimodal-input gap carries to the native harness → Features `#native-runtime`. |
| 28 | Local-mode: session.compacted event unhandled (2026-05-19) | Drop (subsumed) | High | OpenCode-specific event translation; no native-harness equivalent. Archived branch — no carry. |
| 29 | Roadmap: YouCoded Cloud sync transport (2026-07-03) | → ROADMAP | High | Explicit roadmap commitment → Someday idea `#sync` (spec §16). |
| 30 | Local-mode: stuck-detection inactive (2026-05-19) | → ROADMAP | High | PTY-less classifier gap carries to the native harness → Features `#native-runtime`. |
| 31 | Accounts Phase 1: dispositioned follow-ups (2026-07-08) | Drop (resolved) | High | Entry states ALL EIGHT items CLOSED (worker PR #20 + client 814365c4); soak-only. |
| 32 | Accounts Phase 1 plan docs show pre-hardening code (2026-07-08) | Drop (subsumed) | High | Documentation-only caveat; subsumed by the redesign's plan-archival (docs/archive + status markers). Merged worker code is source of truth. |
| 33 | ModelPickerPopup /fast + /effort bypass prompt gate (2026-07-09) | → ROADMAP | High | Bugs `#pty-writes`; thread a guarded sender. |
| 34 | Copilot/AI key buddy floater (2026-07-14) | → ROADMAP | High | Someday idea `#games` (globalShortcut binding). |
| 35 | Unified synced SystemState (2026-07-14) | → ROADMAP | High | Someday idea `#sync`; speculative multi-device state. |
| 36 | Accounts Phase 2: dispositioned follow-ups (2026-07-09) | → ROADMAP | High | Open items (two-person verification is the real risk) → Features `#accounts`. |
| 37 | Android artifact `not-implemented-on-mobile` stubs — mobile Project View v2 (PITFALLS sweep) | → ROADMAP | High | Features `#android`; cites PITFALLS "Artifact Viewer". No knowledge-debt entry. |
| 38 | `.partial` orphan scan IPC — local-models Plan C v1 gap (PITFALLS sweep) | → ROADMAP | High | Someday idea `#local-models`; cites PITFALLS "Phase 1 Plan C". No knowledge-debt entry. |
| 39 | Amendment K2 router hot-reload of --models-dir (PITFALLS sweep) | → ROADMAP | High | Features `#local-models`; cites PITFALLS "Phase 1 Plan C". No knowledge-debt entry. |
| 40 | Android PtyBridge echo-driven submit TODO (PITFALLS sweep) | → ROADMAP | High | Features `#android` `#pty-writes`; cites PITFALLS "PTY Writes → Android". No knowledge-debt entry. |
| 41 | `docs/knowledge-debt.md` | Deleted (triaged to ROADMAP in Task 3) | High | Every dated entry dispositioned in rows 1–36 above; file removed in Task 4. Session-start staleness detection repointed at `docs/audits/`. |
| 42 | `GEMINI.md` | Deleted (Gemini CLI discontinued June 2026) | High | Dead file — Google discontinued the Gemini CLI; the `gemini` provider was removed from the codebase (PITFALLS "Multi-Model Provider Seam"). |
| 43 | `docs/local-dev-vm.md` | Residue — awaiting Destin | Low | Is the VM flow still used? Recommend: archive (superseded by run-dev.sh isolation) unless still in use. File untouched pending the answer. |
| 44 | CLAUDE.md `@import` block → replaced with lazy pointer table | Done | High | Removed the 7 `@docs/...` eager imports; added "Where Knowledge Lives" taxonomy + document-lifecycle convention, a "read on demand" Subsystem References pointer table, the one-product principle, first-screen MAP.md navigation line, and repointed staleness bullets at `docs/audits/` residue. Swept dead refs (knowledge-debt/AUDIT.md/GEMINI — none remain). Eager load (CLAUDE.md + live-app-safety.md + ~/.claude/CLAUDE.md): 33,601 → 2,763 words. |

### Task 7 — workspace document census (rows 45+)

Sorts every lifecycle doc under `docs/superpowers/` (151 files) plus `docs/plans/marketplace-integrations-v2.md`
into `docs/active/{specs,plans,handoffs,investigations,prototypes}` (in-flight only) and
`docs/archive/{…}` (completed/superseded). DEFAULT disposition = **archive · shipped**; the small ACTIVE
set and the SUPERSEDED cluster are the exceptions. Every moved file gets a `status:` frontmatter line
(existing frontmatter had its `status:` value repointed to the lifecycle value; the one `.html` prototype
got an HTML-comment status marker). Moves use `git mv` so history follows.

| # | Item | Disposition | Confidence | Notes |
|---|------|-------------|------------|-------|
| 45 | Handoffs — active set (3) | Active → docs/active/handoffs/ | High | `2026-07-10-sync-completion-handoff` (v1.3 release-gating status doc). `2026-07-10-remote-access-review-handoff` (5 open remote findings for a future rework session — MOSTLY-open live guidance, kept active per the review-handoff rule; ROADMAP feature pointer added). `2026-07-10-review-followups` (deferred perf/simplification catalog — kept active; ROADMAP idea pointer added). |
| 46 | `2026-07-13-sync-project-discovery-review-findings` | Archive · shipped → archive/handoffs | High | Doc self-marks **RESOLVED**; #1–#8 fixed + merged (`1f397c87` + `b5d29f34` followups), #9 intentionally deferred (roots-null boot window unreachable in practice) — too marginal for a ROADMAP line. |
| 47 | `2026-07-14-plan-2b-review-findings-and-moved-gate` | Archive · shipped → archive/handoffs | High | Moved Gate design was implemented (cited as shipped in PITFALLS "Session leases & takeover"). Finding 2's optional hub-down-warn refinement is already documented in PITFALLS as not-required — no ROADMAP line. |
| 48 | Handoffs — shipped (4) | Archive · shipped → archive/handoffs | High | `2026-07-01-friendship-model-session-status`, `2026-07-07-accounts-phase1-execution-handoff`, `2026-07-08-sync-spaces-phase1a-handoff`, `2026-07-09-sync-spaces-post-1a-handoff` — accounts P1/P2 + sync 1a all merged. |
| 49 | `investigations/2026-07-10-harness-design-ideas` | Active → docs/active/investigations/ | High | Feeds the future native-runtime phases (per the active-set list). |
| 50 | Investigations — superseded (2) | Archive · superseded → archive/investigations | High | `2026-05-04-opencode-provider-mvp-plan-review` + `2026-05-21-agent-harness-landscape` — the OpenCode MVP is archived (feat/opencode-mvp) and the harness landscape research is folded into the platform-vision roadmap + phase designs. |
| 51 | Investigations — shipped (7) | Archive · shipped → archive/investigations | High | `2026-04-24-chat-to-pty-submit-reliability`, `2026-04-26-background-task-tracking`, `2026-04-27-android-desktop-divergence-audit` (existing `status: investigation` frontmatter repointed to shipped), `2026-04-29-vbox-hyperv-conflict`, `2026-07-01-github-auth-consolidation-status`, `2026-07-03-sync-accounts-coordination`, `2026-07-13-cross-device-conversation-sync-bugs` — all bug/audit/coordination docs whose work merged. |
| 52 | `plans/research/*` (3) → investigations | Archive · shipped → archive/investigations | High | `2026-04-16-gws-slides-coverage`, `2026-04-16-oauth-brand-automation`, `2026-04-16-refresh-token-findings` — Google-services research; relocated to investigations/ per the plan. `plans/research/` dir removed. |
| 53 | Prototypes (2) | Archive · shipped → archive/prototypes | High | `2026-06-14-project-view-redesign.{md,html}` — the Project View redesign shipped (`feat/artifact-viewer`). The `.html` got its status marker as an HTML comment (`<!-- status: shipped -->`) so rendering isn't broken; the `.md` got normal frontmatter. |
| 54 | Plans — active set (3) | Active → docs/active/plans/ | High | `2026-07-15-workspace-knowledge-mgmt-phases-1-2` (this plan). `2026-07-13-custom-session-tags-plan-a-data-layer` (Plan B in flight). `2026-04-21-deprecate-youcoded-core` (release N+1 pending). |
| 55 | Plans — superseded (5) | Archive · superseded → archive/plans | High | `2026-05-04-opencode-provider-mvp` + `2026-05-04-vercel-ai-sdk-harness-roadmap` (OpenCode/early-harness path abandoned — superseded by the Phase 0/1 provider+engine plans). `2026-04-23-privacy-analytics` (install_id design superseded by 2026-05-01 device-id redesign, per PITFALLS). `2026-04-17-apple-services-implementation` + `-handoff` (Apple-services integration was never built — no `apple-services` code in youcoded; integration strategy evolved). |
| 56 | Plans — shipped (64) | Archive · shipped → archive/plans | High | All remaining top-level `plans/*.md` — merged features (marketplace polish, analytics, artifact viewer, project view, resume browser, sync 1a/1b/2a/2b/2c, cross-device discovery, accounts P1/P2, native-runtime Phase 0/1 plans A/B/C + local-model lifecycle, connect-github modal, buddy floater, android xterm, etc.). Verified against PITFALLS "merged" statements + sub-repo history; default disposition. |
| 57 | `docs/plans/marketplace-integrations-v2.md` → archive/plans; `docs/plans/` dissolved | Archive · shipped → archive/plans | High | Bundled-skills/Google-services integration plan (2026-04-16); marketplace integrations shipped. Moved out of the stray `docs/plans/` dir, which is now removed. |
| 58 | Specs — active set (4) | Active → docs/active/specs/ | High | `2026-07-15-workspace-knowledge-management-design` (this redesign). `2026-07-09-platform-vision-roadmap` (governing roadmap). `2026-07-10-phase2-conversation-sync-design` (Android Phase 3 unimplemented). `2026-07-13-custom-session-tags-design` (Plan B in flight). |
| 59 | Specs — superseded (4) | Archive · superseded → archive/specs | High | `2026-04-23-privacy-analytics-design` + `2026-04-23-analytics-privacy-copy-draft` (install_id design + copy draft → superseded by the 2026-05-01 device-id redesign, per PITFALLS). `2026-05-04-multi-model-harness-design` (superseded by the platform-vision roadmap + Phase 0/1 designs). `2026-04-17-apple-services-design` (Apple-services integration never built). |
| 60 | Specs — shipped (47) | Archive · shipped → archive/specs | High | All remaining `specs/*.md` — merged features. Files with pre-existing `status: design/draft/investigation` frontmatter had that key's value repointed to the lifecycle value (single valid `---` block, no double-stamp). |
| 61 | `docs/superpowers/` — 1 dirty-skipped file remains (residue-adjacent note, NOT a Destin decision) | Left in place | High | `plans/2026-07-13-custom-session-tags-plan-b-ui.md` is modified in the MAIN checkout (uncommitted Plan B UI work), so it was skipped by the dirty-file guard. Move it into `docs/active/plans/` (or `docs/archive/plans/` once shipped) when that session lands. `docs/superpowers/` is kept only to hold it; every other lifecycle doc is now under `docs/active/` + `docs/archive/`. |
| 62 | **Task 8** — youcoded sub-repo lifecycle docs (21) | Absorb → docs/archive/{plans,specs,investigations} + delete from youcoded via PR | High | `docs/plans/*` (4), `docs/specs/*` (3), `docs/superpowers/plans/*` (6), `docs/superpowers/specs/*` (7), `docs/investigations/exec-routing-overhaul-2026-03-26.md` (1). All `shipped` EXCEPT 5 `superseded`: `2026-03-18-multi-session-architecture` (plan+design) and `2026-03-30-unified-ui-architecture` (plan+design+implementation-status) — March-2026 multi-session/unified-UI redesign superseded by the current shared-UI architecture. `origin: youcoded@83ac53fb:...`. Two files (`skill-marketplace-design`, `2026-04-22-install-modal-redesign`) had existing frontmatter — `status:` repointed to `shipped`, `origin:` inserted, single `---` block preserved. |
| 63 | **Task 8** — youcoded-core lifecycle docs (3) | Absorb → docs/archive/{plans,specs} + delete via PR | High | `docs/superpowers/plans/2026-03-23-blocking-permission-hooks.md`, `docs/superpowers/specs/2026-03-23-blocking-permission-hooks-design.md`, `docs/plans/user-configurability-plan (03-30-2026).md` — all `shipped`. `origin: youcoded-core@e6b95a5:...`. |
| 64 | **Task 8** — wecoded-marketplace lifecycle docs (9) | Absorb → docs/archive/plans + delete via PR | High | `docs/phase-{0..6}-implementation-prompt.md` (7), `docs/unified-marketplace-plan.md`, `docs/unified-marketplace-research-report.md` — all `shipped` (unified-marketplace merge landed). No external citations (grep for `unified-marketplace`/`phase-N-implementation` outside `docs/` = empty; no worker README ref). `origin: wecoded-marketplace@eecc843:...`. |
| 65 | **Task 8** — spotify-services plan (COPY, not moved) | Copy → docs/archive/plans/spotify-services-plan.md; ORIGINAL KEPT | High | `wecoded-marketplace/spotify-services/docs/plan.md` cited by workspace PITFALLS as reference MCP-handshake methodology — copied with `origin:` frontmatter, original left in the sub-repo (not deleted). |
| 66 | **Task 8** — strays LEFT IN PLACE (scoping decision) | No action | High | `youcoded/desktop/docs/*` (~27 dated design docs) = a LIVING reference library: `desktop/CLAUDE.md` points to `theme-spec.md`, `transcript-watcher-spec.md`, `2026-03-27-partykit-game-backend-design.md` ("See `desktop/docs/` for older design documents"), and `desktop/src/main/preload.ts:920` cites the drag-session-detach design in a code comment — left whole. Plugin/skill-BUNDLED docs left (out of repo-level-`docs/` scope): `youcoded-core/skills/setup-wizard/plans/*`, `youcoded-core/specs/*` (living spec lib w/ INDEX.md), `youcoded-admin/skills/release/{specs,agents}/*` (release skill's own design+plan+agents), `wecoded-marketplace/{spotify-services/docs/design.md,google-services/docs/DEV-VERIFICATION.md,youcoded-inbox/skills/*/…}`. wecoded-themes: none. Principle applied: move repo-level `docs/` lifecycle files; leave living-reference libraries and plugin/skill-bundled docs. |

### Task 9 — outward-facing docs baseline accuracy review (rows 67+)

Verified every outward-facing doc across the 5 sub-repos (READMEs, LICENSEs, sub-repo CLAUDE.md files, privacy copy) against current code; fixed inaccuracies on sight via one docs-only PR per repo (privacy copy left as residue). Every concrete claim was proven against a source file (cited below). One grouped row per repo.

| # | Item | Disposition | Confidence | Notes |
|---|------|-------------|------------|-------|
| 67 | **youcoded** — README + desktop/CLAUDE.md | Fixed · PR [#130](https://github.com/itsdestin/youcoded/pull/130) MERGED | High | README "Powered by YouCoded" attributed journaling/encyclopedia/inbox/messaging/**cross-device sync** to the youcoded-core toolkit — all stale: those are WeCoded marketplace plugins now (`wecoded-marketplace/{youcoded-encyclopedia,youcoded-inbox,youcoded-messaging}` dirs) and sync is built into the app (`desktop/src/main/sync-service.ts`, workspace CLAUDE.md). Reframed as "Personalization" (marketplace + built-in sync); Related Projects relabeled `YouCoded`→`YouCoded Core` + "being phased out". desktop/CLAUDE.md said `AttentionState` = 3 states / `'error'` deleted; type at `desktop/src/shared/types.ts:486` has **4** (`'error'` reintroduced Phase 1 Plan A via `NATIVE_SESSION_ERROR`) — corrected. youcoded/CLAUDE.md verified CLEAN (`docs/cc-dependencies.md` exists; workspace-scaffold doc refs correctly qualified). |
| 68 | **youcoded-core** — README rewrite | Fixed · PR [#117](https://github.com/itsdestin/youcoded-core/pull/117) MERGED | High | README described the removed three-layer Core/Life/Productivity structure (Phase 3 flattened to one plugin) and listed skills/commands that don't exist. Verified actuals: `hooks/hooks-manifest.json` = 5 hooks; `skills/` = setup-wizard + remote-setup only; `commands/` = update/health/diagnose only (dropped nonexistent `/sync`,`/toolkit`,`/theme-builder`,`/contribute`). Rewrote to reflect the single **deprecated** plugin; moved features point at the marketplace. No CLAUDE.md in repo. |
| 69 | **youcoded-admin** — README | Fixed · PR [#2](https://github.com/itsdestin/youcoded-admin/pull/2) MERGED | High | Skills table + setup symlinks only listed `announce`+`release`; `skills/` also has `analytics` + `feature` (descriptions from their SKILL.md frontmatter). Also: top link pointed at youcoded-core → repointed to the app; release triggers updated to the unified cross-repo flow (release SKILL.md description); Requirements now name both repo clones. |
| 70 | **wecoded-themes** — README | Fixed · PR [#10](https://github.com/itsdestin/wecoded-themes/pull/10) MERGED | High | Available Themes table listed 3 (incl. `Kitty Pink Classic`, gone). `registry/theme-registry.json` has **7** (cotton-candy-sky, devils-garden, golden-sunbreak, halftone-dimension, kuromi-dreamer, meadow-mist, strawberry-kitty) — table rebuilt with real mode+features. CONTRIBUTING.md link valid (file exists). |
| 71 | **wecoded-marketplace** — README + worker/README | Fixed · PR [#24](https://github.com/itsdestin/wecoded-marketplace/pull/24) MERGED | High | README: `151/29/122` → `174/26/148` (index.json length + `sourceMarketplace` group-by); enum `youcoded-core \| claude-plugins-official` → `youcoded \| anthropic` (index.json values); "Adding a Skill" pointed at nonexistent `registry/prompts\|plugins/` → add plugin dir + `marketplace.json` entry (`scripts/sync.js` reads it); surfaced marketplace.json in Structure. worker/README: `ADMIN_USER_IDS` = bare GitHub numeric ids not `github:<id>` (`src/auth/admin.ts`); sessions `user_id` = opaque `acct_<hex>` not `github:<id>` (`migrations/0003_accounts_identity.sql`). |
| 72 | **Verified clean** (no PR) | Logged | High | **LICENSEs:** all present & sane except youcoded-admin (none — private owner-only repo, README claims no license; acceptable). youcoded root=MIT(2026 Destin)/desktop=MIT/app=GPLv3; youcoded-core=MIT; wecoded-themes + wecoded-marketplace = Apache-2.0. **Privacy — AboutPopup.tsx** Privacy section collected-data list (device hardware hash, app version, platform+OS, country+approx region, IPs not stored, opt-out) MATCHES the analytics blob order in `wecoded-marketplace/worker/src/lib/analytics.ts` (deviceIdHash, appVersion, platform, os, country, region) exactly — accurate, no change. youcoded/CLAUDE.md clean. |
| 73 | Auto-memory one-time review (Task 10) | Updated 7 files + index | High | All 20 memory files read. `feedback_dislikes_status_glyphs`: stale "SessionDrawer still uses glyphs" claim corrected (statusInfo() returns plain words — verified in code) + sync-dots exception noted. `project_platform_vision_roadmap`: Plans B+C marked complete, spec/plan paths repointed (active/archive), "next steps" delegated to ROADMAP.md. `project_release_1_3_scope` + `project_sync_release_gating`: planning duplication trimmed to ROADMAP/handoff pointers; the Android-gates-release open question resolved against the 2026-07-14 decision (v1.3.1). `project_restore_removed_2c`: knowledge-debt pointer → ROADMAP. `project_master_review_2026_07_10`: handoff paths → docs/active/handoffs/. MEMORY.md index: 4 lines synced, including correcting the dev-chat-blank line's wrong "PID pipe" theory to the real inherited-CC-env cause (the memory body itself flags the pipe theory as wrong). All feedback_* behavioral memories kept (their home). |

### Task 12A — PITFALLS sync-cluster triage

Triaged the four SYNC-related `##`/`###` sections of `docs/PITFALLS.md` into two new path-scoped rules (`.claude/rules/{sync-spaces,conversations}.md`, both ≤600 body words) + two staging overflow docs (`docs/_staging/youcoded-docs/{sync-spaces,conversations}.md`, destined for `youcoded/docs/`), then DELETED the migrated sections from PITFALLS (168 lines). One row per PITFALLS **section** below (bullets share a disposition within a section: distilled into the rule with the full detail staged). No eager-tier (live-app-safety-class) entries — every sync invariant here ships a catchable/test-pinned bug, not an irreversible in-session action, so all are safe in lazy path-scoped rules. No new residue (all High confidence).

| # | Entry (bullets) | Disposition | Confidence | Notes |
|---|-----------------|-------------|------------|-------|
| 74 | `## Sync Warnings` (7) | rule:sync-spaces + staged-doc | High | Warning-file authority, two-writer code ownership, `extractStderr` timeout-classification fix. Guards: `sync-warnings-lifecycle.test.ts`, `sync-error-classifier.test.ts`. |
| 75 | `## Resume Browser & Conversation Identity` (6) | rule:conversations + staged-doc | High | topic-mtime=lastActive, SessionStart-only `sessionIdMap` remap, name precedence, `SESSION_UUID_RE` gate, `cleanupPeriodDays` 365 seed. Guard: `session-browser.test.ts`. |
| 76 | `### Slug→path resolution` (4) | rule:conversations + staged-doc | High | `walkSlugParts` longest-first, `resolveLocalProject` override, existsSync-mask warn, Android pre-fix bug (Phase 3). Guards: `slug-path-resolution.test.ts`, `resolve-local-project.test.ts`. |
| 77 | `## Connect-GitHub Modal` (8) | rule:sync-spaces + staged-doc | High | Token-never-leaves-main (both `github-{auth,connect}` have token-leak tests), gh client-id reuse, per-flow settle guard, winget→restart. Guards: `github-auth.test.ts`, `github-connect.test.ts`. |
| 78 | `## Sync Spaces (Phase 1a)` (15) | rule:sync-spaces + staged-doc | High | `GIT_DIR`, `* -text`, convergent conflict + `showStage` Buffer/maxBuffer data-loss guard, `allow-unrelated-histories`, `repoNameForSpace`, engine single-flight/ready/error-handler/stop-order, `isIgnoredPath`. Guards: `sync-transport-contract.ts`, `sync-spaces-git-transport.test.ts`, `sync-spaces-engine.test.ts`, `sync-spaces-service.test.ts`. |
| 79 | `### Import existing folders` (9) | rule:sync-spaces + staged-doc | High | Import MOVES (never copy), EXDEV existsSync re-check, `remapTranscriptDir`, remap-failures→warnings, `countFilesBounded`. Guard: `sync-spaces-import.test.ts`. |
| 80 | `### Project & sync management UX` (9) | rule:sync-spaces + staged-doc | High | Sync-dots the ONE color use (`sync-dot-state.ts`), FolderSwitcher portal + `data-folder-switcher-portal`, `SpaceSyncEvent.at`, `syncSpacesSyncNow(spaceId?)`. Guard: `sync-dot-state.test.ts`. |
| 81 | `### SyncHub (Plan 1b)` (12) | rule:sync-spaces + staged-doc | High | Per-account DO accelerant-not-truth, spaceKey=repoName, signal-only-on-pushed loop breaker, hub-send-last isolation, superseded-startEngine no-global-state, env.test DO-binding mirror. Guard: `sync-hub-socket.test.ts`. Worker DO (`SyncGroupRoom` in wecoded-marketplace) is out of the rule's youcoded/desktop glob — referenced in staging, not a verify anchor. |
| 82 | `### Conversation Store (Phase 2a)` (14) | rule:conversations + staged-doc | High | mirror-in shrink-guard, content-timestamp lastActive, convergent `mergeRecords`/`foldConflictCopies`, id path-traversal boundary, materialize-sweep-skips-live, reconciler projectKey recovery + `lstatSync` symlink-skip (youcoded#118), `.catch()` on fire-and-forget. Guards: `transcript-mirror.test.ts`, `conversation-store-core.test.ts`, `conversation-reconciler.test.ts`, `conversations-service.test.ts`. |
| 83 | `### Legacy sync demolition (Plan 2c)` (5) | rule:sync-spaces + staged-doc | High | **DRIFT FLAG:** branch `feat/sync-legacy-demolition` is NOT merged to youcoded master (`snapshot-retention.ts`, `conversations/symlink-sweep.ts`, `sync-spaces/gc-policy.ts`, `sweepProjectSymlinks` absent in the checkout) — rule + staging note the modules "land with the branch"; no verify anchor points at them. Captured: sweep lstat-only/never-recursive (junction-recursion = irreversible transcript loss — the section's one near-catastrophic invariant, but it is shippable app code with a reviewable diff, not a live-app in-session action, so it stays in the lazy rule + flagged here), write-only dated snapshots (restore removed), `notice`-not-`error` >500MB warning. Guard: none — candidate. |
| 84 | `### Cross-device project discovery / rename / stop` (11) | rule:sync-spaces + staged-doc | High | Visible per-file registry (never under `.youcoded/`), `stopped`-dominates-monotonic `state`, fold-on-read, stop=tombstone+`removeSpace`+keep-folder via `activeManagedSpaces()`, `planReconcile` names-not-ids, 4-surface rename/stop parity. Guards: `sync-spaces-project-discovery.test.ts`, `ipc-channels.test.ts`. |
| 85 | `### Session leases & takeover (Plan 2b)` (7) | rule:conversations + staged-doc | High | DO-authoritative lease ops (not signals), per-INSTALL `deviceId`, `noteSessionEnded` skip-on-timeout data-loss guard, holder-takeover ordering, never-block degradation, Bug-1 browse filter, MovedGate stale-closure trap. Guards: `holder-takeover.test.ts`, `requester-takeover.test.ts`, `lease-client.test.ts`. |

### Task 12B — PITFALLS runtime-cluster triage

Triaged the 10 RUNTIME/ENGINE/PTY/CHAT/ANDROID `##`/`###` sections of `docs/PITFALLS.md` into three NEW path-scoped rules (`.claude/rules/{native-runtime,engine-local-models,pty-io,prereq-installer}.md`) + two EXPANDED rules (`chat-reducer.md`, `android-runtime.md` — both rewritten in template format), plus overflow staging (`docs/_staging/youcoded-docs/{native-runtime,pty-io}.md` new; `{chat-reducer,android-runtime-additions}.md` as APPEND-to-existing-lazy-doc additions). Deleted the migrated sections from PITFALLS (5 contiguous blocks, ~198 lines). All six touched rules ≤600 body words (native 505 · engine 561 · pty-io 600 · prereq 375 · chat-reducer 598 · android 484). One row per PITFALLS section (bullets share a section disposition: distilled to the rule's load-bearing invariants, full detail staged). No eager-tier (live-app-safety-class) flags — every runtime invariant here ships a catchable/test-pinned bug (empty chat view, stray CR, R8 release break) reachable through a reviewable diff, NOT an irreversible in-session live-app action. No new residue (all High confidence). Anchors all verified against the main checkout; note `shared-fixtures/transcript-parity/` does NOT exist (only `attention-classifier/` + `artifacts/`) but it's referenced only by the KEEP `## Cross-Platform` section, not by any 12B rule.

| # | Entry (bullets) | Disposition | Confidence | Notes |
|---|-----------------|-------------|------------|-------|
| 86 | `## Multi-Model Provider Seam (Phase 0)` (8) | rule:native-runtime + staged-doc | High | gemini GONE, `native.supported` sole gate (not an IPC channel), createSession throws for non-claude, reasoning-segments dormant + identical App/Buddy predicate. Guard: `ipc-channels.test.ts` ("native runtime capability parity"). |
| 87 | `### Phase 1 Plan A — native chat sessions` (10) | rule:native-runtime + staged-doc | High | safeStorage key encryption (never in `~/.youcoded/`), NativeHome mutateFileUnderLock throws, SessionStore delta coalescing + caller-serialize, NativeSessionHost destroy order, native:send shares the transcript pipe, renderer skips ALL PTY machinery + string==buildOutgoingMessage, transport error-semantics gap, AI SDK v7, ModelCatalog both-sources stamp. Guards: `harness-session.test.ts`, `native-session-host.test.ts`, `native-send.test.ts`, `native-home.test.ts`, `provider-registry.test.ts`. |
| 88 | `### Phase 1 Plan B — local llama.cpp engine` (7) | rule:engine-local-models + youcoded/docs/engine-dependencies.md | High | `--models-dir` (not LLAMA_CACHE) discovers GGUFs, `status` object shape, engine pin in CODE, per-family archive layout + bsdtar, atomic `.complete` install + Vulkan→CPU, baseUrl `/v1` + single-flight stop + crash strike-out, shifted ENGINE_PORT. Depth points at the EXISTING `engine-dependencies.md` (not staged — no duplication). Guards: `engine-supervisor.test.ts`, `engine-acquisition.test.ts`, `test-engine/probe-{health,models,chat}.mjs`. |
| 89 | `### Phase 1 Plan C — model manager` (7) | rule:engine-local-models + youcoded/docs/engine-dependencies.md | High | flat-basename cache naming probe-pinned, no baked sizes (live fit), GPU-aware fit safety bias, quant parser denylist, delete-removes-partials, CUDA Windows-x64-only, set-context nulls supervisorBinary. Open items (router hot-reload K2, `.partial` orphan scan) already in ROADMAP (changelog rows 38–39) — noted in the rule. Guards: `model-downloader.test.ts`, `test-engine/probe-download.mjs`. |
| 90 | `## Chat Reducer` (15) | rule:chat-reducer (rewritten) + staged additions | High | Reducer invariants (toolCalls never cleared, endTurn, 4-state AttentionState, pending-flag dedup) → rule; transcript-watcher read-integrity (readNewLines per-emit + serialized, partialBytes Buffer, stripSystemTags, getHistory dedup, subagent kick/settle, usePromptDetector, compact-summary) + SPINNER_RE `^`-anchor detail → staged (destined to APPEND to `youcoded/docs/chat-reducer.md`, not duplicating its existing reducer/classifier/metadata coverage). Guards: `chat-reducer.test.ts`, `transcript-watcher.test.ts`, `attention-classifier-parity.test.ts`. |
| 91 | `## Vendored Termux terminal-emulator` (7) | rule:chat-reducer (terminal-byte subsection) + staged additions | High | Per the task assignment, folded into chat-reducer.md (the byte pipeline feeds the shared renderer): headless vendored emulator + one documented patch, RawByteListener copies-bytes-on-terminal-thread, `pty:raw-bytes` base64 three-surface parity, xterm display-only-on-touch. chat-reducer paths extended with `terminal-emulator-vendored/**` + `usePtyRawBytes.ts` so the rule triggers there. Guard: `raw-byte-listener-contract.test.ts`, `ipc-channels.test.ts`. |
| 92 | `## Android Runtime` (10) | rule:android-runtime (rewritten) + staged additions | High | System fundamentals (LD_LIBRARY_PATH, linker64 layers, TMPDIR, termux-exec linker variant, shared bridges, sessionFinished, canonical wrapper) → rule; exec-wrappers-0755 + `.netrc`-not-`gh auth setup-git` + `gh --web` flaky-retry → rule + staged additions (destined to APPEND to `youcoded/docs/android-runtime.md`, not duplicating its existing coverage or the Task-3 Go-exec-trap fix). Guard: none unit-level (runtime env). |
| 93 | `## Build-Type Parity (Android)` (5) | rule:android-runtime + staged additions | High | R8 obfuscates string-reflection (the `PluginInstaller.buildEnv` shipped-in-release bug), direct-calls-or-`-keep`, defensive Bootstrap `-keep`, `assembleReleaseTest` parity check, explicit setup-node. Guard: `./gradlew :app:assembleReleaseTest` (CI `android-ci.yml`). |
| 94 | `## PTY Writes (Desktop + Android)` (intro + mechanism 3 + protocol + invariants 5 + diagnostics 5) | rule:pty-io + staged-doc + test-conpty/README.md | High | 64-byte paste threshold, three-path submit (atomic ≤56 / echo-driven), suppress-CR-on-echo-timeout (no blind fallback CR, youcoded#110), pending-interaction gate (`pty-input-gate.ts`), useSubmitConfirmation second-line defense, one-sanitized-string bubble/send. Mechanism + methodology depth → existing `test-conpty/README.md` + staged `pty-io.md`. Guard: `test-conpty/test-worker-submit.mjs`, `cc-snapshot.mjs`. |
| 95 | `## PTY Resize (Desktop Windows)` (2) | rule:pty-io + staged-doc | High | `TerminalView.fitAndSync` dedups unchanged cols/rows (ConPTY reflow re-emits Ink UI into scrollback); renderer owns dimensions, Android unaffected. Guard: none — candidate (visual regression). |
| 96 | `## Keyboard Routing` (3) | rule:pty-io + staged-doc | High | ESC via `useEscClose` stack → chat-passthrough (`defaultPrevented` gate, terminal-view exception), single-byte interrupt, `user-interrupt` transcript markers → `endTurn()`. Guard: none — candidate. |
| 97 | `## Local Dev & Launch Environment` (3) | rule:pty-io + staged-doc | High | Spawn chokepoint strips inherited `CLAUDECODE`/`CLAUDE_CODE_*` (nested CC writes no transcript → permanently empty chat view; youcoded#106); Android unaffected. Diagnostic signature staged. Guard: none — candidate. |
| 98 | `## Prerequisite Installer (First-Run)` (6) | rule:prereq-installer (NEW) | High | `runCommand` `.cmd`/`.bat` EINVAL trap (high-recurrence — kept its own rule per the dispatch note), native Claude installer (not npm), post-install PATH-propagation restart, getRegPath quoted vs getPowerShellPath unquoted, `detectWinget()` upfront guard. Paths: `prerequisite-installer.ts`, `first-run.ts`, `remote-config.ts`, `sync-setup-handlers.ts` (all verified). Guard: none — candidate (Windows machine-state footguns; verify on a clean box). |

### Task 12C — PITFALLS ui/web-cluster triage

Triaged the 15 UI/WEB/REGISTRY `##`/`###` sections of `docs/PITFALLS.md` into two NEW path-scoped rules (`.claude/rules/{artifacts,worker-backend}.md`) + four EXPANDED rules (`react-renderer.md`, `ipc-bridge.md`, `registries.md`, `youcoded-toolkit.md` — all rewritten/extended in template format), plus overflow staging (`docs/_staging/youcoded-docs/{artifacts,renderer-chrome}.md` new; `docs/_staging/wecoded-marketplace-docs/{worker-backend,mcp-authoring}.md` new — a new staging tree for marketplace-repo depth). Deleted all 15 migrated sections from PITFALLS (240 lines: 264→24; only `## Releases`, `## Documentation Drift`, `## Working With Destin` — none of them assigned — remain). All six touched rules ≤600 body words (artifacts 531 · worker-backend 590 · react-renderer 597 · ipc-bridge 596 · registries 539 · youcoded-toolkit 390). One row per PITFALLS section (bullets share a section disposition: distilled to the rule's load-bearing invariants, full detail staged). No eager-tier (live-app-safety-class) flags — every UI/web/registry invariant here ships a catchable/test-pinned bug (blank WebView, black chat area, wrong contrast, a plugin CC can't see, a leaked device hash) reachable through a reviewable diff, NOT an irreversible in-session live-app action; the security-boundary entries (read-binary guard, `adminFilterClause`, "never log raw device_id_hash") are all test-covered reviewable code, so they stay in lazy path-scoped rules with their guards. All anchors verified against the MAIN checkout's sub-repos. No new residue (all High confidence). **One DRIFT FLAG** (row 99 note) carried from the Cross-Platform section.

| # | Entry (bullets) | Disposition | Confidence | Notes |
|---|-----------------|-------------|------------|-------|
| 99 | `## Cross-Platform (Desktop + Android)` (8) | rule:ipc-bridge + depth→`docs/shared-ui-architecture.md` | High | preload/remote-shim shared-shape parity + `.window`/`.android` exceptions, identical message-type strings, desktop-raw/Android-JSONObject normalization, bundleWebUi-before-APK, cc-dependencies.md coupling entry. **DRIFT FLAG:** the "two implementations behind a `TranscriptSource` interface" bullet + its `shared-fixtures/transcript-parity/` fixtures + `transcript-parity.test.ts` are ABSENT in the current youcoded checkout (verified: no `TranscriptSource`/`TranscriptWatcherProcess`/`transcript-watcher-cli.js`; `shared-fixtures/` has only `artifacts/` + `attention-classifier/`) — same absence 12B noted. Rule captures the STILL-TRUE parity invariants and records the drift inline (design not in checkout; canonical parser is `transcript-watcher.ts`, Kotlin mirror still exists) rather than pinning dead anchors. Not residue (a doc-drift note, not a Destin decision). Guard: `ipc-channels.test.ts`. |
| 100 | `## Bundled Plugins & Hooks (youcoded-core)` (5) | rule:youcoded-toolkit (already covered) + `docs/toolkit-structure.md` | High | hooks-manifest.json-not-settings.json, `.sh` execute bit, config.json portable vs config.local.json, `git worktree add`, flat plugin (no core/life/productivity). The existing `youcoded-toolkit.md` already carried all five — only added the `verify:` block (plugin.json + hooks-manifest.json + write-guard.sh + worktree-guard.sh) + bumped `last_verified`. Guard: none unit-level (hook behavior). |
| 101 | `## Remote Access State Sync` (4) | rule:react-renderer + staged `renderer-chrome.md` | High | `chat:hydrate` on connect (no parallel replay buffer), `attentionState` desktop-only via `attentionMap`/`status:data` (remote must NOT run its own classifier), `RemoteSnapshotExporter` Electron-only, 2s export-snapshot timeout. Guard: none — candidate (integration timing). |
| 102 | `## Framed Shell & Chrome Glass` (7) | rule:react-renderer + staged `renderer-chrome.md` | High | ONE backdrop-filter, `destination-out` invalid mix-blend-mode, chrome-glass display:none in floating, drawer-pane z-11, `.chrome-wrapper` transparent, compound-selector same-element trap, right-slot drawer-XOR-games via `--right-pane-width`. TR-corner polygon closer detail → staged. Guard: none — candidate (visual regression). |
| 103 | `## Theme Color Contrast Rules` (4) | rule:react-renderer + staged `renderer-chrome.md` | High | panel/canvas ≥1.07, text thresholds, on-accent/accent ≥4.5, chat-pane bg == drawer-pane bg. Guard: `desktop/scripts/audit-theme-contrast.mjs` + CI `wecoded-themes/scripts/audit-contrast.mjs` (non-blocking). |
| 104 | `## Theme Marketplace Entries` (4) | rule:registries + `docs/registries.md` | High | `isLocal` ≠ `installed`, slug-collision marketplace-wins + merge order, synthesizer PURE / provider does I/O, Android no-synth parity gap. Guard: `local-theme-synthesizer.test.ts`. |
| 105 | `## Header Bar` (5) | rule:react-renderer + staged `renderer-chrome.md` | High | no `min-w-0` left cluster, space-aware-not-viewport, platform-conditional toggle placement, `showCaptionButtons` must include Linux (not-macOS gate, the Win32-excludes-Linux general trap), announcement in StatusBar. Guard: none — candidate. |
| 106 | `## Overlays (Popups, Modals, Drawers)` (4) | rule:react-renderer (points to `docs/shared-ui-architecture.md`) | High | `<Scrim>`/`<OverlayPanel>` not hardcoded, pick-a-layer L1–L4, SessionStrip z-9000 load-bearing, var-driven glassmorphism. Overlay LAYER SYSTEM lives in the workspace lazy doc — not duplicated in the rule or staging. Guard: none — candidate. |
| 107 | `## Plugin Installation & Claude Code Registries` (6) | rule:registries + `docs/registries.md` | High | CC v2.1+ doesn't fs-scan `~/.claude/plugins/`, FOUR registries atomic via `registerPluginInstall()`, install location under the marketplace subtree + two exceptions, reconciler scans BOTH roots, `installed_plugins.json` location, `BUNDLED_PLUGIN_IDS` two-way duplicated. Guard: none unit-level (registry I/O; `ClaudeCodeRegistry` integration). |
| 108 | `## MCP Plugin Authoring (Cross-Platform)` (13) | rule:registries (top invariants) + staged `wecoded-marketplace-docs/mcp-authoring.md` | High | Marketplace-repo guidance → the DEPTH is staged for `wecoded-marketplace/docs/`; the rule keeps the highest-value few (bash-not-on-PATH + 8.3 short name, `${PACKAGE_DIR}` reconciler-not-CC, pin mcp<2, `claude mcp list` lies → verify with `/mcp`, end-to-end handshake probe) and points at the staging doc for the other footguns (venv activate, PYTHONIOENCODING, pywinrt, rsync, system-Python gating, MSYS paths). Guard: end-to-end Node spawn-probe (methodology in the staged doc). |
| 109 | `## Cloudflare Workers (Marketplace Backend)` (4) | rule:worker-backend + staged `wecoded-marketplace-docs/worker-backend.md` | High | var/secret same-name clobber, deploy-before-secret-put ordering, `[env.test]` omit-wrapped-bindings + env-snapshot-not-propagated, per-colo Cache-API rate limits, `app.onError` JSON 500. Guard: worker `test/` suite (miniflare). |
| 110 | `## Analytics` (14) | rule:worker-backend + staged `wecoded-marketplace-docs/worker-backend.md` | High | `blob2`=device_id_hash (HMAC, raw machine_id never sent), only `writeAppEvent` writes + blob-order contract, NEVER-log-raw-hash privacy boundary, one-event-type (install→410), server-side country/region + no cross-tab, `adminFilterClause`/`cutoverClause` SQL safety, AE-SQL-subset quirks, two-path admin auth. Guards: `analytics-lib.test.ts`, `admin-filter.test.ts`, `admin-analytics.test.ts`. |
| 111 | `## Announcements` (6) | rule:registries + `docs/registries.md` | High | source-of-truth `youcoded/announcements.txt` (not youcoded-core), single-fetcher-per-platform (1h, own cache), two expiry filters (fetch + `isExpired()` render), clear=explicit-null-write, cache-shape contract, Android own `~/.claude`. Guard: none unit-level (`isExpired` shared helper is testable — candidate). |
| 112 | `## Settings → Development` (7) | rule:ipc-bridge + depth→`docs/shared-ui-architecture.md` | High | six dev:* IPC types 4-surface parity, `submitIssue` `{kind,title,summary,description,log?,label}` shape (buildIssueBody in main), GitHub labels must exist, setup.sh-rerun idempotency, `claude -p` stdin summarizer + runStreamed deadlock caveat, port-current-not-plan cautionary tale. Guard: `ipc-channels.test.ts`. |
| 113 | `## Artifact Viewer` + 4 `###` children (Project View redesign / Artifacts-vs-All-files split / Binary viewers / Merge-readiness hardening) (~40 bullets) | rule:artifacts (NEW) + staged `youcoded-docs/artifacts.md` | High | The biggest section — compressed HARD into the rule's load-bearing invariants (Artifacts⊆All-files superset + LIST_PROJECT-vs-LIST_ALL_FILES, `trackedArtifacts()` sole visibility decider, discovery stops-at-nested-repo + `discovered:true` never persisted, project-list=saved-folders + synth-id=path, two count helpers, `canonicalize()` single-source, mkdir-lock casWrite + `mutateFileUnderLock`, `read-binary` GUARDED + `BinaryContent` shell + ExcelJS-not-SheetJS, pill-always-artifactifies + `findBestMatch` exact-over-suffix, per-session drawer state, Android stubs = contract, glyph ban). Full ~40-bullet depth (tracking pipeline, Project-View-hub redesign, xlsx formula eval, attachments, context allow-list) → staged. Guards: `read-binary-access.test.ts`, `visible-artifacts.test.ts`, `canonicalize.test.ts`, `cas-write.test.ts`, `ipc-channels.test.ts`, + the `tests/artifacts/` suite. |

### Task 12D — subsystem docs + slim PITFALLS

The finisher: relocated the single-repo subsystem depth + all staged overflow OUT of the workspace repo into the sub-repos via two docs-only PRs, then rewrote PITFALLS.md as the slim cross-cutting doc and reconciled every pointer. **youcoded PR #131 (merged, `c97aeb54`)** created 9 docs — 6 straight migrations from `_staging/youcoded-docs/` (`sync-spaces`, `conversations`, `native-runtime`, `pty-io`, `artifacts`, `renderer-chrome`) + 3 MOVED workspace lazy-docs (`chat-reducer`, `android-runtime`, `shared-ui-architecture`) with the two `-additions` staging files appended under a `## PITFALLS-triage additions (2026-07-15)` heading; each carries a provenance note + `origin: youcoded-dev@1f60c2a:docs/<name>.md` frontmatter. **wecoded-marketplace PR #25 (merged, `23306a06`)** created `docs/{worker-backend,mcp-authoring}.md` (recreates the repo's `docs/` dir — living reference now). No new residue (stays 3). One row per action group below.

| # | Action | Disposition | Confidence | Notes |
|---|--------|-------------|------------|-------|
| 114 | 6 staged youcoded-docs overflow files → `youcoded/docs/` | Shipped (PR #131) | High | `sync-spaces`, `conversations`, `native-runtime`, `pty-io`, `artifacts`, `renderer-chrome`. Stripped each staging `<!--…-->` header line; prepended a provenance note pointing at the matching `.claude/rules/<name>.md` (renderer-chrome → react-renderer). |
| 115 | 3 workspace lazy-docs MOVED into `youcoded/docs/` + `-additions` appended | Shipped (PR #131) | High | `chat-reducer.md` (+ transcript-watcher read-integrity / spinner-regex anchoring / terminal-byte-stream), `android-runtime.md` (+ exec-perms / git-auth / R8 build-type parity), `shared-ui-architecture.md` (no additions; incl. overlay layer system). Additions demoted `##`→`###` under the triage heading. Each got `origin:` frontmatter + provenance note. `git rm`'d from the workspace. |
| 116 | 2 staged marketplace-docs → `wecoded-marketplace/docs/` | Shipped (PR #25) | High | `worker-backend.md` (Cloudflare backend + privacy-analytics blob contract + SQL safety boundary), `mcp-authoring.md` (stdio-MCP Windows footguns). Recreates the repo `docs/` dir. Provenance notes → rules `worker-backend` / `registries`. Also added the `wecoded-marketplace/docs/worker-backend.md` pointer to the worker-backend rule (it previously only pointed at build-and-release.md). |
| 117 | `docs/_staging/` deleted | Done | High | `git rm -r docs/_staging/` — both staging trees gone once shipped. Verified `grep _staging *.md` (excl. archive/changelog) empty. |
| 118 | `docs/toolkit-structure.md` shrunk to stub | Done | High | 61 → 8 lines: what it is + deprecated status + pointers to `docs/active/plans/2026-04-21-deprecate-youcoded-core.md` and the `.claude/rules/youcoded-toolkit.md` rule. Full text in git history. |
| 119 | `docs/PITFALLS.md` rewritten as slim cross-cutting doc | Done | High | 332 → 905 words (≤2500 cap). Sections: header (cross-repo-only + entry template invariant·why·guard + MAP.md nav) · `## Releases` (verified `versionCode = 20` / `versionName = "1.2.4"` in `app/build.gradle.kts`; one-tag-all-platforms; desktop-version-from-tag; youcoded-core auto-tag; youcoded-admin orchestration) · `## Cross-repo invariants` (7: message-type-string parity, window.claude shared-shape, cc-dependencies.md coupling entry, bundled-plugin two-way dup, runtime registries, shared-`~/.claude` locking, Windows junction+worktree-remove trap) · `## Documentation Drift` (rewritten: fix-on-sight, `/audit`+`docs/audits/`+residue, ROADMAP `bug` `#docs`; knowledge-debt.md retired) · `## Working With Destin` (trimmed to point at CLAUDE.md Working Rules + live-app-safety rule, dup bullets dropped). |
| 120 | Pointer reconciliation across workspace | Done | High | 13 pointer edits: CLAUDE.md (cross-cutting-docs bullet drops moved docs; Subsystem table 3 rows → `youcoded/docs/`), `.claude/commands/audit.md` (3 doc paths), rules `chat-reducer`/`ipc-bridge`/`react-renderer`(×2)/`worker-backend`, skills `context-android`(×3)/`context-desktop`(×2). Verified: every `youcoded/docs/*.md` + `wecoded-marketplace/docs/*.md` path the rules point at now EXISTS. Rules already pointing at the (now-real) FUTURE paths (`android-runtime`, `renderer-chrome`, `conversations`, etc.) verified resolving. **Left as history (not edited):** the plan doc `docs/active/plans/2026-07-15-workspace-knowledge-mgmt-phases-1-2.md` before-snapshots/baseline-wc command (describes the migration itself, like the changelog) and the dated `docs/audits/2026-04-23.md` historical audit. |

### Tasks 13+14 — MAP.md + context-skill retirement

Created the workspace routing map and retired the three pre-rules `context-*` skills. No new
residue (stays 3) — nothing load-bearing in the skills was uncovered by the rules + depth docs.

| # | Action | Disposition | Confidence | Notes |
|---|--------|-------------|------------|-------|
| 121 | **Task 13** — create `docs/MAP.md` | Done | High | Compact subsystem→entry-points→rule→depth-doc→guard-tests routing table. **15 rows** = one per real rule (14: android-runtime, artifacts, chat-reducer, conversations, engine-local-models, ipc-bridge, native-runtime, prereq-installer, pty-io, react-renderer, registries, sync-spaces, worker-backend, youcoded-toolkit — skipping README + live-app-safety) + a final **Build & release** row (no rule; depth `docs/build-and-release.md`; guard = CI `desktop-release.yml`). Entry points + guards derived from each rule's `paths:`/`verify:` frontmatter (verify `path:` entries as the load-bearing entry points, verify `test:` entries as the guards — both already exist-verified). Three rules with no `verify: test:` entries got honest non-path guards: react-renderer → "manual (visual regression)", prereq-installer → "manual (clean Windows box)", youcoded-toolkit → "manual (hook runtime)"; android-runtime (no unit test) → CI `android-ci.yml` (assembleReleaseTest R8 parity). **Path verification: all 76 extractable paths exist (0 misses)** — sub-repo paths checked against `/c/Users/desti/youcoded-dev/`, workspace `docs/*` against the worktree. **wc -w = 332** (≤800 target). Header explains inject-on-touch semantics + that /audit verifies the map mechanically. CLAUDE.md's first-screen `docs/MAP.md` reference (previously dangling) now resolves. |
| 122 | **Task 14** — retire `context-{android,desktop,toolkit}` skills | Done | High | Read all three SKILL.md first + cross-checked for load-bearing facts NOT covered by the rules/docs: **none found.** context-android (LD_LIBRARY_PATH, linker64 3-layer exec, TMPDIR, both-bridges, sessionFinished, claude-wrapper canonical, Native UI Bridge pattern) → fully in `android-runtime.md` rule + `youcoded/docs/android-runtime.md`. context-desktop (preload/remote-shim parity, message-type strings, renderer node/browser boundary, toolCalls Map, endTurn) → `ipc-bridge.md` + `chat-reducer.md` rules. context-toolkit (hooks-manifest not settings.json, `.sh` exec bit, config.json portable, worktree add, plugin.json bump, flat structure, hook lifecycle) → `youcoded-toolkit.md` rule + `docs/toolkit-structure.md`. Nothing folded, no residue bump. `git rm -r` all three (`.claude/skills/` now empty). **Reference fix: none needed** — `grep` for the three names (excl. archive/changelog/MAP.md) hit ONLY (a) the skills themselves and (b) historical/planning artifacts that *describe the retirement* (`docs/active/plans/2026-04-21-deprecate-youcoded-core.md`, this task's plan, the design spec, `docs/audits/2026-04-23.md`) — left as history per the row-120 precedent. `.claude/commands/audit.md` and `CLAUDE.md` verified to contain **zero** context-skill references (no live pointers to repoint). |

## Residue (needs Destin)

1. **docs/local-dev-vm.md** — is the VM dev flow still used? Recommendation: archive it (run-dev.sh's isolated dev instance superseded the VM approach) unless it's still part of your workflow.

2. **Landing-page FAQ privacy copy is stale (`youcoded/docs/index.html`, ~line 3326)** — the "How is my data handled?" FAQ says YouCoded sends "one anonymous daily ping — **a random install ID**, the app version, platform and OS, and **the country** from the connection." Two mismatches vs. what the code actually sends (`wecoded-marketplace/worker/src/lib/analytics.ts` + `AboutPopup.tsx`): (a) the **install-ID model was retired** — blob2 is now an irreversible **hash of the device's hardware ID** (the install_id design was superseded by the device-hash redesign); (b) it **omits region** — the code also sends approximate region (ISO 3166-2, e.g. US state; `CF-IPRegionCode`). NOT auto-edited (privacy copy = decision-residue). Recommended wording: "…one anonymous daily ping — an irreversible hash of your device's hardware ID, the app version, platform and OS, and the country **and approximate region** from the connection…". (Same file is the GitHub Pages landing FAQ; youcoded-core/docs/landing-copy.md is the older core landing and omits analytics detail entirely — deprecated, not worth touching.)

3. **`youcoded/app/LICENSE` preamble names a no-longer-linked component** — it states the Android app "links against Termux terminal-emulator **and terminal-view**", but the `terminal-view:v0.118.1` Maven dependency was dropped in Tier 2 (only the vendored `terminal-emulator` remains; per PITFALLS "Vendored Termux terminal-emulator"). The GPLv3 conclusion is unchanged (terminal-emulator is still GPLv3-linked), so this is a minor factual imprecision in the license preamble, not a licensing error. NOT auto-edited (license text). Recommended: drop "and terminal-view" from the preamble sentence — but confirm the aggregation basis first since it's a license file.
