---
status: shipped
---

# UX review 1 — triaged by main agent

Historical review; see the [rollout closure record](../design/2026-09-14-site-marketing-analytics/README.md) for final authorization and verification limits.

Implemented U1, U2, U4, U5 and website-scoped U6. U3 preserved as requested.

Verification: `node --test youcoded-admin/skills/analytics/dashboard/website.test.mjs` → 5 tests, 5 pass, 0 fail. Browser assertions confirmed App remains accessible and the website error has a plain message, closed technical details containing HTTP 503, and Retry.

Contrast rerun: `CDP_PORT=9989 node scripts/ui-review/shot.mjs scratch/analytics-contrast-plan.json scratch/analytics-contrast-check midnight` → exit 0. The plan forces `?theme=halftone-dimension` (manifest palette label is midnight). `node scripts/ui-review/contrast-report.mjs scratch/analytics-contrast-check` → 2 shots probed, 1 distinct failure: existing header theme name at 4.06:1, outside website explanatory labels; left unchanged. No website-label failures reported in these two shots. Halftone lower-page screenshot visually inspected.

Deck: `docs/archive/design/2026-09-14-site-marketing-analytics/site-analytics.review.json`; preview built successfully and contact sheet read. Three numbered changes, Meadow Mist and Light. Baseline and current shots captured from `/before` and `/`; new comparison and builder sections are Brief slides explicitly naming no prior website counterpart. All required risk disclosures included. No arbitrary live URL pane is documented in AUTHORING; provide the operable preview separately at http://127.0.0.1:4792/.

Visual caveat: slide 1's before/after pair is tall; the deck stage clips the bottom explanatory notes at 1440×900 and requires scrolling at 1280×800/1024×768. Its headline, decisions and risk are visible at 1440×900; comparison and builder slides fit. Stopped iterating rather than changing deck infrastructure outside scope. Deck is built, not served; preview server remains running.

- U1 — accepted — I expected every download measurement to distinguish interest from a completed download / the KPI correctly says “Download clicks” and “Clicks, not confirmed installs,” but the platform section switches to “Downloads by platform,” which sounds like completed downloads; rename it “Download clicks by platform” and identify percentages as the share of clicks — Website, platform breakdown — scratch/analytics-ux-1/midnight/narrow-campaigns.png
- U2 — accepted — I expected the campaign table’s “Rate” to be understandable beside its “Visits” and “Clicks” columns / the rate actually uses visits with at least one click, not the adjacent total-click count, and that numerator is not shown; the top-card formula explains the distinction only if I notice it and remember it while comparing campaigns. Use “Visits that clicked (%)” with “Each visit counts once,” or expose the visits-with-clicks count; this is a label/denominator clarity issue, not a claim that the fake arithmetic is wrong — Website, campaign comparison and KPI cards — scratch/analytics-ux-1/midnight/initial.png; scratch/analytics-ux-1/midnight/narrow-campaigns.png
- U3 — rejected for this iteration: existing App wording is outside website scope; preserved unchanged — I expected “My devices” to show only my devices / enabling it includes my devices alongside everyone else, according to the footer’s “Including your own devices”; the header does not communicate that distinction. Rename the toggle “Include my devices” so its effect is clear before scrolling to the footer — App, header filters; behavior confirmed from rendered page text after toggling — scratch/analytics-ux-1/midnight/app.png; scratch/analytics-ux-1/midnight/app-my-devices.png
- U4 — accepted — I expected a first-time explanation of what “Source” and “Campaign” mean when creating a labeled link / the builder gives examples (“reddit” and “september-launch”) but no distinction between where I share a link and the label I choose to group it; the two noun-only fields require marketing knowledge. Add short hints such as “Where you’ll share it” and “A name to group these links” — Website, campaign link builder — scratch/analytics-ux-1/midnight/narrow-link.png
- U5 — accepted — I expected the load error to explain the situation without technical knowledge / “Couldn’t load website analytics (HTTP 503)” exposes an unexplained protocol/code; keep “Couldn’t load website analytics” beside Retry and put the code in optional technical details. Retry is present and was exercised; the forced error remained, which is not reported as a backend defect — Website, simulated error; wording confirmed by rendered page text, screenshot captured but not visually inspectable after the tool’s image-delivery cap — scratch/analytics-ux-1/midnight/error-wide.png; scratch/analytics-ux-1/midnight/error-after-retry.png
- U6 — accepted for website-only explanatory labels using the existing fg-2 token; no whole-app theme edits — I expected explanatory labels to remain legible in Halftone Dimension / the browser screenshot checker flags 29 contrast failures, including the visit/click explanations at 4.06:1 against its 4.5:1 requirement for 11px text; raise the contrast of secondary labels. This is an automated contrast finding, not a visual judgment: the screenshot was captured but the tool’s eight-image delivery cap prevented viewing it — Website, Halftone Dimension — scratch/analytics-ux-1/midnight/halftone.png; scratch/analytics-ux-1/manifest-analytics-ux-extra-midnight-s0of1-1789434853936.json
