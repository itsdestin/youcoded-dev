---
status: superseded
date: 2026-09-23
type: plan
tags: [native-harness, prompt-cache, evaluation, opencode, luna]
---

# Luna request gate offline implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove a content-free pre-dispatch guard can refuse excess ChatGPT OAuth provider HTTP attempts in both isolated clients, including repeat attempts, auxiliary requests on that fetch path and 401 resends, without sending any real model request.

**Architecture:** A private loopback guard server owns an in-memory total request counter across *client* restarts. Each isolated client asks the guard for one reservation immediately before each actual ChatGPT OAuth provider HTTP fetch (including any auxiliary fetch on that path); refusal/unreachable guard prevents the dispatch. The guard never receives or records prompts, URLs, headers, account IDs or session IDs. Its own failure stops the run; it must never be restarted mid-run. This is only an offline proof of the counting seam, not yet the multi-turn fixture, quota policy or a live authorization.

**Tech Stack:** Node.js `node:http`/`node:test`, YouCoded Electron TypeScript + Vitest, OpenCode 1.18.31 Bun + Bun tests. Parent spec: `docs/archive/specs/2026-09-15-youcoded-opencode-luna-prompt-reuse.md`.

## Global Constraints

- OpenCode source/build is the verified private tag `014614d35b397775e5d397a490fc72368c894ec2` at `/home/destin/.cache/youcoded-luna-experiment/opencode-v1.18.31`. The unmodified installed client remains `/usr/bin/opencode` 1.18.2. Changes to the tag checkout stay local, uncommitted and are never upstreamed.
- Modify YouCoded only at `/home/destin/youcoded-dev/worktrees/sessions/prompt-reuse-luna-recovery/youcoded`, and workspace scripts/docs only in its sibling workspace worktree. Never touch the built app, real auth stores, global OpenCode config, Claude hooks, or running client processes.
- `LUNA_GUARD_URL` points to a loopback-only private server. In the two experiment-only client builds, missing/non-loopback/malformed guard URL, non-204 answer, connection failure or timeout **must block model dispatch**. Allow exactly one guard reservation per actual OAuth provider HTTP fetch through the instrumented paths; retries/401 re-sends reserve again. Count auxiliary calls on that fetch path conservatively, even if this stops a run sooner than a model-only cap would. OAuth refresh and model-catalog reads bypass these *provider-fetch* seams and require a separately accounted category in a later runner; never imply this preliminary counter caps every kind of account traffic.
- OpenCode's `experimentalWebSockets` may be enabled via a flag or prerelease channel. For this HTTP-only comparison explicitly refuse the model route if WebSockets are enabled; do not silently count one WebSocket fetch wrapper while it sends multiple wire attempts. Tests must pin this refusal. Also fail closed on non-OAuth OpenCode auth while in experiment mode.
- Synthetic tests only; no authentication, OpenRouter, ChatGPT-plan requests or paid calls. No product behavior changes shipped. Numeric turn/run/whole-experiment limits, fixture, reporter, private profile verification and live go-ahead are separate later stages.
- Do not commit, push or merge. Keep the request guard's logs content-free; the guard response carries no user data. Run desktop `bash scripts/verify.sh <app-worktree>` after any desktop source edit.

## File boundaries

- Create `scripts/luna/request-gate.mjs`: start/stop private loopback HTTP server and expose in-memory reservations/denials; no prompt/body/headers from clients. It will eventually be owned by the offline experiment runner.
- Create `scripts/luna/request-gate.test.mjs`: Node tests for first, limit, refusal and server-down cases.
- Modify `youcoded/desktop/src/main/providers/chatgpt-auth.ts`: optional experiment-only guard dependency; invoke before `realFetch` in its single `send()` function, including the 401 resend. No app-wide fetch monkeypatch.
- Modify `youcoded/desktop/src/main/main.ts`: only for a dev profile, inject a guard built from the env URL into `ChatGptAuth`; the guard must not exist on the installed app path.
- Extend `youcoded/desktop/tests/chatgpt-auth.test.ts`: existing fake OAuth fetch and 401-route harness prove pre-dispatch refusal; do not create a second test file for the same module.
- Modify private OpenCode `packages/opencode/src/plugin/openai/codex.ts`: before its final HTTP `fetch`, reserve with the same protocol. Refuse WebSockets and non-OAuth in experiment mode. No provider input, header or URL is passed to the guard.
- Extend private OpenCode `packages/opencode/test/plugin/codex.test.ts`: local fake Codex endpoint asserts allowed dispatch, denial before HTTP, retry entry and WebSocket refusal.
- After both fake client tests pass, add a cross-client no-network integration test that restarts only fake clients while keeping one guard server. Do not create or copy auth stores.

---

### Task 1: Guard server with a fail-closed reservation protocol

**Interface:** `startRequestGate({ limit })` binds only to 127.0.0.1 and returns `{ url, reserved(), denied(), close() }`; only `POST /reserve` with no request body returns 204 while `reserved < limit`, then 429. No other route accepts a request. The server's own state persists while client processes restart; losing this process fails both client fetches.

- [x] Write a Node test in `scripts/luna/request-gate.test.mjs` that starts a gate at limit 2, sends three `POST /reserve` requests, expects 204/204/429, and asserts `reserved() === 2` and `denied() === 1`. Run `node --test scripts/luna/request-gate.test.mjs` and observe the missing-export failure.
- [x] Implement `startRequestGate` in `scripts/luna/request-gate.mjs` using `node:http.createServer`, bind only to 127.0.0.1 on port 0, validate positive integer `limit`, reject unexpected paths/methods/bodies, increment before 204, and return a clean generic 429 after cap; no logging of request input. Run focused test and confirm green.
- [x] Add tests that close the gate and expect fetch rejection, send an unexpected route and expect 404 without increment, and send a body and expect 400 without increment. Run red before each implementation extension, then green. Run the whole focused file once more.

### Task 2: YouCoded actual-send hook

**Interface:** Add `beforeModelRequest?: () => Promise<void>` to `ChatGptAuthDeps`; `ChatGptAuth.authedFetch` calls it immediately before each `this.realFetch(input, ...)` *inside* `send()`. In experiment-only `main.ts`, create a bounded loopback-only reservation function that reads `LUNA_GUARD_URL` once on startup and sends one bodyless `POST /reserve`; require `res.status === 204` and a timeout, otherwise throw a specific experiment-stop error. Do not expose auth headers, request body, URL or account ID to it. Dev profiles not opted into an experiment retain default behavior.

- [x] Add a test to `youcoded/desktop/tests/chatgpt-auth.test.ts` with a fake signed-in account, fake `beforeModelRequest`, and fake `fetch`: after first allowed HTTP call a second call is refused, and the fake fetch count remains 1. Run only this test with `npx vitest run tests/chatgpt-auth.test.ts -t 'refuses an experiment request before model dispatch'` from `youcoded/desktop/`; watch it fail for the missing dependency/behavior.
- [x] Implement the smallest `ChatGptAuthDeps` and `send()` change; rerun the test green. Add a second test where the first response is 401 and the reservation rejects the resend; assert exactly one model fetch occurred and two reservations were attempted (it passed using the already-tested send hook).
- [x] Add focused tests for invalid/non-loopback/missing URL, 429 and unreachable guard in the dev-only guard helper; test no private payload is sent to the fake gate. Then wire it to `main.ts` behind `YOUCODED_PROFILE` and an explicit experiment guard env flag. Disabled built-app behavior remains byte-identical.

### Task 3: OpenCode actual-send hook

**Interface:** In private tag's Codex OAuth `loader` return fetch, require an explicit experiment flag and loopback guard URL, fail closed for non-OAuth auth and active WebSocket mode, reserve once before final HTTP `fetch(url, withoutInternalHeaders(requestInit))`. SDK stream retries enter this fetch again. This is intentionally an isolated build patch, not a change to the system installation.

- [x] Extend `packages/opencode/test/plugin/codex.test.ts` with a fake OAuth auth loader and local fake endpoint: refusal from a local guard means no Codex endpoint fetch; two successive fake OAuth loader fetches reserve twice, and an auxiliary fetch attempts a third reservation; WebSocket-enabled experiment mode refuses before dispatch. A pinned `@ai-sdk/openai` `generateText` 503 retry with `maxRetries: 1` was subsequently proven with fake I/O and a red/green mutation of the reservation hook; other SDK/provider routes remain unproven. Run `bun test test/plugin/codex.test.ts -t 'refuses Luna request before Codex dispatch'` from `packages/opencode/` and observe red before source edits.
- [x] Implement the local-only guard call in `packages/opencode/src/plugin/openai/codex.ts`; preserve the existing URL rewrite, request body and headers; never pass any of them to the guard. Run focused tests green. Validate the opted-out path against the pinned existing `codex.test.ts` and `openai-ws.test.ts`; no behavior changes when the experiment flag is absent.
- [x] Rebuild only Linux x64 with `env -u OPENCODE_RELEASE OPENCODE_VERSION=1.18.31 OPENCODE_CHANNEL=latest bun run script/build.ts --single --skip-install --skip-embed-web-ui` under the private HOME/XDG/cache variables from the acquisition plan; confirm the private binary reports 1.18.31 and `/usr/bin/opencode` remains unchanged.

### Task 4: Offline integration and release gate

**Review follow-up:** `luna-eval` is the only accepted experiment profile, validated before Electron chooses `userData`; in this opt-in alone, `machineIdentity` is null instead of reading the installed app's `device-id.json` or registering its machine row. The default built/dev branches are unchanged; a focused read-only review confirmed this specific installed-device-ID read is skipped without certifying full path isolation. The cross-client test invokes `ChatGptAuth.fetch()` in fresh test processes (fake account/transport), while a pinned SDK `generateText` 503 retry test proves a second OpenCode send is blocked; neither proves exact same-session continuation. One guard reservation counts each OAuth provider fetch on the instrumented path, including an auxiliary fetch; OAuth refresh and unrelated routes are not covered. A further source sweep found KWin helper startup/IPC could reach the real compositor despite private HOME; an experiment-only facade now returns inert results for startup/status/install/remove without invoking its helper, pinned by fake-only tests. A fresh read-only reviewer found no concrete correctness issue in that facade, confirmed the status also prevents `WorkAreaResolver` startup, and did not certify broader isolation. These are *partial offline proofs*, not approval to launch: effective private HOME/XDG, remaining filesystem/session-bus state, startup network behavior, model/provider routing, numeric ceilings, controller lifecycle and quota/usage-field coverage remain open. The fixture test activates only when `LUNA_FAKE_INTEGRATION=1`; otherwise it intentionally skips.

**Routing follow-up (source-only):** A read-only main-process sweep found the YouCoded reservation at `providers/chatgpt-auth.ts:1008-1017` before actual ChatGPT Responses fetches (including 401 re-sends); `provider-registry.ts:438-463` supplies that wrapper to the SDK. Separate auth refresh and `main.ts:1099-1103` usage polling, WebSearch/WebFetch tools, model-list/catalog/startup requests and other provider branches are **not** guarded by it. `ipc-handlers.ts:2939-2959` can generate an extra model-based session name when AI naming is selected; a fresh private `naming-settings.ts:29-32` defaults to Basic, which makes no naming model call, but the effective preference must be inspected. The source sweep did not prove exact Luna model binding or every SDK retry. OpenCode's `session/llm.ts:224-253` has a separate opt-in native model route; leave it off until separately tested. The guard is a provider-fetch ceiling, not a process-wide network firewall. The separately approved private OpenCode startup guard now skips config-directory `Npm.install` only when `YOUCODED_LUNA_EXPERIMENT=1` and requires `OPENCODE_PURE=1`, refusing merged plugin/MCP/configured-skill paths or URLs rather than silently omitting them; seven fake-service tests (red before policy, green after) and 196 config/Codex/WS tests passed after the review follow-up; package typecheck passed. The private Linux x64 binary rebuilt and its scrubbed-env smoke test reported 1.18.31 after the final skill-path fix; installed OpenCode remains the separately checked 1.18.2 binary, not this private build. This is not a network firewall: Console/well-known remote config, model catalog, provider authentication, tool network, and other startup behavior remain to audit. Tool network must be denied mechanically, and background/auth traffic assessed separately; no account launch is authorized.

- [x] Start a guard at a synthetic limit 2 in a Node test. Spawn YouCoded's real `ChatGptAuth.fetch()` in a Vitest child with fake auth/transport, then OpenCode's Codex OAuth fetch with a fake endpoint in a separate Bun child; start a new YouCoded test process without restarting the guard and assert its send is denied before its fake fetch. Verify one fake send per client, two reservations in total and zero third sends. Stop the guard and assert subsequent dispatches fail closed. No real auth or model request is allowed.
- [x] Search passing focused-test output for request-payload sentinels (`PRIVATE_PROMPT`, `PRIVATE_HEADER`, `PRIVATE_ACCOUNT`, opaque reasoning markers); the fake fixtures and review diff intentionally contain these *synthetic source literals*. Assert none occurs in guard logs or state (which contain only counters). Read only private fake fixtures.
- [x] Run relevant focused tests for both clients, `bash scripts/verify.sh /home/destin/youcoded-dev/worktrees/sessions/prompt-reuse-luna-recovery/youcoded` for desktop, and `git diff --check` plus status in workspace, app and private OpenCode checkout. Fresh read-only privacy/code reviews closed the found profile-path and device-ID issues at the tested seams. **Live verdict remains BLOCKED:** not every provider/auxiliary route or private launcher state has been proven; no compatibility probe or scored run.
- [x] Update the parent spec with the proven seam/limitations (no recovery story).
**Post-restart source audit (2026-09-23; no client launched):** All three isolated worktrees and the private 1.18.31 build survived the machine restart; the workspace session branch is 17 commits behind fetched master, so do not silently merge newer guidance or code into this experiment. The private OpenCode config loader fetches `.well-known/opencode` and optional remote config for `wellknown` auth entries at `config/config.ts:375-415`, *before* the final experiment guard at 603-608. It can merge active Console-org configuration at 502-537 before that guard as well. The private auth root and effective account state must be verified without touching real auth; a merged-config refusal alone does not stop these earlier network paths. `session/instruction.ts:135-163` fetches configured HTTP(S) instruction URLs when assembling the system prompt; the guard currently rejects `skills.paths/urls` but not `instructions`. Config directories can also supply local commands, agents and plugin origins (`config/config.ts:443-489`), and skill discovery scans config directories even when external skill discovery is disabled (`skill/index.ts:205-207`). `OPENCODE_DISABLE_EXTERNAL_SKILLS` and `OPENCODE_DISABLE_CLAUDE_CODE` are separate flags (`effect/runtime-flags.ts:21-30`); neither disables config-directory scanning. Before launch, require an owned empty/private config and fixture tree, refuse unexpected instruction URLs/paths and auto-discovered config content, attest account/config provenance, and mechanically disable tool/startup network or prove each intended exception. This is source evidence of possible paths, not proof they execute with the eventual effective fixture. A read-only app-side audit found YouCoded's `WebFetch` calls `guardedFetch` (`harness/tools/web-fetch.ts:789-795`), `WebSearch` calls the injected search service (`web-search.ts:24-29`), and Bash has **two** child-spawn routes: foreground `bash.ts:530-551` and background `ShellRegistry.start` via `bash.ts:504-524` / `harness/shell-registry.ts:259-266`. `CORE_TOOLS` advertises all three (`harness/tools/index.ts:17-20`); no existing private-profile execution denial was found. A future guard must fail these paths closed without silently removing the advertised tools or altering prompts; per-child network namespaces need both Bash routes and private filesystem mounts. The existing file-tool cwd guard does not sandbox Bash. The read-only audit itself changed no tool or app source. **Follow-up implementation:** The isolated YouCoded `WebFetch` and `WebSearch` now refuse execution when the already profile-gated Luna opt-in is set, before DNS/HTTP or the injected search service. Both tool names/descriptions remain unchanged. Each fake-service test was red before the guard and green after; the complete two web-tool suites passed 81/81. This is **not** network isolation: Bash foreground/background and non-tool app network remain unguarded. Full `bash scripts/verify.sh` on the isolated app passed after this edit. The private OpenCode `webfetch` and `websearch` execution paths have parallel Luna-only refusals before HTTP/ask, with fake HTTP and synthetic loopback tests red before and green after; neither tool is removed from its advertised list. Its web-tool tests passed 16/16; the broader config/Codex/WS/web-tool suites passed 215/215, package typecheck passed, and its scrubbed-env Linux x64 rebuild/version smoke reported 1.18.31. The first red WebFetch test mistakenly used an invalid external hostname and could have attempted DNS; it was replaced with a synthetic loopback-only test before implementation. Do not repeat that mistake or treat this guard as process-wide tool isolation. A fresh read-only reviewer found no issue in the two isolated YouCoded web-tool execution points or their tests; the review explicitly did **not** cover Bash or non-tool network paths.

**Approved offline guard follow-up:** On Destin's request to keep building, the private OpenCode config now refuses well-known auth *before* its first remote fetch, refuses an active Console organization *before* reading its config/token, and refuses merged remote instruction URLs before prompt assembly. Three fake-only tests failed before the corresponding changes and passed after; the well-known client records zero fake HTTP calls, and the Console mock records zero config/token reads on refusal. Normal non-experiment branches are unchanged. These checks do not certify private-account provenance, local instruction/config path containment, tool network denial, provider usage coverage, numeric limits or a live launch; keep compatibility and scored requests blocked pending their separate go-ahead. `tool/registry.ts:209-249,291-302` still registers `fetch` and `search` as built-in tools; `webSearchEnabled` alone does not hide `fetch`, so both need mechanical denial or a tested removal in the experiment. Shell tools are another potential network path and require fixture-specific restrictions, not just model-tool permissions. This machine's `unshare --net -- true` returned `Operation not permitted`, but an offline `bwrap --unshare-user --unshare-net --ro-bind / / --proc /proc --dev /dev -- /usr/bin/true` exited 0. That proves a disposable bubblewrap child *can* have a separate network namespace; it does not prove a client or its tools have been wrapped. A later **synthetic-only** Bubblewrap probe with read-only `/usr`, `/lib`, `/lib64`, `/bin`, a single writable private `/tmp/luna-isolation-check.*` mount, `/proc` and `/dev` showed the marker readable, `/home/destin` invisible, and only `lo` in `/proc/net/dev`; the scratch directory was removed. This is evidence for a possible per-shell sandbox on this Linux host, not evidence that either client uses it, nor a full proof against inherited descriptors or other egress. Wrapping the whole client would also block the provider, so use per-tool/process isolation or a tested narrow tool policy, plus private filesystem mounts, before any live run.

- [ ] Before any dev launch, verify the effective child `HOME`/`os.homedir()` and XDG/Electron/toolkit roots under newly owned private directories, with no symlinks or inherited project/auth/plugin paths. Verify non-KWin session-bus entry points, startup network and private fixture cwd ancestry separately; the KWin facade test alone is not a runtime path audit.
**Shell isolation follow-up (offline only):** `scripts/luna/shell-jail.mjs` is an experiment-only Linux Bubblewrap child launcher: it validates an owned 0700 private clone under the generated `luna-six-turns` root, mounts only that writable clone plus read-only system binaries, substitutes private `/tmp` and `/proc`, clears the child environment and unshares user/network/PID/IPC namespaces. Three synthetic-only tests proved access to the clone and no host home, inherited secret or non-loopback interface; a missing fixture root refuses execution. The isolated YouCoded Bash foreground **and** background spawn routes and private OpenCode shell child now call the launcher only under the Luna opt-in; fake-wrapper route tests failed before implementation and passed after. The app's full `scripts/verify.sh` passed after correcting a type annotation; private OpenCode's 40 shell/web tests and package typecheck passed, and its scrubbed-env Linux x64 binary was rebuilt pinned to version 1.18.31. These tests prove a single wrapper and the respective routing seams separately, **not** a whole-client network/filesystem sandbox or that either full client has launched against a fixture. File tools, non-tool egress, startup state, alternative shell paths and controller attestation are still open. The first scrubbed rebuild accidentally defaulted to preview version `0.0.0--202609230804`; it was discarded and rebuilt with `OPENCODE_VERSION=1.18.31`, `OPENCODE_CHANNEL=latest`, then version-smoked as 1.18.31. No account or model request ran.

**Budget follow-up (offline only):** The existing bodyless loopback reservation gate now accepts optional strict positive-integer `budgets` for per-probe, per-turn, per-repetition and turn wall-clock ceilings alongside its whole-experiment limit. Its controller-only segment methods count probe attempts against the whole total, count all pre-dispatch retries/auxiliaries in the active segment, reject unscoped/out-of-budget/timed-out requests and latch closed after a denial, including a turn timed out without another request. Thirty-four fixture/jail/usage/gate tests passed, including restarted fake client processes. This is only a pre-dispatch component: no live numerical values or allowance stop threshold have been selected or approved, no timer yet kills a hanging client, and no full six-turn controller exists. The old fake tests' `{ limit }` path remains intentionally usable for offline fixtures, so a future live controller must require the strict form explicitly.

**Usage follow-up (offline only):** `scripts/luna/usage.mjs` normalizes pre-allowlisted synthetic per-request records with strict numeric/null validation, bounded client/model/route labels and no raw SDK/session objects. It reports warm clean-subset medians and all-observed operational token totals with unknown counts; a red/green follow-up added separately counted compatibility-probe consumption to the whole-experiment quota view without contaminating scored repetitions. Nineteen fixture/usage tests and four fake request-gate tests passed. This is not a provider usage collector: the concrete YouCoded/OpenCode per-request source, retry coverage and semantics remain unproven before live work.

**Fixture follow-up (offline only):** `scripts/luna/fixture.mjs` now generates six synthetic, dependency-free clone roots and validates initial/one-change/both-change states. A review found the first draft's full checker could not pass after turn 3 while turn 4 remained undone; focused config/greeting checks and prompt references were added after a red test. Further red/green tests pin unchanged instructions and reject symlinked clone ancestors; a semantic JSON test avoids marking an equivalent formatting change wrong. Eight fixture tests and four fake request-gate tests passed in the isolated workspace. A fresh read-only fixture review found that a lookalike directory could pass the verifier and that storing future turn descriptions inside every clone could leak upcoming tasks. Red/green tests now bind verification to controller-minted clone/root inode identities and keep six exact, frozen prompts outside the project as `PROMPTS`; a real-filesystem test covers 0700-only parent directories. The parent's initial emptiness already proves `luna-six-turns` does not exist before creation; a failed build can leave partial output in that private caller-owned root, and the controller must abort, not reuse it. The verifier's static link scan and inode checks are **not** a race-proof runtime filesystem sandbox. A future runner must attest that each client loaded the intended instruction text, sends the exported prompts in order, and keeps all tool paths within private fixture/state roots before live use.

- [ ] In the separate controller/runner plan, prepare numeric per-turn wall-clock/provider-attempt, per-repetition and whole-experiment ceilings and the threshold against observed plan allowance for Destin to approve **before** any live authorization. This preliminary counter cannot enforce those limits yet.
