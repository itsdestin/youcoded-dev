# D1/D2 review 2 — a3a7f9aa..10127ff6

### Spec Compliance
- ❌ Issues found:
  - **D2 "cross-project Always-allow" is not delivered.** The subject for a `user`-scoped helper omits the work dir (`tools/task.ts:315`), but the remembered rule is written and read strictly per project slug: `native-session-host.ts:2220` builds the remembered layer from `permissionStore.rulesFor(cwd)`, `:2520` persists with `remember(cwd, rule)`, and `permission-store.ts:50-57` ("Remembered rules for the project owning `cwd`, or [] if none stored") has no cross-project bucket (`rg "global|allProjects" permission-store.ts` → no hits). So a grant made in project A never fires in project B. The card (`ToolCard.tsx:820` "applies to this helper in every project"), Settings (`describe-rule.ts:869` "in every project") and `docs/native-runtime.md:1871-1872` all promise what the store cannot do.
- ✅ Everything else in the brief is present and verified in the diff: D1 rule after the broad allow (`permission-types.ts:180`) with real-engine tests; `grantScope` stamped only by the catalog/loaders (`catalog.ts:293,307`, `definition-files.ts:286,408`; `loadClaudeCodeDefinition` requires it, no default); the three subject shapes (`task.ts:305-317`); built-in subject unchanged (`task-tool.test.ts` "a built-in keeps its 1c subject exactly"); renderer default-closed while unresolved (`ToolCard.tsx:813`) with the width note gated on the button (`:763`); resume gated on the spawn-time fingerprint (`native-session-host.ts:829-832`, recorded at `:1018`), surfaced on both resume branches with reservation release (`task.ts:608-611, 633-635`); per-instance memo (`task.ts:520-526`); `work_dir` resolved against the session cwd (`harness-session.ts:886`, `task.ts:303`).
- ⚠️ Cannot verify from diff: "no child may hire" — unchanged by this diff (the context line at `definition-files.ts:17` still shows `Task` absent from `NATIVE_CHILD_TOOLS`); not re-checked beyond that.

### Strengths
- The resume gate sits exactly where it must: after the live-roster resolve and before `buildSpecialistSession` (`native-session-host.ts:829-850`), and both the "absent side" cases are pinned so legacy rows and built-ins stay resumable.
- Named-risk (1) check: every child construction goes through `createChild` → `buildSpecialistSession`, and its only callers are `spawnSpecialist`/`spawnSpecialistBackground` (fresh hires, which get a subject and a card) and `resumeSpecialist` (gated). `rg` outside the host and `task.ts` finds only the `ToolServices` interface (`tools/types.ts:171`); the ledger's restart recovery marks rows stale and never respawns (`delegation-ledger.ts:138`). No bypass path.
- Named-risk (2) check: the only Task allow producer is `rulesForMode` (`rg "tool: 'Task'" src` → `permission-types.ts:168,180`); presets only add `tool:'*'` inside the mode layer; `rememberedRuleFor` fails closed for a subject-less Task (`harness-session.ts:~113`), and every remembered non-Bash rule is `match:'exact'` (legacy match-less rows are normalised to exact on read, `permission-store.ts:53-57`). Nothing can out-rank the `*:file:*` ask except an exact grant for that very subject — which is the intent.
- The memo test proves identity (`toBe(handedOut[0])`) rather than field equality, so it genuinely catches a second resolve; the host tests use a real catalog over a real file, so the fingerprint compared is the loader's own.
- The refusal text is specific and accurate (names the helper, says what changed, names the one path that re-asks) — no guessed cause.

### Issues
#### Critical (Must Fix)
None found. No consent bypass or grant leak: the scoping errors below all fail in the *under-granting* direction.

#### Important (Should Fix)
1. **`permission-store.ts:50-57` / `native-session-host.ts:2220,2520` — the "every project" grant is stored and consulted per project only.** What the user experiences: they click Always allow on their own `~/.youcoded/specialists/docs-writer.md`, read "applies in every project", open another project, and get asked again — and in Settings the rule sits under one project's heading while reading "in every project". This is the D2 requirement Destin actually asked for, plus user-facing copy that is false (project rule). Fix: give the store a cross-project bucket that `rulesFor(cwd)` always unions in, route a `user`-scoped Task grant there (the host knows the subject shape at `rememberRule`; a `^(read-only|read-write):file:` subject is the discriminator, or better, thread the scope explicitly from the tool), list that bucket in Settings under its own "All projects" heading, and correct `docs/native-runtime.md:1871`. If that is out of scope for this task, the note and `describeRule` must instead say "in this project" until it lands — but that is a D2 miss, not a fix.
2. **The folder → `grantScope` mapping — the decision that decides whether a repo-shipped helper's grant could ever travel — has no pinning test.** `rg grantScope tests/specialist-catalog.test.ts tests/specialist-definition-files.test.ts` → no hits; every D2 test hand-builds definitions. Swapping the `'user'`/`'project'` literals at `catalog.ts:293,307` passes all 393 tests. Related: `tests/specialist-definition-files.test.ts:141-342` still calls `loadClaudeCodeDefinition(path, raw)` with two arguments (33 sites) — `tsconfig.json:16` includes only `src/**`, so "tsc clean" says nothing about tests, and those runs now produce `grantScope: undefined`. Fix: update those calls; add a catalog test that a file under `<cwd>/.claude/agents` resolves `grantScope:'project'` and one under `claudeUserDir`/personal resolves `'user'`; add a loader test that two different raws yield different 12-hex fingerprints and the same raw the same.

#### Minor (Nice to Have)
3. **`task.ts:311-313` WHY comment overstates: "Naming it in the subject also makes the odd case visible in Settings".** `describe-rule.ts:858` matches `@[0-9a-f]+$` only, so `@unverified` falls through to the plain charter branch and renders as a directory literally named `file:x@unverified` — raw syntax, not a labelled case. Either accept `[^@]+` in the regex or soften the comment.
4. **`ToolCard.tsx:700-706` `grantFolderName`** renders "in . only" for `work_dir: './'` and "in .. only" for `'..'`. Resolve the same way `task.ts` does (against the session cwd) or treat `./` like `.`.
5. **Hire with no `work_dir` in ask mode:** `permissionSubject` returns `undefined` (`task.ts:301`), the card still renders with Always allow and the new width note, but `rememberedRuleFor` returns `null` for a subject-less Task and `execute` refuses the call at `task.ts:457`. The note now makes an explicit promise that nothing will keep. Pre-existing for built-ins; suggest adding `!tool.input?.work_dir` to the hire branch of `suppressAlwaysAllow` (`ToolCard.tsx:810-813`). (In auto-edit the pattern-less allow answers this subject-less call, but `execute` refuses it before any spawn — verified, not a bypass.)
6. **Naming (carried from review 1, now worse):** `SpecialistDefinition.grantScope` (folder scope) and the existing width type `GrantScope` used as `d.grantScope` at `harness-session.ts:2641` are two different things with one name in one subsystem. `definitionScope`/`grantWidth` would stop a future reader conflating them.

### Assessment
**Task quality:** Needs fixes
**Reasoning:** The consent-critical parts (D1 ask, fingerprinted subjects, resume gate, memo, session-cwd) are correct and well-guarded, and I found no bypass or leak. But the headline D2 promise — one grant for the user's own helpers across projects — is not delivered because the remembered-rule store is per project, and the card, Settings and docs all claim otherwise; the folder→scope stamping that keeps repo-shipped helpers narrow is also unpinned.
