# Shared Dialog treatment — post-build UX check

The first independent tester could not reach the popup: the direct live-candidate route displayed only its initial white spinner during the capture (`/tmp/ux-selected.png`), so no usability verdict can be inferred from that attempt. `scripts/workbench-boot-check.mjs 5513` then mounted all 16 dev routes. A browser probe opened **About from the ordinary Workbench Settings**, not a candidate, and measured a 16px title, no full-width border, a content mask, and a real scroll range of 1212px. At the exact end (`scrollTop=1212`), the hook reported `data-fade-bottom=false` and the mask length was 0px, leaving final lines visible. Direct browser probes also confirmed that the answered deck's Today pane still renders 14px bold, full border and painted fade, while its selected pane renders 16px medium, tapered line and no painted overlay.

The second context-free UX pass reached the ordinary Workbench About popup at 1024×768, captured top/middle/end, and found text readable throughout with the title/version remaining visible; no actionable visual issue established. It did not check Halftone Dimension or the physical Escape/Close controls; a page-dispatched synthetic Escape did not close, which does not establish an actual keyboard defect. These checks do not replace Destin's interactive approval. The prior answered popup deck now opens to its submitted-feedback page, so its **preview** fails the live-pane check by design — do not use that failed after-submission contact sheet as a visual comparison. The separate unsubmitted Project detail deck has a valid self-verifying preview.
