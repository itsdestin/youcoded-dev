---
status: active
date: 2026-09-23
related: docs/active/design/2026-09-22-ui-guide-rebuild/popup-files-comparison.review.json
---

# Popup roles and Session Files — visual comparison

The review deck is a **choice of directions**, not approval of T-01/T-03 wording, global restyles, or production implementation. T-02/r2 remains the only exact rule approved in the separate wording deck. No new production Session Files treatment was retained while preparing these pictures.

Sources: isolated session Workbench at port 5513, fixture-backed app surfaces captured by `shot.mjs` into `scratch/ui-consistency-popup-preview/run/`. The four dialog classes are 340px prompt (Donate), 420px panel (About), 600px document (Submit a ticket), and 820px wide (Assistant settings). Other representative classes: 640px project search palette, compact Tags & note, 1000px Resume, and inset Marketplace Details. These are *not all popup instances*: the four-up sheets group representative widths and roles for comparison. The deck also shows a freshly captured right-click menu and near-fullscreen Project file detail as placement exceptions, rather than implying the named dialog ladder covers them. This is a representative role inventory, not a census of every popup instance.

Session Files options were captured with temporary candidate markup in the real `SessionDrawer` component, using existing `ArtifactThumbnail` for actual Workbench sample markdown/HTML/image/svg content and extension fallback for unsupported kinds; no drawn or fabricated thumbnail. The extra branch was removed after capture; `git diff --exit-code -- desktop/src/renderer/components/SessionDrawer.tsx` confirmed the file returned to its prior state, and `npm run typecheck` subsequently passed. The 44px square reveals awkward cropped Markdown text, which is a real trade-off, not a screenshot defect. Project Files stays unchanged. Android file-list APIs have a not-implemented path and these desktop-browser captures do not certify that surface on Android.

Capture evidence: `preview-plan` 36/36 verified; `document-plan` 4/4 verified after correcting a selector; `exceptions-plan` 8/8 verified. `custom-plan` 12/16 verified — all four attempted report-dialog shots were missed and explicitly excluded from the deck, replaced by the `document-plan` verified shot. Contrast probe flags in captures include pre-existing theme/fixture areas; they are not clearance of every app text color. Four themes are shown (Light, Midnight, Halftone Dimension, Meadow Mist); Creme and the live Ivory Schematic palette were not captured for this question. Motion, keyboard navigation, phone layout, and real disk IO remain unverified. Submitted `popup-files-comparison.review.answers.json` (20:45 UTC): **P-1 Other:** make header style/size consistent across the four named dialog sizes; the answer does *not* choose a new width scheme. **P-2 Other:** written options were too abstract; Destin wants actual in-context A/B/C visual treatments before a decision. **P-3 separate roles, qualified:** still wants to see the A/B difference, so this is not blanket approval of the style or guide wording. **F-1 wide preview:** refine its proportions and spacing; remove the separate line under the search input and show the reviewed tapered header and fade on Session Files. Follow-up clarification: the four dialogs' 16px medium title is already supplied by the same `Dialog` header (`ui/Dialog.tsx:230-255`), but Destin means the **whole header** (height, padding, subtitle, close alignment) should be explored for consistency and also wants to tune the title typography *visually across modal types*. Do not mistake his answer for an approval of the current shared title. These are design directions, not production or guide-rule approval. T-01 and T-03 wording remain deferred; the next deck must show visible alternatives rather than another word-only taxonomy.
