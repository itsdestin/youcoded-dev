# Specialists plans — code review (fresh reviewer)

Branch `feat/specialists-plans-ui` (itsdestin/youcoded), worktree
`/home/destin/youcoded-dev/worktrees/specialists-plans`, diffed against `origin/master`
(merge base `d76527c1`; 206 files, +23,544 / −316). Reviewed against
`specialists-plans.contract.json` and the rules native-runtime, native-specialists,
native-permissions, harness-tools, ipc-bridge, chat-reducer, react-renderer, android-runtime,
test-suite-hygiene, plus `docs/PITFALLS.md`.

## Verify

```
verify: /home/destin/youcoded-dev/worktrees/specialists-plans (base origin/master)
  tests: related to 201 changed file(s) + 76 source-scanning guards
PASS  types (tsgo --noEmit)
PASS  types in tests/ (tsgo --noEmit, 57 file(s) still excluded)
PASS  tests (related)
PASS  dead code (knip)
PASS  lint (oxlint)
PASS  design lint (oxlint --max-warnings ratchet)
PASS  invariants (ast-grep)
OK — all checks passed.
   Not covered: Android (./gradlew test), marketplace worker.
```

Also run on their own: `plans-transport`, `remote-shim-plans`, `ipc-channels` and
`android-honest-build` (333 passed), plus the eight contract guard files (117 passed).
Android `./gradlew test` was not run.

## Findings

- F1 accepted — desktop/src/main/harness/plans/plan-budget.ts:555 (with plan-service.ts:514-521, remote-shim.ts:626-633, PlanCard.tsx:240-251) — Add budget is not idempotent, so a lost or late reply followed by Retry, or a second press of the returned "Add budget" button, adds the tranche twice and raises the approved limit by twice what the user meant. — Call chain read: a remote invoke times out after 30 s even though the host may already have added the budget (the shim marks the outcome unknown). The plan stays `paused` after `addTokens`, whose only check is `status === 'paused' && !lease`, and there is no request id or expected `seq` to reject a repeat. The card also shows the raw "Request plans:add-budget timed out" text with Retry and no Report bug. Approve, Stop and Ask are protected by status checks; Add budget is not.
- F2 accepted — desktop/src/main/harness/plans/plan-executor.ts:1420-1422 (with plan-journal.ts:529-530, 672) — If settle's final write fails transiently (lock exhaustion, disk), the error is only logged. The plan stays `running` with this process's lease, and its unreleased holds stay too. Because `ownerState` returns `'self'` whatever the expiry, `recoverInterrupted` skips it and Continue refuses "already running", so the card says "running" with nothing behind it until Stop or an app restart. — Read `settle` → `onJournalError` (a non-fence error returns false and nothing retries), then `retireRun` has already dropped the run and heartbeat. The trigger is rare. The stuck state follows from the code.
- F3 accepted — desktop/src/main/harness/plans/plan-service.ts:365-379 — With "Run small plans without asking" on, nothing limits how many plans a turn or conversation may auto-start. A model that calls `propose_plan` repeatedly with different documents starts one under-limit plan per call, each spending without a click. Plan specialists are exempt from the 30-spawn backstop (native-session-host.ts:4876-4880), and doom-loop detection only catches identical calls. — Read `propose` → `startRun`. No per-turn or per-session count found by rg. [PLAUSIBLE]
- F4 accepted — desktop/src/main/harness/plans/plan-executor.ts:796-801, 1201-1206 — After a non-recovered `invalid-report` pause, pressing Continue does not continue the specialist. The failed attempt is committed as `failed`, so `runWave` creates a fresh attempt and a new session with the original brief, and the whole task runs again from scratch, including any Bash or Write work the first run already did. The pause text says nothing about a rerun. — Read `commitReport` (commits `failed`), `runStep` (`needed` includes it) and `runWave` (committed latest → fresh member with no `childId`). [PLAUSIBLE that this conflicts with the approved intent]
- F5 accepted — desktop/src/renderer/remote-shim.ts:441-456, 484-501, 597 — The eight `plans:*` actions are not in `MESSAGE_KIND`, so a press made while the phone is reconnecting is queued and flushed after sign-in (if under 30 s old) while its own 30 s invoke timer runs at the same time. The card can report a failed Approve or Add budget that the desktop then carries out, which also feeds F1. The ipc-bridge rule says a person's action is never held for later, and `native:interrupt` and `ui:action` follow it. — Read `send`, `flushSendQueue` and `invoke`. [PLAUSIBLE timing]
- F6 accepted — desktop/src/main/harness/tools/propose-plan.ts:31-33 (rendered at PlanCard.tsx:331-338; guard plan-card-review-5b.test.tsx:115) — Contract R27 is broken: every real failed card comes from `failedPlanProjection`, which never sets `failure`, even when the error is known (validation, service throw, no commit). The card shows "Plan: a plan · failed" over an empty body with no reason and no Report bug or Diagnose. The R27 guard pins the no-reason case as correct. — Read. `failure` is only ever set for salvaged unreadable-journal cards (plan-journal.ts:383).
- F7 accepted — desktop/src/main/harness/plans/plan-requests.ts:61 — Contract R44 is not implemented: a plan button pressed during startup answers "The assistant runtime isn't connected." (jargon). The signed copy "The assistant isn't available right now." appears nowhere on the branch. — `rg` across desktop/src and app/src found no match.
- F8 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:630 — Contract R41 is not met: on a narrow window the step title keeps `truncate`, so it is cut off instead of wrapping. The guard (plan-card-ask.test.tsx:306-316) only checks that the title is on the first line. — Read.
- F9 accepted — desktop/src/renderer/components/plans/plan-bridge.ts:108-113 — The "plans unsupported on this device" answer is cached for the life of the page. `connectToHost`/`disconnectFromHost` (remote-shim.ts:1733, 1783) switch hosts without a reload, so an Android client that first got its local `unsupported` keeps every plan card disabled after connecting to a desktop that can run plans. — Confirmed that the cache is never reset. [PLAUSIBLE that a card mounts in local mode first]
- F10 accepted — desktop/src/main/remote-server.ts:3794-3798 with plan-bridge.ts:37-41 — Against an older desktop without the plan cases, the default reply "This feature isn't available over remote access yet (plans:get-auto-approve)." carries `unsupported:true`, so the card shows that developer sentence (with its channel id) as its permanent disabled reason. — Read the default branch and `responseOutcome`. [PLAUSIBLE version mix]
- F11 accepted — desktop/src/main/harness/plans/plan-service.ts:225, 727, 742 — Returned failures put raw `e.message` on the card and in Settings ("Couldn't approve the plan: EACCES …"). The transport comments (plan-requests.ts:118-121, ipc-handlers.ts:3474-3477) say system wording is kept off the card, but that only covers thrown errors. — Read `act`, `getAutoApprove` and `setAutoApprove`.
- F12 accepted — desktop/src/renderer/components/SpecialistsSection.tsx:350, 398 — A failed read of the plan settings leaves the "Run small plans without asking" switch disabled for good (`under === null`). The line says "Please try again." but gives no Retry, against the error-message standard. — Read.
- F13 accepted — desktop/src/renderer/components/SpecialistsSection.tsx:356-364, 395 — Saves have no in-flight guard. Editing the amount and then clicking the switch fires the blur save first, and either the click is swallowed or two writes race and the later reply sets the shown state. — Read. [PLAUSIBLE]
- F14 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:235, 241, 269 — Retry replays the closure captured at the failed press, so a note, question or amount edited after the error is not what gets re-sent. — Read.
- F15 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:519-521 — The action error is not gated on status. If Stop fails and a push then shows the plan finished, the finished card still shows the error with a live Retry that re-sends Stop. — Read.
- F16 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:145, 148 — `adding`/`asking` are not reset when the plan leaves `paused` (for example, resumed from another device). On the next pause the field or box reopens unasked with an old value, and the pause buttons are hidden. — Read. [PLAUSIBLE]
- F17 rejected — per design §2, a "stop" recommendation means continuing would not help; Ask stays available — desktop/src/renderer/components/plans/PlanCard.tsx:371-381 — A "stop" recommendation removes Add budget and Continue, leaving only Ask and Stop, although the WHY comment says a recommendation should lead the buttons, not replace them. — Read. [PLAUSIBLE against intent]
- F18 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:575 — The "writing the plan · Ns" clock always shows 0s: `writingPlanProjection` (propose-plan.ts:17-29) never sets `startedAt`, and the signed-copy test injects one (plan-card-signed-copy.test.tsx:55), which hides this. — Read.
- F19 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:578 — Every writing card says "can take a few minutes on your computer", cloud models included, because the check keys on `ceilingUsd == null` and the writing shell always has `ceilingUsd: null`. — Read.
- F20 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:53 — Writing, failed and stopped cards always read "Plan: a plan". `planDisplay` falls back to `input.title`, but the tool field is `goal` (plans/schema.ts:47), and the shell's truthy placeholder `'a plan'` wins first anyway. — Read.
- F21 accepted — desktop/src/renderer/components/plans/plan-status.ts:130 — A proposal the user interrupted reads "stopped — 0 of 0 steps done" (`stoppedPlanProjection` has no steps) over an empty body. — Read.
- F22 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:112, 125 — `usd()` can return "less than a cent", which is then glued into "about less than a cent", "~less than a cent" and "the ~less than a cent limit". — Read.
- F23 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:387 — The Add budget dollar figure never gets "~" on an approximate (ChatGPT) plan, which R26 requires. — Read. [PLAUSIBLE]
- F24 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:615, 659 — The step "up to …" and "stops at its N-token limit" figures ignore `setupTokens`, so the rows don't add up to the plan ceiling and understate where a specialist really stops. — Read. [PLAUSIBLE against intent]
- F25 accepted — desktop/src/renderer/components/PreviewTimeline.tsx:61-63 — Conversation previews draw plan cards with live buttons under the preview key, and no `plans:event` re-send reaches that key. A plan that already ran shows "waiting for your approval" with Approve, which then gets a refusal. — Read. [PLAUSIBLE]
- F26 accepted — desktop/src/renderer/components/plans/PlanCard.tsx:616 — Copy is always plural: "0 of 1 reviewers done". — Read.
- F27 accepted — desktop/tests/plan-card-review-5b.test.tsx:271, 308; plan-card-signed-copy.test.tsx:92; plan-card-review-r7.test.tsx:142-170; plan-card-ask.test.tsx — Several mechanical guards don't test their whole row. R17 never checks that the budget field is inside the amber pill. R22 checks label order but not which button is filled or light, and covers only the plan card, not "wherever". R28 checks the words only, not the muted style or empty circle. R30 checks only "Wren wants to:", never Yes, No, Note or Stop. R37 checks the Ask button's position, not that a pause "never hands itself to the assistant". — Read each guard against its row.
- F28 accepted — add a syncPlanTool refusal test; the fail-closed treatment of unreviewed larger local models stays until model information exists and is reported to the owner as a known limit — desktop/tests/plan-eligibility.test.ts — R1's guard tests `isPlanEligible` on its own, never that an ineligible session is actually refused `propose_plan` (`syncPlanTool`). It also pins larger unreviewed local models (122B, 29B, Gemma 27B) as rejected, which is narrower than the row's "from the 9B class up". R24, R41 and R45 are checked only through CSS class names, so real wrapping is unproven in jsdom. — Read.
- F29 accepted — desktop/tests/plans-transport.test.ts:153-162 — The "desktop and remote answer identically" test sends hand-written payloads and never runs the real `preload.ts` `plans` object. A preload method sending `amount` instead of `tokens` would pass every test. The real getAutoApprove payload (`undefined`) also differs from the tested `{}`. — Read. The current preload mapping is correct when read by hand.
- F30 accepted — app/src/main/kotlin/com/youcoded/app/runtime/PlansBridge.kt:23-33 — `PlansBridge.CHANNELS` is read only by `PlansBridgeTest.kt`, while SessionService.kt:4103-4110 hard-codes its own eight labels, so the Kotlin test proves nothing about what the phone answers. Parity really rests on `ipc-channels.test.ts` and `android-honest-build.test.ts`. — Repo-wide `rg`.
- F31 rejected — preload is sandboxed and cannot import the shared list; per-surface parity guards catch drift — desktop/src/main/harness/plans/plan-requests.ts:21-36 — `PLAN_REQUEST_CHANNELS` and `PLANS_EVENT_CHANNEL` are exported as the shared list but only tests import them. Every surface repeats the strings by hand (per-surface guards do catch drift). — `rg`.
- F32 accepted — duplicates and dead code: `isPlanCard` exists twice (chat-reducer.ts:432, plan-activity.ts:61), and specialist-cards.ts:32 repeats the `'propose_plan'` test a third time. `hasHelperAsk` (specialist-cards.ts:73) is now a test-only alias. `PlanView.revisionOf` (shared/types.ts:868) is never read by the renderer. `planSubs` in mock-shim.ts:1654/2813 is written but never read. PlanCard.tsx:306-308 has a ternary with identical branches. — `rg` / read. knip is green because these are exported or used by tests.
- F33 accepted — desktop/src/renderer/hooks/useIpc.ts:418-419, mock-only.ts:113, plans-transport.test.ts:8, SpecialistsSection.tsx:377 — The comments still say "seven" plan channels (Ask makes eight), and the setting's description says "ceiling" where the card standardised on "limit". — Read.
- F34 accepted — desktop/tests/plans-lifecycle.integration.test.ts:146, 213, 224, 266, 344, 453; plan-executor.test.ts:598, 714 — Fixed sleeps stand in for "nothing happened" checks, and wall-clock lower bounds are asserted. Neither turns red under load (lower bounds, negative checks), but under test-suite-hygiene a slow run makes the negative checks vacuous rather than failing. — Read.

## Not covered

- Android: `./gradlew test` and `PlansBridgeTest.kt` were not run. Only the Kotlin sources were read.
- No runtime check (no dev instance): the reconnect and timeout timing (F1, F5), real layout wrapping (R24, R41, R45), the alert sound (R32).
- `harness-session.ts` was read only on the plan-child request path (`runPlanStep`, the plan tool gate, the report-only turn). The rest of its +806 lines (writing-shell pairing, repair turn, history-rebuild interplay) got a skim, not a review.
- `native-session-host.ts` plan-notice queue/withdraw (`queuePlanNotice`, `planNoticeRefusal`, delivery holds) and the `getHistory` plan-child replay were not traced.
- Multi-window ownership (`sendForSession` with no owning window), and the order of the remote first-page `plans:event` against an in-flight `chat:hydrate`.
- The workbench mock-shim, fixture-loader and specialist-runs changes, beyond the dead-state finding.

Design note, one line: auto-approve is judged in tokens only, so a priced plan under the token setting starts with no dollar threshold the user set (as approved in R12; noted, not a finding).
