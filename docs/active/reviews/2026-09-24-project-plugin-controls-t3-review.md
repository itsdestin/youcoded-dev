# Project plugin controls — T3 (IPC) code review

Commit reviewed: `bf1eebb2d` (branch `session/plugin-project-controls`, worktree
`/home/destin/youcoded-dev/worktrees/sessions/plugin-project-controls/youcoded`).

`bash scripts/verify.sh youcoded` (from the workspace root): **all checks passed** —
types, types in tests/, related tests, knip, lint, design lint, ast-grep invariants.
Not covered by that run: Android (`./gradlew test`), marketplace worker.

- F1 — `desktop/src/main/project-extensions/import-skill.ts:46` (`importSkillFolder`) called from `desktop/src/main/remote-server.ts`'s `case 'project-extensions:import-skill'` (~line 2311) with a raw, client-supplied `payload.skillMdPath` — copies an entire directory tree (`fs.promises.cp(sourceDir, destination, {recursive:true})`) with NO `realpath`/symlink resolution and NO sensitive-path denylist, unlike every other remote-reachable arbitrary-path read in the same file. Confirmed by reading `fs-read-head.ts` (`readFileHead`, used by the `fs:read-head` WS case at `remote-server.ts:2263`), which resolves `fs.promises.realpath(rawPath)` and rejects `isSensitivePath()` on both the raw and resolved path before touching the file, and `remote-server.ts`'s `artifacts:read-binary`/`artifacts:*` cases, which are gated by `refuseUnknownRoot`. `importSkillFolder` only checks `path.basename(skillMdPath) === 'SKILL.md'` and that the destination doesn't already exist — any absolute path a remote client sends (e.g. one resolving into `~/.ssh/…/SKILL.md`, or a symlink inside a controlled folder pointing elsewhere — Node's `fs.cp` default `dereference:false` copies a symlink as a symlink, not its target's content, so a planted symlink lands intact under `~/.claude/skills/<name>/` and can be read back through it) is accepted and its whole containing folder copied into `~/.claude/skills/<name>/`, which `scanSkills()` then treats as an installed, usable skill. On desktop this path is normally reached only through an OS file picker (the user's own access), so it's benign there; over `remote-server.ts` there is no picker — the WS payload is the only gate, unlike `fs:read-head`/`artifacts:read-binary`'s explicit denylist+realpath pattern for exactly this "arbitrary path from a remote payload" shape. (Mitigating context found while confirming this: `remote-server.ts`'s own `session:create` comment concedes an authenticated remote client already has shell-equivalent reach via `engine:run-in-terminal`/`session:input`, so this is not a zero-to-full-compromise escalation — but it IS a silent, one-IPC-call copy primitive with none of the deny-list/symlink protections the codebase already applies to its other remote-payload-path channels, and `project-extensions-import-skill.test.ts` has no traversal/symlink/root-scoping test at all — confirmed by reading that file in full.) [PLAUSIBLE — severity depends on how much weight the workspace puts on defense-in-depth for an already-shell-reachable remote client, but the inconsistency with `fs:read-head`'s own precedent in the same file is verified fact, not a guess.]

- F2 — `desktop/src/main/project-extensions/ipc-shell.ts:86` (`gatherCatalog` → `discoverSkillEntries(cwd)`, called from `projectExtensionsGet`, `projectExtensionsSet` and `projectExtensionsForSession`) drives a fully SYNCHRONOUS skill-directory scan on the Electron main thread. `discoverSkillEntries` (`harness/skills/skill-catalog.ts:80`) calls `scanProjectSkills()`/`scanSkills()`, and `desktop/tests/main-blocking-calls.allowlist.json` lines 561-568 confirm those functions contain `fs.readdirSync` (×4), `fs.existsSync` (×4), `fs.lstatSync`, and `fs.readFileSync` — a real, non-trivial directory walk + per-skill frontmatter read, not a cheap check. This is pre-existing code, already allowlisted for its older call sites (session create / tool wiring), but this commit wires it onto a NEW, much higher-frequency path: design §5's `useSessionAvailability(sessionId, open)` calls `project-extensions:for-session` every time the CommandDrawer opens AND again on every session switch while it stays open — confirmed by reading `ipc-shell.ts`'s `projectExtensionsForSession`, which calls `gatherCatalog` unconditionally on every invocation with no caching. Performance rule 1 ("the main process never blocks... on any path... an IPC call... reaches") is written exactly for this shape, and its own text ("review misses it") anticipates this class of miss — reusing an already-allowlisted sync call is not exempt just because the call site is new. Confirmed by reading `ipc-shell.ts` (no cache/memoization, no async alternative used), `skill-catalog.ts`, and the allowlist entries above; not run against a live main-thread stall measurement. [PLAUSIBLE for real-world severity (depends on skills-directory size), but the code path and its synchronous nature are verified, not inferred.]

- F3 — No test in this commit exercises the SYNCED-project path for `project-extensions:get`/`:set`/`:for-session`. `project-extensions-ipc-shell.test.ts` only ever registers an unsynced folder (`youcoded-folders.json` entry with no sync root / `syncName`) — confirmed by reading the whole file and grepping it for `syncName`/`synced` (zero hits). The hunt question ("does a synced project's path differ per device, and does `get` on device B find the record device A wrote") is answered correctly by the existing (unmodified-by-this-commit) `project-key.ts:resolveProjectKey`, which returns `candidate.syncName` — a cross-device-stable identity — whenever the matched candidate has one, so each device's own local path resolves to the SAME internal storage key regardless of the two devices' absolute paths differing. That logic reads correct on inspection, but this commit ships the four new IPC entry points with zero regression coverage proving it end-to-end (e.g. two `projectExtensionsGet` calls against the same `store.ts` record with two different candidate lists, one per "device"). [PLAUSIBLE — logic traced and looks correct; this is a coverage gap, not a demonstrated bug.]

## Not covered

- Android (`./gradlew test -x bundleWebUi`) was not re-run by this review; the commit message claims it was run and green, but that claim wasn't independently re-verified here (budget).
- `project-extensions-view.test.ts` and `project-extensions-ipc-shell.test.ts`'s set-semantics assertions (first-enable materialization, pause-preserves-parts, uninstall-tombstone-clear) were spot-read against `view.ts` and look correct by inspection, but not exhaustively cross-checked line-by-line against every branch in `applyPluginChange`/`applyItemChange`.
- `remote-shim.ts`/`preload.ts`/`shared/types.ts` type shapes (`ProjectExtensionsGetResult` etc.) were read for the four channels but not diffed field-by-field against every renderer consumer (T4/T5 haven't landed yet per the plan, so there may be no consumer to check against).
- Did not independently re-run `./gradlew :app:compileDebugKotlin` for the `SessionService.kt` stub arms.

## Triage (implementing session)
- F1 accepted — fixed in 5f8073907: remote-server refuses import-skill; import-skill realpaths, refuses symlinks, reuses the sensitive-path denylist, refuses nesting; tests added.
- F2 accepted — fixed in 5f8073907: async scan twins used by the new handlers; no new blocking-call allowlist entries.
- F3 accepted — fixed in 5f8073907: cross-device synced-project get/set test.
