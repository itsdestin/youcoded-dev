<!-- first run of the 2026-09-04 feature flow's code reviewer (scripts/ui-review/code-reviewer.md), on a pre-flow branch: data for the 'measure after three features' roadmap item -->

> **Triage (implementing session, 2026-09-04): F1–F9 all accepted; the design note (hide the raw review path on the acceptance deck) not taken up. Fix: youcoded-dev 58045ca. Merged 2026-09-05.**

# Code review — docs/feature-flow-two-reviewers (fdf7839 + merge of docs/specialists-stage-two-probes)

Reviewer: context-free code reviewer per `scripts/ui-review/code-reviewer.md`. Budget ~20 min, used.
Repo: `/tmp/claude-1000/-home-destin-youcoded-dev/f5560bc6-25a6-4b2e-b8ea-9af097591c35/scratchpad/wt-feature-flow`, diffed against `origin/master`.

## Verify summary (no verify.sh for this repo — the two commands the brief named)

- `cd scripts/ui-review && YOUCODED_WORKSPACE=/home/destin/youcoded-dev python3 -m unittest tests.test_contract tests.test_spec tests.test_build tests.test_cli tests.test_words` → **Ran 72 tests … OK** (`tests.test_contract` alone: 26, incl. the 3 new `ReviewSourcedRowTests`).
- `YOUCODED_WORKSPACE=/home/destin/youcoded-dev node scripts/audit-anchors.mjs` → **anchors 398/398 ok · MAP paths 408/409 ok · eager ≈8838 tokens · MECHANICAL PASS: FAILURES** — the one failure is this branch's (F1 below). The `scripts/promo/*` "matching no rule" lines are another session's uncommitted work, ignored as instructed.
- Rule `.claude/rules/feature-flow.md`: all four `verify:` anchors resolve (`def check_contract` present in contract.py; contract-agent.md, test_contract.py, test_words.py exist). Named guards exist: `ContractCheckTests`, `AcceptanceTests`, `ReviewSourcedRowTests` (test_contract.py:110/263/296), `close-out-contract.test.sh` (`scripts/ui-review/tests/`), `test_words.py`. Body 598 words (budget 600).

## Findings, most severe first

- F1 — `docs/MAP.md:20` — the new MAP cell `scripts/ui-review/{tester-kit,ux-tester,code-reviewer,grader,contract-agent}.md` uses shell-brace shorthand, which `harvestMapPaths` (scripts/audit-anchors.mjs:194-204) takes literally, so the MAP check fails and `workspace-ci.yml` (which runs this audit on push/PR and on the nightly cron) goes red the moment this merges — confirmed by running the audit (`FAIL MAP paths missing: scripts/ui-review/{…}.md`; `origin/master`'s MAP has zero brace paths, so it is this branch). Write five backticked paths (CLAUDE.md uses the same shorthand but is not harvested).
- F2 — `scripts/ui-review/tester-kit.md:23-28` with `ux-tester.md:108-111` — the briefing offers a "real development copy" mode (`run-dev.sh` plus the CDP recipe), but the kit's only command `node scripts/ui-review/shot.mjs plan.json out-dir` spawns its own headless Chrome and `Page.navigate`s to `base` unless the env var `ATTACH_PORT` is set (shot.mjs:52-67, 122-131, 196-198); neither brief mentions `ATTACH_PORT`, so a tester handed an Electron dev address drives a plain Chrome tab of the renderer's Vite URL (no preload, no `window.claude`) and never the dev app — confirmed by reading the call chain; not executed against a dev instance. Either the briefing tells the session to hand over `ATTACH_PORT=<port>` and the kit says how to use it, or the real-app mode is removed from the template until it is drivable.
- F3 — `scripts/ui-review/tests/test_contract.py:292-296` — `ReviewSourcedRowTests` is defined AFTER `if __name__ == '__main__': unittest.main()`, so running the file as a script never sees the class: `python3 tests/test_contract.py` → **Ran 23**, `python3 -m unittest tests.test_contract` → **Ran 26** (both run, this session). CI and MAP use the module form so it passes today, but the rule's guard silently shrinks under the file-form the ContractCheck/Acceptance classes were written for. Move the class above the `__main__` block.
- F4 — `scripts/ui-review/deck/contract.py:39-43` — `review_finding` reports `no finding "F1" in <file>` when the finding line EXISTS but is not marked as the regex expects; probed with a review file: an unmarked raw line `- F1 — deck/contract.py:40 — …`, a capitalised `- F2 Accepted — …`, a bold id `- **F3** accepted`, and `- F5: accepted` all return `no finding "F<n>"`, while `- F4 accepted — …` returns ''. The finding exists in every case; the message names a cause that is false (workspace standard: never a guessed cause — `docs/error-message-standards.md`). Distinguish "id present but the line is not marked accepted/rejected/already handled" from "id absent", and match the id case-insensitively or strip `**`/`:`.
- F5 — `scripts/ui-review/deck/contract.py:125-126` — the `mechanical`-guard check inside the new `review:` branch is a verbatim copy of lines 149-150; and the module docstring (lines 3-6) still says every row's `source` "names a step … in a deck the spec's `sources` map points at", which the branch made false — confirmed by reading. Hoist the guard check below the `if/continue` so both branches share it, and add the review-source sentence to the docstring.
- F6 — `.claude/rules/feature-flow.md:7-12` — the rule now governs four new briefs (lines 34-35, 79, 83 name ux-tester.md, tester-kit.md, code-reviewer.md, grader.md) but `paths:` lists only `contract-agent.md`; editing any of the four loads `landing-page.md` (its `scripts/ui-review/**`, landing-page.md:7) and NOT this rule — confirmed with `rg` over `.claude/rules/*.md`. `last_verified: 2026-09-01` was not bumped for a 2026-09-04 rewrite, and the body sits at 598 of 600 words, so the next sentence anyone adds fails the audit's budget.
- F7 — `scripts/ui-review/tester-kit.md:50` — the kit tells the tester to "prefer `aria-label`, `title`, role or `data-testid` selectors", but `dump` (shot.mjs:175) prints `tag | aria-label | title | text | rect` — no `role`, no `data-testid` — so half the advice cannot be followed from the only discovery tool the tester has; and the kit never names the default theme, so the promised `out-dir/<theme>/<name>.png` is `out-dir/midnight/…` (shot.mjs:60) with no way for a context-free tester to know it — confirmed by reading. Either extend the dump columns or trim the advice to what the dump shows, and name the default theme.
- F8 — `.claude/skills/ui-mockup/SKILL.md:94` with `scripts/ui-review/contract-agent.md:33` and `ux-tester.md:115-117` — both UX-tester runs are told to write `docs/active/reviews/<date>-<feature>-ux-review.md`; the contract agent gets only the files and must use "the second run" alone, but nothing in the file name says which run a file is, and two runs on one date are the same path (the second overwrites the first, taking the run-1 triage counts §8e says to measure with it) — confirmed by reading the three files. Suffix the runs (`-ux-review-1.md` / `-2.md`) or have the briefing name the file explicitly. [PLAUSIBLE as to whether one-day runs happen in practice; the naming ambiguity is certain]
- F9 — `scripts/ui-review/code-reviewer.md:36`, `grader.md:197` — both briefs require `bash scripts/verify.sh <worktree>` and say what to paste, but verify.sh covers `youcoded/desktop` only (CLAUDE.md → Local build & test); on a workspace-only branch like this one the brief has no fallback and the dispatching session had to improvise one in the prompt. One clause ("if the branch touches no desktop code, run the tests the rules for the touched files name instead and say so") closes it.

Checked and found consistent (no finding): finding-line shape in code-reviewer.md/ux-tester.md (`- F1 accepted — …`, `- U3 accepted — …`) matches the regex at contract.py:40; source shape `review:../../reviews/<file>#F2` (contract-agent.md:234) matches `REVIEW_SOURCE_RE` (spec.py:35) and resolves from `docs/active/design/<date>-<feature>/` to `docs/active/reviews/`; `contract-check`/`acceptance` exist in review-cards.py:79 and `acceptance` reads `<feature>.contract.verdicts.json` as grader.md says (review-cards.py:126); every shot.mjs action the kit lists (`click`, `clickText`+`tag`, `rightClick`, `hover`, `type`, `key`, `wait`, `eval`, `dump`, `settle`, `expect`, `sameAsBaseline`, plan `base`/`boot`/`width`/`height`, third-arg theme list) exists (shot.mjs:21-40, 159-176); a `dump`-only shot with `sameAsBaseline` IS verified (dumps are split off `realFails`, shot.mjs:214-215); `ui-probe.mjs` flags `--wait`/`--eval`/`--shot`/`--size` exist; `run-workbench.sh` takes a target argument; `found` survives `ROW_KEYS` into page data (pinned by the new test); the human-row copy for review rows contains no banned word; design spec §8e (lines 167-171) says the same as the code.

## Not covered
- `page.js:235` was read, not rendered — `node --test scripts/ui-review/tests/deck-render.test.mjs` needs Chrome and was not run, so the "found in review" cell's appearance is unverified.
- The merged docs-only branch (`docs/specialists-stage-two-probes`) was not read beyond the spec lines that describe this code.
- grader.md:174's `npx vitest run <workspace-relative path>` from `youcoded/desktop` relies on vitest's substring filter matching a path that does not exist relative to that cwd; it likely works, not executed.
- No dead-code tool applies to the Python here; `knip` was not run (it covers the desktop TS only).

## Design note (one line)
The acceptance deck's From column shows Destin the raw `review:../../reviews/…#F2` string beside the new "found in review" tag — a file path in a deck built for a non-developer; the tag alone, with the path in a tooltip, would read better.
