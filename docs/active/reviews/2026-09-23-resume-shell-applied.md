# Resume Session — selected shell treatment, applied (2026-09-23)

**Decision:** `ui-guide-resume-shell-1#R-1` picked Matching at 11:53 UTC in Meadow Mist. This approves only the list-side outer title/divider/content edge. It does not approve global popup rules, transcript/row/filter restyling, or final UI-guide wording. Destin picked **Applied** in the post-build `ui-guide-resume-shell-acceptance-1#R-1` deck at 12:08 UTC on 2026-09-23; no other custom popup or final guide wording was approved by that answer.

**Implementation:** `ResumeBrowser.tsx` now imports a scoped stylesheet, uses 16px medium for its list-side `Resume Session` title, and replaces its inline 14% gradient with an 8%-tapered 1px rule inset 16px. The existing list scroll hook still decides if content is hidden at top or bottom; CSS uses its flags for a transparent 42px/4%-side mask instead of the generic painted pseudo. The existing two-column preview, list cards, actions and nested controls are unchanged. Dev-only Today styling explicitly restores the original 14px bold, 14% divider and painted scroll edge for honest comparison. Android WebView sources share this renderer; its web UI bundle was **not** rebuilt.

**Red → green:** two new production shell tests failed before the edit, then passed; the dev-only real-component fixture tests also pass. `scripts/verify.sh <isolated-app-worktree>` passed types, test types, related tests, knip, lint, design lint and ast-grep. Android Gradle `test -x bundleWebUi` succeeded with 103 tasks up to date; 102 test XML files contain 843 tests, zero failures/errors. The Workbench boot check passed all 16 routes. Both worktrees pass `git diff --check`.

**Browser evidence (isolated Workbench, not the built app):** at 1100×600 in Halftone, Applied computed `16px/500` with a real selected transcript and lower fade flag; Today computed `14px/700` with no mask. At the end of a long Meadow Mist list, the Applied list had `data-fade-top=true`, `data-fade-bottom=false`, and a content mask; at 390×600 Halftone it remained one-column with 16px title, lower fade and no horizontal page overflow. Direct screenshot captures were opened before the acceptance deck. Acceptance deck uses new post-build captures in both themes, self-previewed at 1440×900 and 1024×768. Phone screenshot is narrow desktop Chromium, **not** an Android device or WebView check.

**Independent review:** fresh scoped code reviewer found no actionable issues. Independent post-build UX tester passed the checked 1100×600 and 390×600 Meadow Mist/Halftone states with real fixture text, a selected preview and top/end list fade; the first selector wait timed out, then later probes confirmed the rendered list. Touch, high-density devices and broader interactions remain untested. Independent source-contract grader passed all selected requirements, including scoped title, divider, fade flags, unchanged transcript/controls, honest Today fixture and narrow/Android source path; it made no claim about global popup styling.
