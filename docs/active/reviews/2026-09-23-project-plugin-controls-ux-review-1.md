- U1 accepted and fixed in mockup — Reducing the candidate frame minimum from 560px to 320px restored all switches within the 390px viewport; rechecked screenshot `/tmp/project-controls-narrow-fixed.png` and DOM scrollWidth=390. At 390px I expected the project controls to fit the screen and remain usable / Instead the 494px-wide project panels are centered with their left edges at -52px, and the switches/buttons at x=389 are beyond the viewport; multiple projects and controls are cut off. This prevents choosing which projects can use the plugin on a phone-sized screen. — Project-first stacked, 390px — `/tmp/project-plugin-review/stacked/midnight/stacked-narrow-dump.png`
- U2 accepted as an open visual concern (not a verified interaction failure) — I expected “Personal · Skills & tools” to be a clearly distinguishable existing-project plugin list / It shows the Personal project and five named items, then “Research Kit” collapsed under “Show 3 items”; that wording is understandable, but makes the plugin's skills/server easy to miss until expanded. Expanding was attempted, but the scripted click did not complete verification before the review budget. — Project library — `/tmp/project-plugin-review/library/midnight/library-start-dump.png`
- U3 already handled for the sample master switch: a follow-up isolated browser probe opened the MCP confirmation (screenshot `/tmp/project-controls-confirm.png`); the scripted tester's switch dump remained incomplete — I expected toggles to make each plugin/skill/server available to its project / The initial screenshots show toggles, but the tool's clickable-control dump did not expose the switches as clickable controls, and the attempted toggle sequence did not verify. Toggle behavior and any confirmation step therefore remain unverified. — Project-first stacked — `/tmp/project-plugin-review/stacked/midnight/stacked-start-dump.png`

Completion verdict: Both candidate screens were opened and initially dumped. Reviewed the desktop starting views and captured a 390px view of the stacked candidate; that view is severely clipped. Existing-project list expansion and toggle/confirmation behavior were not fully verified within the 15-minute budget. The scripted actions cannot test physical touch input.