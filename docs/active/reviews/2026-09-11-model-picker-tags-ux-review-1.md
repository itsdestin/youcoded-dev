# Model picker tags — UX tester run 1 (2026-09-11)

Screenshots: `/tmp/claude-1000/-home-destin-youcoded-dev/e5f50546-d169-4ebf-a788-8bc2831e4b60/scratchpad/ux1/` (abbreviated below as `ux1/`). Theme is midnight unless the path says `light`. Not tested: touch input, 1.5x screen density.

Triage (implementing session, 2026-09-11): 11 accepted · 11 rejected · 4 already handled. Each line: verdict and reason, then the tester's finding as written.

- U1 rejected — by design: the status bar's Model dialog lists only models the conversation's own runtime can switch to (`ModelPickerPopup.tsx` `includeClaude={!isNative}`), so a Claude Code conversation shows Claude models. The unexplained "No models match." for a model of the other kind is a real, pre-existing gap, not the tags' — for the roadmap. Finding: Expected the full app's model list to show the models I have, so I could compare them / It shows exactly one model, "Sonnet · Claude Code". Searching "gpt" says "No models match.", yet "Manage models…" shows I am signed in to ChatGPT Plus. Nothing in the list says why models are missing or what to do. I could not do the task in the real app. — Full app, Model & Effort dialog — ux1/outApp2/midnight/app2-search-gpt.png, ux1/outAP/midnight/app-picker-open.png, ux1/outApp4/midnight/app4-manage-models.png
- U2 accepted — confirmed pre-existing bug the tag filters now depend on: in the dialog's inline layout there is no trigger, and `measure()` returned before positioning the filter popover. Fixed. Finding: Expected the filter button inside the full app's Model & Effort dialog to open the filters / The button lights up but no panel appears. It is a dead end. (In the stand-alone designs the same button does open a panel.) — Full app, Model & Effort dialog — ux1/outApp2/midnight/app2-filter-panel.png
- U3 accepted — filters make an empty favourites view far more likely; the message now says no models match the filters and offers Clear filters (design guide G-18). Finding: Expected a "no matches" message when my filters exclude everything / With "Great value" and "Intelligence 80+" both on, it says "No favorites yet. Search for a model, then star it to keep it here." I have 9 favourites, so this is wrong. Propose: "No models match these filters." with a "Clear filters" link. While the filter panel is open, even this message is hidden behind it. — Design B (same list in A/C) — ux1/outBE/midnight/B-empty-after-close.png, ux1/outMg/midnight/B-filter-two-chips.png
- U4 rejected — Destin's decision: no speed tag for online models (questions deck Q-9). Finding: Expected to compare speed across models, since speed is one of my three questions / Only the 3 models on this computer get a speed tag ("~Medium", "Fast", "Slow"). The 6 online models show no speed at all, so I cannot compare speed between Opus, GPT-5.6 Luna, DeepSeek etc. — All three designs — ux1/outA/midnight/A-open.png
- U5 rejected — Destin's decisions: a price that cannot be found is left off (S-1), an unscored priced model gets a price level (questions-2 F-3), exact dollars are on hover (F-1 note). Finding: Expected every model to show its cost the same way / Grok 4 has no price tag at all. Others mix two different scales: "Poor value" and "Great value" (cost compared to smarts) next to "Mid price" (cost alone). Actual dollars appear only when hovering. So I can't sort cheapest-to-priciest by eye. — All three designs — ux1/outA/midnight/A-open.png, ux1/outHA/midnight/A-hover-price.png
- U6 accepted — tag hints now open below the tag, so the row's own name stays visible. Finding: Expected the hover box to sit beside the tag / It pops up over the hovered row's own model name (and the row above). While reading "Intelligence 79 of 100…" I can no longer see which model it is about. Same for the price, "Not rated" and plan tooltips, and inside the full app. — A, B, C and full app — ux1/outHA/midnight/A-hover-intel.png, ux1/outHA/midnight/A-hover-notrated.png, ux1/outHB/midnight/B-hover-intel.png, ux1/outApp2/midnight/app2-hover-intel.png
- U7 rejected — pre-existing favourites behaviour, untouched by the tags — for the roadmap. Finding: Expected the star to switch off and stay put / Clicking a filled star makes the model vanish from the list at once, with no undo. The rows below slide up, so the next star (tooltip "Remove from favourites") lands right under my pointer, and a second click removes another model. — Design A — ux1/outCL/midnight/A-unfavourite-grok.png
- U8 rejected — the filter panel stays open so several filters can be combined; Esc or a click elsewhere closes it. Pre-existing behaviour. Finding: Expected the filter panel to close, or stay out of the way, after I pick a filter / It stays open and covers the results. After "Great value" the only match (DeepSeek) sits hidden behind the panel. — Design A — ux1/outCL/midnight/A-filter-greatvalue.png
- U9 already handled — Design C's short forms are stated on the review deck for Destin to weigh. Finding: Expected Design C's tags to say what they measure / They show bare numbers "94", "79", "70", "24" and single words "Poor", "Great", "Plan". I only learned "79" means intelligence by hovering it. — Design C — ux1/outC/midnight/C-open.png, ux1/outHC/midnight/C-hover-number.png
- U10 already handled — the deck's Design C risk names the cut-off names. Finding: Expected to read full model names in Design C / Names and providers are cut off: "Gemini 3.8 Flas…", "Qwen3.8 27B · …", "Claude Sonnet 4.6 · OpenRo…". Hovering the cut-off name showed nothing (the shot matched the page before hovering). — Design C — ux1/outC/midnight/C-open.png, ux1/outHC/midnight/_unverified/C-hover-truncated-name.png
- U11 accepted — the plan row's hint now reads "Scores for Claude Sonnet 5, the model it runs today". Finding: Expected one model to have one score / Searching "sonnet" shows "Sonnet · Claude Code" with Intelligence 84 and "Claude Sonnet 4.6 · OpenRouter" with Intelligence 79. The hover on the first says "Scores for Claude Sonnet 5", a name that appears nowhere in the list. Are these the same model? — Design A search, full app — ux1/outCL/midnight/A-search-sonnet.png, ux1/outApp2/midnight/app2-hover-intel.png
- U12 accepted, in part — reworded ("Intelligence 79 out of 100", "100 is today's best model, GPT-6 Astra", "Tests passed: coding 30% · …", "18th of 399 at following instructions"). "Smarts" rejected: intelligence is Destin's word. Source names kept: both licences require credit. Finding: Intelligence tooltip wording is hard to read / "Intelligence 79 of 100 (100 is GPT-6 Astra)": GPT-6 Astra is not in my list. "Coding 30% · Science 87% · Facts 36%": percent of what? "Following instructions: #18 of 399". "Epoch AI, LMArena": unknown names. Propose: "Smarts 79/100 · coding 30 · science 87 · facts 36 · 18th of 399 at following instructions · source: Epoch AI, LMArena, 11 Sep". — Tooltip, all designs — ux1/outHA/midnight/A-hover-intel.png
- U13 accepted, in part — now "$3 for what you send · $15 for what it writes / per million tokens (about 750,000 words)". A rough cost per chat rejected: questions deck Q-1 found a per-reply figure is wrong most of the time. Finding: Price tooltip jargon / "$3 in · $15 out, per million": per million what? What are "in" and "out"? A student would not know. Propose: "$3 per million words you send · $15 per million words it writes", or better, a rough cost per chat. — Tooltip — ux1/outHA/midnight/A-hover-price.png
- U14 accepted — "Costs more than most models this smart" (and "Cheaper than…" / "Priced like…"). Finding: "Poor value among models scoring 50 to 79" needs a second read / Propose: "Costs more than others this smart". — Tooltip — ux1/outHA/midnight/A-hover-price.png
- U15 accepted — tilde removed; an estimated speed has a dashed edge and its hint says it is an estimate. Finding: Speed wording / "~Medium": the tilde means nothing to most people. Propose "Medium speed". The tooltip "About 13 words a second on this computer / An estimate until its first reply" can be "About 13 words a second (estimate)". — Tag + tooltip — ux1/outHA/midnight/A-hover-speed.png
- U16 rejected, in part — "Not rated" is Destin's pick (questions deck Q-6). The hollow dot's faintness in Design B goes on the review deck as B's risk. Finding: "Not rated" does not say what is not rated / Propose "No smarts score". In Design B its hollow dot is almost invisible, especially in the light theme. — A, B — ux1/outB/midnight/B-open.png, ux1/outLight-dot/light/B-open.png
- U17 accepted — goes to Destin as a question on the review deck: "SUBSCRIPTION PLAN" and "FREE - LOCAL" are his own wording (questions deck Q-2, Q-3). Finding: Inconsistent capitals and punctuation on tags / "SUBSCRIPTION PLAN" and "FREE - LOCAL" are all caps with a hyphen, while "Poor value" and "Mid price" are normal case. Propose "In your plan" and "Free · on this computer". Design C already uses "Free" and "Plan". — A, B — ux1/outA/midnight/A-open.png
- U18 rejected — Destin chose filters, not sorting (questions deck Q-11). The "Source" / "Show" headings and "Runs on this device" are pre-existing. Finding: Expected "Filter and sort" to let me sort (e.g. cheapest first, smartest first) / The panel has filters only, no sort. There is no "Free" or price filter even though price is a main tag. "Runs on this device" repeats the "Local Models" chip. Headings "SOURCE" and "SHOW" are vague. — Filter panel — ux1/outHA/midnight/A-filter-open.png
- U19 already handled — filed: `docs/roadmap/chat-data.md` names the British "Unfavourite" in the model picker. Finding: Mixed spelling / The rows say "Unfavourite" and "Remove from favourites", but the empty message says "No favorites yet", and the skill cards elsewhere in the app say "Unfavorite". — List + empty state — ux1/outCL/midnight/A-unfavourite-grok.png, ux1/outBE/midnight/B-empty-after-close.png
- U20 accepted, in part — the current row's chips keep their colour on a panel backing. A colour key rejected: every tag's hint explains itself. Finding: Expected colours to mean the same thing on every row / In Design A the current (highlighted) row loses its colours: Opus's "Intelligence 94" turns plain grey while GPT's lower 85 is green, so the smartest model looks unremarkable. Design B keeps the green dot on that row. Also no key explains amber (69, 70, 79 are all amber). — Design A vs B — ux1/outA/midnight/A-open.png, ux1/outB/midnight/B-open.png
- U21 accepted — goes on the review deck as Design B's risk. Finding: Design B's good/bad signal is a dot about 5px wide, colour only / Hard to spot at a glance and unusable for colour-blind users. Design A's tinted boxes carry the same colours on a larger area. — Design B — ux1/outB/midnight/B-open.png
- U22 rejected — "favourites until you type" is a standing decision (Destin, 2026-07-30); an unlabelled favourites view is pre-existing — for the roadmap. Finding: Expected to know the list is only my favourites / Nothing says so. The only hint is "Search all models…". I found out when searching "sonnet" brought up an unstarred "Sonnet" I had never seen. Propose a small "Favourites" label above the rows. — Design A — ux1/outCL/midnight/A-search-sonnet.png
- U23 rejected — pre-existing "Manage models…" routing, not the tags — for the roadmap. Finding: Expected "Manage models…" to open next to the picker / In the full app it closes the Model & Effort dialog and opens Settings with "Assistant settings" on top, two layers deep. After closing it I have to reopen the picker from the status bar. (In the stand-alone design it only closed the list.) — Full app — ux1/outApp4/midnight/app4-manage-models.png, ux1/outMg/midnight/B-manage-models.png
- U24 rejected — pre-existing Model & Effort dialog copy, not the tags — for the roadmap. Finding: Dialog text around the list / "How hard Claude thinks before responding. Higher = slower but smarter." says Claude even when a non-Claude model is picked; propose "How hard the model thinks. Higher is slower but smarter." The hint "Max effort requires Opus or Fable": "Fable" is not in my list. "Same model, faster output streaming": "streaming" is jargon; propose "Same model, replies appear faster". — Full app, Model & Effort dialog — ux1/outAP/midnight/app-picker-open.png
- U25 already handled — this is the problem the tags exist to solve. Finding: Model names mean nothing to a non-developer / "Qwen3.6 35B A3B", "Qwen2.5 Coder 14B", "DeepSeek V3.2". The tags are the only thing that makes them choosable, which is why tags that are missing or cut off (U4, U5, U9, U10) hurt. — All designs — ux1/outA/midnight/A-open.png
- U26 rejected — pre-existing keyboard behaviour, not the tags. Finding: Expected arrow keys in the search box to move through the list / Pressing Down three times showed no visible change. This may be intended; mention only. — Design B — ux1/outMg/midnight/B-keyboard-arrows.png

**Summary.** I completed the task only in the stand-alone designs. In the full app the list held a single model and its filter button did nothing (U1, U2), so there was nothing to choose between. In the stand-alone list I would pick Opus: it is included in my plan (no per-use cost) and scores highest (94). But I could not tell how fast it is, because no online model shows speed (U4). **Design A (tinted boxes) helped me choose best.** Every tag says in words what it measures ("Intelligence 79", "Poor value"), the coloured boxes let me scan for green, and nothing is cut off. At phone width the tags wrap to a second line without clipping. Design B has the same words, but its tiny dots make good and bad hard to spot. Design C fits more rows, but bare numbers and cut-off names forced me to hover every row, and the hover box then covers the name (U6, U9, U10). **Most confusing moment:** opening the model list in the full app, seeing only "Sonnet", and getting "No models match." for "gpt", then finding under "Manage models…" that I am signed in to ChatGPT Plus. Stopped after covering all three designs, the full app list, filters, search, favourites, hover text, light theme and narrow width. Did not test touch.
