# T2 code review — project/plugin controls enforcement (commit b24c53706)

`bash scripts/verify.sh youcoded` (from the workspace root): **OK — all checks passed**
(types, types in tests/, related tests, knip, oxlint, design lint, ast-grep invariants).
Budget used: ~26 of ~30 tool calls.

- F1 — `desktop/src/main/harness/native-session-host.ts:2849-2850` — `resolveAvailabilityForCreate` awaits `this.resolveProjectAvailabilityInputs()` and `this.mcpManager?.listEnabled?.()` with no try/catch of its own, and the caller (`createInner`, called from `create()` at line 3394-3405 via a bare `try { await this.createInner(opts); } finally { ... }`, no `catch`) has none either — so a throw from either call propagates out of `create()` and fails session creation outright instead of "failing open" as the whole feature's stated contract requires ("resolution failure must never block a session from opening", `session-availability.ts:71-76`). `resolveSessionAvailability`'s own try/catch (session-availability.ts:89-139) only wraps code that runs *after* candidates/stores are already in hand — it never sees an error from the caller-supplied `resolveProjectAvailabilityInputs` closure or from `mcpManager.listEnabled()`, both of which run in `resolveAvailabilityForCreate`, outside that boundary. Concretely: `mcp-registry.ts`'s `list()` (called by `resolveAllEnabled()`, called by `listEnabled()`) reads `~/.youcoded/mcp.json` via `NativeHome.readJson`, which **deliberately rethrows any non-ENOENT fs error** (`native-home.ts:73-80`, "Any other I/O error ... must RETHROW") — e.g. a transient EACCES/EBUSY on that file during session create would currently just skip MCP for that session (old code path, `acquireMcp`'s own try/catch at native-session-host.ts:2804-2817 catches it), but after this diff the SAME error, reached one call earlier via `listEnabled()` in `resolveAvailabilityForCreate`, is uncaught and would abort the whole session-open with an unhandled rejection. Confirmed by reading the call chain and by `native-session-host.test.ts`'s new "project availability enforcement" describe block (line 903 on): every test there either omits `resolveProjectAvailabilityInputs` (returns null immediately, never reaching the unguarded await) or supplies a closure/`listEnabled` that always resolves successfully — none exercises either throwing, so this gap ships with a fully green suite. [confirmed, not just PLAUSIBLE]

- F2 — `desktop/src/main/harness/native-session-host.ts:2850` (via `resolveAvailabilityForCreate`) plus the pre-existing call inside `acquireMcp` → `McpManager.acquire` (`mcp-manager.ts:156`) — every native session **create()** now resolves the MCP registry (`registry.resolveAllEnabled()`) **twice**: once via the new `listEnabled()` read (for availability resolution) and once more inside `acquire()` itself. `resolveAllEnabled()` → `list()` → `NativeHome.readJson()` is a synchronous `fs.readFileSync` (`native-home.ts:69-73`) — a genuine main-process-blocking call per `.claude/rules/performance.md` rule 1 — and `resolveEntry()`/`decryptRefs()` does one secrets-store lookup per env/header ref for every enabled server. This diff doubles both the blocking file read and the per-secret decrypt work on the hot session-create path, for every native session opened, forever (not just projects with the feature engaged — B-1/no-project sessions pay it too, since `listEnabled()` is called unconditionally whenever `resolveProjectAvailabilityInputs` is wired). The commit's own comment at `mcp-manager.ts:333-346` calls this "the accepted cost of the design's given contract," but the actual mechanism (a synchronous fs read, doubled) is exactly the class rule 1 exists to catch, and `main-blocking-calls.test.ts`'s allowlist (line 121-123) only checks the async `*Async` NativeHome methods for sync fs — it does not, and structurally cannot, catch a doubled call to the already-allowed sync `readJson`. No test measures or bounds this per-session-create cost. [confirmed — read the full call chain in mcp-registry.ts, native-home.ts, mcp-manager.ts; PLAUSIBLE only on real-world severity/frequency of the resulting jank, since the file is small]

- F3 — `desktop/src/main/harness/native-session-host.ts` (`resolveAvailabilityForCreate`, ~line 2843-2860) — the "don't know" (`null`) fail-open branch (constructor not wired) and a genuine "resolved successfully to nothing" (B-1 empty sets) both flow through the exact same `availability` variable and are told apart only by `availability === null` vs. `availability.mcpServerIds.size === 0`. That distinction is correct in the code as written (confirmed by reading `create()`'s `...(availability ? {...} : {})` header-write guard and `acquireMcp(sessionId, availability?.mcpServerIds)`), but it depends entirely on every current and future caller of `resolveSessionAvailability`/`resolveAvailabilityForCreate` preserving `null` vs. `Set()` exactly — there is no type-level or test-level guard forcing a future edit to keep them distinct (e.g. a refactor that defaults `availability?.mcpServerIds` to `new Set()` "to simplify" would silently convert every fail-open resolution into a B-1 lockout). Not a live bug today, but the invariant is load-bearing and undocumented at the point of use (only documented in session-availability.ts's own header comment, three files away from where the distinction is actually consumed). [PLAUSIBLE — a latent-fragility finding, not a demonstrated defect]

## Not covered
- Did not read `desktop/tests/mcp-manager.test.ts`, `project-extensions-candidates.test.ts`, or `project-extensions-session-availability.test.ts` line-by-line for assertion strength beyond what F1 already used them for.
- Did not verify Android/remote-browser parity for the new IPC-adjacent wiring (spec says B-2 defers Android; remote browsers reuse the desktop host, not independently checked here).
- Did not chase the renderer/drawer side (out of scope for T2 per the plan — this commit is host/enforcement only).
- Did not exhaustively audit every `resolve.ts` default rule (T1 territory, already reviewed separately).

## Triage (implementing session)
- F1 accepted — fixed in T3 commit bf1eebb2d; two regression tests seen red without the fix.
- F2 accepted — fixed in bf1eebb2d (registry resolved once, threaded into acquire).
- F3 accepted — fixed in bf1eebb2d (typed unknown/resolved union).
