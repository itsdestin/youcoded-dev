# Marketplace file viewer + Tags & note — pre-review UX pass

The first independent pass made no captures and explicitly withdrew its initial Chrome-availability assumption. A second independent tester used `shot.mjs`: 16 verified captures across all six candidates (file: Today/two-lines/one-line; Tags: Today/divider/fade), Meadow Mist and Halftone Dimension at 1024×768, file top/end, and Tags at 700×380. It visually inspected Meadow Mist file comparisons and the proposed 1024px Tags view; it could not inspect its short-height captures due to image-delivery limits. I independently opened and inspected the Meadow Mist and Halftone Dimension **short-height fade screenshots**, and both Meadow Mist file-end screenshots via `Read`.

1. At the file's scroll end, Today's heading is sharply clipped beneath the full-width header. The two-line candidate softens the edge; its final line remains readable at the bottom. The one-line candidate loses plugin/type context, intentionally disclosed in the deck. These are comparison observations, not production approval.
2. At 700×380, the selected Tags fade is visible on the note field at the bottom; the tag chips and Manage tags control remain legible above it. This is the intended conditional trade-off: the note can be reached by scrolling; at 1024×768 the body fits and the mask's bottom fade is off. A direct browser measurement on the short viewport reported 95px scroll room and `data-fade-bottom=true` with a computed 42px mask, although that probe's 30s wait timed out before its later evaluation succeeded. The screenshot verification is the visual evidence.
3. The deck's live panels are shallow on a 1024×768 review page. Its Tags step now explicitly asks Destin to open each version in a New Window and shrink that window to compare a genuinely scrolling body. Risk copy is kept in the deck, not painted over the actual app. Preview self-verified two themes at 1440×900 and 1024×768 and its contact sheet was inspected.

No production Marketplace or Tags styling was changed. The Workbench fake backend serves a long **sample** Markdown file for the review-only id; all other plugin ids retain the prior mock response. Workbench boot check: all 16 routes mounted; desktop `verify.sh` passed.
