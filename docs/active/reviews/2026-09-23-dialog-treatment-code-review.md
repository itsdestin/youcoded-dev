# Shared Dialog treatment — code review

Fresh reviewer found no production correctness or scope issue in `ui/Dialog.tsx`, `ui/Dialog.css` and the dev-only Today reset. One test blind spot accepted: `dialog-shell.test.tsx` matched the 4% side taper anywhere in a mask declaration, without pinning it specifically to the second, horizontal gradient. The test now asserts separate bottom/top and horizontal gradients and `mask-composite: add`. An intentional mutation of the real CSS from 4%/96% to 5%/95% made the targeted guard fail; restoring the selected values returned all 12 Dialog tests to green. The reviewer's note that jsdom cannot verify browser compositing is handled by the isolated browser probes and Destin's interactive visual review, not by pretending this text guard can assert pixels.
