---
status: active
date: 2026-09-24
related: docs/active/plans/2026-09-21-ui-ux-consistency-audit.md
---

# UI element review — Destin's decisions

The new design guide is written from this file. Each row is one submitted answer; the
source is `<deck key>#<step id>`. Research behind each question: `inventory/`.

| Topic | Decision | Open follow-up | Source |
|---|---|---|---|
| Full-screen titles | Every full screen names itself in the window's top strip, **centered, small, with the screen's icon** (Pages style). Marketplace and Library move to it. | — | `ui-element-review-headers#H-1` |
| Popup and side-panel titles | Every popup and side panel uses the **shared popup title** (16px semibold, one line, tapered line under it, close button right). Settings, Resume, Session Files, Add a project and Games conform. | — | `ui-element-review-headers#H-2` |
| Heading ladder | Three levels. **Large:** 18px medium for big groups on full screens (Destin's picks, Favorites). **Popup/panel title:** the shared popup title (above). **Small section labels:** 12px medium grey, **normal case — no spaced-out capitals** (VOLUME → Volume). Marketplace's bigger caps and the 9/11px variants fold into it. | Decided below (underlined words for labels heading reading sections). | `ui-element-review-headers#H-3`; `ui-element-review-labels#L-1…L-4` (all "No capitals"; note on `#L-2`) |
| Counts beside a label or tab | **Faint smaller number after the word** (Files 17, All 42 — one style). **Bold number, grey word** (17 files) stays for stat lines. Brackets and number chips are not standard. | Number chip on the header Session Files button: decide with status/badges. | `ui-element-review-headers#H-4` |
| Way back to chat | **Filled button** on every full screen, reading **"Esc · Back to chat"** (key on the left). | — | `ui-element-review-headers#H-5` |
| Closing popups and side panels | Every popup and side panel closes with the **drawn ✕ button** (28px, soft hover square), top right. Games' × letter and Marketplace's "Esc · Close" text conform. | — | `ui-element-review-buttons#B-1` |
| Control shape follows the theme | **Buttons, tabs, filters and search boxes take their corner roundness from the theme's shape setting and move with the rest of the app's roundness.** Nothing keeps a hard-coded pill. On Projects and Marketplace, pills looked best. | — | `ui-element-review-button-shapes#P-1/P-2` (pill), `#P-3` ("should adjust dynamically with the other app elements for roundness"), `#P-5` ("should take theme radius settings"), `#P-4/P-6/P-7` (same) |
| "Pick one" controls | Folded into the shape rule above; the earlier B-3 deck repeated one style. | Revisit only if a distinct style is still wanted after the roundness level is chosen. | `ui-element-review-buttons#B-3` (other) |
| Secondary buttons | Decided below (outlined). | `ui-element-review-buttons#B-4` (other) |
| Default roundness level | Built-in themes use **Round**: buttons, tabs, filters and search boxes 14px; cards and popups 18–24px. Community themes set their own level through the theme's shape setting. | — | `ui-element-review-roundness#R-1` |
| Card look | **Raised everywhere**: panel color, thin border and a shadow, on Round corners, with a 12px gap between cards. The shadow sits **between today's heavy Marketplace shadow and the soft proposal** (Destin: "somewhere between today and raised" on Library, Marketplace, Project Files). | Decided below (medium). | `ui-element-review-cards#K-4…K-7` (raised); `#K-1…K-3` (between today and raised) |
| Menu rows | **Plain rows** in menus (right-click and similar): no box, no line; hover highlights. | — | `ui-element-review-lists#R-2` |
| List rows by job | **Settings-style lists** (each row opens or changes a setting): **boxed rows** — each row its own soft tinted box with a small gap. **Pick-one menus and switchers** (session list, right-click menu): **plain rows** — no box, no line, hover highlights. Both match today, so this becomes the rule. | — | `ui-element-review-lists-2#R-5` (boxed), `#R-6` (plain); `ui-element-review-lists#R-2` (plain) |
| Status labels | A **small tinted pill in the label's color, normal case** ("Installed"). A live status (a session Working / Inactive) also carries its **colored dot inside the pill** — today's session-list look. No capitals, no bare grey words. | — | `ui-element-review-status#S-1` ("today for the session switcher… tinted pill looks good for installed") |
| Warnings and notices | **Tinted box** with a matching border — today's Callout. One style for every passive info, warning and danger notice. | — | `ui-element-review-status#W-1` |
| Labels over reading sections | Small labels that head **sections of reading text** (About's Disclaimer, Privacy) get a **soft underline under the words** (edge colour, 4px offset). Labels over short settings groups stay plain. | — | `ui-element-review-final#F-1` |
| Secondary buttons | Every less-important action beside a main one (Cancel, Preview, Not now) is **outlined** — never bare text. | — | `ui-element-review-final#F-2` |
| Card shadow | **Medium**: `0 4px 20px rgb(0 0 0 / .16), 0 1px 3px rgb(0 0 0 / .08)` — between the soft proposal and today's deep Marketplace shadow. | — | `ui-element-review-final#F-3` |
| Button arrangement by width | Two actions **stack** (full width) in **narrow popups and at phone width**; they sit **side by side** in wide popups and chat messages. Side by side: the **filled button on the right, the outlined one directly left of it**. A destructive confirm follows the same rule (stacked when narrow; red on the right when side by side). | Narrow = popups up to 420px (prompt, panel) and phone width; wide = larger popups and chat messages — to confirm in the guide review. | `ui-element-review-button-placement#BP-1` ("stacked looks good in narrow popups, but much worse in wide popups or messages"), `#BP-4` |
| Single button | A lone main action in a popup is **full width**. | — | `ui-element-review-button-placement#BP-3` |
| Chat question cards | Follow the popup rule: **outlined Dismiss directly left of the filled Submit, at the right edge** (flipped from today). | — | `ui-element-review-button-placement#BP-5` |
| Permission row and tool cards | **Keep today's permission row** (Yes · Always Allow · No from the left). Tool cards and permission prompts will be **reworked as a separate effort**; the guide must say they are **not final** and are exempt until then. | — | `ui-element-review-button-placement#BP-6` |
| Stacked order and wide pairs | When stacked, the **filled button is on top**, the outlined one under it. In wide popups the pair **hugs its labels at the right edge** — equal halves were rejected as looking odd when that wide (*inferred from the note; confirm in the guide review*). | — | `ui-element-review-button-placement-2#BW-2` (filled-top), `#BW-1` ("the buttons start to look weird when they get that wide") |
| No controls bottom-left | **A filled button, switch or selection never sits at the bottom-left or left-aligned on its own** — "left-aligned buttons are broken buttons". Allowed only when balanced by a right-aligned control on the same line. Applies app-wide (a guide principle). | — | `ui-element-review-settings-anatomy#SA-1` (note) |
| Setting row layout | **By control size:** small controls (switches) sit **beside** the title and hint, on the right; wide controls (text boxes, sets of choices) go **below** the title and hint, full width. | — | `ui-element-review-settings-anatomy#SA-1` |
| Offering choices | Destin: "it depends". *Proposed for the guide review:* 2–4 short choices → a switch strip; many choices or long names → a dropdown; choices that each need an explanation → a boxed list. | Confirm in the guide review. Keep awake gets its own redesign (shows time remaining) — roadmap. | `ui-element-review-settings-anatomy#SA-2` |
| Groups of settings | **Flat by default:** a small label, the rows directly under it, a normal button — no box inside a box. Destin: may differ for other uses. | Name any exception when one comes up. | `ui-element-review-settings-anatomy#SA-3` |
| Settings spacing | **Tighter:** 16px between groups, 6px between rows. | — | `ui-element-review-settings-anatomy#SA-4` |
| Item card text order | Keep today's order and its **chip style** for the trust badge ("Likely safe") and the author; make the **footer details chips too** (e.g. "93% liked", "412 installs", "Plugin"). Name and status pill on the top line; description under the chips. | Revised by U-1 below. | `ui-element-review-card-anatomy#CA-1` ("keep the chip-ish styling … we can make the other footer elements chips too") |
| Conversation card date | ~~Date top right~~ — revised by U-2 below. | — | `ui-element-review-card-anatomy#CA-2` |
| Item card chips | **All chips in one row directly under the name** — trust, author, kind and the numbers together — then the description. Name and tinted status pill on the top line. | — | `ui-element-review-unreviewed#U-1` ("all the chips should be in the same row right below the name") |
| Conversation card corners | **Date bottom right** (end of the details line, right-aligned); **tag and note buttons top right**. Supersedes CA-2's date-top-right (*Destin: "maybe" — confirm in the guide review*). | — | `ui-element-review-unreviewed#U-2` |
| Terminal | Question was confusing (words only). **Assistant's call pending Destin:** the terminal stays exactly as it is and is named, like tool cards, as outside the guide. | Show in the guide review; revisit with pictures if he objects. | `ui-element-review-unreviewed#U-3` ("confused?") |
| Applied from earlier rules (no question) | File viewer and Git review top bars use the shared popup/panel title (H-2). Buddy windows follow the popup rules (their new-session form stacks its buttons). The guided tour gets a ✕ (B-1). Dragging a file in gets the same highlight as dragging a session tab (one style per job). | Shown in the fix batches' before/after. | `audit/unreviewed-surfaces.md` |
| Guide wording | Approved as written: principles 2, 4, 6, 7, 8; Full screens; Popups; Headings; Spacing (incl. the 4·8·12·16·24 scale and 420px narrow line); Buttons (incl. wide pairs hugging the right); Cards; Conversation cards; Status; Not covered (incl. the terminal); Checklist. **Rewrite as concrete instructions:** the taste paragraph ("word salad rather than meaningful content"), principles 1 and 5, Text and numbers. | — | `ui-guide-approval-2#G-1…G-15, P-1…P-8` |
| Quiet by default (wording) | Principle 3 drops "details nobody asked for **and options or information that would confuse someone who has never seen the app or used its features**". | — | `ui-guide-approval-2#P-3` |
| Full-screen reference | Name **Pages or the Projects view** as the example. | — | `ui-guide-approval-2#G-2` |
| Actions inside text boxes | A text box with its own action (password Set, search filter, send) keeps the action **inside the box, at the right**, like the message box and today's search boxes. | Show applied on Settings. | `ui-guide-approval-2#G-8` |
| Card chip row | The chip row **never wraps**: one line that **fades out at the end**. | — | `ui-guide-approval-2#G-9` |
| Real icons | Mockups used placeholder glyphs; the real app uses its existing icons. | — | `ui-guide-approval-2#G-10` |
| See it applied | Settings rules and list/menu rules to be judged **applied to the worst real screens** (e.g. Assistant settings, Remote Access, Backup & Sync; session drawer, project switcher), before/after. | Fix batch 1. | `ui-guide-approval-2#G-8`, `#G-11` |
| Pages | The guide can give Page builders **more elements and guidance**; improve the Pages kit accordingly. | Pages kit step. | `ui-guide-approval-2#G-13` |
| Batch 1 verdict | **All seven screens rejected.** Removing capitals and one box was too shallow: the pieces Destin objected to had no rule yet (see the rows below). Permissions page: **rebuild and simplify later** (follow-up). Specialists: the gap after the heading is too big. Remote Access: **remove Generate**. Guide wording W-1…W-5 approved. | — | `ui-fix-batch-1#B1-1…B1-7`, `#W-1…W-5` |
| Fold-out sections | "Sync log", "Advanced", "Show details": a **boxed row like a setting, arrow on the right**. One style everywhere. | — | `ui-element-review-settings-pieces#P-1` |
| Errors and warnings in a setting | The problem is the **same tinted box as every warning** (Backup & Sync's "too big to sync"), sitting **inside the setting's own container** as a sub-box. Its **actions (Try again, Show details, Resume, Cancel…) go inside the box**, at the right. **Text is normal grey/black, never red.** | Overrides Callout's old "no action slot" rule. | `ui-element-review-settings-pieces#P-2` (note) |
| Problem on a list item | Same as above: the shared warning/error box inside the item's card, with the item's options inside the box. No coloured strips, no custom styles. | — | `ui-element-review-settings-pieces#P-4` (note) |
| Follow-up actions | An action under a group ("Back up all now", "Sync now") is a **full-width outlined button**, never underlined text. | — | `ui-element-review-settings-pieces#P-3` |
| Button inside a text box | **Small filled button** (password "Set"). | — | `ui-element-review-settings-pieces#P-5` |
| Consent tick box | The whole line is a **tappable box, tick box on the left**, lighting up when ticked. | — | `ui-element-review-settings-pieces#P-6` |
| Status at the top of a popup | **Not decided.** All options "cluttered/messy", and the demo didn't look like the real app ("makes this impossible to review"). Lesson: **show pieces on the real screen, not a rebuilt demo.** | Re-ask on the real Remote Access screen. | `ui-element-review-settings-pieces#P-7` |
| Prior art first | Before building any new UI, read the guide **and** find 2–3 existing app surfaces doing the same job; use them as references and name them. Added to the guide's "How to use" and checklist and to `CLAUDE.md`. | — | Destin in chat, 2026-09-24 (a process rule, not a look) |
| Rules stay general | Guide rules must be **general and not over-constrain** future work: each recipe describes a kind of job, names screens only as examples, and leaves exact sizes to the shared pieces. Niche one-screen details were cut or generalised (popup line/fade measurements, the card chip list, conversation-card corners → "quick actions top right, date bottom right"). The 11px text floor is marked an open question. | — | Destin in chat, 2026-09-24 |
| Empty states | Two jobs. **First time / nothing exists yet** (Pages): a card with a short explanation and one full-width filled button. **Nothing picked yet** (Resume's preview pane): one quiet grey line. Not yet ruled: a plain empty list (Library "Nothing installed") — inferred as the first-time card; confirm in the fix batch. | Inferred for lists. | `ui-element-review-pieces#Q-1` (note) |
| Quick pickers | The project switcher's "Esc" becomes the **✕**, and it is restyled to match other popups (title + ✕). Skills drawer, filters and tag notes: Destin unsure — not ruled. | — | `ui-element-review-pieces#Q-2` (note) |
| Sign-in choices | **Keep five equal outlined choices**; add **provider logos** to each. The app already uses `ProviderIcon` (12 brand marks) in the picker, Resume cards, status bar and provider rows, so this breaks no rule. | — | `ui-element-review-pieces#Q-3` (pick equal + note) |
| Smallest text | **11px** replaces 10px everywhere (text-3xs). About 460 sites grow; tight spots (status bar, chips, cards) get checked in the fix batch. | — | `ui-element-review-pieces#T-1`, `#T-2` |
| Proposed rules S-1…S-9 | **Not answered.** S-1 "confused", S-2 "unsure, need to see", S-3 "confused, need to see", S-4 "where my visuals". S-5…S-9 were skipped (0–1 s, no note). Words-only rules do not work for looks — redo as pictures. | Re-ask with pictures. | `ui-element-review-pieces#S-1…S-9` |
| Stronger outline (question) | Destin: "can see a difference" — answered **Other** without a yes/no. **Not decided**; ask again as yes/no. | Re-ask. | `ui-fix-batch-2-outline#O-1` |
| Too many unit styles | Destin: Backup & Sync has "like 7 different card/button/unit styles on this single popup". Confirmed by count (8–10; Local models 12). **Rule:** a Settings popup uses four kinds of unit only — small label, row, notice box, button; a screen with more than four kinds needs simplifying. Wrapper cards, chip tabs, plain tables, icon-disc cards and boxed one-offs map onto them (`audit/unit-vocabulary.md`). Rebuild Backup & Sync, Local models, Cloud providers, Remote Access, Specialists to it, shown as before/after. | Pending Destin's verdict on batch 2 before rebuilding. | `ui-fix-batch-2-outline#O-1` (note) |
