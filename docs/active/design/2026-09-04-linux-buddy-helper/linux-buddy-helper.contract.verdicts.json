{
  "R2": {
    "verdict": "pass",
    "evidence": "The false sentence (\"and it is removed when you uninstall YouCoded\") is gone from every user-visible string in SettingsPanel.tsx — it survives only inside explanatory comments. Its replacement is the wording Destin approved, verbatim: \"It only ever touches the buddy's own window. You can remove it again any time from this menu.\" The first paragraph, which says the buddy cannot be turned on without the helper, is unchanged from review2#B-1. Verified by a string-level diff of BuddyButton against the merge base, not by reading the builder's summary — the B5 review's finding F5 is that one further string, the removal-failure line \"Couldn't remove the helper from your KDE settings.\", appears in no deck and no design section. It mirrors the already-approved \"Couldn't add the helper to your KDE settings.\" exactly, but it is new copy and Destin has not seen it. Flagged on this deck rather than passed silently."
  },
  "R3": {
    "verdict": "pass",
    "evidence": "The consent card's two actions are Add helper and Not now, unchanged from review2#B-1. \"Add helper\" shows \"Adding…\" while it runs and clears on failure — the stuck-on-Adding defect the design listed under pre-existing defects (§11) is fixed. Declining runs nothing at all: no install call, no show call."
  },
  "R5": {
    "verdict": "pass",
    "evidence": "The row reads \"Not yet supported on this desktop\" and is disabled, full-width, with no value column — the approved shape from review2#B-3, unchanged. What changed is WHEN it fires, and it had to: as first built it keyed off a single gate, and on KDE X11 that gate answers no — so users whose buddy works today would have found this row and a dead switch where they had a working feature. It now requires both that a helper is needed here AND that one cannot work here (design §4, three-state table), so it reaches GNOME, wlroots and Plasma 5 and nobody else. Three tests hold that line. Open question for Destin, unanswered since 2026-09-04: \"not yet\" implies support is coming and nothing is planned — real roadmap item, or reword?"
  },
  "R6": {
    "verdict": "pass",
    "evidence": "The pin-above-other-windows switch is gone, guarded by a source scan. A new user sees one row: the amendment note on this contract (decide-uninstall#D-1) says the Remove helper action appears only when the helper is installed, so the one-row state R6 was signed against is still what a new user gets. Not re-shot from the built branch: the acceptance images for this row and R2 still come from the review deck, because a fresh capture needs a workbench run — flagged rather than quietly reused."
  },
  "_note": {
    "verdict": "",
    "evidence": "Nine of the thirteen rows are checkedBy live-app and cannot be graded from source at all — R1, R4, R7, R8, R9, R10, R11, R12, R13. They are the human steps on this deck. Two things must happen on a real KDE Wayland desktop before they can be answered honestly: a dev instance for the interactive rows, and one packaged build, because the helper files are bundled inside app.asar and nothing yet proves they unpack correctly in a shipped build — if they do not, Add helper does nothing at all, forever, in the real app only."
  }
}
