# Error-handling inventory 1: Chat and Sessions

Worktree: `worktrees/sessions/error-states-unit-b/youcoded/desktop/src` (HEAD a89b2cd9). Read-only. Judged against today's code only; every backend claim was traced renderer to preload to main.
Paths below are relative to `desktop/src/renderer/` unless they start with `main/`.

**Totals:** 34 defect rows. Severity 1: 7 · Severity 2: 12 · Severity 3: 15. Intentional fallbacks are listed after the table.

## Ledger

| # | file:line | What the user sees | Trigger | Kind | Defects | Sev | Fix |
|---|---|---|---|---|---|---|---|
| 1 | components/ToolCard.tsx:596-609 (also :1080-1100 AskUserQuestion, :1105-1118 deny) to ToolCard.tsx:1444-1449 to state/chat-reducer.ts:2304-2330 | The approval card turns **Failed**, with an "Error" block reading "Permission request expired — socket closed before a response was sent". The expiry is broadcast to every connected device. No way to answer again. | `respondToPermission` returned false, OR the call **rejected**. On remote access a rejection is the 30s timeout (`remote-shim.ts:306` "Request permission:respond timed out"), and `remote-shim.ts:30` documents such a request as "Sent, no reply, timed out: it MAY have run". | uncertain-mutation | FALSE, RAW, DEADEND, HANDROLLED | 1 | Split the cases. `false` means the request is no longer open: say "already answered or closed" in plain words. A rejection means the outcome is unknown: keep the buttons, say the app couldn't confirm the answer arrived, offer Retry, and don't broadcast an expiry. |
| 2 | components/InputBar.tsx:646-664 (copy at :136) | Toast "The message could not be sent — no response from the session host." and the draft is refilled, which invites sending again. | `native.send` invoke rejected. On remote this is the 30s timeout, where the host may already have queued it (`remote-shim.ts:30` "it MAY have run"; `remote-shim.ts:34-37` "NOTHING LISTENS TO THIS YET ... the person is told nothing"). | uncertain-mutation | FALSE | 1 | On rejection, say the app couldn't confirm the send and to check the conversation before resending. Keep asserting "could not be sent" only for a `status:'failed'` ack. |
| 3 | components/InputBar.tsx:1058-1070 | Terminal-touch composer (`minimal`): press Enter and the box clears as if sent, but nothing reaches the session. | Remote connection down. `remote-shim.ts:173` classes `'session:input'` as `'user-action'`, and `send()` returns false for those while disconnected (`remote-shim.ts:206`). This Enter path skips the `canSend()` check that `handleSubmit` does (:810-813) and clears the text anyway (`setText('')`). | user-resolvable | SILENT | 1 | Route Enter through `handleSubmit` (or check `canSend()`/the `fire` return value) so the draft stays and the "Not connected" toast shows. |
| 4 | state/slash-command-dispatcher.ts:293-294 | Toast "Copied to clipboard" after `/copy` with a single block. | Clipboard write rejected (unfocused document, permission denied). The code is `void navigator.clipboard.writeText(payload.content).catch(() => {}); input.callbacks.onToast?.('Copied to clipboard');`, so the toast fires before, and regardless of, the result. On an http remote page `navigator.clipboard` is undefined and the call throws synchronously, so there is no toast and no message at all. | transient | FALSE | 1 | Await the write; toast success only when it resolves; on failure say "Couldn't copy" and show the picker/text so the user can select it. |
| 5 | hooks/useOpenFilepath.ts:56-63, :80, :146-150, rendered at components/SessionDrawer.tsx:666-670 | Files drawer opens on the note "Couldn’t open <name> — the file wasn’t found in this project." in a bordered div with no action. | Same sentence for: no project root (`if (!cwd) { failed(); return; }`), a `~/` path the renderer can't expand (`if (!args) { failed(); return; }`), **any thrown IPC call** (`} catch { failed(); }`), and an `appendVersion` whose result is never checked before the re-list misses. None of these is "not found". | app-side/unknown | FALSE, DEADEND, HANDROLLED | 1 | Keep "wasn't found" only for a completed search with no match. Show a general `<ErrorState>` (Retry + Report bug) for thrown calls and the no-root case, and a specific sentence for the `~/` case. |
| 6 | components/model/ModelPicker.tsx:360-369, :469, :757-771 | "You have not set up any model providers." with an **Add provider** button, in native-session pickers: the status-bar model dialog for a native session, the Resume options popover and Resume Browser native rows. | `providers.list()` / `providers.catalog()` rejected: `.catch(() => [])` turns a failed load into empty lists, so no native entries exist and `anyPickable` is false. Remote timeouts reach this too. Claude-including pickers are unaffected because Claude rows survive with status `unknown`. | transient-read | FALSE, EMPTY-AS-NONE | 1 | Track a load error separately from empty; render `<ErrorState message="Couldn't load your models." onRetry>` instead of the not-set-up block. |
| 7 | state/skill-context.tsx:59-87, rendered at components/CommandDrawer.tsx:361-367 | Command drawer: "No skills installed yet." + **Browse the Marketplace**. | Any one of `skills.list/getFavorites/getChips/getCuratedDefaults` rejects, so the whole `Promise.all` fails, the `.catch` only does `console.error('[SkillContext] Failed to load:', err)`, and `installed` stays `[]`. Runs once at mount with no retry. | transient-read | FALSE, EMPTY-AS-NONE, SILENT | 1 | Add a load-error state to SkillContext; the drawer shows `<ErrorState>` with Retry instead of the empty state. |
| 8 | components/SessionDrawer.tsx:263-269; components/ChatView.tsx:146-151 | Drawer list: "Nothing here yet. Files Claude writes or edits in this chat will appear here." | `artifacts.listSession` rejected; both catches swallow it (`.catch(() => { /* the drawer keeps whatever it already had */ })`, `.catch(() => {})`). Backend `main/ipc-handlers.ts:4539` also answers `{ ok: true, artifacts: [] }` for a **corrupted** sidecar, which renders the same sentence. | transient-read | EMPTY-AS-NONE, SILENT | 2 | Hold a list-error state and show `<ErrorState>` with Retry. Have the handler report `corrupted` distinctly. |
| 9 | components/SessionDrawer.tsx:422-449 | Rename field stays open with no error; further Enter/blur do nothing. | `artifacts.rename` rejects: the `await` at :434 is uncaught, and `renameActiveRef.current = false` already ran at :426, so the field is disarmed. | uncertain-mutation | SILENT, DEADEND | 2 | try/catch: re-arm the ref and show `plainMessage(e)` in the existing `<FieldError>`. |
| 10 | components/SessionDrawer.tsx:565-567 | Row "remove" does nothing. | `removeRecord` returns `{ok:false}` (`sidecar-missing` / `artifact-not-found` / `write-conflict`, main/artifacts/artifact-store.ts:489-499): `if (!res?.ok) return;`. A rejection is unhandled. | uncertain-mutation | SILENT | 2 | Toast specific copy per code; general copy + Report for a rejection. |
| 11 | components/SessionDrawer.tsx:474-476 | "Open with the default app" does nothing. | `shell.openPath` resolves with the OS's error sentence (`main/ipc-handlers.ts:1205-1210`: "resolves with '' on success or an error string on failure"). The return value is ignored. | user-resolvable | SILENT | 2 | If the returned string is non-empty, toast it. |
| 12 | components/ChatView.tsx:363-406 (sentinel :1045-1047); state/chat-reducer.ts:2437-2443 | Scrolling up just stops; older messages never appear; no text. | `requestTranscriptPage` rejected, returned nothing, or stayed `unresolved` after `UNRESOLVED_RETRY_LIMIT = 8`. `HISTORY_PAGE_FAILED` only clears `loading`; the sentinel is an invisible 1px div. Because the observer effect depends on `history.loading`, each failure re-attaches the observer, which fires again at once while the sentinel is in view, re-requesting with no backoff for non-`unresolved` failures. | transient-read | SILENT, DEADEND | 2 | Record a page error in the reducer; render an inline `<ErrorState message="Couldn't load earlier messages." onRetry>` at the sentinel and stop auto-retrying. |
| 13 | components/ChatView.tsx:1359 | "Model unloaded to save memory. [Reload model]": tapping Reload changes nothing. | `void window.claude.models.load(modelId)`: the handler (`main/ipc-handlers.ts:3410`) awaits `engineManager.loadModel`, which throws "The local engine is not installed yet." (`main/engine/engine-manager.ts:1298`) or from `ensureServable`/`ensureRunning`. The rejection is unhandled. | user-resolvable | SILENT | 2 | Catch; show `plainMessage(e)` in ModelLoadingBar with Retry. |
| 14 | components/ResumeBrowser.tsx:845-900; hooks/usePreviewMeta.ts:66-95 (SessionDrawer preview header) | A tag, Complete flag or note snaps back to its old value (a typed note vanishes) with no explanation. | `setFlag/setTag/setNote` `{ok:false}` or rejection. The backend returns user-safe sentences that are discarded, e.g. `main/ipc-handlers.ts` SET_NOTE: `'Could not save — conversation storage is not available on this device.'`, `'note exceeds 8000 characters'`. | uncertain-mutation | SILENT, VAGUE | 2 | Revert AND toast `res.error` when it is a sentence (general text + Report for a rejection). |
| 15 | hooks/useSessionMeta.ts:63, :91-125 (feeds SessionStrip.tsx:264-290 tag/note marks) | (a) After a transport rejection the new tag/note/flag stays on screen as if saved. (b) An ordinary refusal reverts silently. (c) A failed `getMeta` shows no tags or note. | (a) `.catch(() => {})` deliberately leaves the optimistic value, although a remote timeout's outcome is unknown. (b) `noteRefusal` only acts when `res.unsupported`. (c) `.catch(() => { setTags(new Set()); ... setNoteState('') ... })`. | uncertain-mutation | SILENT, EMPTY-AS-NONE | 2 | On rejection mark the value unconfirmed (or revert) and say so; toast `res.error` sentences on refusal; keep the last known meta on a failed read. |
| 16 | components/FirstRunView.tsx:270-273, :323-325 | Setup screen crashes to the root error screen with a raw JS message, or (getState rejects) stays on the bare "YouCoded" title forever. | Main's fallback `catch { return { currentStep: 'COMPLETE' }; }` (`main/main.ts:440-443`) has no `prerequisites`, and `state?.prerequisites.some(` then throws on render. `api.getState().then(...)` has no `.catch`. | app-side/unknown | RAW, DEADEND | 2 | `state?.prerequisites?.some(...)`; catch getState and show a general `<ErrorState>` (Retry + Report). |
| 17 | components/FirstRunView.tsx:331-356, :429-443 (backend main/main.ts:445-477) | Sign-in/Try Again buttons can silently do nothing. When `lastError` is set it shows in a hand-rolled `<p>`, sometimes carrying raw tool output. | Main wraps each handler in `catch (e) { log('ERROR', 'FirstRun', ...) }`: a throw that the manager did not convert to `lastError` reaches no screen. `lastError` strings embed raw detail, e.g. `first-run.ts:336` `Failed to install ${label}: ${result.error}`, `:446` `Login check failed: ${detail}`. | app-side/unknown | SILENT, RAW, HANDROLLED | 2 | Main sets `lastError` (general sentence) in those catches; render with `<ErrorState>` and map known raw errors to sentences. |
| 18 | components/InputBar.tsx:850-866 (paste), :839-848 (attach) | Pasting a screenshot does nothing; Attach does nothing. | Paste: `e.preventDefault()` then an uncaught `await saveClipboardImage()`, whose handler (`main/ipc-handlers.ts:1161-1175`) can throw on mkdir/writeFile. On remote, `saveClipboardImage: async () => null` (`remote-shim.ts:1487`) silently eats image pastes. Attach: `catch (err) { console.error(...) }`. | user-resolvable | SILENT | 2 | Catch and toast `plainMessage`; on remote say image paste isn't available there. |
| 19 | components/ResumeBrowser.tsx:488-491, :1798-1803 | "Couldn’t load your conversations: Error invoking remote method 'session:browse': Error: …" or "…: Request session:browse timed out". | `session.browse` rejected; `setLoadError(err?.message ? String(err.message) : '')`. `plainMessage()` is not used. | transient-read | RAW | 3 | `setLoadError(plainMessage(err, ''))`, and phrase the timeout. |
| 20 | components/SessionPreviewPane.tsx:77-89, :116-124, :140-147, :186, :205 | "Couldn't read this transcript: ENOENT: no such file or directory, stat '/home/…'" or the IPC wrapper text. | `chatsearch:read` returns raw fs messages (`main/chatsearch-index/transcript-reader.ts:173,177,182` `error: e?.message`). A rejected invoke is not caught in `load()`, so `e.message` carries the Electron wrapper. | transient-read | RAW | 3 | Map fs codes to sentences in transcript-reader; `plainMessage` for rejections. |
| 21 | components/ModelPickerPopup.tsx:264-272, :400 | "Couldn't switch models. The session may have ended — try again." (red `<p>`). | Accurate for `false` (`main/harness/native-session-host.ts:4328-4329` returns false only when the session is not live), but a **rejection** (e.g. `resolveContextAndProfile` throwing) gets the same guess and its message is dropped (`catch { ok = false; }`). | app-side/unknown | VAGUE, HANDROLLED | 3 | `<FieldError>`; on rejection show `plainMessage(e)`. |
| 22 | components/ModelPickerPopup.tsx:231-237, :287, :306 | Effort shows "auto" and Fast shows off as current when unknown; after a change, reopening shows the old value. | `modes.get` failure: `.catch(() => setLoaded(true))` with defaults. `modes.set` failure: `.catch(() => {})` even though `/fast` or `/effort` already reached Claude Code. | transient-read | EMPTY-AS-NONE, SILENT | 3 | Show "unknown" on a failed read; retry or flag a failed persist. |
| 23 | components/VoiceButton.tsx:188-191, :247-255 (hooks/useVoiceInput.ts:246, :370, :387) | Mic button labelled "Voice stopped — see why"; card titled "Voice stopped" with only OK. | `bridge.status()` at mount, `cancel()` or `recheck()` failed. The mic was never open, which is the same mismatch the component calls "a lie" for the download case (:182-187). | app-side/unknown | FALSE, DEADEND | 3 | A neutral "Voice isn't working right now" title for non-listening errors, plus Check again. |
| 24 | components/SessionContextPopup.tsx:198-199, :274-275 (components/session-context-facts.ts:64-66) | "This file couldn’t be read." / "This skill couldn’t be read." (muted `<p>`, no action). | Backend returns `{error:'not-live'}`, `'not-found'` or `'unreadable'` (`main/harness/native-session-host.ts:3037-3053`, `main/claude-code-context.ts:107-121`). The specific, safe reason is discarded (`'text' in res ? ready : error`). | transient-read | VAGUE, DEADEND, HANDROLLED | 3 | Map the three codes to sentences; `<ErrorState variant="inline">` with Retry. |
| 25 | components/tool-views/ToolBody.tsx:355-363 | Background-command **Stop** flips back to "Stop"; command keeps running; no reason. | `killShell` `{ok:false, reason:'not-live' / 'unknown-shell' / 'not-running'}` (`main/harness/native-session-host.ts:1599-1604`) or rejection (`console.error('KillShell failed:', err)`). | uncertain-mutation | SILENT | 3 | Put the reason (or `plainMessage`) in the StatusStrip detail. |
| 26 | components/SessionDrawer.tsx:55-68 | "Couldn’t rename the file. Try a different name." | Default branch used for `sidecar-missing` and `rename-failed` (`main/artifacts/artifact-store.ts:668, :701`, i.e. permission/lock errors), where a different name cannot help. | app-side/unknown | FALSE | 3 | Dedicated copy for `sidecar-missing`/`rename-failed` without the "different name" remedy. |
| 27 | components/SessionDrawer.tsx:838-844 via components/git/discard-guard.ts:23-26 (shown by GitReviewView, outside this scope) | Discard error shows raw git stderr, `path-outside-project`, `not-a-git-repository`, "git discard failed", or the IPC wrapper. | `main/git/git-service.ts:299-322` returns `errText(status)` and machine codes; the guard passes `e.message` and the fallback through. | app-side/unknown | RAW | 3 | Map codes to sentences; `plainMessage` for rejections. |
| 28 | components/SessionDrawer.tsx:463-465; components/ChatView.tsx:1160-1165 | Copy path / copy-picker choice: nothing happens (the picker closes anyway). | `navigator.clipboard.writeText(...).catch(() => {})`. | transient | SILENT | 3 | Toast on failure (ChatView needs an `onToast`). |
| 29 | components/AttentionBanner.tsx:52-54, :105-118 (wired at components/ChatView.tsx:1276-1283) | "Session ended unexpectedly." with no action; provider error text shown verbatim; the "Try again" link never appears. | `session-died` has no Resume action. The `'error'` line is `errorMessage` verbatim (the component's own comment at :40-43 cites "502…" stream failures). ChatView passes `onRetry` only for `'stalled'`. | app-side/unknown | DEADEND, RAW, HANDROLLED | 3 | Add Resume to session-died; phrase bare HTTP failures; wire or remove the error Retry. |
| 30 | components/StatusBar.tsx:974-976 | Clicking a usage chip does nothing. | `window.claude.shell.openExternal(...)` promise unhandled; the handler (`main/ipc-handlers.ts:1190-1194`) awaits `shell.openExternal`, which can throw. | user-resolvable | SILENT | 3 | Catch; toast "Couldn't open the browser" with the URL. |
| 31 | hooks/useTagRegistry.ts:27-30 (to components/SessionStrip.tsx:264-265) | Session tag marks disappear from the strip, as if untagged. | `tags.list` failure, then `.catch(() => setTags([]))`; the strip filters applied tag ids through the empty registry (`.filter((t): t is TagRecord => !!t)`). | transient-read | EMPTY-AS-NONE | 3 | Keep the last good registry on failure. |
| 32 | state/chat-reducer.ts:777-780 | A reconnecting remote client keeps its previous (possibly empty) chat with no message. | `deserializeChatState` throws, then `console.error(...); return state;`. | app-side/unknown | SILENT | 3 | Dispatch a visible "Couldn't load this conversation from the computer" state with Retry. |
| 33 | components/model/availability.ts:157-165 (`useAvailabilityData`) | Same failed-load-reads-as-nothing-set-up pattern as row 6 for its consumers (ModelProvidersPopup, Assistant settings default-model row). The consumer screens are outside this scope and were not judged. | `api.list().catch(() => [])`, `api.catalog().catch(() => [])`. | transient-read | EMPTY-AS-NONE | 2 | Return a `loadError` alongside `loaded`. |
| 34 | components/ErrorBoundary.tsx:26-40; components/RootErrorBoundary.tsx:22-40 | Crash panel shows the raw JS `error.message` with a reset button only. | Any render exception. | app-side/unknown | RAW, HANDROLLED | 3 | `<ErrorState>` general (title + explainer, Report bug + Diagnose); keep the raw message for the report payload only. |

## Intentional fallbacks (no defect)

- components/TerminalView.tsx:225 (WebGL falls back to DOM renderer), :338 (fit during teardown), :285 (workbench-only fixture).
- components/AttachmentChip.tsx:45-66, :105: a failed file-head or image read shows the type glyph.
- components/SessionDrawer.tsx:204-209: `defaults.get` failure keeps Sonnet / skip-permissions off for the resume popover.
- components/FirstRunView.tsx:310-316: ChatGPT model seed is best-effort; forms pick their own default.
- components/model/availability.ts:133: Claude status failure means `unknown`, deliberately counted available.
- components/ModelPickerPopup.tsx:204-217: catalog/sessionsList only highlight the current row; the picker fetches its own list.
- components/SessionStrip.tsx:594, :951, :1070, :1247, :1332, :2351: unsubscribe, pointer capture, live tear-off and drop-resolve fall back to in-strip behaviour.
- components/StatusBar.tsx:260-288 (date formatting), :655/:663 (localStorage); components/model/ModelPicker.tsx:118-122 (favourites localStorage).
- components/ToolCard.tsx:228, :333; components/tool-views/ToolBody.tsx:1032 (URL parse); components/tool-views/ToolBody.tsx:222 (copy button shows "Copied" only on success).
- components/ToolCard.tsx:917: plan-approval socket close; the decision travels as keystrokes.
- components/ResumeBrowser.tsx:925 (listener cleanup); :1016 resume failure (App toasts, browser stays open).
- components/SessionPreviewPane.tsx: the specific/general ErrorState split itself is correct (only RAW flagged, row 20).
- hooks/useResolvedConversations.ts:38: cards hide and ToolBody falls back to the plain Bash view.
- hooks/usePtyRawBytes.ts:46, hooks/useSessionTasks.ts:20-28, hooks/useMissingArtifacts.ts:106, hooks/useGitFileStatus.ts:53/60/91, hooks/useProjectWatch.ts:19/25, hooks/useActiveProject.ts:41, hooks/use-provider-type.ts:56-57, hooks/useAttentionClassifier.ts:162, hooks/useAttentionSummary.ts:48-49, hooks/use-esc-close.tsx:89/147, hooks/terminal-registry.ts:79, hooks/useZoomControls.ts:16.
- state/resume-lease-gate.ts:65: never blocks a resume, and warns on an `'error'` outcome.
- state/artifact-tool-use-tracker.ts:98/115/187: background tracking; the next refresh repairs it.
- state/native-slash-actions.ts:59/119/167: correct; the real detail or specific refusal copy is toasted.
- hooks/useVoiceInput.ts:178, :247, :278, :296, :312, :316, :326, :360, :376: correct cards (download failure has Retry; mic refusal has Check again).

## (a) In-scope files checked with no defects

CompactingCard.tsx, QueuedMessagesStrip.tsx, StopButton.tsx (desktop interrupt is a fire-and-forget `ipcRenderer.send` with no failure signal; remote disconnect behaviour of Stop not verified), TrustGate.tsx, MovedGate.tsx, SessionDropZone.tsx, TerminalRightSlot.tsx, TerminalToolbar.tsx, ResumeFilterPopover.tsx, SessionContextBanner.tsx, SkillInvocationCard.tsx, SpecialistReportCard.tsx, SpecialistsChip.tsx, PromptCard.tsx, UserMessage.tsx, AssistantTurnBubble.tsx, ThinkingIndicator.tsx, QuickChips.tsx, RemoteUnsupportedNotice.tsx ("catch" only in a comment), CommandDrawer.tsx (its defect originates in state/skill-context.tsx, row 7), first-run/describe-step.ts, tool-views/SessionRefActions.tsx, tool-views/ResumeOptionsPopover.tsx, tool-views/SubagentTimeline.tsx, tool-views/ChatsearchFindCard.tsx, tool-views/ChatsearchRefBlock.tsx, tool-views/ChatsearchShowCard.tsx, tool-views/ChatsearchMetaLine.tsx, tool-views/chatsearch-tags.tsx, model/availability.ts `useClaudeStatus`, state/native-slash-actions.ts, state/resume-lease-gate.ts, hooks/useAttentionClassifier.ts, hooks/useAttentionSummary.ts.

## (b) In-scope files containing `catch` that were NOT inspected (from `rg -c catch` over the scope)

All 16 component files in scope that contain `catch` were inspected (AttachmentChip, ChatView, FirstRunView, InputBar, RemoteUnsupportedNotice, SessionDrawer, SessionPreviewPane, TerminalView, model/availability, model/ModelPicker, ModelPickerPopup, StatusBar, ResumeBrowser, SessionStrip, ToolCard, tool-views/ToolBody).

hooks/ and state/ files containing `catch` not inspected, or only partly:
- state/theme-context.tsx (45 catches): themes screen, a different audit unit.
- state/marketplace-context.tsx (16), state/marketplace-api-client.ts (4), state/marketplace-stats-context.tsx (1), state/held-comments.ts (2, localStorage only glanced): marketplace.
- state/account-context.tsx (3): account/settings.
- hooks/usePerformanceConfig.ts (2): performance settings.
- hooks/usePresence.ts (1, glanced :52 only) and hooks/useMatchReport.ts (1): social/games.
- hooks/useSpecialists.ts (3): catches at :294/:436 read; they feed Settings specialist screens, which were not judged.
- state/skill-context.tsx (4): only the initial load (:40-87) judged; the favourite-seeding catches are not.
- state/drawer-width.ts (1): localStorage only, glanced.
- state/chat-types.ts (1) and state/attention-classifier.ts (1): the word appears only in comments.
