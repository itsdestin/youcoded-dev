# Inventory 5: how failures travel from the backend to the screen

Worktree: `worktrees/sessions/error-states-unit-b/youcoded` at a89b2cd9. Read-only. All paths below are relative to `youcoded/`.
Nothing was run. No tests, builds or app launches.

Severity: 1 = user told something false about their data or work · 2 = failure hidden or task dead-ended · 3 = raw or vague only.

## Verdicts on the six leads

| # | Lead | Verdict |
|---|---|---|
| 1 | handler-utils maps non-API errors to status 0, read as "offline" | **Status 0: confirmed** (`handler-utils.ts:22-23`). **"Read as offline": refuted.** No code in `main/` or `renderer/` reads `status === 0` at all. The only places status 0 appears create it: handler-utils:23, social-handlers:227, marketplace-api-handlers:252 and remote-server:2969. **"Includes JSON parse failures": refuted.** On a success response a parse failure becomes a silent success (row 2). On an error response a parse failure falls back to the raw body text (`marketplace-api-client.ts:266-278`). The real defect is that Node's text "fetch failed" reaches the screen (row 1). |
| 2 | session-manager reports an unexpected worker exit as code 0 | **Confirmed**, and it goes further (rows 4, 5, 6). |
| 3 | preload strips Electron's invoke wrapper only for some calls | **Confirmed: only 4 invokes are unwrapped**, all `chatgpt:*` (`preload.ts:1497-1500`). |
| 4a | A timed-out remote request can still run after reconnect | **Already fixed** (row 8). Remaining gap: the answer to "did it run?" is thrown away (row 9). |
| 4b | Unsupported channel over remote: hang or raw rejection? | **No hang, and honest for named features.** Remaining gap: unnamed channels (row 10). |
| 5 | Handlers returning a bare `{ok:false}` or a guessed cause | **Confirmed.** Includes a new severity-1 finding (row 12), plus rows 13 and 15. |
| 6 | Android: SessionService, ServiceBinder, MarketplaceApiClient | **All confirmed** (rows 16-19). The file-read lead hides a severity-1 case (row 17). The theme read-file variant has no visible effect (see "Reviewed, no row"). |

## Ledger

| # | file:line | What happens on screen | Platform | Defects | Sev | Already fixed? | Fix |
|---|---|---|---|---|---|---|---|
| 1 | `desktop/src/main/handler-utils.ts:22-23` → `renderer/state/account-context.tsx:246,262,280` → `components/AccountSection.tsx:637,711,785`, `components/HandlePrompt.tsx:131` | Offline while renaming, claiming a handle or deleting the account, the field error reads **"fetch failed"**. That is Node's fetch TypeError text. The real reason lives in `e.cause`, which `wrap` drops. `main/voice/voice-assets.ts:414` already notes: *"the message is the useless 'fetch failed'; the cause has it."* | desktop | RAW, LOST-DETAIL | 3 | No | In `wrap`, recognise a fetch TypeError and return a plain sentence plus `e.cause?.message`. |
| 2 | `desktop/src/renderer/state/marketplace-api-client.ts:282` `return (await res.json().catch(() => ({}))) as T;` · `app/.../marketplace/MarketplaceApiClient.kt:106` `try { JSONObject(raw) } catch (_: Exception) { JSONObject() }` | A 200 response whose body is not JSON (a captive portal or proxy HTML page) becomes `{ok:true, value:{}}`. Callers expecting `{comments:[…]}` and similar get an empty object and show nothing. The comment "tolerate 204" hides every other parse failure too. | all | SILENT, EMPTY-AS-NONE | 2 | No | Tolerate an empty body only on 204. Otherwise throw a status-0 "The server sent a response the app couldn't read." |
| 3 | `desktop/src/renderer/components/marketplace/FeedbackSection.tsx:198-201,215-217`, `LikeButton.tsx:158-163` | A vote, comment or like the server refused (for example a 429 rate cap or a 400 validation error) always reads "Couldn't save your vote. Try again." / "Couldn't post your comment. Try again." / "Couldn't like theme — try again". `r.message` carries the server's reason and is discarded, so "Try again" can repeat forever against a cap. | all | LOST-DETAIL | 3 | No | Show `r.message` when present. Keep the current sentence as the fallback. |
| 4 | `desktop/src/main/session-manager.ts:381-387` `worker.on('exit', () => { … this.emit('session-exit', id, 0);` | The pty worker dies without first sending its own exit message (native crash, OOM kill). The exit is reported as code 0, with nothing logged. Route: `ipc-handlers.ts:1980-1981` → `App.tsx:1157` → `chat-reducer.ts:937` `if (action.exitCode === 0 && !hadInFlight) return state;`. `App.tsx:1158-1166` then removes the session. **An idle session's tab silently disappears, with no "Session ended unexpectedly." banner.** When a turn was in flight the banner still shows. Normal exits are unaffected: `pty-worker.js:291` sends the real code first, and `session-manager.ts:370-378` deletes the session, so this handler returns early. | desktop, remote browser (`remote-server.ts:734-745` forwards the same 0) | SILENT, LOST-DETAIL (internally reports a false "clean exit") | 2 | No | `worker.on('exit', (code, signal) => …)`: emit the real code, or non-zero when a signal is set, and log both. |
| 5 | `desktop/src/main/pty-worker.js:289-291` `ptyProcess.onExit(({ exitCode }) => { … process.send({ type: 'exit', exitCode });` | node-pty reports `signal` separately. In `node_modules/node-pty/src/unix/pty.cc`, `exit_code` starts at 0 (:112) and `signal_code = WTERMSIG` (:228-229). So Claude Code killed by a signal (SIGKILL from the OOM killer, SIGSEGV) arrives as **exitCode 0**, and the result is the same silent tab removal as row 4. | desktop (macOS/Linux; Windows unverified) | SILENT, LOST-DETAIL | 2 | No | Forward `signal` and treat any signal as non-zero. |
| 6 | `app/.../runtime/ManagedSession.kt:317-320` (sessionFinished only sets `_isRunningFlow.value = false`); `SessionService.kt:997-1001` is the only `session:destroyed` broadcast in the Kotlin tree (checked with rg), and it has no `exitCode` | When Claude Code exits on its own on the phone, nothing tells React. `SESSION_PROCESS_EXITED` never fires, so there is **no "Session ended unexpectedly." banner** and the session stays listed as if alive. The explicit-destroy broadcast omits `exitCode`, and the shim defaults it to 0 (`remote-shim.ts:585-589`). | Android | SILENT | 2 | No | On `sessionFinished`, broadcast `session:destroyed` with the real exit code. |
| 7 | `desktop/src/main/preload.ts:456-465` (`unwrapInvokeError`), used only at `:1497-1500` | Every other rejected invoke reaches the renderer as "Error invoking remote method '<channel>': Error: …". The shared stripper `renderer/utils/ipc-error.ts` `plainMessage` is used by EngineCard, ProvidersSection, LocalModelsSection, ImportProjectModal, SyncPanel, RuntimeBinding, ReportDesign, ContributionDesign and AddProjectModal. `hooks/useVoiceInput.ts:57` has its own copy. **These sites print `err.message` / `String(err)` unstripped:** `marketplace/InstallingFooterStrip.tsx:55` ("Failed to install X: {err.message}", fed by `marketplace-context.tsx:256`), `marketplace/UpdateButton.tsx:48`, `marketplace-context.tsx:199`, `project-view/ContextEditorOverlay.tsx:87,115`, `buddy/BuddyNewSessionForm.tsx:134`, `ShareSheet.tsx:36,69`, `ThemeShareSheet.tsx:73`, `SettingsPanel.tsx:2242,2278,2894` (`String(err)` adds another "Error: "), `SyncSetupWizard.tsx:339,646,819,954`, `ResumeBrowser.tsx:491`. | desktop | RAW | 3 | No | Wrap every `ipcRenderer.invoke` in preload with `unwrapInvokeError` (one helper), or route these sites through `plainMessage`. |
| 8 | `desktop/src/renderer/remote-shim.ts:268-281` (flush drops items older than `REQUEST_TIMEOUT_MS`), `:299-307` (timeout marks `outcomeUnknown`, keeps the entry), `:243-257` (asks the host on reconnect); user actions never queued (`:161-202`, `:197`) | A timed-out request is no longer replayed after reconnect. | remote browser, Android WebView | none | — | **Yes.** Behavioural pin: `desktop/tests/remote-shim-send-queue.test.ts` "drops a queued message whose caller was already told it failed". Source-string pin only: `desktop/tests/remote-recovery.test.ts` "a timed-out request is kept and asked about, not dropped". The server ring is pinned by the same file's "the host answers unknown rather than guessing". | — |
| 9 | `desktop/src/renderer/remote-shim.ts:252` dispatches `OUTCOME_UNKNOWN_EVENT` (`:43`) | **No listener exists anywhere in `renderer/`** (rg finds only the definition and the dispatch). When the host answers "completed" for a request the user was told timed out, nobody says so. A late real reply is also dropped: `:535-537` settles an already-rejected promise, a no-op. The user keeps e.g. "Couldn't load your conversations: Request session:browse timed out" (`ResumeBrowser.tsx:1800`), or a create or install that did run. The "Request <channel> timed out" text shows a channel id. | remote browser, Android WebView | LOST-DETAIL, SILENT, RAW | 2 | No (the replay part is fixed; this part is not) | Add a listener that turns "completed" into a notice ("That finished after all") and refreshes. Word the timeout rejection in plain language. |
| 10 | `desktop/src/main/remote-server.ts:3000-3033` (default case) → `renderer/remote-shim.ts:470-477` (reject `remote-unsupported: <ch>`), `:424-445` (`noteUnsupported`), `renderer/remote-unsupported.ts` | **Named channels (16 prefixes): an immediate toast, "<Feature> isn't available via remote access yet."** No hang. **Unnamed channels** (any prefix outside FEATURE_NAMES) only `console.warn`. Channels called within `BOOT_QUIET_MS` of connecting are silent too. Callers written as `.catch(() => [])` then show an empty panel. The server's own sentence is discarded. Where `plainMessage` is used, an unnamed channel shows `"<channel-id> isn't available via remote access yet."` (`remoteFeatureName` falls back to the raw id). | remote browser, Android | EMPTY-AS-NONE, RAW (unnamed only) | 2 | Hang: yes, fixed by the default case | Use a generic non-committal notice for unnamed channels, and pass the server's `error` through. |
| 11 | `desktop/src/main/remote-server.ts:1005` `void this.handleMessage(client, raw)`. Awaited cases with no try/catch include `:1149-1156` session:destroy, `:1163-1172` session:browse, `:2791-2795` sync:add-backend | A handler that throws never responds. `main.ts:120-124` only logs the unhandled rejection. The browser waits **30 s**, then gets "Request <type> timed out". `noteCompleted` never runs, so the reconciliation in row 9 answers "unknown" even though the host knew the real error. | remote browser | HANG (30 s), LOST-DETAIL, RAW | 2 | No | One try/catch around the switch that responds `{ok:false, error: err.message}`. |
| 12 | `desktop/src/renderer/components/SyncPanel.tsx:953-959` `onComplete={async (instance) => { try { await claude.sync.addBackend(instance); await claude.sync.force(); await refreshStatus(); } catch {} }}` → `SyncSetupWizard.tsx:330` `await onComplete({ type: backendType, label, syncEnabled, config });` → `:337` `setStep('done');` → `:565` `title="Setup Complete"`, `:571` **"You're all set!"** | If saving the new backup destination fails, the wizard still says **"Setup Complete / You're all set!"**, and so does a failed first sync. The save can fail through `sync-state.ts:587` `atomicWrite` throwing, a 30 s remote timeout (row 11), or a phone crash (row 16). The wizard's own error branch (`:338-339`) is unreachable because SyncPanel swallows the rejection. **The user believes their backup is configured when nothing was saved.** | all | FALSE, SILENT | **1** | No | Delete the empty `catch {}`, or rethrow, so the wizard's error step shows the real message. Report a `force()` failure separately. |
| 13 | `desktop/src/main/ipc-handlers.ts:4218-4235` rename/remove-device: `catch { return { ok: false }; }` · `renderer/components/SyncPanel.tsx:800-802` `try { await fn(id, name); } catch {}` | **Rename:** the result is never read. The list reloads with the old name and no message is shown. **Remove:** "Could not remove this device." (`SyncPanel.tsx:822`). That wording is honest, but the handler discarded the real exception (EACCES, EBUSY…). | desktop, remote browser | SILENT (rename), LOST-DETAIL (both) | 2 | No | Return `error: e.message` from both handlers. Show a rename failure. |
| 14 | `desktop/src/main/sync-setup-handlers.ts:43-48` (`safeExec`: `code: e.code ?? 1`, `stderr: e.stderr \|\| e.message`) → `:245` `result.stderr \|\| 'Google sign-in failed or timed out'`, `:184` `result.stderr \|\| 'Installation failed'` | The guessed fallbacks are nearly unreachable, because `safeExec` fills an empty stderr with Node's `e.message`. So a timed-out Google sign-in shows **"Command failed: rclone config create gdrive drive"**. The `e.killed` / `e.signal` fields that would prove a timeout are discarded, and `:245` still carries an unverified "or timed out" when it does fire. `:195` "Installed but not found in PATH — try restarting the app" is backed by a check, so it is acceptable. | desktop | RAW, LOST-DETAIL, FALSE-risk (`:245` wording) | 3 | No | Return `timedOut` from `safeExec`. Say "didn't finish within N minutes" only when it is true; otherwise use the non-committal wording. |
| 15 | (renumbered, see rows 16-19) | | | | | | |
| 16 | `app/.../runtime/SessionService.kt:315-318` `bridgeServer.start { ws, msg -> serviceScope.launch { handleBridgeMessage(ws, msg) } }`; scope `:118` `CoroutineScope(Dispatchers.IO + SupervisorJob())`; no `CoroutineExceptionHandler` or `setDefaultUncaughtExceptionHandler` anywhere in `app/src/main/kotlin` (rg); `handleBridgeMessage` (`:929`) has no outer try | Any handler without its own try, such as `sync:add-backend` `:2107-2132` (`configFile.writeText`, `bootstrap!!`) or `sync:remove-backend` `:2135-2150`, lets the exception reach Android's default uncaught handler, which **kills the app**. The request never gets an answer. There are 26 `!!` in the file. `msg.payload.getString("type"/"label")` (`:2113-2114`) cannot crash from the UI, because the wizard always sends both. | Android | HANG, SILENT | 2 | No | Add a `CoroutineExceptionHandler`, or a try/catch in the launch that responds `{ok:false, error}` when `msg.id` is set. |
| 17 | `app/.../runtime/SessionService.kt:3622-3628` `val bytes = try { resolved.readBytes() } catch (_: java.io.IOException) { null }` → responds `ok:true … content: null, orphan: true` → `renderer/components/artifact-views/useArtifactContent.ts:97` phase `missing` → `ActiveArtifactView.tsx:501` **"This file is no longer on disk."** / `BinaryContent.tsx:21` **"This <noun> no longer exists on disk."** | The file exists (the `exists()` check at `:3591` passed) but could not be read, for example permission denied. **The phone tells the user their file was deleted.** Desktop rethrows every non-ENOENT error (`ipc-handlers.ts:4755-4757` `if (e.code !== 'ENOENT') throw e;`). The renderer's own comment (`ActiveArtifactView.tsx:504-505`) says "a permissions error is not a deleted file". | Android | FALSE, LOST-DETAIL | **1** | No | Respond `{ok:false, error: e.message}` so the Retry error state shows. |
| 18 | `app/.../runtime/ServiceBinder.kt:40-43` (`bindService` result ignored), `:48` `context.startForegroundService(intent)` outside the try, `:51` `serviceBound.filterNotNull().first()` with no timeout → `MainActivity.kt:379-383` | If binding fails, the phone shows `SetupScreen(Bootstrap.Progress.Installing("YouCoded session"))` **forever, with no Retry**. `LaunchedEffect(Unit)` does not relaunch. If `startForegroundService` throws, the exception escapes the LaunchedEffect and the app crashes. The Error screen (`MainActivity.kt:360-376`, which shows "Error: $error" and Retry) is reachable only through an exception inside `first()`, so in practice never. | Android | HANG, SILENT | 2 | No | Check the bind result, put `startForegroundService` in the try, and wrap `first()` in `withTimeout` so a failure reaches the Error state. |
| 19 | `app/.../marketplace/MarketplaceApiClient.kt:115-116` `ApiResult.Err(code, body.optString("message", "HTTP $code"))`, with `body` from `:106` (non-JSON becomes `{}`); `:111` `Pair(0, JSONObject().put("message", e.message ?: "network error"))` | The Worker's plain-text 400/409 errors ("that handle is taken") and JSON 500s (`{ok:false, error}`) reach the phone as **"HTTP 409" / "HTTP 500"** in the handle and name fields. Network failures show OkHttp's raw text. Desktop fixed exactly this (`marketplace-api-client.ts:260-279`). The phone's own `extractMessage` (`:150-155`) is used only by the three array endpoints. | Android | LOST-DETAIL, RAW | 3 | No (desktop only) | Make `request()` keep the raw body and build every `Err` with `extractMessage`. |

Row 15 is intentionally empty (a merged duplicate). Real rows: 18 (17 defects plus 1 already-fixed).

**Counts:** severity 1 = 2 (rows 12, 17) · severity 2 = 10 (rows 2, 4, 5, 6, 9, 10, 11, 13, 16, 18) · severity 3 = 5 (rows 1, 3, 7, 14, 19) · already fixed = 1 (row 8).

## Reviewed, no row
- `ipc-handlers.ts:4206` LEASE_FORCE `?? { ok: false }`, `:4937/:4949` artifact watch/unwatch, `:5012-5025` git watch/unwatch `{ok:false}`. These are internal subscriptions with no user-facing error line that I could find.
- `SessionService.kt:2280-2296` theme:read-file turns an exception into `""`. The renderer runs `JSON.parse("")` (`theme-context.tsx:292-293`, `:489-491`), which throws into the same catch as desktop's rejection. No visible difference.
- `ipc-handlers.ts:1810-1824` remote-access start failure: shows the real OS error. Good.
- `remote-server.ts:2969`: `{ok:false, status:0, message:'Game scores are unavailable on this host.'}` is accurate.
- `social-handlers.ts:227`: presenceSend `status:0 "not connected"` is accurate, and callers only check `ok`.
- `marketplace-api-handlers.ts:135-172`: account:user/refresh return null or cached on non-401 failure. This is deliberate: a transient failure keeps the signed-in UI rather than blanking it.
- `session-manager.ts:306-313` spawn error emits 1 and logs. Fine.

## Could not verify (and why)
1. **Which of the row-7 sites' handlers actually throw in practice.** I confirmed the wrapper is not stripped and that some handlers throw (theme read/write `ipc-handlers.ts:483-492`, anything unguarded in `skill-provider.install`). I did not trace every channel behind every site.
2. **Row 2 per-consumer screen effect.** I confirmed the client returns `{}`. I did not open every consumer to see whether it paints empty or crashes.
3. **Android crash behaviour (rows 16, 18).** The code shapes are confirmed. That an uncaught exception in a `SupervisorJob` scope or a `LaunchedEffect` kills the process is standard Kotlin/Android behaviour, but was not reproduced: no device, and launching was forbidden.
4. **Row 6: what the phone shows after a self-exit** (a spinner forever, or the stall classifier's banner later). I only confirmed that no exit event reaches React.
5. **Row 5 on Windows.** Only the Unix node-pty source was read; ConPTY exit and signal semantics were not checked.
6. **The wording `SetupScreen` renders for `Progress.Installing("YouCoded session")`** (row 18). Not opened.
7. **`File.exists()` on a permission-denied parent** (`SessionService.kt:3591`). If it returns false, that path also reports "no longer on disk". This is JVM/Android runtime behaviour and was not testable.
8. **Row 1 over the remote browser.** I did not confirm whether the remote host routes `account:*` through the same `wrap` handlers.
9. **No tests were run** (read-only instruction), so the pinning tests named in row 8 are cited from source, not from a green run.
