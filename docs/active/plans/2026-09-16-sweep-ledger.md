# Source-grep sweep ledger (Plan B)

Baseline 2026-09-16: Test Files 888 (886 passed, 1 skipped, 1 failed), Tests 11613 total (11569–11570 passed, 43–47 skipped across two runs), `it(`/`test(` sites 9958, EXPECTED_VIOLATIONS=11.

The one failing file is load-sensitive and outside this plan: `tests/chatgpt-request-diagnostics.test.ts` › "evicts inactive fingerprints within 8 MiB and drops an oversized observation" took 46.6s under the full run and passes alone in 5.3s. The budget to reconcile against is the **total** (11613), since skip counts wobble between runs.

Packaging (Destin, 2026-09-16): one PR per repo for the whole sweep, one "ready to merge?" at the end — Task 7's wording governs over the per-group line in Global Constraints.

| file | cases before | cases after | deleted case titles | rule / tool added |
|---|---|---|---|---|
| Group 5 (20 files — the table under `## Group 5` lists 20 rows though its heading says "19 files"; all 20 processed) + guard-scope-reader.test.ts | 279 | 280 | none | readSource (tests/helpers/guard-scope.ts) |
| `tests/installer-artifact-names.test.ts` | 4 | 4 | none | yaml parse |
| `tests/app-icons.test.ts` | 12 | 11 | `every theme without its own appIcon falls back to the bundled icon, not a synthesized one` | yaml parse |
| `tests/android-manifest-voice.test.ts` | 2 | 2 | none | DOMParser |
| `tests/landing-demo-fade.test.ts` | 2 | 0 (file deleted) | `clears and centers the demo exactly once on wide-screen activation`; `finishes the passive reveal before the demo top leaves the viewport` | — |
| `tests/remote-preview-gate.test.ts` | 1 | 0 (file deleted) | `keeps remote Info available in the preview, and gates its body on a rendered view` | — |
| `tests/remote-place-app-wiring.test.ts` | 14 | 0 (file deleted) | `the session list on mount selects only when allowed`; `a newly created session is focused only when allowed`; `switching to a remote host selects only when allowed`; `no ungated "first session" select is left behind`; `the hydrate handler chooses the place, marks it decided and reports what was kept`; `the destroyed handler uses the desktop's focus in remote mode`; `the first page asks only when the rule allows it`; `the place is remembered on every selection change once decided`; `every setSessionId call site is accounted for — a new one must be looked at`; `the hydrate decides the place, wakes waiting first pages, and selects the choice — remote only`; `the place on screen wins over storage, in the hydrate and when a restore ends without one`; `a restore starting resets the decision; one ending without a hydrate decides with what exists`; `the place is written only once decided, and first pages re-run when a hydrate lands`; `back on the device's own runtime the strip is cleared, and the welcome screen waits while catching up` | — (Task 4/global.md: deleted, not replaced — App still cannot be mounted in a unit test) |
| `tests/remote-tailnet-bind.test.ts` | 3 | 3 | none (all three rewritten as behaviour tests) | real `start()`/`stop()` against a real socket on 127.0.0.1, mocked `RemoteConfig.detectTailscale` |
| `tests/html-view-sealed.test.ts` | 1 | 0 (file deleted) | `the sandbox attribute never gains allow-same-origin` | ast-grep rule `iframe-sandbox-no-allow-same-origin` + `iframe-sandbox-requires-allow-scripts` (added in the review-of-batch-A fix pass, 2026-09-16 — the first rule only fires when a sandbox attribute is present, so it was silent if the attribute or its allow-scripts token was simply missing, which the retired test also required) |
| `tests/app-quit-routes.test.ts` | 2 | 0 (file deleted) | `registers teardown on all three quit routes`; `routes every quit through one idempotent teardown` | ast-grep rule `main-registers-all-quit-routes` (its preventDefault branch was unanchored — satisfied by any of main.ts's four unrelated preventDefault() calls — and re-anchored to the before-quit handler specifically in the review-of-batch-A fix pass, 2026-09-16) |
| `tests/app-resume-session-listener.test.ts` | 4 | 0 (file deleted) | `registers a listener for the event SessionRefActions.requestResume (and the preview header) dispatch`; `bails when the detail is missing any of the three required fields, before calling handleResumeSession`; `passes the popover's model/skip-permissions through, and keeps launchInNewWindow undefined`; `is declared AFTER handleResumeSession — a TDZ constraint, documented at the call site` | ast-grep rule `resume-listener-guards-detail-before-call` |
| `tests/appearance-buddy-broadcast.test.ts` | 1 | 0 (file deleted) | `include Buddy floater windows when a theme changes` | ast-grep rule `appearance-broadcast-reaches-all-windows` + `appearance-broadcast-handler-registered` (added in the review-of-batch-A fix pass, 2026-09-16 — the first rule matches the call site itself, so it was silent if the whole handler registration were deleted) |
| `tests/background-layer-authority.test.ts` | 3 | 0 (file deleted) | `no class string in the renderer sets two unprefixed backgrounds` (converted to ast-grep); `the pattern matches the string that actually shipped` (Class H non-vacuity self-test of the retired JS regex, nothing left to guard); `does not flag a state variant, which is the common legitimate case` (Class H, same reason) | ast-grep rule `no-two-bare-bg-utilities` + `.ts` twin `no-two-bare-bg-utilities-ts` (review-of-batch-A fix pass, 2026-09-16 — the retired test's `inScopeFiles(['.tsx','.ts'])` read both extensions, but the batch-A rule's `.ts` glob did nothing under `language: tsx`); the same pass added structural matching for a bare bg- pair split across a template-string substitution (the shipped bug's own shape, `` `bg-inset ${x} bg-accent/[0.07]` ``) |
| `tests/choice-group-authority.test.ts` | 2 | 1 (file kept, trimmed) | `no hand-rolled segmented control ships` (converted to ast-grep) | ast-grep rule `no-hand-rolled-segmented-control` (+ `.ts` twin `no-hand-rolled-segmented-control-ts`, added in the review-of-batch-A fix pass, 2026-09-16 — the batch-A rule's `.ts` glob did nothing under `language: tsx`); kept case `SegmentedTabs has real consumers` routed through `readSource` (title and `>=4` threshold restored to the original in the same fix pass — a brief rename/weakening to `>=1` was reverted, since the cross-file count being inexpressible in ast-grep didn't require weakening the threshold) |
| `tests/dialog-chrome-authority.test.ts` | 4 | 0 (file deleted) | `this guard can see what it claims to cover` (Class H non-vacuity, dropped — the ast-grep fixture pass is the non-vacuity check now); `no view inside a dialog paints its own header` (converted to ast-grep); `every exemption still exists and still applies` (dropped — exemption bookkeeping a whole-file `ignores:` no longer tracks precisely); `only Dialog and the wizard implement a back chevron` (converted to ast-grep) | ast-grep rules `no-hand-rolled-dialog-header` + `no-hand-rolled-dialog-back-chevron` |
| `tests/field-error-adoption.test.ts` | 3 | 1 (file kept, trimmed) | `no file hand-rolls the primitive markup` (converted to ast-grep); `every exemption still matches something` (dropped — exemption-relevance bookkeeping a whole-file `ignores:` no longer tracks) | ast-grep rule `no-hand-rolled-field-error`; kept case `an exemption covers exactly the occurrences it was granted for` (exact-count exemption an `ignores:` cannot express — global.md's "at most N matches" guidance) |
| `tests/item-list-authority.test.ts` | 4 | 1 (file kept, trimmed) | `no bare glyph survives as a list-item action or a container dismiss` (converted to ast-grep); `this guard can see what it claims to cover` (Class H non-vacuity, dropped — the ast-grep fixture pass is the non-vacuity check now); `no in-menu copy tells the user to press a control that was removed` (converted to ast-grep) | ast-grep rules `no-bare-glyph-item-action` + `no-bare-glyph-item-action-stale-copy`; kept case `every exemption still exists and still applies` (exact-count exemption an `ignores:` cannot express; briefly renamed to `an exemption covers exactly the occurrences it was granted for` and read via `readSource`, then RESTORED to its original title + `readStripped` — review of batch B, 2026-09-16, fix 3); both `no-bare-glyph-item-action*` rules' `files:` scoping fixed the same review (review of batch B, 2026-09-16, fix 2) — a bare `*` in this ast-grep's glob crosses `/`, so `ignores:` now lists every components/ subdirectory `inScopeFiles()` does not walk, plus `**/*.test.tsx` |
| `tests/main-hot-path-no-sync-fs.test.ts` | 3 | 0 (file deleted) | `lease client (createLeaseClient body)`; `transcript mirror (whole file)`; `git-transport gitDirSizeBytes` | ast-grep rules `no-sync-fs-in-main-hot-path` + `-git-transport`; `transcript mirror (whole file)` now guarded by the merged `no-sync-fs-whole-file` (review of batch B, 2026-09-16 — see that row below) |
| `tests/main-read-path-no-sync-fs.test.ts` | 4 | 0 (file deleted) | `marketplace-file-reader (whole file)`; `theme-preview-generator buildPreviewHTML`; `transcript-cwd (whole file)`; `session-browser listing path (readIndexMeta through the end of listPastSessions)` (FIX, review of batch B, 2026-09-16: the retired slice covered EVERYTHING between `readIndexMeta` and `loadHistory` — extractStoreMeta, withRetry, cleanTitle and top-level code too, not just the 8 functions this rule originally named; restated as a whole-file ban outside `loadHistory`) | `marketplace-file-reader (whole file)` and `transcript-cwd (whole file)` now guarded by the merged `no-sync-fs-whole-file` (review of batch B, 2026-09-16 — MERGED from `no-sync-fs-in-main-read-path` (the base rule) + `-transcript-cwd`, byte-for-byte-identical whole-file bans differing only by `files:`); `theme-preview-generator buildPreviewHTML` still `-theme-preview`; `session-browser listing path` still `-session-browser` |
| `tests/menu-row-reachability.test.ts` | 7 | 1 (file kept, trimmed) | `the shortcuts dialog keeps the scrolling body`; `the shortcuts dialog is the wider size`; `every shortcut in the list is rendered from the list itself` (SHORTCUTS.map half converted; the `>=13 rows` half dropped — Class H quantity check, nothing left to guard once the shape check exists); `a session name is one truncated line with the full name on hover`; `the session menu gives its list the remaining screen height`; `opens a model panel upward when its trigger lacks room below` | ast-grep rules `shortcuts-dialog-keeps-scroll-body` + `session-name-line-clamp` + `session-menu-height-capped` + `model-picker-opens-upward` (renamed from `-session-name`/`-session-menu-height`/`-model-picker-upward`, review of batch B, 2026-09-16 — none of the three guard the shortcuts dialog); kept case `the model filter portal remains inside its session-menu host` (exact count of 2, which no whole-file rule can pin) |
| `tests/overlay-layer-authority.test.ts` | 5 | 0 (file deleted) | `ProjectView renders at the shared screen layer, not above overlays`; `the three screens agree on their z-index`; `project-view has no hardcoded 9000-band z-index`; `no renderer component hand-rolls a scrim (bg-black/N in a className)`; `SettingsPanel has no hand-rolled overlay z-index` | ast-grep rules `no-hardcoded-z-index-or-scrim` + `-screen-layer` + `-9000-band` + `-9000-band-ts` + `-scrim` + `-scrim-ts` + `-settings-panel` |
| `tests/perf-marks-renderer.test.ts` | 3 | 0 (file deleted) | `index.tsx marks modules-evaluated and root render, in that order`; `App.tsx marks mount and sessions-listed`; `sessions-listed marks the MOUNT-time session.list, not the connection-mode reload` | ast-grep rules `perf-mark-index-order` + `perf-mark-sessions-listed-inside-session-list` (t5c batch — `follows`/`precedes` only compares true siblings under the same parent even with `stopBy: end`, verified empirically, so the order check matches each mark's ENCLOSING statement/call rather than the mark itself; the reload path's `window.claude.session.list().then(...)` is structurally identical to the mount path's, so the "not the reload" half is expressed as "not inside `onConnectionModeChange(...)`", not by the `.then()` shape alone). REVIEW FIX 2026-09-16: `perf-mark-index-order`'s first draft only checked order when `root-render` sat inside an `if_statement`, missing a bare top-level `root-render` placed before `modules-evaluated`, and required `kind: expression_statement` on the modules-evaluated side (a false-positive source if that mark were ever wrapped differently). Made shape-independent: finds whichever direct child of `program` contains each mark (any kind) and compares with `follows`. Proved by inverting the real order in `index.tsx` (moved the `modules-evaluated` mark below `root-render`, not a rename) -> FAIL naming `index.tsx` -> `git checkout` reverted -> OK. |
| `tests/permission-shell-context.test.ts` | 3 | 0 (file deleted) | `the pattern recognises the wiring it is looking for` (Class H non-vacuity self-test of the retired regex, dropped — the ast-grep fixture pass is the non-vacuity check now); `native-session-host passes the shell to decidePermission` (converted to ast-grep); `the label it compares against is the one detectShell actually returns` (converted to ast-grep) | ast-grep rules `decide-permission-passes-powershell-flag` + `-shell-label` twin (different file: bash.ts, not native-session-host.ts). REVIEW NOTE 2026-09-16: no rule change for a misplaced/misspelled flag — `decidePermission`'s typed options parameter already gets that from `tsc`; this rule only needs to guard the wiring's presence, which types can't do. |
| `tests/section-label-authority.test.ts` | 3 | 1 (file kept, trimmed) | `every section label spells the recipe the same way` (converted to ast-grep); `no <h4> stands in as a section label` (converted to ast-grep) | ast-grep rules `section-label-canonical-classes` + `-ts` twin (real `.ts` site found: `dev/workbench/compare/permission-modes.ts`'s `SECTION_LABEL`, whose own source comment was repointed to the rule) + `section-label-no-h4`; a `not: inside: template_string` guard was added after a scratch probe found `FilesTab.tsx`'s `` `${fullW} text-3xs uppercase tracking-wider text-fg-muted...}` `` matching the token check even though the retired test's `"([^"\n]*)"` regex never reads inside a template literal — global.md's "don't be stricter than the test"; kept case `the canonical recipe is actually in use` (Class H non-vacuity, >25 threshold preserved, routed through `readStripped`). REVIEW FIXES 2026-09-16: `section-label-canonical-classes` tightened to `inside: {kind: string, regex: '^"'}` (double-quoted only, one narrowness with the template-literal exclusion, not two — the `-ts` twin deliberately stays broader since its only real site, `permission-modes.ts:75`, is single-quoted and the retired test's literal scope never actually covered it); `rounded-full`/`border`/`bg-inset` exemptions now require the exact token, not a `\b` word boundary (`md:rounded-full`/`border-edge` no longer count); `section-label-no-h4` now requires the attribute be named `className`; kept-case comment reworded from "proof the walk works" to state the adoption intent; `PermissionsSection.tsx:129`'s stale `(section-label-authority)` comment repointed to the rule |
| `tests/tooltip-adoption.test.ts` | 4 | 0 (file deleted — REVIEW CORRECTION, 2026-09-16) | `never wraps a component whose title is a heading, not a hint` (converted to ast-grep); `only wraps an element, or a component that forwards to one` (converted to ast-grep); `never leaves a list key on the wrapped child` (converted to ast-grep); `this guard can see what it claims to cover` (Class H non-vacuity self-test, dropped on review — it only proved the walk reaches files, guarding nothing once the three shape checks it existed to backstop are gone) | ast-grep rules `tooltip-wraps-forwarding-element` + `tooltip-title-is-not-data` + `tooltip-key-on-wrapper-not-child` — all three check only the DIRECT wrapped child (not `stopBy: end`), since a forwarding component's own body may legitimately contain other components deeper down, and the retired test itself only inspected the first tag after `<Tooltip>`; `jsx_attribute`'s name has no `field:` label in this grammar (matched via `kind: property_identifier` instead); REVIEW FIX 2026-09-16: all three rules also now match the wrapped child hidden behind a JSX expression container (`{cond ? <X/> : <Y/>}` / `{cond && <X/>}`) — the retired test's textual scan found the first tag regardless of a `{...}` wrapper, so the first-draft direct-child-only rules were narrower; matched structurally via a `ternary_expression`'s `consequence`/`alternative` field or a `$A && $B` expression's `right` field, not a deep `stopBy: end` search (+6 fixture matches, `EXPECTED_VIOLATIONS` 70→76); repointed `docs/MAP.md`'s "Hover hints" row to the three rules (no test file left to name) |
| `tests/type-scale-authority.test.ts` | 2 | 0 (file deleted) | `every step of the sub-Tailwind scale is defined in globals.css` (converted to ast-grep — a whole-stylesheet presence check via `language: css`); `no component sets a font size with an arbitrary text-[Npx]` (converted to ast-grep, decimal + whole) | ast-grep rules `type-scale-named-tokens-declared` + `no-arbitrary-text-size` + `-ts` twin; no separate `template_string` branch on the px rule — the banned pixel digits are always a literal contiguous run, so `text-[${expr}px]` can never match either way, and a `template_string` branch double-counted the same violation when the whole token sat inside one fragment (scratch probe, 2026-09-16); repointed `docs/MAP.md`'s "React renderer / chrome" and "Desktop checks" rows; `.claude/rules/react-renderer.md:24`'s stale `- test: type-scale-authority.test.ts` anchor repointed to `- path: scripts/ast-grep/rules/no-arbitrary-text-size.yml` (review, 2026-09-16; `node scripts/audit-anchors.mjs --no-diff` confirmed clean). REVIEW FIX 2026-09-16: `type-scale-named-tokens-declared` now also accepts a decimal declared px value — deliberately wider than the retired test (whose declaration-check regex was whole-number only; decimal tolerance belonged only to the sibling arbitrary-usage case) |
| `tests/view-switch-stays-local.test.ts` | 2 | 0 (file deleted) | `App never broadcasts switch-view` (converted to ast-grep); `a switch-view action received over remote changes no view mode` (converted to ast-grep) | ast-grep rules `app-no-switch-view-broadcast` + `app-no-switch-view-receiver`; the broadcast rule's first draft used `pattern: "broadcastAction?.($ARG)"` (bare-call only) and went silent on the real call site's actual receiver chain `(window as any).claude?.remote?.broadcastAction?.(...)` — fixed to match the callee via `call_expression`'s `function` field regardless of chain depth (scratch probe, 2026-09-16); the receiver rule is `kind: program` `any:` of the ban and the negation of the one relay (`_SESSION_INITIALIZED`) App still applies. REVIEW FIX 2026-09-16: `app-no-switch-view-receiver`'s bare `pattern: "action.action === 'switch-view'"` only matched the literal two-level shape, missing a real receiver reading off a parsed variable (`msg.action.action === '...'`) which the retired test's substring regex would have caught either way — added `pattern: "$RECEIVER.action.action === 'switch-view'"` alongside it. Proved on real source: temporarily added the msg-prefixed shape to `App.tsx:1772` -> FAIL naming `App.tsx:1775` -> reverted -> OK. |
| `tests/accepted-history-privacy.test.ts` (t6a) | 1 | 1 (file kept, trimmed) | none (the file's single `it(...)` — "keeps the reasoning ciphertext in the private manifest and out of every reader the app has" — stays; only its Reader 6 sub-block, the source-scanning half, was removed) | ast-grep rule `private-continuation-dir-single-owner` (single-owner ban, all of src/main + src/renderer .ts except accepted-history-store.ts) + twin `-devtools` (the narrower `userData` ban, scoped to dev-tools.ts alone since `userData` is a common legitimate literal everywhere else); inverted dev-tools.ts (appended a line naming both strings), both rules fired naming dev-tools.ts:911, restored |
| `tests/animation-frame-budget.test.ts` (t6a) | 45 | 43 (13 moved to a new file, 30 kept in place, 2 deleted) | `bans NEW inline infinite animations without steps timing in components`; `bans Tailwind arbitrary-value infinite animations without steps()` (both converted to ast-grep) | ast-grep rule `no-unstepped-infinite-animation` (2 branches: inline `animation: '...infinite...'` style without steps(), and Tailwind `animate-[name_..._infinite]` without steps() and not in the character-motion/companion exception list); inverted SessionStrip.tsx's breathing-dot `steps(8)` to `ease`, fired naming SessionStrip.tsx, restored. CORRECTION: this file's classification row described only its first describe block (15 cases); its second describe block "motion vocabulary" (30 cases) is unrelated SessionStrip drag/hover mechanics that `.claude/rules/session-strip-motion.md` names as its guard by this file's path — left untouched, file NOT renamed. Of the first block's 15: 2 → the rule above, 13 (CSS + per-component TSX pins the rule can't express without one rule per component) moved via `readStripped`/`readSource` to new `tests/animation-css-budget.test.ts`. Both files run: 43 passed (30 + 13). |
| `tests/callout-authority.test.tsx` (t6a) | 8 | 6 | `this guard can see what it claims to cover` (Class H non-vacuity, deleted — the ast-grep fixture pass is the non-vacuity check now); `no in-scope file grows a new hand-rolled callout` (converted to ast-grep) | ast-grep rule `no-hand-rolled-callout-tint` (a callout-palette tint token + the literal word `border` in the same `className` string or template literal, scoped to `inScopeFiles()`'s exact walk — `ignores:` lists every components/ subdirectory it doesn't reach, mirroring the `no-bare-glyph-item-action` precedent, plus the 8 NOT_CALLOUTS files and Callout.tsx itself); scope probe (scratch copies) fired on an in-scope components/ path and stayed silent on a components/game/ path; inverted AboutPopup.tsx (appended a hand-rolled tint+border div), fired naming AboutPopup.tsx:249, restored. CORRECTION: total is 8 cases not 7. Kept: `every exemption still exists and still applies` (exact per-file count `ignores:` can't express — same precedent as field-error-adoption's kept case); the 5 render cases are untouched. |
| `tests/buddy-consent-gate.test.ts` (t6a) | 24 | 23 | `asks the gate, and re-reads the status rather than trusting launch` (converted to ast-grep) | ast-grep rule `buddy-show-consults-refusal-gate` (`kind: program`, two `any` branches: the `ipcMain.handle(IPC.BUDDY_SHOW, …)` handler present but missing `buddyShowRefusal(`/`refreshBuddyHelperStatus()`, or the handler gone entirely); inverted main.ts (removed both calls from the real handler body), fired naming main.ts, restored. Row was accurate as classified (6/20). Kept as text reads (now via `readSource`): the other 5 "main.ts wires the gate" cases (handler brevity, refusal-before-show ORDERING, the drag-path cache-only dependency, the conditional work-area-resolver construction, three debounced display-event wires) — each a distinct, narrow region; the gate/cache/consent behaviour cases (17) are untouched. |
| `tests/buddy-caption-channel.test.ts` (t6a) | 22 | 20 | `no window-moving API is called outside place()`; `setTitle is called in exactly one place, inside place()` (both converted to ast-grep) | ast-grep rule `buddy-window-move-only-in-place` (a `setPosition`/`setBounds`/`setContentBounds`/`setTitle` call outside the `place()` method); inverted buddy-window-manager.ts (added a `win.setPosition(x, y)` call inside `create()`), fired naming the file, restored. CORRECTION: row said 3/22 source-reading, actually 4 — the other 2 ("nine write sites and three creation sites is still there", "the three windows are built in exactly one place, inside create()") are exact call-count assertions, kept as text reads (now via `readSource`) since ast-grep can't detect a legitimate call site being silently deleted. |
| `tests/auto-title-contract.test.ts` (t6a) | 8 | 1 (file kept, trimmed) | `an untitled session is still told to write a title`; `a titled session is shown its title and told doing nothing is allowed`; `the reminder still fires on a real interval where nothing schedules it`; `Off and Basic stop the request, rather than discarding a title already paid for`; `AI naming asks only when the app scheduled a review, and asks once`; `the ask files are pruned like the topic and marker files`; `the CLAUDE.md prose deployed by both platforms agrees with the hook` | — (no ast-grep replacement: this row's disposition names none, and bash is not one of this workspace's ast-grep languages, so there is no structural conversion target for a literal wording/timer/ordering pin on a shell script). Kept: `desktop and Android build the identical message (bundled-hook parity)`, now routed through `readSource` for CRLF safety; the `installHooks`/`bootstrapKt` CLAUDE.md-prose-parity check was deleted along with its case since it was only asserted inside the deleted case's file list. |
| `tests/attention-banner.test.tsx` (t6a) | 14 | 13 | `ChatView passes the session provider to the banner` (converted to ast-grep) | ast-grep rule `chatview-passes-provider-to-attentionbanner` (a `jsx_self_closing_element` named AttentionBanner without a `provider={provider}` attribute); inverted ChatView.tsx (removed the `provider={provider}` line), fired naming ChatView.tsx:1337, restored. Row was accurate as classified — no correction needed. Removed now-unused imports (`join`, `RENDERER`, `readStripped`). |
| `tests/arcade-authority.test.ts` (t6a) | 21 (17 `it(` sites; the assistant-finishing rule was 1 site × 5 loop expansions) | 14 | `no game file references playSound — a game must not make its own sound when a turn ends`; `no game file references useAnyAttentionNeeded — a game must not react to the attention summary`; `no game file references onAttentionSummary — a game must not subscribe to the attention summary`; `no game file references isThinking — a game must not watch whether the assistant is working`; `no game file references sessionAttention — a game must not read session attention` (all 5, converted to ast-grep); `the palette pattern actually matches a known positive` (Class H non-vacuity, dropped — its one real-product half, "the allowed set has green-400", is already covered by `sanctionedStatusColours()`'s own internal `>=4` non-emptiness check); `the game-word list actually matches a known positive` (Class H non-vacuity, dropped) | ast-grep rules `arcade-no-forbidden-attention-apis` + `-ts` twin (game dir mixes .ts/.tsx); inverted GamePanel.tsx (added a `playSound(...)` call) and game-registry.ts (added an `isThinking` reference), both fired naming file:line, restored. CORRECTION: of the 13 source-reading cases, 10 (state.play single-owner, shared-state vocabulary, challenge wiring, ChatView key yield, game key-claim, palette ban, main-process vocabulary ban, arcade-handler no-ranking, resize-effect ordering ×2) are kept via `readStripped`, unchanged — each is a narrow, low-reuse, one-or-two-file text pin; converting all of them would mean 6+ more bespoke rules for one row of eight assigned this batch. `docs/MAP.md`'s games-arcade row repointed from "(no rule — guarded by tests)" to the new rule. |
| `tests/accepted-history-privacy.test.ts` (t6a review fix I1) | 1 | 1 | none | `private-continuation-dir-single-owner` widened to all `src/**/*.ts` and every token kind (comments, identifiers, strings); new `-tsx` twin; new `-present` owner branch; `-devtools` now matches any token spelling `userData`. Inverted: comment in src/shared/announcement.ts, comment in AboutPopup.tsx, comment naming userDataRoot in dev-tools.ts, owner string split in accepted-history-store.ts — all four fired, restored |
| `tests/animation-frame-budget.test.ts` (t6a review fix I2) | — | — | none (rule-only fix) | `no-unstepped-infinite-animation` rebuilt from `scripts/ast-grep/generators/no-unstepped-infinite-animation.py`: every `animate-[…]` bracket judged separately (exempt/stepped neighbour no longer hides an unstepped one), `${}`-split brackets matched on the template, comments counted, inline branch narrowed to the old single-quoted `animation: '…'` shape, `.test.tsx` no longer ignored. Inverted: StatusBar.tsx:1679 given an exempt bracket beside an unstepped one — fired, restored |
| `tests/animation-css-budget.test.ts` (t6a review fix I5) | 13 | 7 | deleted as already covered by `no-unstepped-infinite-animation`: `quantizes the SessionStrip breathing dot (inline animation)`; `quantizes the HeaderBar challenge pulse (inline animation)`. Converted: `BrailleSpinner is interval-driven, not a rAF chain`; `ThemeEffects draws from an interval, not a rAF chain`; `MascotRig runs rAF only for the drag chain, idle from an interval`; `transitions explicit properties on session pills, never \`all\``. Trimmed to their CSS half (TSX half converted): `defines .stepped-hover and applies it to the dense hover surfaces`; `pauses mascot motion when the document is hidden` | ast-grep `braille-spinner-interval-driven`, `theme-effects-draws-from-interval`, `mascot-rig-raf-only-for-drag`, `mascot-rig-pauses-when-hidden`, `setting-row-base-is-stepped-hover`, `session-strip-menu-rows-stepped-hover`, `session-strip-no-transition-all`. Every branch mutation-probed on scratch copies; real inversions (one run): BrailleSpinner setInterval→setTimeout, ThemeEffects setInterval(draw→paint), MascotRig first rAF(rafTick→tick) and last=performance.now()→Date.now(), SettingRow lost stepped-hover, SessionStrip menu rows ease + one `'all 150ms'` — all seven fired, restored. Remaining 7 cases read stylesheets only (block-comment-deleting reader, as originally) |
| `tests/animation-frame-budget.test.ts` (t6a review fix I5) | 30 | 13 | converted: `arms the label window from the stylesheet, not a number tuned to an older vocabulary`; `animates the incoming conversation, never the outgoing one`; `draws the pill in hand as a TWIN of the real pill, never a ghost or an insertion line`; `keeps the pill in hand under the cursor, with no transition on its position`; `keeps the hover peek open through pointer-down and the drag`; `keys drag state by session id, never by index`; `keeps the open name in hand while dragging`; `packs the row into the room it really has, so a drag is judged against the widths that get drawn`; `tears a pill off only above or below the window — sideways stays in the row, as in Chrome`; `leaves no hand-written curve in SessionStrip`; `holds the drag visuals as STATE until the drop is committed — never the isDragging ref`; `never glides a dot at the drop — the flow owns its two images; yields judge the visible edge`; `opens a hover peek only after the hand rests on the dot — never in passing`; `attaches pill hover handlers unconditionally`; `routes the session pill label through pillLabelStyle`; `steps the level ring — the motion that actually ships`; `keeps the ring on whole-pixel steps, so it repaints only when the level moves a step`. Trimmed to their stylesheet half: `never draws a dot touching the pill in hand — veiled by proximity, moved while hidden`; `has no review scaffolds left — every round was picked on 2026-09-02`; `puts nothing but the dot and the name on a pill — the runtime lives in the menu`; `lays the name out once and fades what does not fit` (keeps the cross-file LABEL_TAIL_PX read) | ast-grep: `session-strip-label-window-from-stylesheet`, `session-strip-dot-flows-never-touches`, `session-strip-no-select-on-scaffold`, `index-no-arrival-scaffold`, `chatview-arrival-only-on-incoming`, `session-strip-twin-is-the-real-pill`, `session-strip-twin-follows-cursor`, `session-strip-peek-survives-press-and-drag`, `session-strip-drag-state-keyed-by-id`, `session-strip-held-pill-keeps-name`, `session-strip-packs-real-room`, `session-strip-tear-off-vertical-only`, `session-strip-pill-is-dot-and-name`, `session-strip-name-laid-out-once`, `session-strip-no-hand-written-curve`, `session-strip-drag-visuals-are-state`, `session-strip-drop-never-glides-dot`, `session-strip-peek-waits-for-dwell`, `session-strip-hover-handlers-unconditional`, `session-strip-label-via-pill-label-style`, `pill-label-reveals-with-motion-tokens`, `voice-button-level-ring-stepped`. Every branch mutation-probed on scratch copies (95 probes, all fire; pristine silent); real inversions in one run (one per rule, SessionStrip/ChatView/index/pill-label-style/VoiceButton) — all 22 fired, restored. Pointers repointed: .claude/rules/session-strip-motion.md, docs/MAP.md, globals.css, VoiceButton.tsx |
| `tests/arcade-authority.test.ts` (t6a review fix I3/I5) | 14 | 8 | converted: `only a game's own board narrows state.play`; `Connect 4's vocabulary is gone from the shared state`; `the challenge carries which game, end to end`; `ChatView yields arrow keys to a focused game board`; `the arcade handler formats nothing and decides no ranking`; `the width helper is not in that effect`. Trimmed to its count: `setPlaying(false) lives in an effect that depends on openGame alone` | `arcade-no-forbidden-attention-apis`(-ts) widened to every non-comment token containing a symbol (property, shorthand, JSX attribute, string, longer name); new `arcade-state-play-only-in-own-board`(-ts), `arcade-shared-state-no-connect4-vocabulary`, `arcade-shared-state-has-seat-vocabulary`, `arcade-challenge-game-in-presence-hook`, `-in-reducer`, `-in-lobby`, `-not-hardcoded`, `chatview-yields-keys-to-game-board`, `arcade-handlers-no-ranking-or-formatting`, `arcade-stop-play-keyed-on-open-game`. Every branch mutation-probed on scratch (22 probes fire, 2 out-of-scope controls silent); real inversions in one run (one per rule) — all 13 fired, restored. Kept with WHY: palette allowlist, GAME_WORDS, key-claim count, stop-play count |
| `tests/attention-banner.test.tsx` (t6a review fix I4) | 13 | 13 | none (rule-only fix) | `chatview-passes-provider-to-attentionbanner` now checks the opening tag of `<AttentionBanner …>…</AttentionBanner>` too, and fires on a ChatView that renders no banner. Inverted: ChatView.tsx:1337 turned into an open/close tag without `provider` — fired at 1337; the tag renamed to AttentionNotice — fired (whole file); restored |
| `tests/buddy-consent-gate.test.ts` (t6a review fix I5) | 23 | 18 | `found the show handler (otherwise every assertion below is vacuous)` (show() presence converted; the length check only guarded the old slice); converted: `returns the refusal WITHOUT creating the buddy`; `answers the "is the helper live" question from the cache, never with a fresh call`; `builds the usable-screen-area lookup only where a helper is needed`; `re-asks for the usable screen area on all three display events, debounced` | `buddy-show-refuses-before-creating` (ORDER, searched only inside the handler — main.ts:465 has an unrelated earlier `return { ok: false`), `buddy-drag-reads-cached-helper-status`, `buddy-work-area-only-where-needed`, `buddy-work-area-reresolved-on-display-change`. 10 scratch probes fire; real inversions in one run: show() moved ABOVE the refusal (fired at 2139), `await helperStatus()` in captionChannelLive (2104), unconditional WorkAreaResolver, display-removed wire dropped — all fired, restored |
| `tests/callout-authority.test.tsx` (t6a review fix I6/M4) | 6 | 6 | none | `no-hand-rolled-callout-tint` gains a branch for a tint and `border` split across one `className` attribute or one `mergeClasses(…)` call (reported once); note corrected (the old ±150-char window was broader than one attribute). Kept exact-count case now reads through readSource; dead readdirSync/join/readFileSync imports removed. Inverted: AboutPopup.tsx given `className={\`rounded-lg border ${w ? 'bg-destructive/10' : ''}\`}` — fired at 247, restored |
| `tests/buddy-caption-channel.test.ts` (t6a review fix M1) | 20 | 20 | none | comment-only: the kept count case's "The scans above…" now names the rule buddy-window-move-only-in-place; the create() count case carries its one-line WHY (an exact call-site count) |
| `tests/chatgpt-oauth.test.ts` (u1) | 29 sites (31 runs) | 29 sites (31 runs) | none — the source half of `the 5-hour sentence is byte-identical to the approved wording, on every locale` removed (its toLocaleTimeString spy + exact-sentence behaviour half stays) | oxlint `no-restricted-properties` (toLocaleString/-TimeString/-DateString) scoped to `src/shared/chatgpt-types.ts`; ast-grep `limit-sentence-uses-shared-time-format` (import/export from './time-format' present) + `status-bar-uses-shared-format-time12` (formatTime12 imported from ../../shared/time-format; no local function formatTime12). Fixtures +3 (172). 14 scratch probes (6 fire as expected, 8 silent incl. 2 out-of-scope); oxlint probed on 5 member shapes (all fire) + an out-of-scope copy (silent). Real inversion, one run: StatusBar.tsx:275 local `function formatTime12`, chatgpt-types.ts:11 import replaced by a local toLocaleTimeString formatter — ast-grep fired on both (StatusBar.tsx:275, chatgpt-types.ts:1), `npm run lint` fired at chatgpt-types.ts:12:45; restored, OK. Kept: all 29 cases (behaviour). **Review fix round 1, item 1 (Important):** the oxlint ban only sees a direct property access; added ast-grep `chatgpt-types-no-locale-formatter` as a backstop for an indirect string key, a `Reflect.get` lookup, a bare-call import, and an aliased import specifier's original name. Fixtures +5 (205). 8 scratch probes (4 fire, incl. an out-of-scope filename that stays silent by design; pristine and comment-only mentions stay silent). Real inversion: chatgpt-types.ts got a `toLocaleTimeString` string-key line — fired; restored, OK |
| `tests/close-prompt-meta-unreadable.test.tsx` (u1) | 5 | 3 | `main's session:get-meta marks a thrown read or a missing store as unreadable`; `remote-server's session:get-meta does the same` (both converted) | ast-grep `get-meta-marks-failed-read-unreadable` (ipc-handlers.ts: inside the SESSION_GET_META handler, an `if (!store)` returning an object with an `unreadable` pair AND a catch-with-parameter whose body holds one; presence branch) + `-remote` (remote-server.ts: the `case 'session:get-meta'` holds an `unreadable:` key; no "fall through to empty" string in it; presence branch). Fixtures +5 (177). 14 scratch probes: 8 fire (store/catch unreadable removed, param-less catch, unreadable only in a comment, handler renamed; remote unreadable removed, case renamed, fall-through string), 6 silent (2 pristine, braced `if (!store) { return … }`, one remaining remote pair, 2 out-of-scope copies). Real inversion, one run: ipc-handlers.ts:4294 catch lost `unreadable`, remote-server.ts:2304/2316 lost both — fired at ipc-handlers.ts:4268 and remote-server.ts:2288; restored, OK. Kept: the 3 render cases (behaviour). Unused `join`/`readStripped` imports removed. **Review fix round 1, item 2 (Minor):** `get-meta-marks-failed-read-unreadable`'s no-store branch was false-firing on a legitimate `return { …, unreadable: … } as Meta` / `satisfies Meta` (only a bare `kind: object` child was accepted). Added an `as_expression`/`satisfies_expression` branch. 3 scratch probes: an `as`-wrapped and a `satisfies`-wrapped unreadable object both go silent (were false-firing before); a wrapped but genuinely blank object still fires |
| `tests/create-session-feedback.test.ts` (u1) | 11 | 4 | `App asks the function rather than re-deriving it`; `a failed or unrecognised answer is unknown, and is asked again after a reconnect`; `the list of open sessions is marked as arrived even when it is empty`; `the new-session form does not open itself over a catch-up`; `the wait is a state of its own, set before the computer is asked`; `the session goes on screen from the computer’s answer, not only its announcement`; `a start that fails says so and offers to try again, instead of an empty screen` | — (row disposition: drop App pins; App cannot be mounted in a unit test). Kept: the 4 `showFirstRunWelcome` unit cases (behaviour). Unused `fileURLToPath`/`readStripped`/`assertPatternMatches` imports and the `read`/`app` helpers removed |
| `tests/dev-load-recovery.test.tsx` (u1) | 3 | 2 | `main.ts uses the shared probe rather than an inlined copy` (converted) | ast-grep `main-uses-shared-mount-probe` (main.ts: a `….executeJavaScript(MOUNT_PROBE_JS)` call, any receiver or none, AND an import/export from './dev-mount-probe'), not tsc — see the classification row. Fixtures +2 (179). 8 scratch probes: 4 fire (local same-name const, inline-string call, extra call argument, import from another path), 4 silent (pristine multi-line chain, bare `executeJavaScript(MOUNT_PROBE_JS)`, `export * from './dev-mount-probe'`, out-of-scope copy). Real inversion: main.ts:36 import replaced by a local `const MOUNT_PROBE_JS` — fired at main.ts:1 (and `npm run typecheck` stayed green, proving tsc alone would miss it); restored, OK. main.ts:663 comment now names the rule. Kept: the 2 jsdom probe cases (behaviour; they read index.html as the DOM under test). **Review fix round 1, item 3 (Minor):** the required call's pattern only matched a plain-dot receiver (`$OBJ.executeJavaScript(...)`), so `win.webContents?.executeJavaScript(MOUNT_PROBE_JS)` (an optional-chain receiver) was false-firing as a missing call. Added a `$OBJ?.executeJavaScript(MOUNT_PROBE_JS)` pattern branch. 3 scratch probes: a one-level and a two-level optional chain both go silent (were false-firing before); an import with no call at all still fires |
| `tests/dialog-shell.test.tsx` (u2) | 12 | 9 | `this guard can see what it claims to cover` (Class H non-vacuity, deleted — the fixture pass is the non-vacuity check now); `nothing in scope hand-rolls the shell` (converted); `every exempted file still exists and still hand-rolls` (converted — the "still hand-rolls" half; the "still exists" half first dropped, then restored for every rule by check.sh's rule-paths pass — review of u2, 2026-09-16) | ast-grep `no-hand-rolled-dialog-shell` (an open or self-closing JSX tag whose name starts with OverlayPanel; inScopeFiles() scope mirrored — App.tsx, flat components/*.tsx, components/development + ui, with every other components/ subdirectory and `*.test.tsx` in `ignores:`, ui/Dialog.tsx and the 9 NOT_DIALOGS file names ignored) + `no-hand-rolled-dialog-shell-exemption-still-applies` (whole-file presence over the 9 exempted files at their real paths). Fixtures +3 (182). 14 scratch probes: main rule fired on App.tsx, components/, development/, ui/ and an `<OverlayPanelX/>` prefix (5), silent on game/, overlays/, `*.test.tsx`, ui/Dialog.tsx, a basename-exempt Toast.tsx and a comment-only mention (6); exemption rule fired on ui/Toast.tsx and ui/Select.tsx with no panel (2), silent on ui/Tooltip.tsx with one (1). Real inversion, one run: AboutPopup.tsx:247 gained `<OverlayPanel />` and ui/ZoomPill.tsx lost every OverlayPanel — fired at AboutPopup.tsx:247 and ZoomPill.tsx:1; restored, OK. Kept: the 9 render/width cases (behaviour). Unused fs/path/guard-scope imports removed |
| `tests/explainer-shell.test.tsx` (u2) | 5 | 3 | `no host passes the explainer chrome props it no longer has` (converted); `every host drives Dialog onBack from its showInfo flag` (converted) | ast-grep `settings-explainer-no-chrome-props` (a SettingsExplainer-prefixed tag, self-closing or open, with an attribute named exactly title/onBack/onClose; the 4 HOSTS files) + `explainer-hosts-pass-onback-showinfo` (ContextPopup/SettingsPanel/SyncPanel: a file with the explainer tag and no `onBack={…}` whose expression holds a non-comment showInfo piece) + `-lifted` (ThemeScreen: explainer tag and no non-comment showInfo anywhere). Fixtures +4 (186). 15 scratch probes: chrome — multi-line onClose and a `SettingsExplainerV2` prefix fire, a Dialog `title` beside the explainer and an out-of-scope host stay silent; onBack — `onBack={undefined}`, a non-showInfo arrow and a host with no onBack fire, SettingsPanel's two-onBack shape (one multi-line showInfo ternary), `state.showInfo` and a host without the explainer stay silent; lifted — comment-only showInfo fires, a destructured prop, `props.showInfo` and a file without the explainer stay silent. Real inversion, one run: ContextPopup.tsx:125 explainer given `title=`, SyncPanel.tsx:957 onBack ternary no longer on showInfo, ThemeScreen.tsx showInfo renamed — fired at ContextPopup.tsx:125, SyncPanel.tsx:1, ThemeScreen.tsx:1; restored, OK. Kept: the 3 render cases (behaviour). Unused fs/path/stripComments imports removed |
| `tests/filter-chip.test.tsx` (u2) | 6 | 4 | `no longer carries a chip recipe of its own` (converted); `the skills drawer draws its chips with FilterChip too, not a local recipe` (converted) | ast-grep `no-local-filter-chip-recipe` (MarketplaceFilterBar.tsx: a string piece or JSX text holding `bg-accent text-on-accent` or `rounded-full text-sm`) + `-command-drawer` (CommandDrawer.tsx: no tag whose name starts with FilterChip — presence branch — or a string piece / JSX text holding `text-xs px-2 py-0.5 rounded-full`, `bg-accent/20 text-accent border-accent/50` or `ml-auto`). Fixtures +5 (191). 9 scratch probes: fire on JSX-text recipe, `ml-auto` in a class string beside a FilterChip, a template-literal drawer recipe; silent on comment-only mentions (2), a double-spaced recipe, an out-of-scope marketplace file, a subdirectory CommandDrawer.tsx, and a `<FilterChipGroup>` prefix (satisfies presence, as `includes('<FilterChip')` did). Real inversion, two runs: (1) MarketplaceFilterBar.tsx:156 className gained `rounded-full text-sm` and CommandDrawer.tsx's two FilterChip tags renamed — fired at MarketplaceFilterBar.tsx:156 and CommandDrawer.tsx:1; (2) CommandDrawer.tsx:314 FilterChip given `className="ml-auto"` — fired at 314; restored, OK. Kept: the 4 render cases (behaviour) — classification row corrected from "three" to four. Unused fs/path/guard-scope imports removed |
| `tests/filter-menu-chip.test.tsx` (u2) | 9 | 6 | `is what the Resume browser renders on both chips` (converted); `uses the shared chips, the shared search pill and the checkbox mark` (converted); `draws no text-glyph chevrons or arrows and no hand-made check box` (converted) | ast-grep `resume-browser-uses-shared-filter-chips` (ResumeBrowser.tsx, whole file: a FilterMenuChip / SearchFilterPill / CheckboxMark tag and a FilterChip tag with `kind="toggle"` each present; `pickLabel('Projects'` and `pickLabel('Tags'` each exactly once, Projects first — "twice"/"before" via `follows` between the children of the two calls' common ancestor) + `resume-browser-no-local-filter-chip-parts` (a function named FilterPill; ▾▴▼▲↓↑ in a string piece, JSX text or regex literal; a template with `rounded-sm border ${checked` or `Projects (${`; a `…join(', ')` call). Fixtures +9 (200). 29 scratch probes — presence rule: silent on a good file (multi-line FilterChip with kind first, ternary-wrapped SearchFilterPill, map-wrapped CheckboxMark, a comment-only pickLabel), on both calls in one array in order, and out of scope; fires on each primitive missing (4, incl. `FilterMenuChipX` and `kind={'toggle'}`), each call missing (2), Tags first (in separate statements and in one array), Projects twice, Tags twice, a double-quoted and a space-padded call (as the old regex). Absence rule: fires on `function FilterPill` expression, `/▾/`, bare `join(', ')`, `Projects (${n})`, `rounded-sm border ${checked`, `{'▲'}`; silent on comment mentions, `FilterPillX`, `join(",")`, `join(', ', 1)`, `Projects ${n}`, `${isChecked`, out of scope. Real inversion, two runs: (1) ResumeBrowser.tsx:871/876 labels swapped and :1575 given `↑` — fired at 1:1 and 1575; (2) the SearchFilterPill tag renamed — fired at 1:1; restored, OK. Kept: the 6 render/unit cases (behaviour). Unused fs/path/guard-scope imports and the describe-level source read removed. REVIEW FIX (u2, 2026-09-16): the one `-absent` fixture (which tripped all six presence branches together) split into six, one per presence branch, plus a "Tags labelled twice" fixture — 9 fixtures, one finding each; a scratch control with everything correct stays silent |
| `tests/folders-service.test.ts` (u3) | 7 | 5 | `remote-server.ts no longer reads or writes youcoded-folders.json itself` (converted); `both transports call the shared service for all five folder channels` (converted) | ast-grep `no-folders-json-outside-service` (remote-server.ts: the innermost node — string/template piece, comment, regex, or code — whose text holds `youcoded-folders.json`, raw text as the retired check read it) + `folders-service-called-by-both-transports` (remote-server.ts and ipc-handlers.ts, whole file: no call whose callee text ends in each of the five names — a comment, string or uncalled declaration no longer counts). Fixtures +2 (213). 11 scratch probes: json rule fired on a comment, a template after a substitution, code spelling the name, and a `youcoded-folders.jsonx` superstring (as `toContain` did); silent on saved-folders.ts (out of scope), `youcoded-foldersXjson`, and a clean file. Presence rule silent on `svc?.listPickerFolders()`/`this.s.addFolder()`/`await …`, on an extra `addFolderX()`, and on `resetFolderDescription(` (a substring, as before); fired on a shorthand `{ setFolderDescription }` and a `const setFolderDescription` with no call. Real inversion, one run: remote-server.ts:3043 gained a `youcoded-folders.json` comment and ipc-handlers.ts:1460's setFolderDescription call became `saveDesc(…)` — fired at remote-server.ts:3043 and ipc-handlers.ts:1; restored, OK. Kept: the 5 service cases (behaviour). folders-service.ts header comment repointed to the rules |
| `tests/primitive-adoption.test.ts` (u3) | 5 | 3 | `the toast is not re-hand-rolled` (converted); `no raw black/white overlay fills remain` (converted) | ast-grep `no-hand-rolled-toast` + `-ts` twin (the retired regex `className="[^"]*fixed bottom-16[^"]*"` on the innermost non-comment node holding it — JSX attribute, JSX text, string/template piece, or code), `toast-auto-dismiss-owned-by-primitive` (App.tsx: `setTimeout\(\(\) => setToast\(null\)` on the innermost node, comments included as the raw read had them), `no-literal-black-white-wash` + `-ts` twin (`hover:bg-(black|white)/\d+`, innermost non-comment node). Scope mirrors CONSUMERS: all renderer .ts/.tsx, minus `*.test.*` and every path starting `components/ui` (the old startsWith had no slash; ignore `components/ui*`). Fixtures +7 (220). 17 scratch probes: toast fired on a components/ attribute; wash fired on a state/ .ts string, an object `{hover:bg-black/20}`, a `panels/Pui.tsx` file (not a ui prefix); both silent on components/ui/, a `.test.tsx`, a trailing `//` comment, `className={'fixed bottom-16'}`, a double-spaced recipe, src/main/, `components/ui-extras/` and `components/uilist/` (the old prefix test), and `hover:bg-white/[.1]`/`hover:bg-black`; timer fired on `window.setTimeout(…)`, a comment, a delay-less call inside useEffect; silent on a braced body, `setToastX(null)` and a non-App file. Real inversion, one run: App.tsx:715 gained `setTimeout(() => setToast(null), 3000)`, AboutPopup.tsx:77 `fixed bottom-16`, AboutPopup.tsx:100 `hover:bg-white/10`, state/game-reducer.ts a `hover:bg-black/20` string — fired at App.tsx:715, AboutPopup.tsx:77 and :100, game-reducer.ts:289 (the existing no-hardcoded-z-index-or-scrim-scrim-ts fired there too); restored, OK. Kept as text, WHY comment: `every ui/ primitive is used outside components/ui/` (cross-file run-time list), `the intentionally-unadopted list does not rot` (same list), `the settings drawer header keeps its macOS traffic-light clearance` (row's CSS coupling); reads via readSource/readStripped. FINDING: the "does not rot" case found its names by regex over its own file (`new Set\(\[`), which never matched the typed `new Set<string>([` — it checked nothing; INTENTIONALLY_UNADOPTED moved to module scope and the case now iterates it (proved: adding 'Button' turned it red, restored) |
| `tests/project-view-files-tab-stays-mounted.test.tsx` (u3) | 2 | 1 | `ProjectView mounts FilesTab unconditionally and hands it \`hidden\`` (converted) | ast-grep `filestab-mounted-with-hidden-prop` (ProjectView.tsx: whole file with no FilesTab tag; a FilesTab tag, self-closing or opening, without the exact attribute `hidden={tab !== 'files'}`; the old conditional-mount regex on the innermost node holding it, comments included as the raw read had them). Deliberate difference: every tag named exactly FilesTab is checked, where the old read took the first `<FilesTab…/>` prefix match (one tag exists today). Fixtures +4 (224). 13 scratch probes: silent on the real shape, a multi-line tag with arrow props, an open/close tag, a ternary mount and a `'files' === tab` mount (neither caught before), a ContextTab conditional beside a good FilesTab, and a conditional in another project-view file; fired on `a && tab === 'files' && <FilesTab …>`, a comment quoting it, the no-space form, `hidden={ tab !== 'files' }`, `hidden={tab !== "files"}` (both failed the old `toContain` too), and a file with only `<FilesTabHeader />`. Real inversion, two runs: (1) ProjectView.tsx:949 became `activeProject && tab === 'files' && (` and :950 lost `hidden` — fired at 949 and 950; (2) the :950 tag renamed FilesPane — fired at ProjectView.tsx:1; restored, OK. Kept: `hides on \`hidden\` without refetching the file list` (jsdom behaviour). Unused fs/path imports removed |
| `tests/project-watcher.test.ts` (u3) | 14 | 14 | none | — (the row's disposition keeps the case). `skip-dir set stays in lockstep with project-file-discovery` stays a text read — equality of WATCH_SKIP_DIRS with discovery's unexported SKIP_DIRS is cross-file — now through readSource (CRLF-safe) with a WHY comment, same title and assertion. Proved still live: adding `'zzz-probe'` to project-file-discovery.ts:39's set turned it red; restored. 13 behaviour cases unchanged, 14 passed |
| `tests/prompt-assembly.test.ts` (u4) | 28 | 27 | `the prompt is assembled FROM the parts, never beside them` (deleted, row `—`) | — (deleted per the row; its five-line "TAUTOLOGICAL" comment went with it). The kept join case `joining the parts reproduces the prompt exactly, in every shape` still compares the two exports at runtime. `fs`/`path` imports stay (fixture setup uses them). 27 behaviour cases unchanged, 27 passed |
| `tests/read-pdf.test.ts` (u4) | 17 | 16 | `does not build those dirs with path.sep — the assertion above is VACUOUS on POSIX` (converted) | ast-grep `pdfjs-asset-dirs-no-path-sep` (pdf-text.ts; the retired slice's whole region: inside the `export function pdfjsAssetDirs` statement, plus any top-level node after it and before `export const DEFAULT_PDF_PAGES` — or to end of file when that export is gone; innermost node whose text holds `path.sep`, comments included as the raw read had them; plus a whole-file branch when no exported `pdfjsAssetDirs` exists, as the old empty-slice check failed). Deliberate difference: DEFAULT_PDF_PAGES moved above the function no longer fails (an artifact of the slice, not the invariant). Fixtures +3 (227). 17 scratch probes: silent on the real file, `nodePath.sep` (not a `path.sep` substring), path.sep in a function after DEFAULT_PDF_PAGES, a destructured `sep`, DEFAULT_PDF_PAGES above the function, and an out-of-scope `pdf-text-old.ts`; fired on code `path.sep`, a comment in the function, a `'path.sep'` string, a template substitution, `mypath.sep`, `path.separator`, a `/path.sep/` regex, a const between the function and DEFAULT_PDF_PAGES, path.sep at file end once DEFAULT_PDF_PAGES is unexported, the function renamed, and the function un-exported. Real inversion, two runs: pdf-text.ts:55 `+ '/'` → `+ path.sep` fired at pdf-text.ts:55; the function renamed fired at pdf-text.ts:1; restored, OK. Kept: 16 behaviour cases (incl. the runtime trailing-slash contract), unchanged; WHY comment points at the rule. docs/testing-under-load.md repointed |
| `tests/remote-appearance-relay.test.ts` (u4) | 4 | 2 | `the window relay also broadcasts appearance:sync to remote clients` (converted); `the remote server is told how to reach the computer's windows` (converted) | ast-grep `appearance-broadcast-relays-to-remote` (main.ts; branch 1: the `ipcMain.on(IPC.APPEARANCE_BROADCAST, …)` call with no call inside whose callee ends in `remoteServer.broadcast`/`remoteServer?.broadcast` and whose first-argument object holds `type: IPC.APPEARANCE_SYNC`; branch 2: whole file with no `onAppearanceBroadcast` object property — pair, shorthand or method). A deleted registration is already appearance-broadcast-handler-registered's job. Deliberate differences: a comment spelling either no longer satisfies it (stricter); `type` need not be the first key, and shorthand/method spellings of the callback count (the same invariant). Fixtures +2 (229). 15 scratch probes: silent on the real file, `remoteServer?.broadcast`, `deps.remoteServer.broadcast`, reordered keys, a call nested in `queueMicrotask(() => …)`, shorthand and method callbacks, and an out-of-scope `main-old.ts`; fired on the relay line removed, commented out, sent with `IPC.APPEARANCE_BROADCAST`, moved outside the handler, sent via a prebuilt `msg` variable (the old regex needed the literal too), the callback renamed, and the callback surviving only in a comment. Real inversion, one run: main.ts:1315's relay line deleted and :314's key renamed onThemeBroadcast — fired at main.ts:1305 and main.ts:1; restored, OK. Kept: the 2 server behaviour cases, unchanged; unused fs/path imports removed; WHY comment points at the rule |
| `tests/remote-download.test.ts` (u4) | 31 sites (39 run) | 30 sites (38 run) | `the route is matched before the static handler AND before the Vite proxy` (converted) | ast-grep `download-route-before-static` (remote-server.ts; whole file with no `http.createServer((req, res) => { … })`; that call with no `this.downloads.handleHttpRequest(req, res)`, no `this.handleHttpRequest(req, res, staticDir)` or no `this.proxyToVite(req, res, viteDevUrl)` inside; a static/proxy call inside it that the route call does not precede in source order — the call or an ancestor inside the server call follows a sibling that is/contains the route, or it sits in the route's own arguments). Deliberate differences: all three calls must be inside the server callback (the old text region ran to end of file, so a fallback moved into a helper below passed whatever the callback's order); a string spelling the route no longer counts. Fixtures +5 (234). 15 scratch probes: silent on the real file, the route and fallback inside one `if (!route) if (…) {…}` statement, `route || static` in one expression, and an out-of-scope `remote-server-old.ts`; fired on the route moved below the if/else (2), the route moved into the else branch (static, 1), the route removed (3), commented out (3), only in a string (3), only in an arrow defined before the server call (3), the static call renamed (1), the static call moved into a helper method (1), the server's params renamed (1), a static call in an arrow at the top of the callback (1), and a proxy call before the route (1). Real inversion (ORDER), one run: remote-server.ts:592's route line moved below the if/else — fired at remote-server.ts:593 and :595; restored, OK. Kept: 38 HTTP behaviour cases, unchanged; unused fileURLToPath and guard-scope imports removed; WHY comment points at the rule |
| `tests/remote-host-admin-desktop-only.test.ts` (u5) | 4 | 2 | `refuses the administration channels, and not by comparing an address` (converted); `a phone cannot unpair a device` (converted); plus the source half (`case 'remote:get-config':` present) of the kept `reading the configuration is still allowed` | ast-grep `remote-admin-case-refuses` (remote-server.ts: a presence branch per channel — set-password, set-config, get-config, devices:rename, devices:unpair — requiring a real `switch_case`; a rename/unpair case whose own text lacks HOST_ADMIN_REFUSAL, unless it is an empty fall-through label directly followed — up to three labels deep — by a refusing case; `client.ip === '127.0.0.1'` or `this.disconnectClient(payload.clientId` on the innermost node holding it — comment, string/template piece, regex, binary/call/member expression) + `unpair-button-disabled-on-remote` (SettingsPanel.tsx, whole file: no tag with both an ``aria-label={`Unpair …`` attribute and `disabled={hostOnly}`; no conditional whose condition ends in `hostOnly` with the exact `${row.online ? 'Online' : 'Offline'} · unpair on the computer itself` template). "HOST_ADMIN_REFUSAL anywhere" is implied by the arm check. Deliberate differences (in the notes): a comment/string spelling a case or either SettingsPanel shape no longer counts; double-quoted labels accepted; "inside this arm" replaces "200 chars after the first label" (every same-label case checked; a non-empty arm can't borrow the next case's refusal; a refusal deep in a long arm counts); attribute order/line breaks free. Fixtures +10 (244). 39 scratch probes, all as expected — server: pristine, `"remote:set-config"`, an empty unpair label (and two chained) falling to a refusing rename, a refusal only in an arm comment, `client.ip !==`, `client.ip===`, `disconnectClient(payload.id)` silent; a renamed/comment-only case, a performed rename, an empty label onto a performing case (2), a non-empty arm falling through, the refusal constant renamed (2), the ip check as code/parenthesised/string/template substitution/template text/regex/line and block comment, the disconnect call as code/awaited with two args/`clientIds`/comment/string, and a second switch performing rename fire. Panel: pristine, reordered multi-line attributes, a self-closing button, `!hostOnly ?`, a multi-line `hostOnly\n ?` silent; disabled removed or widened to `hostOnly \|\| busy`, a plain-string label, the hint text changed, and either shape surviving only in a comment fire. Out-of-scope copies silent. Real inversion, two runs: (1) remote-server.ts:3256 unpair arm performs `this.disconnectClient(payload.clientId)` and answers ok, SettingsPanel.tsx:2024 lost `disabled={hostOnly}` — fired at remote-server.ts:3256 and :3257, SettingsPanel.tsx:1; (2) the get-config case renamed — fired at remote-server.ts:1; restored, OK. Kept: 2 responseOutcome cases (behaviour; the get-config one lost only its source line). Unused `readFileSync` import and `SERVER` read removed. MAP remote-access row names both rules. Classification row corrected (mixed third case; second rule) |
| `tests/remote-page-source.test.ts` (u5) | 4 | 3 | `main.ts serves the built copy only when packaged or when run-dev.sh built one` (deleted, row `—`) | — (deleted per the row; it pinned one exact expression text in main.ts, `serveBuiltPage: app.isPackaged \|\| process.env.YOUCODED_REMOTE_BUILT === '1'`, and main.ts:294's comment already explains the choice). Nothing to invert. Kept: the 3 `choosePhonePageSource` pure-function cases, unchanged, 3 passed. Unused `readFileSync`/`resolve` imports removed. remote-server.ts:248's "Pinned by tests/remote-page-source.test.ts" stays true (the function's behaviour is still pinned there) |
| `tests/remote-host-admin-desktop-only.test.ts` (u5 rule fix) | 2 | 2 | none (rule-only fix) | `remote-admin-case-refuses`'s banned-text branch now also has a whole-file backstop: when the raw text holds `client.ip === '127.0.0.1'` or `this.disconnectClient(payload.clientId` and no listed innermost node reports it (e.g. inside a `new` expression), the file fires — so every node kind the retired raw-text check saw is covered. Fixture +1 (245). Probes: `new this.disconnectClient(payload.clientId)` fires at the file; `new Foo(client.ip === '127.0.0.1')` fires at the binary expression; the other 28 server probes unchanged |
| `tests/remote-rate-limit.test.ts` (u5) | 5 | 2 | `no failure bucket is keyed by network address` (converted); `a burst slows new connections instead of refusing them` (converted); `no failure budget is shared between connections` (converted) | ast-grep `no-ip-keyed-failure-bucket` (remote-server.ts: the five retired raw-text spellings — `failedAttempts.get(ip)`, `failedAttempts.set(ip`, `isRateLimited(ip)`, `recordFailedAttempt(ip)`, `this.\w*[aA]ttempts\w*.(get\|set)(ip` — on the innermost call/new/method/function definition/comment/string/template/regex node, plus a whole-file backstop when the raw text holds one that no such node reports; no deliberate difference) + `remote-burst-slows-not-refuses` (whole-file presence: raw `HOST_FAILURES_BEFORE_SLOWDOWN`, as before; a `const slowStart = this.$CHECK() ? HOST_SLOWDOWN_MS : 0` declaration; an `if ((slowStart))` whose consequence starts with `await new Promise`, braced or not — whitespace free, comment/string spellings of the two shapes no longer count). Fixtures +8 (253). 37 scratch probes, all as expected — bucket rule fires on get/set/`ipAddr`/call-in-if/untyped method and function definitions/`recordFailedAttempt(ip)`/line and block comments/string/template/regex/`new`/`this.hostAttempts.get(ip)`/`this.perIpAttemptsMap.set(ip`, and (backstop, line 1) an interface signature; silent on a typed `isRateLimited(ip: string)`, `recordFailedAttempt(ip as string)`, `.has(ip)`, a `hostAttempts` without `this.`, `failedAttempts?.get(ip)` (none matched the old text either). Burst rule silent on a renamed helper, a multi-line declaration, a braced wait, a wait with `else`, the constant surviving only in a comment (old spelling kept); fires on the constant renamed, a refusal instead of the slow start, the shape only in a comment, `let`, a helper taking an argument, `: 1`, the wait removed, un-awaited, under `slowStart > 0`, or only in a string. Out-of-scope copy silent. Real inversion, one run: remote-server.ts:1336 `this.recordFailedAttempt(ip)` and :1234 replaced by a refusal + `const slowStart = 0` — fired at remote-server.ts:1337 and remote-server.ts:1; restored, OK. Kept: the 2 socket-driving cases, unchanged, 2 passed. Unused `readFileSync` import and `server` read removed. remote-server.ts:1232's pointer to the behaviour test stays true |
| `tests/remote-readiness.test.ts` (u5) | 14 | 13 | `replays with no 500 ms timer — the only timers on the restore path are the two fallbacks` (deleted, row `—`; its `describe('the host has no fixed wait')` went with it) | — (deleted per the row; it grepped remote-server.ts for no `}, 500)`, the two fallback constants' exact values and the fallback `setTimeout` spelling). What it guarded is also behaviour here: inserting `await new Promise((r) => setTimeout(r, 500))` before the replay in remote-server.ts:1520 turned all 13 kept cases red under fake timers; restored. youcoded/docs/renderer-chrome.md:79 repointed from "the test guards against its return" to that measured fact. The READY fallback's value stays pinned by remote-host-reliability.test.ts (behaviour). Kept: 13 WS behaviour cases, unchanged, 13 passed. Unused `join` and guard-scope imports removed |
| `tests/remote-reconnect-reloads.test.tsx` (u6) | 10 | 9 | `App's session defaults (App cannot be rendered in a unit test; its wiring is pinned here)` (deleted, row `—`) | — (deleted per the row; it pinned two exact App.tsx lines, `useOnRemoteReconnect(loadSessionDefaults);` and the `useEffect(() => { loadSessionDefaults(); }, [settingsOpen, loadSessionDefaults]);` spelling. App.tsx:639's comment already explains the wiring; the hook itself stays pinned by the kept `useOnRemoteReconnect` case). Nothing to invert (App cannot be rendered in a unit test). Kept: the 9 hook/provider behaviour cases, unchanged, 9 passed. Unused `readFileSync`/`resolve` imports removed. No live pointers |
| `tests/remote-shim-permission-inflight.test.ts` (u6) | 2 | 1 | `App marks a resolution silent outside remote mode — cleared, never "Answered on the computer"` (deleted, row `—`; its `describe('a desktop window clears resolutions quietly')` went with it) | — (deleted per the row; it pinned the exact App.tsx:1339 line `if (action?.type === 'PERMISSION_RESOLVED_ELSEWHERE' && !isRemoteMode()) action.silent = true;`, whose WHY comment at App.tsx:1336 already explains it). Nothing to invert (App cannot be rendered in a unit test). Kept: the shim in-flight case, unchanged, 1 passed. Unused `join` and guard-scope imports removed. No live pointers |
| `tests/resume-browser-organize.test.tsx` (u6) | 10 | 10 (review fix; 9 at u6) | `matches the file viewer styling with always-visible underline and pencil` (trimmed — REVIEW FIX (u6), 2026-09-16: its source-free half is back as a new case, `keeps both rename cues visible at rest, with no hover or focus reveal` — renamed because it no longer compares anything to the file viewer; it renders the card, finds `Rename CC Chat`, asserts the name carries `underline decoration-dotted decoration-fg-muted` and that neither the name nor the pencil's wrapper carries `hover:`/`focus:`/`opacity-0`/`invisible`/`hidden`/`touch-reveal`; no source read. Proved red: ResumeBrowser.tsx:1250 `underline` → `group-hover:underline` failed it ("Expected the element to have class"); restored; 10 passed. The rest of this cell is the u6 record) | — (deleted per the row; the case regex-scraped SessionDrawer.tsx for the rename trigger/label/pencil classNames, the pencil path and the `Ic` svg attributes, then compared ResumeBrowser's rendered card to them — every expectation was derived from source text, so it cannot survive as behaviour). Its source-free part (the name has `underline decoration-dotted decoration-fg-muted`, neither cue carries hover/opacity-0/hidden classes at rest — R5-1, ResumeBrowser.tsx:1222 comment) went with it; flagged in the u6 report. Nothing to invert. Kept: the 9 interaction cases, unchanged, 9 passed. Unused `readFileSync`/`resolve` imports removed. No live pointers (wrap-ups.md:308 names the file for its viewport lesson, still true) |
| `tests/run-over-card.test.tsx` (u6) | 12 | 8 | `endRun does not stop the run` (converted); `the games are handed a way out, so leaving is still possible` (converted); `uses the shared card rather than a hand-rolled overlay` (converted); `offers a keyboard retry` (converted) — the describes `the shell lets a finished run finish` and `every solo game ends the same way` went with them | ast-grep `solo-game-uses-run-over-card` (every components/game/*Game.tsx, subfolders ignored as the non-recursive readdirSync did: whole file lacks a `<RunOverCard>`/`<x.RunOverCard>` tag, self-closing or open; whole file lacks `retryKeyHint` as a JSX attribute, object key or shorthand) + `arcade-end-run-keeps-playing` (ArcadeShell.tsx: the text `setPlaying(false)` on the innermost call/new/comment/string/template/regex/JSX-text node inside any `const endRun` declaration, plus a whole-declaration backstop for any other kind; whole file lacks an `onExit` JSX attribute). Deliberate differences (in the notes): presence now needs code — a comment/string/unrendered import no longer satisfies; names matched whole (`RunOverCardLegacy`, `retryKeyHintText`, `endRunner` no longer count); the whole endRun declaration replaces the 900-char window (endRun is ~1,500 chars, so the window watched ~60% of it); every `const endRun`, not the first; `onExit =` with spaces now counts. The ">= 2 solo games found" scan sanity check dropped (Plan B deletes such checks). No presence branch for endRun: the old slice was silently empty when endRun was renamed. Fixtures +6 (259). 49 scratch probes, all as expected — arcade (22, plus 4 shapes tried for the backstop): pristine, `setPlaying (false)`, `setPlaying(true)`, `setPlaying?.(false)`, a call in another function, `function endRun`, a renamed `endRunner`, `onExit = {…}` and an out-of-scope copy silent; `this.` call, a call in a nested `.then` past the old window, string, template, `new`, regex, JSX text, JSX attribute string, template literal type, block comment, a call as an argument, a `useCallback`-wrapped endRun, and `onExit=` surviving only in a comment fire; the backstop fires once on `let x: setPlaying(false)`. Solo (23): Flappy and 2048 pristine, hint via spread pair/shorthand/string key, hint on another element, open/close card, card in a ternary, an extra card-less component, `<RunOverCard retryKeyHint />`, `<ui.RunOverCard>`, a subfolder, a `GameOverlay.tsx`, another folder and a `.ts` file silent; card renamed with import kept, hint removed, hint only in a comment, a new game with no card, a bare `Game.tsx`, the card only in a string, `<RunOverCardLegacy>` and `retryKeyHintText` fire. Real inversion, two runs: (1) ArcadeShell.tsx:120 `setPlaying(false);` in endRun + the `onExit` prop removed — fired at ArcadeShell.tsx:120 and :1; (2) TwentyFortyEightGame.tsx `<RunOverCard` → `<HandRolledOverlay` + FlappyGame.tsx lost `retryKeyHint="Space"` — fired at both files' line 1 (re-run after the member-name tweak); restored, OK. Kept: the 8 RunOverCard render cases, unchanged, 8 passed. Unused `readdirSync`/`readFileSync`/`join` imports removed; header WHY names the two rules. MAP arcade row names both rules. Classification row corrected (second rule) |
| `tests/runtime-default.test.tsx` (u7) | 6 | 4 | `(e) exactly one file under src mentions youcoded-runtime-default (RuntimeBinding.tsx)` (converted); `(f) both forms initialise from defaultRuntime() and reset to it after a create` (converted) — the describe `runtime default — source guards` went with them | ast-grep `runtime-default-key-single-owner` (.ts under src/, test files ignored: the key in a string/template piece or regex) + `-tsx` (.tsx under src/ except RuntimeBinding.tsx and tests: also JSX text, element and attribute names) + `-present` (RuntimeBinding.tsx still names the key outside a comment — the old `toEqual([owner])` failed on its absence) + `new-session-forms-use-default-runtime` (SessionStrip.tsx/App.tsx: literal `useState<Runtime>('claude')` seed; no lazy `defaultRuntime()` seed; neither close call; a literal `<setter>('claude')` after a close statement in its block; no close statement followed by `<setter>(defaultRuntime())`; a second literal in the form's own file). Comments never count (the old case blanked them), including a comment inside a call. Deliberate differences (in the notes): "rest of the close statement's block" replaces the 600-char window; a non-statement close (App's Cancel arrow) has no tail; presence needs real calls; a `//` inside a string no longer blanks the key. Fixtures +16 (275). 30 scratch probes as expected — forms (15): pristine both files, a comment literal after the close, a `/* */` seed literal, `setRuntime('claude')` twice in App, and an out-of-scope copy silent; literal reset at SessionStrip:820 (2 findings), literal seed (2), the literal in a string after the close, App:4046 literal (2), App's Create close removed (1), `x.`/`new` literal after the close, a third literal in a useEffect (fires on the later one, :434), a literal after the :790 close, JSX-text literal after App's close fire. Key (15 findings over 11 dirs): .tsx/.ts/.d.ts readers, template, regex, import path, template literal type, JSX element name fire; `.test.ts(x)`, comments (line, block, JSX), pristine RuntimeBinding, `scripts/` and `tests/` silent; RuntimeBinding with the key built by `join` fires -present. Real inversion, two runs: (1) App.tsx:543 direct `localStorage.getItem('youcoded-runtime-default')` + RuntimeBinding.tsx's key built from parts — fired at App.tsx:543 and RuntimeBinding.tsx:1; (2) SessionStrip.tsx:820 reset to `'claude'` + App.tsx:543 seeded with `'claude'` — fired at SessionStrip.tsx:1, :820, App.tsx:1, :543; restored, OK. Kept: the 4 defaultRuntime()/persistRuntimeDefault() unit cases, unchanged, 4 passed. Unused fs/path/guard-scope imports and the walk/read helpers removed; header WHY names the rules. RuntimeBinding.tsx:140 comment repointed to the rule. Classification row corrected (second rule) |
| `tests/session-drag-model.test.ts` (u7) | 10 | 7 | `exposes the raw facts` (converted); `does not name a drag model` (converted); `never starts a drag from main — Linux startDrag crops the picture and carries only a file` (converted) — the describe `preload reports facts, not a verdict` went with them | ast-grep `preload-no-drag-model-decision` (preload.ts: whole file lacks a `platformFacts: { … }` pair whose object holds a `wayland:` pair; a quoted `'html-drag'`/`"html-drag"` on its innermost string/string piece/comment/regex; `chooseTearOffModel`/`startDrag`/`dragHandoff` in any identifier leaf, string piece, comment or regex — comments count, as the retired raw read did not strip them). Deliberate differences (in the note): presence needs code (a comment or type annotation spelling `platformFacts: {` no longer counts) and `wayland:` must be a pair inside platformFacts, not the text anywhere. Fixtures +8 (283). 13 scratch probes as expected: pristine, `mode: 'html-dragging'` + an unquoted html-drag comment, a backtick `html-drag` template, and an out-of-scope `main/other.ts` copy silent; platformFacts renamed, the wayland line removed, wayland only as a shorthand, platformFacts only in a comment, `platformFacts: Object.freeze({…})` fire on the file; a quoted html-drag comment, `chooseTearOffModel(...)`, a `startDrag` member and a `"html-drag"` string fire on the line. Real inversion, two runs: (1) preload.ts:784 `wayland: …` → `tearOffModel: (… ? 'html-drag' : 'live-window')` — fired at preload.ts:1 and :784; (2) a `startDrag:` member added at preload.ts:785 — fired at :785; restored, OK. Kept: the 7 model/payload/local-drag cases, unchanged, 7 passed. Unused fs/path imports removed; header WHY names the rule. Repointed: preload.ts:779 comment, `.claude/rules/multi-window-detach.md` (startDrag guard line + a `verify:` path anchor for the rule; the test anchor stays, the file still exists), MAP "Multi-window session detach" row. `node scripts/audit-anchors.mjs --no-diff`: anchors 467/467, only the 6 pre-existing wecoded-marketplace MAP failures |
| `tests/session-drawer-skips-parent-rerenders.test.tsx` (u7) | 2 | 1 | `App gives ArtifactContext a memoized value, not a fresh object per render` (converted) | ast-grep `artifact-provider-value-memoized` (App.tsx: every `<ArtifactProvider>` tag, open or self-closing, whose `value` is not exactly `{artifactContextValue}` — the inline `value={{…}}` is one; whole file with no provider tag; whole file with no `const artifactContextValue = useMemo(…)`; that declarator's useMemo second argument not an array containing `artifactState`, the frozen-context half). Deliberate differences (in the note): every tag, not the first; whole tag name; whitespace-insensitive; presence needs code; the deps are useMemo's own second argument instead of "the first `}), [` after the declaration". The worked example's separate `value={{` attribute branch was folded into "value is exactly the identifier" (the inline literal fires there; a second branch could only add a duplicate report). Fixtures +5 (288). 11 scratch probes as expected: pristine, `value={ artifactContextValue }`, deps `[artifactStateRef, …]` and an out-of-scope `components/Other.tsx` copy silent; `value={{...artifactContextValue}}` (:3497), deps `[]` and `[dispatchArtifact]` (:686), one-argument useMemo (:686), the useMemo removed (file), the const renamed (file + :3497) and the provider tags removed (file) fire. Real inversion, one run: App.tsx:3497 `value={{...artifactContextValue}}` + App.tsx:688 deps `[dispatchArtifact]` — fired at App.tsx:686 and :3497; restored, OK. Kept: the render-count case, unchanged, 1 passed. Unused fs/path imports removed; header WHY names the rule. No live pointers |
| `tests/session-meta-unreadable.test.tsx` (u7) | 5 | 4 | `the drawer does not open a note editor for an unreadable note` (converted) | ast-grep `note-editor-guarded-on-unreadable` (SessionDrawer.tsx: whole file with no `onNote={previewMeta.saveNote}` JSX attribute; every such attribute not guarded by a `previewMeta.unreadable` member read in an enclosing ternary/if condition, the left operand of an enclosing `&&`/`\|\|`/`??`, or an if statement before an enclosing statement — the early-return shape). Deliberate differences (in the note): the text must be a guarding condition — a mention only in a message string before the editor no longer counts (stricter: the retired 1,500-char window accepted the ErrorState message's `${previewMeta.unreadable}`, so a ternary whose condition was replaced by `false` passed it; the rule fires, scratch probe N2); no distance limit; every editor, not the first; whitespace-insensitive. Fixtures +3 (291). 7 scratch probes as expected: pristine, a `!previewMeta.unreadable && (…)` rewrite and an out-of-scope `Other.tsx` copy silent; the ternary's condition replaced by `false` (:1051), the ternary removed (:1044), the same with spaced braces (:1044) and `onNote={previewMeta.save}` (file) fire. Real inversion, one run: SessionDrawer.tsx:1039–1045 + :1061 (the `previewMeta.unreadable ? <ErrorState/> : (…)` wrapper) removed — fired at SessionDrawer.tsx:1044; restored, OK. Kept: the 4 useSessionMeta/usePreviewMeta hook cases, unchanged, 4 passed. Unused `join`/`readStripped` imports removed; header WHY names the rule. No live pointers |
