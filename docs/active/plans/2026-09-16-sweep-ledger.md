# Source-grep sweep ledger (Plan B)

Baseline 2026-09-16: Test Files 888 (886 passed, 1 skipped, 1 failed), Tests 11613 total (11569–11570 passed, 43–47 skipped across two runs), `it(`/`test(` sites 9958, EXPECTED_VIOLATIONS=11.

The one failing file is load-sensitive and outside this plan: `tests/chatgpt-request-diagnostics.test.ts` › "evicts inactive fingerprints within 8 MiB and drops an oversized observation" took 46.6s under the full run and passes alone in 5.3s. The budget to reconcile against is the **total** (11613), since skip counts wobble between runs.

Packaging (Destin, 2026-09-16): one PR per repo for the whole sweep, one "ready to merge?" at the end — Task 7's wording governs over the per-group line in Global Constraints.

| file | cases before | cases after | deleted case titles | rule / tool added |
|---|---|---|---|---|
| Group 5 (20 files — the table under `## Group 5` lists 20 rows though its heading says "19 files"; all 20 processed) + guard-scope-reader.test.ts | 279 | 280 | none | readSource (tests/helpers/guard-scope.ts) |
| `tests/installer-artifact-names.test.ts` | 4 | 4 | none | yaml parse |
| `tests/app-icons.test.ts` | 12 | 11 | `every theme without its own appIcon falls back to the bundled icon, not a synthesized one` | yaml parse |
| `tests/android-manifest-voice.test.ts` | 2 | 2 | none | DOMParser |
| `tests/landing-demo-fade.test.ts` | 2 | 0 (file deleted) | `clears and centers the demo exactly once on wide-screen activation`; `finishes the passive reveal before the demo top leaves the viewport` | — |
| `tests/remote-preview-gate.test.ts` | 1 | 0 (file deleted) | `keeps remote Info available in the preview, and gates its body on a rendered view` | — |
| `tests/remote-place-app-wiring.test.ts` | 14 | 0 (file deleted) | `the session list on mount selects only when allowed`; `a newly created session is focused only when allowed`; `switching to a remote host selects only when allowed`; `no ungated "first session" select is left behind`; `the hydrate handler chooses the place, marks it decided and reports what was kept`; `the destroyed handler uses the desktop's focus in remote mode`; `the first page asks only when the rule allows it`; `the place is remembered on every selection change once decided`; `every setSessionId call site is accounted for — a new one must be looked at`; `the hydrate decides the place, wakes waiting first pages, and selects the choice — remote only`; `the place on screen wins over storage, in the hydrate and when a restore ends without one`; `a restore starting resets the decision; one ending without a hydrate decides with what exists`; `the place is written only once decided, and first pages re-run when a hydrate lands`; `back on the device's own runtime the strip is cleared, and the welcome screen waits while catching up` | — (Task 4/global.md: deleted, not replaced — App still cannot be mounted in a unit test) |
| `tests/remote-tailnet-bind.test.ts` | 3 | 3 | none (all three rewritten as behaviour tests) | real `start()`/`stop()` against a real socket on 127.0.0.1, mocked `RemoteConfig.detectTailscale` |
| `tests/html-view-sealed.test.ts` | 1 | 0 (file deleted) | `the sandbox attribute never gains allow-same-origin` | ast-grep rule `iframe-sandbox-no-allow-same-origin` + `iframe-sandbox-requires-allow-scripts` (added in the review-of-batch-A fix pass, 2026-09-16 — the first rule only fires when a sandbox attribute is present, so it was silent if the attribute or its allow-scripts token was simply missing, which the retired test also required) |
| `tests/app-quit-routes.test.ts` | 2 | 0 (file deleted) | `registers teardown on all three quit routes`; `routes every quit through one idempotent teardown` | ast-grep rule `main-registers-all-quit-routes` (its preventDefault branch was unanchored — satisfied by any of main.ts's four unrelated preventDefault() calls — and re-anchored to the before-quit handler specifically in the review-of-batch-A fix pass, 2026-09-16) |
| `tests/app-resume-session-listener.test.ts` | 4 | 0 (file deleted) | `registers a listener for the event SessionRefActions.requestResume (and the preview header) dispatch`; `bails when the detail is missing any of the three required fields, before calling handleResumeSession`; `passes the popover's model/skip-permissions through, and keeps launchInNewWindow undefined`; `is declared AFTER handleResumeSession — a TDZ constraint, documented at the call site` | ast-grep rule `resume-listener-guards-detail-before-call` |
| `tests/appearance-buddy-broadcast.test.ts` | 1 | 0 (file deleted) | `include Buddy floater windows when a theme changes` | ast-grep rule `appearance-broadcast-reaches-all-windows` + `appearance-broadcast-handler-registered` (added in the review-of-batch-A fix pass, 2026-09-16 — the first rule matches the call site itself, so it was silent if the whole handler registration were deleted) |
| `tests/background-layer-authority.test.ts` | 3 | 0 (file deleted) | `no class string in the renderer sets two unprefixed backgrounds` (converted to ast-grep); `the pattern matches the string that actually shipped` (Class H non-vacuity self-test of the retired JS regex, nothing left to guard); `does not flag a state variant, which is the common legitimate case` (Class H, same reason) | ast-grep rule `no-two-bare-bg-utilities` + `.ts` twin `no-two-bare-bg-utilities-ts` (review-of-batch-A fix pass, 2026-09-16 — the retired test's `inScopeFiles(['.tsx','.ts'])` read both extensions, but the batch-A rule's `.ts` glob did nothing under `language: tsx`); the same pass added structural matching for a bare bg- pair split across a template-string substitution (the shipped bug's own shape, `` `bg-inset ${x} bg-accent/[0.07]` ``) |
| `tests/choice-group-authority.test.ts` | 2 | 1 (file kept, trimmed) | `no hand-rolled segmented control ships` (converted to ast-grep) | ast-grep rule `no-hand-rolled-segmented-control` (+ `.ts` twin `no-hand-rolled-segmented-control-ts`, added in the review-of-batch-A fix pass, 2026-09-16 — the batch-A rule's `.ts` glob did nothing under `language: tsx`); kept case `SegmentedTabs has real consumers` routed through `readSource` (title and `>=4` threshold restored to the original in the same fix pass — a brief rename/weakening to `>=1` was reverted, since the cross-file count being inexpressible in ast-grep didn't require weakening the threshold) |
| `tests/dialog-chrome-authority.test.ts` | 4 | 0 (file deleted) | `this guard can see what it claims to cover` (Class H non-vacuity, dropped — the ast-grep fixture pass is the non-vacuity check now); `no view inside a dialog paints its own header` (converted to ast-grep); `every exemption still exists and still applies` (dropped — exemption bookkeeping a whole-file `ignores:` no longer tracks precisely); `only Dialog and the wizard implement a back chevron` (converted to ast-grep) | ast-grep rules `no-hand-rolled-dialog-header` + `no-hand-rolled-dialog-back-chevron` |
| `tests/field-error-adoption.test.ts` | 3 | 1 (file kept, trimmed) | `no file hand-rolls the primitive markup` (converted to ast-grep); `every exemption still matches something` (dropped — exemption-relevance bookkeeping a whole-file `ignores:` no longer tracks) | ast-grep rule `no-hand-rolled-field-error`; kept case `an exemption covers exactly the occurrences it was granted for` (exact-count exemption an `ignores:` cannot express — global.md's "at most N matches" guidance) |
| `tests/item-list-authority.test.ts` | 4 | 1 (file kept, trimmed) | `no bare glyph survives as a list-item action or a container dismiss` (converted to ast-grep); `this guard can see what it claims to cover` (Class H non-vacuity, dropped — the ast-grep fixture pass is the non-vacuity check now); `no in-menu copy tells the user to press a control that was removed` (converted to ast-grep) | ast-grep rules `no-bare-glyph-item-action` + `no-bare-glyph-item-action-stale-copy`; kept case `every exemption still exists and still applies` (exact-count exemption an `ignores:` cannot express; briefly renamed to `an exemption covers exactly the occurrences it was granted for` and read via `readSource`, then RESTORED to its original title + `readStripped` — review of batch B, 2026-09-16, fix 3); both `no-bare-glyph-item-action*` rules' `files:` scoping fixed the same review (review of batch B, 2026-09-16, fix 2) — a bare `*` in this ast-grep's glob crosses `/`, so `ignores:` now lists every components/ subdirectory `inScopeFiles()` does not walk, plus `**/*.test.tsx` |
| `tests/main-hot-path-no-sync-fs.test.ts` | 3 | 0 (file deleted) | `lease client (createLeaseClient body)`; `transcript mirror (whole file)`; `git-transport gitDirSizeBytes` | ast-grep rules `no-sync-fs-in-main-hot-path` + `-git-transport`; `transcript mirror (whole file)` now guarded by the merged `no-sync-fs-whole-file` (review of batch B, 2026-09-16 — see that row below) |
| `tests/main-read-path-no-sync-fs.test.ts` | 4 | 0 (file deleted) | `marketplace-file-reader (whole file)`; `theme-preview-generator buildPreviewHTML`; `transcript-cwd (whole file)`; `session-browser listing path (readIndexMeta through the end of listPastSessions)` (FIX, review of batch B, 2026-09-16: the retired slice covered EVERYTHING between `readIndexMeta` and `loadHistory` — extractStoreMeta, withRetry, cleanTitle and top-level code too, not just the 8 functions this rule originally named; restated as a whole-file ban outside `loadHistory`) | `marketplace-file-reader (whole file)` and `transcript-cwd (whole file)` now guarded by the merged `no-sync-fs-whole-file` (review of batch B, 2026-09-16 — MERGED from `no-sync-fs-in-main-read-path` (the base rule) + `-transcript-cwd`, byte-for-byte-identical whole-file bans differing only by `files:`); `theme-preview-generator buildPreviewHTML` still `-theme-preview`; `session-browser listing path` still `-session-browser` |
| `tests/menu-row-reachability.test.ts` | 7 | 1 (file kept, trimmed) | `the shortcuts dialog keeps the scrolling body`; `the shortcuts dialog is the wider size`; `every shortcut in the list is rendered from the list itself` (SHORTCUTS.map half converted; the `>=13 rows` half dropped — Class H quantity check, nothing left to guard once the shape check exists); `a session name is one truncated line with the full name on hover`; `the session menu gives its list the remaining screen height`; `opens a model panel upward when its trigger lacks room below` | ast-grep rules `shortcuts-dialog-keeps-scroll-body` + `session-name-line-clamp` + `session-menu-height-capped` + `model-picker-opens-upward` (renamed from `-session-name`/`-session-menu-height`/`-model-picker-upward`, review of batch B, 2026-09-16 — none of the three guard the shortcuts dialog); kept case `the model filter portal remains inside its session-menu host` (exact count of 2, which no whole-file rule can pin) |
| `tests/overlay-layer-authority.test.ts` | 5 | 0 (file deleted) | `ProjectView renders at the shared screen layer, not above overlays`; `the three screens agree on their z-index`; `project-view has no hardcoded 9000-band z-index`; `no renderer component hand-rolls a scrim (bg-black/N in a className)`; `SettingsPanel has no hand-rolled overlay z-index` | ast-grep rules `no-hardcoded-z-index-or-scrim` + `-screen-layer` + `-9000-band` + `-9000-band-ts` + `-scrim` + `-scrim-ts` + `-settings-panel` |
| `tests/perf-marks-renderer.test.ts` | 3 | 0 (file deleted) | `index.tsx marks modules-evaluated and root render, in that order`; `App.tsx marks mount and sessions-listed`; `sessions-listed marks the MOUNT-time session.list, not the connection-mode reload` | ast-grep rules `perf-mark-index-order` + `perf-mark-sessions-listed-inside-session-list` (t5c batch — `follows`/`precedes` only compares true siblings under the same parent even with `stopBy: end`, verified empirically, so the order check matches each mark's ENCLOSING statement/call rather than the mark itself; the reload path's `window.claude.session.list().then(...)` is structurally identical to the mount path's, so the "not the reload" half is expressed as "not inside `onConnectionModeChange(...)`", not by the `.then()` shape alone) |
| `tests/permission-shell-context.test.ts` | 3 | 0 (file deleted) | `the pattern recognises the wiring it is looking for` (Class H non-vacuity self-test of the retired regex, dropped — the ast-grep fixture pass is the non-vacuity check now); `native-session-host passes the shell to decidePermission` (converted to ast-grep); `the label it compares against is the one detectShell actually returns` (converted to ast-grep) | ast-grep rules `decide-permission-passes-powershell-flag` + `-shell-label` twin (different file: bash.ts, not native-session-host.ts) |
| `tests/section-label-authority.test.ts` | 3 | 1 (file kept, trimmed) | `every section label spells the recipe the same way` (converted to ast-grep); `no <h4> stands in as a section label` (converted to ast-grep) | ast-grep rules `section-label-canonical-classes` + `-ts` twin (real `.ts` site found: `dev/workbench/compare/permission-modes.ts`'s `SECTION_LABEL`, whose own source comment was repointed to the rule) + `section-label-no-h4`; a `not: inside: template_string` guard was added after a scratch probe found `FilesTab.tsx`'s `` `${fullW} text-3xs uppercase tracking-wider text-fg-muted...}` `` matching the token check even though the retired test's `"([^"\n]*)"` regex never reads inside a template literal — global.md's "don't be stricter than the test"; kept case `the canonical recipe is actually in use` (Class H non-vacuity, >25 threshold preserved, routed through `readStripped`) |
| `tests/tooltip-adoption.test.ts` | 4 | 1 (file kept, trimmed) | `never wraps a component whose title is a heading, not a hint` (converted to ast-grep); `only wraps an element, or a component that forwards to one` (converted to ast-grep); `never leaves a list key on the wrapped child` (converted to ast-grep) | ast-grep rules `tooltip-wraps-forwarding-element` + `tooltip-title-is-not-data` + `tooltip-key-on-wrapper-not-child` — all three check only the DIRECT wrapped child (not `stopBy: end`), since a forwarding component's own body may legitimately contain other components deeper down, and the retired test itself only inspected the first tag after `<Tooltip>`; `jsx_attribute`'s name has no `field:` label in this grammar (matched via `kind: property_identifier` instead); kept case `this guard can see what it claims to cover` (Class H non-vacuity, >40 threshold preserved, routed through `readSource`); repointed `docs/MAP.md`'s "Hover hints" row to the three new rules |
| `tests/type-scale-authority.test.ts` | 2 | 0 (file deleted) | `every step of the sub-Tailwind scale is defined in globals.css` (converted to ast-grep — a whole-stylesheet presence check via `language: css`); `no component sets a font size with an arbitrary text-[Npx]` (converted to ast-grep, decimal + whole) | ast-grep rules `type-scale-named-tokens-declared` + `no-arbitrary-text-size` + `-ts` twin; no separate `template_string` branch on the px rule — the banned pixel digits are always a literal contiguous run, so `text-[${expr}px]` can never match either way, and a `template_string` branch double-counted the same violation when the whole token sat inside one fragment (scratch probe, 2026-09-16); repointed `docs/MAP.md`'s "React renderer / chrome" and "Desktop checks" rows |
| `tests/view-switch-stays-local.test.ts` | 2 | 0 (file deleted) | `App never broadcasts switch-view` (converted to ast-grep); `a switch-view action received over remote changes no view mode` (converted to ast-grep) | ast-grep rules `app-no-switch-view-broadcast` + `app-no-switch-view-receiver`; the broadcast rule's first draft used `pattern: "broadcastAction?.($ARG)"` (bare-call only) and went silent on the real call site's actual receiver chain `(window as any).claude?.remote?.broadcastAction?.(...)` — fixed to match the callee via `call_expression`'s `function` field regardless of chain depth (scratch probe, 2026-09-16); the receiver rule is `kind: program` `any:` of the ban and the negation of the one relay (`_SESSION_INITIALIZED`) App still applies |
| `tests/accepted-history-privacy.test.ts` (t6a) | 1 | 1 (file kept, trimmed) | none (the file's single `it(...)` — "keeps the reasoning ciphertext in the private manifest and out of every reader the app has" — stays; only its Reader 6 sub-block, the source-scanning half, was removed) | ast-grep rule `private-continuation-dir-single-owner` (single-owner ban, all of src/main + src/renderer .ts except accepted-history-store.ts) + twin `-devtools` (the narrower `userData` ban, scoped to dev-tools.ts alone since `userData` is a common legitimate literal everywhere else); inverted dev-tools.ts (appended a line naming both strings), both rules fired naming dev-tools.ts:911, restored |
| `tests/animation-frame-budget.test.ts` (t6a) | 45 | 43 (13 moved to a new file, 30 kept in place, 2 deleted) | `bans NEW inline infinite animations without steps timing in components`; `bans Tailwind arbitrary-value infinite animations without steps()` (both converted to ast-grep) | ast-grep rule `no-unstepped-infinite-animation` (2 branches: inline `animation: '...infinite...'` style without steps(), and Tailwind `animate-[name_..._infinite]` without steps() and not in the character-motion/companion exception list); inverted SessionStrip.tsx's breathing-dot `steps(8)` to `ease`, fired naming SessionStrip.tsx, restored. CORRECTION: this file's classification row described only its first describe block (15 cases); its second describe block "motion vocabulary" (30 cases) is unrelated SessionStrip drag/hover mechanics that `.claude/rules/session-strip-motion.md` names as its guard by this file's path — left untouched, file NOT renamed. Of the first block's 15: 2 → the rule above, 13 (CSS + per-component TSX pins the rule can't express without one rule per component) moved via `readStripped`/`readSource` to new `tests/animation-css-budget.test.ts`. Both files run: 43 passed (30 + 13). |
| `tests/arcade-authority.test.ts` (t6a) | 21 (17 `it(` sites; the assistant-finishing rule was 1 site × 5 loop expansions) | 14 | `no game file references playSound — a game must not make its own sound when a turn ends`; `no game file references useAnyAttentionNeeded — a game must not react to the attention summary`; `no game file references onAttentionSummary — a game must not subscribe to the attention summary`; `no game file references isThinking — a game must not watch whether the assistant is working`; `no game file references sessionAttention — a game must not read session attention` (all 5, converted to ast-grep); `the palette pattern actually matches a known positive` (Class H non-vacuity, dropped — its one real-product half, "the allowed set has green-400", is already covered by `sanctionedStatusColours()`'s own internal `>=4` non-emptiness check); `the game-word list actually matches a known positive` (Class H non-vacuity, dropped) | ast-grep rules `arcade-no-forbidden-attention-apis` + `-ts` twin (game dir mixes .ts/.tsx); inverted GamePanel.tsx (added a `playSound(...)` call) and game-registry.ts (added an `isThinking` reference), both fired naming file:line, restored. CORRECTION: of the 13 source-reading cases, 10 (state.play single-owner, shared-state vocabulary, challenge wiring, ChatView key yield, game key-claim, palette ban, main-process vocabulary ban, arcade-handler no-ranking, resize-effect ordering ×2) are kept via `readStripped`, unchanged — each is a narrow, low-reuse, one-or-two-file text pin; converting all of them would mean 6+ more bespoke rules for one row of eight assigned this batch. `docs/MAP.md`'s games-arcade row repointed from "(no rule — guarded by tests)" to the new rule. |
