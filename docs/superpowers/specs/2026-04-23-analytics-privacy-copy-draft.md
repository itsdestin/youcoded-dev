# Privacy copy draft — edit freely

Scratch file for iterating on the wording that will land in `AboutPopup.tsx` and the landing page. Once you're happy with it, I'll fold the final text into the design spec.

---

## 1. About popup — Privacy section (DESKTOP variant)

**Privacy**

Your Claude Pro/Max sign-in is stored locally on your device. It is never transmitted to or collected by YouCoded. All Claude Code interactions happen directly between the on-device CLI and Anthropic's servers. YouCoded does not collect any personal data or message content.



By default, your device may send anonymous analytics data to YouCoded, including: 

- A random install ID generated by the app
- Installed app version (e.g. `1.2.1`)
- Platform and OS (e.g. `desktop / mac`)
- Country (from the connection)

The collection of this information helps improve YouCoded for yourself and future users. You may disable this below at any time.

**[ Toggle: Share anonymous usage stats — default ON ]**

**Remote access.** (existing copy — unchanged) Remote access (when enabled) serves the UI over your local network or Tailscale. Remote connections are NOT TLS-encrypted — use Tailscale for sensitive conversations since it provides WireGuard encryption end-to-end.

**Multiplayer games.** (existing copy — unchanged) Multiplayer games connect to a PartyKit server (Cloudflare) only while a lobby or game is open. No game traffic is retained server-side beyond the active room.

---

## 2. About popup — Privacy section (ANDROID variant)

**Privacy**

Your Claude Pro/Max sign-in is stored locally on your device. It is never transmitted to or collected by YouCoded. All Claude Code interactions happen directly between the on-device CLI and Anthropic's servers. YouCoded does not collect any personal data or message content.



By default, your device may send anonymous analytics data to YouCoded, including:

- A random install ID generated by the app
- Installed app version (e.g. `1.2.1`)
- Platform (`android`)
- Country (from the connection)

The collection of this information helps improve YouCoded for yourself and future users. You may disable this below at any time.

**[ Toggle: Share anonymous usage stats — default ON ]**

**Termux packages.** (existing copy — unchanged) During initial setup, Termux runtime packages are downloaded from packages.termux.dev over HTTPS with SHA256 verification.

---

## 3. Landing page — privacy / data-collection section

(Adapt to the existing landing-page tone once we find the repo. Draft below assumes a short FAQ-ish block.)

**What data does YouCoded collect?**

Your Claude Pro/Max sign-in is stored locally on your device. It is never transmitted to or collected by YouCoded. All Claude Code interactions happen directly between the on-device CLI and Anthropic's servers. YouCoded does not collect any personal data or message content.

By default, your device may send anonymous analytics data to YouCoded, including:

- A random install ID generated by the app
- Installed app version (e.g. `1.2.1`)
- Platform and OS (e.g. `desktop / mac`, `android`)
- Country (from the connection)

The collection of this information helps improve YouCoded for yourself and future users. You may disable this at any time in **Settings → About → Privacy**.

---

## Notes for Destin as you edit

- The three bullet-lists of analytics fields should match each other word-for-word once you settle on wording, so readers get a consistent promise across surfaces.
- Anything promised in copy is enforced by code (see Design section 5 of the spec) — don't add promises we can't keep (e.g., don't say "you can request deletion" because the data isn't keyed by install_id in a lookup-able way; we literally cannot find your row to delete it).
