{
 "base": "http://127.0.0.1:5233/?mode=workbench&child=1&latency=0&scenario=default",
 "width": 1440,
 "height": 900,
 "pre": [
  {
   "click": "[title=Settings]",
   "settle": 900
  }
 ],
 "shots": [
  {
   "name": "settings-rows",
   "actions": [
    {
     "eval": "[...document.querySelectorAll('button')].find(b=>b.textContent.includes('Remote Access'))?.scrollIntoView({block:'center'})",
     "settle": 400
    }
   ],
   "expect": "js:[...document.querySelectorAll('button')].some(b=>b.textContent.includes('Backup & Sync'))&&[...document.querySelectorAll('button')].some(b=>b.textContent.includes('Remote Access'))",
   "sameAsBaseline": true
  }
 ]
}