{
 "_comment": "Real-app pass for YouCoded Pages: the isolated dev instance opens the Pages landing screen. WHY: Manage pages is intentionally absent when there are no pages, so a backend-dependent capture cannot assume it is clickable.",
 "base": "http://localhost:5233/",
 "boot": 4500,
 "shots": [
  {
   "name": "e-pages-view",
   "actions": [
    {
     "click": "[aria-label='Open Pages']",
     "settle": 1200
    }
   ],
   "expect": "[aria-label='Back to chat']"
  }
 ]
}
