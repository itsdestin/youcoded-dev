// Violation fixture for chrome-root-select-none-header-bar (fires once, on the
// file): select-none moved to a class list that does not start with header-bar.
export const HeaderBar = () => <div className="flex header-bar select-none">x</div>;
