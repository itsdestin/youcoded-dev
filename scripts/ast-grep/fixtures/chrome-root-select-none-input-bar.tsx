// Violation fixture for chrome-root-select-none-input-bar (fires once, on the
// file): the composer root lost select-none.
export const InputBar = () => <div className="input-bar-container shrink-0">x</div>;
