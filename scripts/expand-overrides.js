// Backfills longer (~2 paragraph) longDescriptions into each override file.
// Re-run safe: overwrites longDescription field only.
//
// NOTE (2026-04): Some LONG entries below use keys from the pre-flatten bundle
// naming (`youcoded-core-drive`, `youcoded-core-food`, etc.) that no longer
// match any override file — the script SKIPs those at write time, they're
// effectively dead entries kept for reference. Live keys with "toolkit"
// framing were rewritten in-place; updating here keeps re-runs idempotent.

const fs = require('fs');
const path = require('path');

const OVERRIDES = path.resolve(__dirname, '../wecoded-marketplace/overrides');

const LONG = {
  'civic-report': "Drop in your address and Claude generates a personalized briefing on every federal official who represents you — President, VP, both U.S. Senators, and your House Rep — with state-level officials included as named stubs. Pick the angle you care about (housing, climate, immigration, foreign policy, or anything you type), choose a tone (neutral, critical, positive, civic, friendly, deep-research), and decide whether you want the full eight-section report per rep or a custom subset.\n\nEvery factual claim in the output is footnoted with a source URL you can click through to verify, and reports are saved to your Encyclopedia by default so you can reread them before voting, calling an office, or writing a letter. The first-time setup runs a one-line interactive prompt to capture an API key for the data source; everything after that is just conversation.",

  'youcoded-core': "`youcoded-core` is a bundled YouCoded plugin that ships with the app. It contributes a small number of safety hooks (write-guard, worktree-guard), a session-start hook, and two first-run helper skills (`setup-wizard`, `remote-setup`). It's auto-installed at `~/.claude/plugins/youcoded-core/` so most users never touch it directly.\n\n**Heads up — this plugin is being deprecated.** `write-guard` is moving into the app natively, the remaining helpers are being folded in or retired, and the repo will eventually be archived. Existing installs keep working; new capabilities are landing in the app itself or in dedicated marketplace plugins (`youcoded-encyclopedia`, `youcoded-inbox`, `wecoded-themes-plugin`, etc.) rather than here.",

  'youcoded-core-drive': "Browse, search, read, upload, and organize Google Drive without leaving the chat. Talk to Claude in natural language (\"find the slide deck I started in March\", \"download the latest sales spreadsheet to my Desktop\", \"create a folder called 2026-Taxes and move these into it\") and it executes against the real Drive via `rclone` — no copy-paste, no tab-switching, no flaky browser automation.\n\nFirst run walks you through `rclone config` once to authorize a `gdrive:` remote; after that every Drive operation is an instant CLI call under the hood. Works with personal Drive and Workspace accounts, supports nested paths, and respects your existing folder structure. Pairs naturally with `youcoded-core-encyclopedia` if you want Claude to pull source material from your Drive into your personal knowledge base.",

  'youcoded-core-encyclopedia': "Your life, written down once and reachable forever. The Encyclopedia bundle is four skills working together: `journaling-assistant` runs a daily check-in that prompts you through the parts of your day worth remembering; `encyclopedia-update` routes new journal content into eight modular source files (people, places, beliefs, goals, work, etc.); `encyclopedia-interviewer` proactively notices gaps and runs targeted interviews to fill them; and `encyclopedia-librarian` produces purpose-built briefings — \"prep me for tomorrow's meeting with X\", \"review my last six months\", \"pull together everything I've said about Y\".\n\nThe whole system is plain markdown stored locally in `~/.claude/encyclopedia/` — you own every word, you can read and edit it in any text editor, and you can sync it across devices via the toolkit's sync skill. Four detail levels (Full, Personal, Professional, Public) let you control how much intimate context appears in any given output, so the same encyclopedia can power both deeply personal reflection and cleanly shareable professional summaries.",

  'youcoded-core-food': "A two-part food tracker with personality. The first part keeps a live inventory of what's actually in your pantry, fridge, and freezer — log a grocery run by texting Claude a receipt or a fridge photo, ask \"what do I have for dinner?\", and get age/freshness warnings on items getting old. The second part tracks fast food spending and ordering patterns by restaurant and item, so you can see how much that DoorDash habit is actually costing you.\n\nClaude addresses you with rotating affectionate-roast nicknames (Fatty, Chonk, Sir Eats-a-Lot…) and the teasing escalates with the size of the order — light ribbing for a $30 grocery run, full grief for a $14 fast food trip for one. Data lives in two CSVs under `~/.claude/fork-file/` so you can always export, edit, or back it up yourself. Built on `fork-file` underneath; install this bundle for the full UX.",

  'youcoded-core-inbox': "Pulls items from every inbox you actually use — Todoist tasks, Google Drive notes, Gmail, Apple Notes, Apple Reminders, iCloud Drive, even local screenshots dropped in a staging folder — and triages them in one conversation. Claude isn't just routing items; it actively *resolves* them: answering questions, applying feedback, creating tasks, deleting noise, filing screenshots into your journal or encyclopedia.\n\nProvider-agnostic by design — pick the sources you actually use during setup, leave the rest off. The session-start hook quietly counts unprocessed items and surfaces a reminder when there's something to look at, so you never have to remember to open your inbox; it comes to you. Works hand-in-glove with `youcoded-core-encyclopedia` (journal entries get filed) and the toolkit's sync skill (multi-device captures all flow into one queue).",

  'youcoded-core-messaging': "Send and read SMS and iMessage from inside Claude. Drafts replies in your voice, sends from the phone number bridged through the configured MCP server, and surfaces incoming messages so Claude can react to them in your normal chat session. Useful for catching up on a thread without picking up your phone, or for asking Claude to draft something tricky and then send it for you.\n\nThe heavy lifting is in the bundled MCP servers — `gmessages` for Android/Google Messages, `imessage` for macOS — both of which the plugin will configure during install. Pairs naturally with `youcoded-core-inbox` for inbound triage, and with `youcoded-core-encyclopedia`'s deep-search mode for \"go find that thing I texted Mom about in October\" queries.",

  'youcoded-core-output-styles': "A small library of tone presets for Claude — `casual`, `conversational`, `academic`, and `professional` — that you can flip between with a single command depending on what you're doing. Use `casual` for chat with friends, `academic` for paper drafts and lit-review summaries, `professional` for emails and proposals, `conversational` for blog posts and personal essays.\n\nEach style is a plain markdown file under `styles/` describing the voice rules (formality, structure, vocabulary, hedging) so the presets are inspectable and forkable — copy one, tweak it, and now you have your own. No model switching, no settings menu — just type the name and Claude takes on the voice for the rest of the conversation.",

  'wecoded-themes-plugin': "The `/theme-builder` skill — describe a vibe, drop in a wallpaper, or write a detailed brief, and Claude generates three full YouCoded theme concepts in a side-by-side browser preview. Pick one and you land on a single-page kit refiner where palette, chrome, bubbles, fonts, effects, wallpaper, mascots, and icons are all swappable columns; iterate until it feels like you, then build the pack and the app hot-reloads with your new look.\n\nFinished themes are real YouCoded theme packs (manifest + assets + custom CSS) you can install locally, share with friends via QR/link, or publish to the community theme registry where they get an auto-generated preview PNG. The skill enforces the 15 required theme tokens and runs contrast validation on the way out, so anything you publish is guaranteed legible.",

  'claudes-inbox': "The single skill at the heart of `youcoded-core-inbox` — checks your configured capture sources (Todoist, Drive, Gmail, Apple Notes/Reminders, iCloud, local staging) for unprocessed items and works through them with you in conversation. Items are normalized into a common shape, classified by intent, then resolved one at a time with your approval (answer the question, file the note, create the task, delete the noise).\n\nUse this skill standalone if you only want the triage logic without the rest of the inbox bundle, or invoke it directly with \"check my inbox\" / \"process my notes\" whenever you've been collecting captures throughout the day. The session-start hook in `youcoded-core-inbox` calls it automatically when items are waiting.",

  'encyclopedia-compile': "Compiles the eight modular source files of your Encyclopedia into one unified, narratively coherent life-history document — designed to be read as a comprehensive biography, both now and decades from now. Run it monthly (or whenever you say \"compile my encyclopedia\") and the skill reads every source file, internalizes the content, then writes a single document with one consistent voice and structure rather than a section-per-file dump.\n\nFour detail levels (Full, Personal, Professional, Public) control how much intimate detail surfaces in the output, so the same encyclopedia can produce a private full-fat version for yourself and a sharable Professional version for a job application. The compiled output saves to `~/.claude/encyclopedia/Encyclopedia.md` and gets versioned so you can compare year-over-year.",

  'encyclopedia-interviewer': "Turns the Encyclopedia from passive storage into an active conversation. Reads all eight source files, identifies what's missing or stale (gaps in backstory, cross-file contradictions, underdeveloped positions, unconfirmed leads), then runs a structured interview with you to fill them. Unlike the journaling assistant which follows your lead, the interviewer *pushes* — it has a prepared agenda and probes for specifics.\n\nProduces both a journal record of the conversation and targeted updates to the modular files (delegated through `encyclopedia-update`). Trigger it with \"interview me\", \"interview me about X\", \"what's missing in my encyclopedia\", or let the monthly gap-audit prompt suggest it for you. The depth and pace are conversational — expect 15–45 minutes per session depending on how much you have to say.",

  'encyclopedia-librarian': "Produces purpose-built briefings on demand from your Encyclopedia — career summaries for job applications, person briefs before meetings, relationship context before family visits, period reviews, political profiles, topic syntheses. Six predefined templates plus a freeform mode, plus an optional Deep Search mode that mines text-message histories, journal entries, and misc documents for material the modular files might have missed.\n\nFour detail levels (Full, Personal, Professional, Public) control privacy in the output — same encyclopedia, different surface depending on whether the briefing is for you, a coworker, or a public document. Trigger with \"brief me on [person]\", \"deep search [topic]\", \"prep me for [situation]\", \"pull together everything about X\". Outputs save to `~/.claude/encyclopedia/reports/` and are included in future Deep Search runs.",

  'encyclopedia-update': "The processing layer underneath the Encyclopedia. Reads new journal entries (incremental by default, full-rebuild on request) and routes their contents into the eight modular source files: people, places, beliefs, work, goals, references, etc. Applies file-specific update rules, deduplicates, surfaces conflicts for your approval, and never writes without confirming.\n\nInvoked automatically at the end of every daily journaling session by `journaling-assistant` so the encyclopedia stays current without you thinking about it, but you can also run it directly — \"update my encyclopedia\", \"sync the encyclopedia\", \"add a goal\" — when you've made changes outside the daily-journaling flow. Safe to re-run; idempotent by design.",

  'fork-file': "The single skill at the core of `youcoded-core-food` — the actual food tracker. Manages a grocery/pantry inventory across the storage locations you configure (pantry, fridge, freezer, garage shelves, dorm mini-fridge — whatever you've got) and tracks fast food spending broken out by restaurant, item, and size. Handles receipt processing from photos or text, food identification from pictures, manual entry, age/freshness warnings, and spending summaries.\n\nData lives in two CSVs under `~/.claude/fork-file/` — `pantry.csv` for inventory, `fastfood.csv` for spending — both human-readable and exportable. Install this if you want the food-tracking logic without the full `youcoded-core-food` bundle's UX wrapping; install `youcoded-core-food` if you want the snarky-friend nicknames and the rest of the experience on top.",

  'google-drive': "The single skill that powers `youcoded-core-drive`'s Google Drive integration. Wraps `rclone` with a strict rule: every Drive operation goes through the configured `gdrive:` remote — never browser automation, never the web UI, never another path. List, search, upload, download, copy, move, delete, create folders, all in conversation: \"show me my Drive\", \"download the latest report to Desktop\", \"move these into a 2026-Taxes folder\".\n\nFirst-run setup walks you through `rclone config` to authorize Drive once. Install this skill if you want raw Drive access without the rest of `youcoded-core-drive`'s polish, or install `youcoded-core-drive` for the full bundle including helper commands and integration with the broader YouCoded toolkit.",

  'google-workspace': "Drafts emails, edits spreadsheets, modifies slides, creates calendar events, and reads docs through Google's official APIs — auth once and Claude has hands-on access to your Workspace from chat. Useful for the boring 80% of Workspace tasks: \"reply to that email from Sarah and CC the team\", \"add a row to the budget tracker for this week's Stripe payout\", \"move Tuesday's 3pm meeting to Wednesday at 10\", \"summarize the doc I just pasted a link to\".\n\nUses official Google APIs rather than browser automation, so it's stable, fast, and survives Drive UI changes. Pairs with `youcoded-core-encyclopedia` for \"read this doc and update my notes\" workflows, and with `youcoded-core-inbox` so Gmail-sourced captures flow into your normal triage. Workspace and personal Google accounts both supported.",

  'health': "A lightweight diagnostic command — verifies that your YouCoded plugins are installed, their hooks are reconciled into your Claude Code settings, and the core dependencies are present. Run it after troubleshooting an issue, after upgrading the app, or any time something feels off and you want to confirm the foundation is healthy before going deeper.\n\nUnlike `/diagnose` (which is the full deep-scan), `/health` is a quick yes/no check meant to fit on one screen — you'll know in seconds whether everything is in order. Reports specific failures with the exact command to fix each one. Read-only; never modifies your settings.",

  'journaling-assistant': "A guided daily journaling session — Claude prompts you through the parts of your day worth remembering rather than asking you to start from a blank page. Two modes: structured daily entries that capture a full breadth of the day, and freeform misc entries for rants, topic deep-dives, beliefs, or anything else worth writing down. The assistant is proactive — it surfaces relevant unresolved threads from past entries and asks about them, rather than waiting for you to bring them up.\n\nNot therapy: the assistant draws out detail with questions, but it never interprets, advises, or editorializes. At the end of each daily session it automatically calls `encyclopedia-update` to route what you said into your modular source files, so the encyclopedia stays current as a side effect of journaling. Triggers on \"let's journal\", \"debrief my day\", \"I want to rant about X\", and similar phrases.",

  'remote-setup': "Walks you through enabling remote access to YouCoded from your phone (or any other device) in one conversation. Installs Tailscale if you don't have it, runs the auth flow, sets a remote-access password, and confirms everything works — explained in plain language for non-technical users, with no \"open a terminal and run this\" handoffs. Claude does the work; you just sign in to Tailscale when the browser pops up.\n\nBy the end you'll have YouCoded reachable at a stable Tailscale URL on any device on your tailnet, which means full chat + terminal access from your phone over an encrypted WireGuard tunnel. Idempotent — safe to re-run if you want to change the password or move to a new device.",

  'setup-wizard': "A pure-CLI fallback first-run wizard for people using `youcoded-core` directly in Claude Code without the YouCoded app. Walks you through three short questions — name, comfort level, sync backend — and writes the result to `~/.claude/toolkit-state/config.json`. That config then drives the rest of the plugin's behavior.\n\nMost people don't need this because the YouCoded app runs its own onboarding (with curated package selection, plugin registration, and hooks reconciliation built in). This skill exists for the users who installed `youcoded-core` standalone via `git clone` and need a graceful first-run experience. Additive and non-destructive — never overwrites existing config without permission.",

  'sync': "Cross-device sync used to be a standalone skill but is now built directly into the YouCoded app — no plugin to install. Open the app, go to Settings → Sync, and pick one of the built-in backends (Google Drive via rclone, GitHub via a private repo, or iCloud via the native folder). The app handles upload/download/conflict resolution automatically on every session start and end.\n\nIf you're using Claude Code standalone without the YouCoded app and need sync, the legacy skill lived inside `youcoded-core` before being folded into the app; that repo is being deprecated. Use the app for the best experience.",

  'toolkit': "The legacy `/toolkit` inventory command no longer exists. Feature discovery has moved to the YouCoded marketplace in the app — browse, search, and install plugins visually rather than reading a printed list. This registry entry is kept for historical clients and will be removed in a future cleanup.\n\nIf you installed this in the past: uninstall it from the app's marketplace. The commands it used to index (`/health`, `/update`, `/diagnose`) still exist inside the `youcoded-core` plugin.",

  'update': "Fetches the latest `youcoded-core` release from GitHub and installs it in place — bumps the `VERSION` file, pulls new hooks/commands, runs migrations, reconciles your settings.json hooks against the new manifest, and reports exactly what changed. Run it whenever you remember (no auto-update; you're in control), or when `/health` warns that you're behind.\n\nSafe to re-run; idempotent by design. Verifies the working tree is clean before touching anything, never overwrites local edits, and tells you exactly which commands to run if it can't auto-resolve a conflict. Offline-safe: gracefully tells you it can't check when there's no network rather than failing partway through.",
};

let written = 0;
for (const id of Object.keys(LONG)) {
  const file = path.join(OVERRIDES, `${id}.json`);
  if (!fs.existsSync(file)) {
    console.warn(`SKIP ${id}: override file missing`);
    continue;
  }
  const obj = JSON.parse(fs.readFileSync(file, 'utf8'));
  obj.longDescription = LONG[id];
  fs.writeFileSync(file, JSON.stringify(obj, null, 2) + '\n', 'utf8');
  written++;
}
console.log(`Updated ${written} override files.`);
